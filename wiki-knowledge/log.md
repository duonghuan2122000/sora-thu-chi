# Log cập nhật wiki

## [2026-09-17] implement | Nhân bản giao dịch (PBI 40) — nguồn .specify/specs/40
- Cập nhật [[Giao dịch]] §"Sửa / xóa / nhân bản": tách § **"Nhân bản giao dịch — đã triển khai (PBI 40)"** riêng khỏi § "Xóa / Undo / Định kỳ — chưa triển khai" (trước gộp chung, nay Nhân bản đã xong). Nút "Nhân bản" (trước no-op) tái dùng đúng `AddTransactionScreen`/`WalletTransferScreen` của PBI 39 nhưng ở **chế độ tạo mới điền sẵn** (không truyền `editing`/`editingTransferGroupId`, ngày giờ ép `DateTime.now()`); `AddTransactionScreen` thêm 5 tham số optional `initialAmount/initialNote/initialDate/initialTags/initialReceiptImage`; `_originalReceiptImage` mở rộng để không xóa nhầm ảnh nhân bản (vẫn được giao dịch gốc tham chiếu); baseline `isDirty` đọc từ `initial*` khi tạo mới nên mở-rồi-thoát-ngay không hỏi xác nhận; `WalletTransferScreen` không cần sửa gì. **⚠ Lệch cố ý so với docs/transaction §3.3/§4.4**: không sao chép trường "Vị trí" — màn này chưa từng có ô nhập Vị trí (chỉ luồng quét OCR ghi được), không mở rộng repository API cho tính năng nhỏ. Không đổi schema.
- Cập nhật [[Lộ trình phát triển]] "Ghi chú giai đoạn": thêm bullet **"Nhân bản giao dịch (MVP) — đã triển khai (PBI 40)"**, tóm tắt cơ chế + lệch cố ý (Vị trí) + phần còn lại (Xóa, Undo, định kỳ, QA tay emulator chưa chạy).
- Cập nhật `index.md` dòng Entities → [[Giao dịch]]: thêm tóm tắt Nhân bản (PBI 40).
- Kỹ thuật: 16/16 task (US1 nhân bản Thu/Chi P1 + US2 nhân bản Chuyển khoản P2), không schema mới, `flutter analyze` sạch, **1373 test pass** (baseline trước PBI 40 là 1371, +2 test tích hợp mới trong `transaction_detail_screen_test.dart`: case Chi và case Chuyển khoản, phủ luôn coverage dự kiến cho `add_transaction_screen_test.dart`/`wallet_transfer_screen_test.dart` — không viết trùng). Phát sinh khi chạy full suite: thiếu bản dịch `en` cho snackbar lỗi mới (đã bổ sung `sora_translations.dart`). Phát sinh khi khảo sát code: gap "Vị trí" không sao chép được (không có trong research.md ban đầu — phát hiện lúc đọc `WalletRepository.addTransaction`, không có tham số `location`); thêm `initialAmount` ngoài kế hoạch research.md (bị sót). Chưa commit; chưa QA tay trên emulator/máy thật (T008/T012 chỉ chạy test tự động).

## [2026-09-17] implement | Sửa giao dịch (PBI 39) — nguồn .specify/specs/39
- Cập nhật [[Giao dịch]] §"Sửa / xóa / nhân bản": tách thành § **"Sửa giao dịch — đã triển khai (PBI 39)"** (nút "Sửa" ở màn Chi tiết, trước no-op từ PBI 10/38, nay tái dùng `AddTransactionScreen` chế độ sửa cho Thu/Chi và `WalletTransferScreen` chế độ sửa cho Chuyển khoản; 2 method mới `WalletRepository.updateTransaction`/`updateTransfer` — hoàn tác-rồi-áp-lại số dư atomic, không đổi schema; ⚠ quyết định thu hẹp phạm vi cố ý: chỉ đổi loại Thu⇄Chi, không đổi nhóm sang/từ Chuyển khoản khi sửa) + § **"Xóa / Nhân bản / Undo / Định kỳ — chưa triển khai"** (giữ nguyên trạng thái no-op, tách rõ khỏi phần Sửa đã xong). Sửa dòng "Chưa làm" cũ ở mục PBI 38 (không còn đúng vì Sửa đã có).
- Cập nhật [[Lộ trình phát triển]] "Ghi chú giai đoạn" (mục MVP): thêm bullet **"Sửa giao dịch (MVP) — đã triển khai (PBI 39)"** tóm tắt cơ chế + quyết định thu hẹp phạm vi + phần còn lại (Xóa/Nhân bản/Undo/định kỳ).
- Kỹ thuật: 22/22 task (US1 sửa Thu/Chi P1 + US2 sửa Chuyển khoản P2), không schema mới, `flutter analyze` sạch, **1371 test pass** (baseline trước PBI 39 là 1352 theo PBI 38 + ~19 test mới — repository atomic revert-apply, widget chế độ sửa 2 màn, điều hướng từ Chi tiết). Phát sinh khi chạy full suite: thiếu bản dịch `en` cho vài chuỗi mới (đã bổ sung `sora_translations.dart`); bẫy `late final` tự tham chiếu field mutable (`_originalReceiptImage`/`_initialTags` đọc `_receiptImagePath`/`_tags` sau khi đã bị đổi) — sửa bằng tính thẳng từ `widget.editing` thay vì đọc lại field khác. Chưa commit; chưa QA tay trên emulator/máy thật.

## [2026-09-17] implement | Cập nhật giao diện màn Thêm giao dịch (PBI 38) — nguồn .specify/specs/38
- Cập nhật [[Giao dịch]] §"Thêm giao dịch": thêm § **"Cập nhật giao diện màn Thêm giao dịch (PBI 38, đã triển khai)"** — bàn phím số hệ thống thay numpad tự vẽ (bám pattern `_onAmountChanged` đã có ở màn Chuyển tiền); **Tag nay ghi được** qua màn `TagPickerScreen` mới (không bảng riêng, suy gợi ý bằng `distinctTags` quét toàn bộ giao dịch); **Ảnh hóa đơn nay đính kèm được** ở đường nhập tay (tái dùng `image_picker` + seam `ScanImageStore` của PBI 24, không dựng lại `ScanCameraScreen`). Sửa bảng mockup: `02-them-giao-dich.svg` (numpad) → `02-them-giao-dich-v2.svg`. Ghi rõ ngoài phạm vi: chưa có luồng "Sửa giao dịch", chưa có màn quản lý Tag độc lập.
- Cập nhật [[Design system]] dòng Numpad/Typography: numpad tự vẽ **vẫn dùng** ở màn xác nhận quét hóa đơn + khoảng số tiền màn Tìm kiếm & Lọc; **chỉ màn Thêm giao dịch đổi sang bàn phím hệ thống** (sửa claim cũ nói numpad dùng cho "màn thêm gd").
- Cập nhật [[Hồ sơ & Bảo mật]] dòng liên kết Design system — bỏ claim "numpad dùng lại cho màn nhập tiền", thay bằng mô tả chính xác phạm vi còn lại.
- Cập nhật [[Stack kỹ thuật]] thêm § **"Cập nhật giao diện màn Thêm giao dịch — tái dùng seam có sẵn (PBI 38)"**: liệt kê từng seam/API tái dùng (không dependency mới, không migration) — `formatAmount`/`parseAmount`, `distinctTags`/`joinTags` (mới, cạnh `parseTags`), `image_picker` + `ScanImageStore`, `addTransaction` thêm 2 tham số optional, `isDirty` thêm `hasTags`/`hasReceiptImage`.
- Cập nhật [[Lộ trình phát triển]]: thêm bullet PBI 38 trong "Ghi chú giai đoạn" (mục MVP) — đóng khoảng cách giữa đặc tả `docs/transaction §3.1` (Tag/Ảnh hóa đơn là trường tùy chọn từ đầu) và thực tế trước đó (`addTransaction` luôn để trống 2 trường này).
- Kỹ thuật: 23/23 task, không schema mới (cột `tags`/`receipt_image` đã có từ v3), **1352 test pass** (baseline trước PBI 38 là 1330 theo PBI 37 + 22 test mới), `flutter analyze` sạch. Phát sinh khi chạy full suite: thiếu bản dịch `en` cho 8 chuỗi mới (đã bổ sung `sora_translations.dart`) và `widget_test.dart` luồng FAB còn gõ numpad kiểu cũ (đã sửa sang `enterText`). ⚠ QA tay mới chạy **smoke test một phần** qua emulator + adb (xác nhận trực quan UI đúng) — chưa chạy hết 10 bước quickstart vì app có khóa PIN từ phiên trước chặn mở lại sau khi restart tiến trình.

## [2026-09-15] implement | Tier A đọc ảnh trực tiếp qua prompt đa phương thức (PBI 37) — nguồn .specify/specs/37
- Cập nhật [[Giao dịch]] §"Chọn engine & fallback (chặng 2)": thêm dòng chốt cơ chế mới — **Tier A không còn nhét văn bản OCR vào prompt**, gửi thẳng ảnh đã tiền xử lý cho Gemini Nano đọc trực tiếp (đa phương thức); OCR **vẫn chạy song song** nhưng chỉ còn dùng cho fallback bộ luật khi AI lỗi/timeout và đối chiếu số tiền ảnh ngân hàng (không đổi 2 cơ chế đó). Tier B (Gemma 3n) giữ nguyên hành vi cũ (vẫn dựa trên văn bản OCR).
- Cập nhật [[Stack kỹ thuật]] dòng `com.google.mlkit:genai-prompt` + đoạn kênh native `sora_thu_chi/device_probe`: `genAiGenerate` nhận thêm argument `image` (bytes) tuỳ chọn — có ảnh ⇒ dựng `GenerateContentRequest` với `ImagePart`+`TextPart` (API đã xác nhận tồn tại trong bản `1.0.0-beta4` đang dùng, không nâng version); không có ⇒ giữ nguyên `generateContent(prompt)` text thuần (đường Tier B/test cũ).
- Kỹ thuật: không đổi schema DB (giữ v9-tương-đương từ PBI 32); đổi chữ ký `ScanLlm.generate`/`ReceiptExtractor.extract` thêm tham số ảnh tuỳ chọn + `ScanLlm.supportsImage` (mặc định `false`). **1330 test pass** (thêm ~15 test mới: `llm_extractor_test.dart` nhánh ảnh + fallback, `gemini_nano_llm_test.dart` mới, `scan_processing_screen_test.dart` xác nhận truyền đúng bytes), `flutter analyze` sạch. ⚠ Còn **T021 QA tay emulator/máy thật hỗ trợ Tier A** (quickstart.md mục 2) chưa chạy — cần máy thật để xác nhận SC-001 (độ chính xác đọc ảnh so với bản cũ).

## [2026-09-10] implement | Màn "Ngôn ngữ" (Tiếng Việt / English) + dịch toàn bộ nhãn tĩnh (PBI 19) — nguồn .specify/specs/19
- Cập nhật [[Hồ sơ & Bảo mật]] thêm § **"Màn 'Ngôn ngữ' (03) — đã triển khai PBI 19"**: hàng "Ngôn ngữ" màn `01` **bỏ no-op** → push màn con `03` (`LanguageScreen`, `SubPageScaffold` teal back, không bottom nav) — **2 card đúng thứ tự Tiếng Việt → English**: vòng tròn mã `VI`/`EN` + endonym đậm + dòng phụ tên ngôn ngữ kia + radio tự dựng; **4 chuỗi tên/dòng phụ là hằng số cố định** (R8, chốt theo mockup `03`), chỉ tiêu đề app bar + ghi chú chân màn đi qua `.tr`. Sửa claim cũ "còn 4 hàng no-op" → **còn 3** (Định dạng & Tiền tệ, Tìm kiếm toàn cục, Quản lý Tag). Phạm vi PBI = dịch **toàn bộ** nhãn tĩnh của mọi màn hiện có (không dịch dần).
- Ghi chốt nghiệp vụ: **2 ngôn ngữ `vi` (mặc định) + `en`**, *không* có "theo ngôn ngữ hệ thống" (khác theme); lưu **1 row `('locale','vi'|'en')`** trong `AppSettings` v5 — **không migration, không seed**; key vắng = `vi`, chuỗi lạ ⇒ `vi` (parse an toàn); ghi chỉ upsert key `locale` (**độc lập** `themeMode`/2 công tắc). **Tên ví KHÔNG dịch** kể cả ví mẫu (chốt R7 — ví là dữ liệu, không có cờ "do app tạo"); **định dạng ngày/số không đổi** theo ngôn ngữ (§2.2).
- Cập nhật [[Stack kỹ thuật]] viết lại § **"Đa ngôn ngữ (i18n) — rule & cơ chế đã thi công"**: **khóa dịch LÀ chuỗi tiếng Việt đang hiển thị**; bản đồ `SoraTranslations` chỉ có **nhánh `'en'`**, không nhánh `'vi'`, không `fallbackLocale` (thiếu nhánh/khóa hoặc `Get.locale == null` ⇒ `.tr` trả khóa — nhờ vậy 436 test cũ giữ nguyên). Root: `GetMaterialApp(translations, locale, supportedLocales [vi,en], 3 delegate)` + **`flutter_localizations` (chỉ SDK, không package bên thứ ba)**. **Bài học đo được:** hạ `locale:` là **không đủ** — route đang mở không tự rebuild (probe `LEAF BUILDS before=1 after=1`), nên `LocaleController.setLocale` gọi `Get.updateLocale()` (reassemble toàn cây, giữ state + stack) và gọi **bắn-và-quên** (`await` làm test treo). Seam pattern PBI 17/18: `locale_prefs.dart` (hằng khóa + parse + `kLanguageOptions`) + `LocaleStore`/`DriftLocaleStore`/`ensureLocale*`/`FakeLocaleStore` + `SoraApp.localeStore?`. Thêm rule: test `sora_translations_test.dart` quét literal trước `.tr/.trArgs/.trParams` — bắt lỗi *thiếu khóa*, **không** bắt lỗi *quên gắn `.tr`* (phải QA tay).
- Cập nhật [[Danh mục]] thêm § **"Tên danh mục theo ngôn ngữ (PBI 19 — rule)"**: dịch **ở tầng hiển thị** — 15 tên `CategorySource` thành khóa `'en'` (`Ăn uống`→`Food & Drink`, `Khác`→`Other`…), chỗ render gọi `category.name.tr`; khớp khóa ⇒ dịch, **đã đổi tên/tự tạo ⇒ nguyên văn** (tự động thoả "không dịch dữ liệu người dùng"). **DB không đổi**, không cột `slug`, schema v4 giữ nguyên; snapshot `transactions.category` dùng cùng khóa; **không** dịch chỗ dùng làm `ValueKey`.
- Cập nhật [[Ví & Tài khoản]] thêm § **"Ngôn ngữ hiển thị (rule, PBI 19)"**: tên ví không dịch (kể cả ví mẫu) nhưng **nhãn loại ví có** dịch; hậu tố `(đã ẩn)` dịch riêng rồi ghép; số dư giữ `42.500.000 đ`.
- Cập nhật [[Lộ trình phát triển]]: ghi chú PBI 19 xong; **đóng phần "đa ngôn ngữ" của quyết định mở #7** (danh sách ngôn ngữ đã chốt `vi` + `en`); còn màn con `04`–`07`, hiệu ứng 2 công tắc, theme màu tùy biến, tablet, ngôn ngữ thứ ba/gói tải ngoài.
- Kỹ thuật: schema drift **giữ v5** (chỉ thêm row runtime, không migration, không `build_runner`); dịch ~36 file lib qua `.tr`/`trParams`. **472 test pass** (36 test mới của PBI 19), `flutter analyze` **sạch**; **1 test đỏ có sẵn từ trước, không liên quan** (`transactions_dao_test` — đối chiếu `git stash`: đỏ cả trên HEAD sạch). ⚠ Còn **T029 QA emulator thủ công** (quickstart nhóm A–L, đặc biệt nhóm K quét sót nhãn tĩnh — cách duy nhất bắt lỗi "quên gắn `.tr`") chưa chạy.

## [2026-09-10] implement | Màn "Giao diện" Sáng/Tối/Theo hệ thống + dark mode toàn app (PBI 18) — nguồn .specify/specs/18
- Cập nhật [[Hồ sơ & Bảo mật]] thêm § **"Màn 'Giao diện' (02) — đã triển khai PBI 18"**: hàng "Giao diện" màn `01` **bỏ no-op** → push màn con `02`; màn `02` (`ThemeScreen`) `SubPageScaffold` teal back + tiêu đề, không bottom nav, **3 card đúng thứ tự Sáng – Tối – Theo hệ thống** (vòng nền nhạt + icon minh họa, tên đậm + dòng phụ, radio **tự dựng** không `RadioListTile`; hàng chọn tô nền nhạt + vòng icon viền teal; ghi chú chân màn nguyên văn svg). Sửa claim cũ "5 hàng no-op" → **còn 4** (Ngôn ngữ, Định dạng & Tiền tệ, Tìm kiếm toàn cục, Quản lý Tag).
- Ghi chốt nghiệp vụ: 3 lựa chọn ánh xạ thẳng **`ThemeMode` Flutter** (không tạo enum riêng); lưu **1 row `('themeMode','light'|'dark'|'system')`** trong `AppSettings` v5 — **không migration, không seed**, row ghi write-through khi chọn; **key vắng = "Theo hệ thống"**, chuỗi lạ cũng về `system` (parse an toàn); nhãn thống nhất `system`→**"Theo hệ thống"** (màn `01` bỏ chữ cũ "Hệ thống"). Hàng "Giao diện" màn `01` trailing **reactive** → đổi ở màn `02` rồi back vẫn đúng.
- Cập nhật [[Design system]] thêm § **"Giao diện Sáng / Tối / Theo hệ thống"**: bộ đôi theme qua **`ThemeExtension<SoraColors>`** (không thêm dependency); nguyên tắc tách token **bất biến** (`AppColors`: teal fill `#0F6E56`, trắng **on-brand**, coral fill, bảng màu danh mục/ví, avatar) vs **đổi theo theme** (`SoraColors`: nền/card/chữ/kẻ ngang/dot/vòng nền nhạt/glyph teal-coral trên nền trung tính) + **bảng token light↔dark** đầy đủ (nền `#121212`, surface `#1E1E1E`, card `#242420`, chữ `#F2F2F0`/`#A8A8A3`, kẻ `#3A3A36`, glyph teal `#3FA98A` / coral `#E8734C`). Rule nhấn: **`white` 2 vai trò** (nền → token đổi; trên teal → giữ trắng) — VD ô tìm kiếm pill màn `05` giữ "đảo sáng cố định"; **light giữ nguyên giá trị** nên giao diện sáng không đổi. Phạm vi: mọi màn/chrome + màn PIN/boot, **trừ** ảnh hóa đơn/avatar, nội dung người nhập, màu nhận diện danh mục/ví. Cập nhật §Triển khai trong code: token màu nay **tách 2 file**.
- Cập nhật [[Stack kỹ thuật]] thêm § **"Giao diện Sáng/Tối — cơ chế"**: `ThemeController` (GetX, `Rx<ThemeMode>`) đăng ký gốc `SoraApp` + `GetMaterialApp(theme/darkTheme/themeMode)` → **đổi ngay toàn cây, không restart** — **lệch gợi ý doc §1.2** dùng `Get.changeThemeMode()` (lý do: tránh cơ chế ngầm khó bơm seam test). "Theo hệ thống" dựa sẵn `ThemeMode.system` của Material, **không** tự viết `didChangePlatformBrightness`. Nạp lúc boot mặc định `system`, cổng boot nền trống nên chấp nhận nháy khung đầu. Seam `ThemeStore`/`DriftThemeStore`/`ensureThemeStore()`/`FakeThemeStore` + `SoraApp.themeStore?` (pattern PBI 17); ghi nối đuôi.
- Cập nhật [[Lộ trình phát triển]]: ghi chú PBI 18 xong + đóng phần "Light/Dark mode" của **quyết định mở #7** (mặc định chưa chọn = "Theo hệ thống"); còn màn con `03`–`07`, hiệu ứng 2 công tắc, theme màu tùy biến, tablet.
- Schema drift **giữ v5** (chỉ thêm row runtime, **không migration, không `build_runner`**). Refactor token màu ~26 file widget. 436 test (27 mới của PBI 18) — `flutter analyze` **sạch**; **1 test đỏ có sẵn từ trước, không liên quan** (`transactions_dao_test` "addTransaction: income/expense…" — đã đối chiếu bằng `git stash`: đỏ cả trên HEAD sạch). QA emulator quickstart A–G **pass** (user kiểm thủ công 2026-09-10).

## [2026-09-06] implement | Màn Tiện ích & Cá nhân hóa — danh sách (PBI 17) — nguồn .specify/specs/17
- Cập nhật [[Hồ sơ & Bảo mật]] thêm § **"Màn Tiện ích & Cá nhân hóa (danh sách) — đã triển khai PBI 17"**: entry Cài đặt nhóm KHÁC → "Tiện ích & Cá nhân hóa" (dưới "Danh mục"), `SubPageScaffold` teal back, không bottom nav; 3 nhóm/8 hàng mockup `01` (HIỂN THỊ / TRẢI NGHIỆM / DỮ LIỆU & TÌM KIẾM; hàng = vòng `tealLightBg` + icon teal + tên + dòng phụ + trailing). Ghi rõ: **2 công tắc thật** (Ẩn số dư tắt / Máy tính bật) nhớ trạng thái bằng bảng drift key-value **`AppSettings` schema v5** (key `hideBalance`/`amountCalculatorEnabled`, key vắng = mặc định domain, row ghi write-through khi bật/tắt); hàng Widget = switch "bật" câm + dialog hướng dẫn ghim theo nền tảng (**không lưu**); 5 hàng điều hướng no-op chờ màn con `02`–`07`; hàng Quản lý Tag dòng phụ **mô tả** không số/tag giả (SC-008). Cấu trúc code: domain `UtilitiesPrefs` + seam `UtilitiesStore`/`DriftUtilitiesStore` + `ensureUtilitiesStore()` (GetX singleton) + `FakeUtilitiesStore`. **Chưa kéo hiệu ứng công tắc** (che số dư/đổi bàn phím = PBI sau).
- Cập nhật §Bảo mật mở rộng (Privacy mode): công tắc "Ẩn số dư" **đã lưu được** (PBI 17), hiệu ứng che số tiền vẫn PBI sau.
- Cập nhật [[Lộ trình phát triển]]: ghi chú giai đoạn — module Tiện ích màn `01` **đã MVP (PBI 17)**; quyết định #6/#7 ghi rõ còn màn con `02`–`07` + hiệu ứng công tắc chưa gắn GĐ; hàng Widget màn hình chính là hướng dẫn ghim (chưa có widget quick-add thật).
- Schema drift **v4 → v5** (bảng `AppSettings`, migration `from < 5` thuần tạo bảng, **không seed** — R8); chạy `build_runner` đúng 1 lần. PBI cài đặt sau chỉ **thêm row**, không thêm migration. 409 test pass; QA emulator quickstart bước 1–9 **pass** (user kiểm thủ công 2026-09-06).

## [2026-09-06] implement | Màn sắp xếp danh mục (PBI 16) — nguồn .specify/specs/16
- Cập nhật [[Danh mục]] §Sắp xếp & hiển thị + §Màn hình + thêm §Màn `04`: `04-sap-xep-danh-muc.svg` ✅ **đã triển khai** (`CategorySortScreen`). Entry = màn `01` icon **"Sắp xếp"** (no-op PBI 13 → kích hoạt) mở theo **đúng tab** (`initialType`); `SubPageScaffold` app bar teal back + tiêu đề "Sắp xếp danh mục" + nút **"Xong"**; `ReorderableListView.builder` liệt kê **cha cấp 1 đúng loại** — dòng `CategoryRow` + **tay cầm trái** (`ReorderableDragStartListener`, kéo chỉ từ tay cầm), `showChevron: false`; ẩn/hệ thống vẫn hiện + sắp được.
- Ghi quyết định: **ghi ngay khi thả** qua seam **`reorderCategories(orderedIds)`** (thêm vào "4 seam ghi" — đánh `sort_order = 0..n−1` cho đủ cha cấp 1 đúng loại trong **1 transaction**; con cấp 2 & loại kia bất biến — thứ tự nhóm con giữ nguyên); helper thuần **`moveCategoryAt`** (`core/category/category_sort.dart`) theo `ReorderableListView.onReorderItem` (SDK mới đã trừ `newIndex` khi kéo xuống — **lệch** R7 research vốn mô tả `onReorder` trừ thủ công); "Xong"/back pop không xác nhận (đã ghi khi thả), về màn `01` reload lặng → thứ tự mới hiện ngay. `CategoryRow` +2 param optional `leading`/`showChevron` (default giữ → màn `01`/`03` không đổi). Không đổi schema drift v4, không `build_runner`.
- Cập nhật [[Lộ trình phát triển]] ghi chú MVP: module Danh mục xong `01`–`04`; còn xóa/gộp + sắp con trong nhóm. 386 test pass; QA emulator quickstart A–C core pass (mở từ icon đúng mockup, kéo–thả ghi ngay, "Xong"/back về đúng tab + mở lại thứ tự bền vững; B/D/E/F/G phủ bằng widget test).

## [2026-09-06] implement | Màn danh sách danh mục con (PBI 15) — nguồn .specify/specs/15
- Cập nhật [[Danh mục]] §Màn hình: `03` ✅ **đã triển khai** (`CategoryChildListScreen`) — mô tả: tự dựng `Scaffold`+`AppBar` teal (tiêu đề 2 dòng chạm được = **tên cha + "Danh mục con"**, `FittedBox`+`InkWell` giữa back và "+"); list chỉ **con trực tiếp cùng loại** của cha đang xem theo `sortOrder` ổn định (dòng `CategoryRow`, không dòng phụ đếm con), con ẩn vẫn hiện đúng vị trí + mờ + "Đã ẩn", cuối list hàng **"+ Thêm danh mục con"** (`Expanded` chống tràn cỡ chữ lớn); empty phòng thủ giữ 2 điểm thêm; không bottom nav/số tiền. Nạp **1 loại** khi mở + cha hiển thị **derive** từ list; sau **mỗi pop form reload lặng**.
- Ghi quyết định: **vùng tiêu đề = điểm sửa danh mục cha có con** (loại khóa + ô cha không mở sheet — PBI 14 để lại); chạm con → Sửa con; thêm nhanh con preset cha qua param **`initialParentId`** ở `02` (Sửa `category.parentId` vẫn thắng; không truyền = hành vi cũ); con **đổi cha rời nhóm cũ**; màn `01` nối dòng CÓ con → push màn `03` + **reload lặng 2 loại** khi về (số con/tên cha phản ánh, đúng tab). Dòng danh mục trích thành **`CategoryRow`** (`core/widgets/category_row.dart`) dùng chung `01`/`03`. Không đổi schema drift v4, không `build_runner`.
- Cập nhật [[Lộ trình phát triển]] ghi chú MVP: module Danh mục xong `01`+`02`+`03`; còn `04` (sắp xếp) + xóa/gộp. 369 test pass; QA emulator quickstart A–H **pass** (thủ công, giới hạn seed: chỉ "Ăn uống" có con — cha loại khác tự tạo qua form).

## [2026-09-05] implement | Màn thêm/sửa danh mục (PBI 14) — nguồn .specify/specs/14
- Cập nhật [[Danh mục]] §Màn hình: `02` ✅ **đã triển khai** (`CategoryFormScreen`) — Thêm: loại mặc định theo tab lúc FAB, đổi được; Sửa: prefill 100% + luật khóa (đổi loại chỉ khi gốc sạch `canChangeType` = chưa gd + không con + đang gốc; danh mục **có con** không đổi cha/loại — phải luôn gốc giữ cây 2 cấp; con cùng loại cha). Tên chặn trống/trùng nhóm `(type, parentId)` **gồm bản ẩn** (bỏ chính nó khi Sửa). Xếp **cuối nhóm** khi Thêm/đổi nhóm (cùng nhóm giữ `sortOrder`); switch ẩn (Thêm tắt / Sửa theo `isHidden`); cờ `_saving` chặn lưu trùng; back trước lưu không ghi. `03` (DS con, entry dòng CÓ con) + `04` (sắp xếp) vẫn PBI sau.
- Ghi **3 seam ghi/đếm**: `insertCategory`/`updateCategory` (ghi literal, `isSystem` Sửa giữ / Thêm false, không tự validate/tính order) + `categoryHasTransactions` (nguồn khóa loại) — drift **v4 giữ nguyên**, không `build_runner`. Module thuần `category_form.dart` + presets icon/màu `category_presets.dart` (15 khóa + 13 ARGB = 5 màu seed + 8 token `AppColors.category*`; bất biến test seed ⊆ palette). Màn `01` nối điểm vào FAB → Thêm / dòng không con → Sửa + **refresh lặng 2 loại** sau pop.
- Cập nhật [[Lộ trình phát triển]] ghi chú MVP: module Danh mục xong `01` + `02`; còn `03`/`04` + xóa/gộp. 353 test pass; QA emulator quickstart A–I pass (entry sửa con/có con chưa mở tay — phủ widget test pump trực tiếp). Không đổi schema drift v4.

## [2026-09-05] implement | Màn danh sách danh mục (PBI 13) + sync schema v4 Danh mục (backlog PBI 11) — nguồn .specify/specs/13, /11
- Cập nhật [[Danh mục]] §Mô hình: sửa claim lệch (wiki cũ ghi `id(UUID)/created_at/updated_at`) → đúng drift schema **v4**: `categories` `id(int autoincrement), name(≤30), type(income|expense), icon, color(ARGB), parent_id(null=cha), sort_order, is_system, is_hidden`; giao dịch tham chiếu `transactions.category_id` (nullable) + `category` text **snapshot** tên. **Backlog PBI 11 chưa sync giờ đã ghi** (trước PBI 13 mới chạm bảng này).
- Cập nhật §Seed data: sửa thiếu "Khác" (nay đủ **4 cha thu** gồm Khác + 8 cha chi + 3 con Ăn uống), khớp `CategorySource.all`.
- Thêm §Màn `01` + đổi bảng Màn hình sang trạng thái: `01` ✅ **đã triển khai** (PBI 13 `CategoryListScreen`), `02/03/04` ⏳ PBI sau. Mô tả: entry Cài đặt nhóm KHÁC sau "Quản lý ví"; StatefulWidget nạp **2 loại 1 lúc** + chuyển tab lọc local (không GetX); hiện **cả danh mục ẩn** + nhãn "Đã ẩn"/mờ; "N danh mục con" **gồm con ẩn**; empty thật vs **ẩn-toàn-bộ không empty giả**; FAB/dòng/icon "Sắp xếp" no-op có ripple chờ `02/03/04`; mỗi lần vào reload.
- Ghi 2 seam đọc phân vai: `categories({type})` (đang hoạt động, picker PBI 11) vs `categoriesIncludingHidden({type})` (gồm ẩn, màn quản lý PBI 13) + module thuần `category_list.dart` (`topLevelParents`/`childrenOf` sort ổn định).
- Cập nhật [[Lộ trình phát triển]] ghi chú MVP: module Danh mục màn `01` xong, còn `02/03/04`. Không đổi schema drift v4 (PBI 13 chỉ đọc, không `build_runner`); 314 test pass; QA emulator A–F pass (G qua widget test — chưa tạo ẩn bằng UI).

## [2026-09-05] implement | Tìm kiếm & Lọc giao dịch (PBI 12) — nguồn .specify/specs/12
- Cập nhật [[Giao dịch]] §Tìm/lọc/sắp xếp: mô tả **đã triển khai** màn `05` (chip 4 loại, 5 bộ lọc nâng cao, bản nháp + Áp dụng/back giữ tập cũ, bỏ dấu tiếng Việt tự viết, AND, summary `N/Tổng` với transfer/adjustment ngoài tổng, group-keep transfer khi lọc ví).
- Ghi 2 lựa chọn hiển thị **user đã chốt** (R4 sort tiền → list phẳng; R5 đang lọc → ẩn card tháng, thay thanh `N kết quả · Tổng` + Bỏ lọc) + kỹ thuật: lọc trong bộ nhớ (cache mỗi load), filter sống `TransactionController` bền qua tab, không đổi schema.
- ⚠ `docs/transaction` chưa chép phần triển khai này (raw chỉ đọc — nghiệp vụ tinh chỉnh từ mô tả đặc tả); PBI 12 vẫn còn T011 QA emulator thủ công.

## [2026-09-03] rule | Khóa app = mã PIN bắt buộc lần đầu (PBI 3 đã implement) — nguồn .specify/specs/3
- Cập nhật [[Hồ sơ & Bảo mật]] §Khóa app: chốt **PIN bắt buộc ngay lần đầu mở app** + luôn có hiệu lực (lệch `docs/auth §2.1` "bật/tắt tùy chọn" — quyết định user, đã chốt) — ghi chi tiết: thiết lập 2 lần khớp, PIN 4 số cố định, PIN yếu cảnh báo-cho phép, hash SHA-256 có muối (`crypto`) + `lock_state` JSON trong flutter_secure_storage, mở khóa mỗi boot/resume về đúng màn, chống dò ladder 30s→1p→5p→15p giữ khi thoát app, sinh trắc & đổi/quên PIN & độ dài 4/6 ngoài đợt. Numpad hàng cuối trái để trống (chưa sinh trắc).
- Cập nhật [[Lộ trình phát triển]]: ghi chú MVP — khóa PIN đã xong PBI 3 (sinh trắc để sau).
- Cập nhật [[Stack kỹ thuật]]: thêm `crypto` (SHA-256) + mô tả key secure storage của khóa PIN.
- ⚠ `docs/auth/chi-tiet-quan-ly-tai-khoan-nguoi-dung.md §2.1` (và doc tính năng tổng phần lần đầu/Onboarding) **chưa đồng bộ** với quyết định bắt buộc — cần cập nhật tài liệu nghiệp vụ theo (raw sources chỉ đọc, sửa tay ngoài wiki).

## [2026-09-03] bootstrap | Dựng wiki từ toàn bộ docs/ (7 doc markdown + mockup SVG)
- Tạo schema CLAUDE.md + index + 9 page (5 entity, 4 concept).
- Raw = `../docs/` trỏ thẳng, không copy.
- Gom các quyết định mở vào [[Lộ trình phát triển]].

## [2026-09-03] rule | Cấu hình style tập trung 1 nơi khi triển khai app
- Thêm mục "Triển khai trong code (rule)" vào [[Design system]]: màu/size/typography config tại 1 nơi (AppTheme + token), widget không nhúng hex/số cứng rải rác. Quyết định của user (chưa nằm trong `docs/`).

## [2026-09-03] rule | Đa ngôn ngữ dùng GetX Translations
- Thêm mục "Đa ngôn ngữ (i18n) — rule" vào [[Stack kỹ thuật]]: chuỗi UI qua key i18n từ lúc dựng UI, cấu hình root `GetMaterialApp(translations/locale/fallbackLocale)`, mặc định + fallback tiếng Việt, không dịch data user, tách i18n khỏi format số tiền/ngày.
- Mở quyết định #7 trong [[Lộ trình phát triển]]: nhóm §12 Tiện ích chưa gắn GĐ; đa ngôn ngữ cần chốt danh sách ngôn ngữ. Quyết định của user (cơ chế GetX Translations) + nguồn gốc doc §12.

## [2026-09-12] implement | Ngân sách phạm vi 2a — PBI 20 (màn `01` Tổng quan + `02` Thêm/Sửa)
- Cập nhật [[Ngân sách]]: viết lại theo **trạng thái đã triển khai** — bảng drift **`budgets` schema v6** chỉ **6 cột** (`id, category_id, amount, period, is_recurring, start_date`), migration thuần tạo bảng **không seed**; 10 cột của doc §3.1 (`scope/wallet_ids/currency/period_start_rule/end_date/alert_thresholds/rollover_unused/status/created_at/updated_at`) **chưa có hiệu lực**.
- Chốt kỹ thuật đáng nhớ: **bỏ hẳn bảng `budget_period_snapshots`** — "đã chi / % / còn lại / ngày còn lại" là đại lượng **tính lại mỗi lần nạp màn** (doc §9 cho phép khi dữ liệu nhỏ) ⇒ sửa `amount` không thể hồi tố kỳ trước, giao dịch Chi bị sửa/xoá tự khớp lại.
- Rule ghi rõ: chỉ **Chi** (Thu/transfer loại); khớp `category_id` = chính nó **hoặc con trực tiếp** (không cháu, không suy đoán theo tên cho dòng `< v4`); `transaction_date ∈ [start, end)` **end độc quyền**; **Tuần bắt đầu Thứ Hai**, Tháng/Năm dương lịch; **chồng lấn** = cùng `category_id` + cùng `period` + khoảng **hiệu lực** giao nhau (lặp lại → ∞, không lặp lại → đúng kỳ chứa `start_date`).
- Trạng thái `active/ended/invalid` **suy ra**, không lưu cột; `ended`/`invalid` **không** vào thẻ tổng; danh sách sắp **giảm dần theo %**; bộ chọn kỳ chỉ điều khiển dòng chu kỳ Tháng (Tuần/Năm giữ kỳ riêng + nhãn chu kỳ).
- Ghi nhận ngoại lệ có lý do: màn `01` là **màn cấp tab** (điểm vào từ tab Báo cáo) nên **có bottom nav thật** + FAB dùng chung + callback đổi tab từ `AppShell`; màn `02` vẫn là sub-page không bottom nav. Seam: mở rộng `WalletRepository` 3 method + module thuần `core/budget/*`, **không** thêm repository/controller.
- Giới hạn đã biết ghi vào page: picker danh mục tái dùng (PBI 11) **không chọn được cha có con** ⇒ UI chưa tạo được ngân sách cho danh mục cha (luật gộp con vẫn đúng ở tầng tính toán).
- Cập nhật [[Design system]]: **đóng ⚠ mốc 80–99%** — thanh coral `#D85A30` `alpha 0.6` + % coral (giữ hệ 2 màu, không thêm token mới).
- Cập nhật [[Lộ trình phát triển]]: ghi PBI 20 xong (2a + màn `01`/`02`), **đóng quyết định mở #2**, còn lại 2b/2c + màn `03`.
- QA emulator A–N: phần lớn ĐẠT; bắt được **1 lỗi dịch** (hàng "Ngưỡng cảnh báo" màn `02` thiếu `.tr` ⇒ ở English sót "80% và 100%") — đã sửa + thêm khẳng định chống hồi quy. Nhóm không tái hiện được trên emulator (E/F/L/N một phần) phủ bằng test tự động; chi tiết ở `.specify/specs/20/quickstart.md`.

## [2026-09-12] implement | Ngân sách màn `03` Chi tiết + 2c mức tối thiểu — PBI 21
- Cập nhật [[Ngân sách]]: **schema v7** — `budgets` thêm cột `is_archived` (bool default `false`), migration = `addColumn` có default (bọc `from >= 6 && from < 7` để không đụng nhánh `createTable` của DB cũ hơn), **không seed**. `budgets()` **giữ nguyên hợp đồng** (trả cả ngân sách đã lưu trữ) — **lọc ở tầng view duy nhất**: `buildBudgetOverview` bỏ qua dòng `isArchived` khỏi cả danh sách lẫn thẻ tổng.
- Ghi **màn `03` là điểm vào mới**: chạm dòng ở màn `01` mở Chi tiết (FR-001 thay hành vi cũ mở thẳng form `02`; form nay vào từ nút "Chỉnh sửa"). App bar **2 dòng** qua tham số `subtitle` mới của `SubPageScaffold` (additive, `toolbarHeight 68`) + nút đổi kỳ góc phải.
- Rule mới đáng nhớ: **một phép lọc duy nhất** `budgetPeriodTransactions()` — `budgetSpent` cộng lại chính danh sách đó, màn `03` hiển thị `take(5)` ⇒ tổng danh sách không thể lệch số "đã dùng" (SC-005); **đừng viết bộ lọc thứ hai**.
- Công thức ghi rõ + **⚠ lệch 1 ngày có chủ ý**: màn `03` tính ngày còn lại **gồm hôm nay** (`clamp(today − start + 1, 0, total)`, tháng 9 ngày 12 → **18 ngày**, khớp mockup `03`); màn `01` giữ `end − today` → 19. PBI 21 **không** đồng bộ màn `01` (ngoài phạm vi, có test riêng). Cảnh báo nhịp = `!ended && %dùng > %ngày`, **chỉ một băng**, ẩn khi kỳ đã kết thúc.
- Biểu đồ: `fl_chart` `BarChart` **dùng thật lần đầu**; cột "Dự kiến" = `amount` **hiện tại** cho **mọi** kỳ (sửa giới hạn → mốc đổi, số thực tế kỳ trước **không** hồi tố); luôn **3 kỳ gần nhất tính đến kỳ đang xem** (kỳ đang xem là cột cuối), không vượt quá kỳ chứa `start_date` ⇒ ngân sách 1–2 kỳ vẫn vẽ được. Bộ chọn kỳ chỉ liệt kê **từ kỳ `start_date` đến kỳ hiện tại**; không lặp lại ⇒ đúng 1 lựa chọn.
- **Lưu trữ một chiều, chưa có nơi xem lại** (giữ nguyên chốt 2026-09-12): chỉ ghi `is_archived = true` qua `updateBudget(copyWith)` — **không** thêm method repository, **không** đụng giao dịch nào. 2 nút "Chỉnh sửa"/"Lưu trữ" hiện ở **mọi kỳ** và **cả khi danh mục đã bị xoá**.
- Cập nhật [[Design system]]: cột "Dự kiến" một **màu trung tính** (`dotEmpty` alpha 0.5) cho mọi kỳ + **đường mốc nét đứt** `dashArray [3,3]`; cột "Thực tế" teal/coral theo kết quả kỳ; nhãn kỳ đang xem in đậm.
- Cập nhật [[Lộ trình phát triển]]: **2c đóng ở mức tối thiểu** (so sánh 3 kỳ + tốc độ tiêu tính từ dữ liệu, không snapshot); còn lại **2b** + phần còn lại của 2c (dự báo tới hết kỳ, băng "chi chậm hơn", ngưỡng cảnh báo tuỳ chỉnh) + một PBI riêng nếu muốn **xem lại ngân sách đã lưu trữ**.
- Cập nhật [[Stack kỹ thuật]]: `fl_chart` từ "khai báo nhưng chưa dùng" → **đã dùng thật** (BarChart màn `03`).
- Test: 597 xanh (thêm `test/budget_detail_test.dart` + `test/budget_detail_screen_test.dart`; sửa 4 file test cũ + `FakeWalletRepository`). **Bẫy `trParams`**: tham số **không** được chứa `%` (`'Vượt @p%'` phải truyền `p='107'`).
- ⚠ QA emulator **mới chạy một phần** (đường vàng + đổi kỳ + "Xem tất cả" khớp số); nhóm H/I/L/M và đối chiếu số tính tay (SC-003/SC-012) **còn lại cho người dùng** — chi tiết ở `.specify/specs/21/quickstart.md`. Trong đó **Lưu trữ chưa thao tác tay** vì nó làm thay đổi dữ liệu ngân sách trên emulator.

## [2026-09-12] qa | PBI 21 — QA tay emulator nhóm A–N ĐẠT
- Người dùng chạy trọn A–N trên emulator Android ⇒ đạt toàn bộ, gồm các nhóm Claude không tự chạy (H Chỉnh sửa, I Lưu trữ, L giao dịch thay đổi, M English/cỡ chữ/dark mode, K danh mục bị xóa) và đối chiếu số tính tay SC-003/SC-012. **T033 hoàn tất — PBI 21 xong trọn 35/35.** Chi tiết ở `.specify/specs/21/quickstart.md`.

## [2026-09-12] implement | Báo cáo màn `01` Tổng quan — PBI 22
- **Page mới [[Báo cáo]]**: module Báo cáo — màn `01` Tổng quan đã xong; `02` Chi tiết theo Danh mục / `03` So sánh Kỳ / `04` Xuất báo cáo còn lại cho PBI sau.
- **Không schema riêng**: chỉ **đọc** `transactions` + `categories` (schema giữ **v7**), **không** migration, **không** chạm `lib/data/db/*`, **không** `build_runner`. Mọi số liệu là **đại lượng tính toán**; **không** bảng tổng hợp/cache (`report_monthly_summary` mà doc §2.2/§7 gợi ý **đã bỏ** — lần thứ hai đi ngược đề xuất snapshot, sau PBI 21).
- **Rule mới đáng nhớ**: **một phép lọc duy nhất** `reportTotals(transactions, range)` loại `transfer` + `adjustment` — 2 số tổng, 6 cột, vòng tròn phân bổ và top đều đi qua nó (SC-003/SC-004); **đừng viết bộ lọc thứ hai**. Kỳ **chỉ có chuyển khoản** = kỳ rỗng.
- **Mốc kỳ**: nửa mở `[start, end)`, **Tuần bắt đầu Thứ Hai**, Tháng/Năm **số học ngày** (không cộng `Duration`); giao dịch đúng ngày `end` **không** thuộc kỳ; **giao dịch ngày tương lai trong kỳ vẫn tính**. Biểu đồ = **6 đơn vị liền trước kết thúc ở kỳ đang chọn**.
- **Gộp danh mục cha + nhóm "Khác"**: khóa nhóm `parentId ?? id`, tên/icon/màu lấy từ **cha**; top 5 + "Khác" (phần ngoài top 5 **và** tiền chi `categoryId == null`) xếp **cuối**, chỉ hiện khi có dư; danh mục **ẩn vẫn tính**; con trỏ tới cha đã xoá ⇒ nhóm theo chính nó. **Chia % theo phần dư lớn nhất** (`percentSplit`) ⇒ `Σ%` = **đúng 100**. Đồng hạng sắp theo **tên đã bỏ dấu** (`normalizeSearch`) — so code-unit thuần sẽ xếp "Nhà" trước "Ăn".
- **Trạng thái rỗng theo KỲ ĐANG CHỌN** (không xét cả cửa sổ 6 đơn vị): không thu/chi ⇒ **cả 3 thẻ** "Chưa có giao dịch nào trong kỳ này"; **chỉ có Thu** ⇒ biểu đồ **vẫn vẽ đủ 6 đơn vị**, riêng thẻ phân bổ + thẻ top "Chưa có chi tiêu nào trong kỳ này".
- **Drill-down**: chạm một danh mục (lát cắt **hoặc** dòng chú giải) → đặt `TxnSearchFilter` (Chi + `dateStart = range.start` + `dateEnd = range.end − 1 ngày` + `categoryIds = {id cha}` + sort ngày mới nhất) rồi `onSelectTab(1)` — màn Báo cáo **là** tab nên **không** `popUntil`. Bộ lọc PBI 12 tự mở rộng cha → con nên tổng khớp. Nhóm **"Khác" không có đích** ⇒ không chạm được.
- **Nạp lại**: `AppShell._onTabSelected` gọi `load()` khi chọn tab 2 (bám khuôn tab Giao dịch) + `_openAddTransaction` nạp lại controller báo cáo **khi** đang đứng ở tab Báo cáo (FAB hiện trên mọi tab — SC-008). Đổi kỳ **không** đọc DB (tính lại từ RAM — SC-007).
- **Kiểu dùng chung**: `DateRange` (nửa mở) **tách sang `lib/core/date_range.dart`**, `budget_view.dart` **re-export** ⇒ **0 import phải sửa** ở module Ngân sách.
- Cập nhật [[Design system]]: **token mới `SoraColors.chartPalette`** (6 màu **đổi theo theme**, phần tử cuối cho "Khác") — bảng màu **định tính** của vòng tròn theo doc §5, **tuyệt đối không coral** (coral = chi tiêu/cảnh báo), gán cố định **theo hạng** chứ không lấy `Category.color`; cột biểu đồ + 2 số tổng thì ngược lại vẫn dùng token teal/coral.
- Cập nhật [[Lộ trình phát triển]]: MVP "Báo cáo cơ bản" **màn `01` xong**; liệt kê phần còn lại (3 màn + bộ lọc nâng cao, trong đó **nút biểu tượng lịch mockup `01` chưa hiển thị**) và giới hạn **chưa quy đổi đa tiền tệ**.
- Cập nhật [[Stack kỹ thuật]]: `fl_chart` nay **đã dùng cả `BarChart` lẫn `PieChart`**; ghi thêm ghi chú "đã bỏ snapshot/cache 2 lần, chỉ thêm khi đo thấy chậm".
- **Bẫy đã gặp khi thi công**: (1) `ReportScreen` build sẵn offstage trong `IndexedStack` ⇒ đừng render `CircularProgressIndicator` khi **chưa** nạp (vòng lặp animation vô hạn làm `pumpAndSettle` timeout ở mọi test pump shell) — chỉ quay spinner khi `isLoading`; (2) test pump cả app (`pin_flow_test`) **phải** đăng ký `FakeWalletRepository` vì tab Báo cáo nay đọc DB khi chọn tab, thiếu fake sẽ khởi tạo drift/sqlite native và treo.
- Test: **657 xanh** (+3 file mới `report_view/controller/screen_test.dart`; sửa `pin_flow_test`, `dark_theme_smoke_test`), `flutter analyze` sạch; **1 test đỏ có sẵn** ở `transactions_dao_test` (đã đỏ từ trước PBI 22, không liên quan).
- ⚠ **QA tay emulator nhóm A–K chưa chạy** (T039/T040 của `.specify/specs/22/tasks.md` còn mở) — cần người dùng chạy trên máy Android.

## [2026-09-12] qa | PBI 22 — QA tay emulator nhóm A–K ĐẠT
- Người dùng chạy trọn **A–K** trên emulator Android ⇒ **đạt toàn bộ, không lệch cần sửa**, gồm các nhóm Claude không tự chạy (C chạm cột hiện số tiền, E drill-down sang màn Giao dịch đã lọc, G đổi kỳ, H 3 thẻ rỗng, I dữ liệu đổi → số liệu đổi, J English/chế độ Tối, K cỡ chữ lớn/màn nhỏ/số hàng tỉ/tắt mạng). **T039/T040 hoàn tất — PBI 22 xong trọn 42/42.** Chi tiết ở `.specify/specs/22/quickstart.md`.

## [2026-09-12] implement | Báo cáo màn `02` Chi tiết theo danh mục — PBI 23
- **Màn `02` xong**: màn **con đè shell** (`SubPageScaffold`, app bar teal + back, **không** bottom nav/FAB), điểm vào **1 lần chạm** = liên kết **"Xem tất cả"** ở hàng tiêu đề thẻ Top màn `01` (chỉ hiện khi kỳ có chi tiêu). Bố cục: chip kỳ → vòng tròn 200×200 (nhãn giữa = `Tổng chi ngày/tuần/tháng/năm` + tổng chi, `Stack` phủ tâm + `FittedBox`) → `DANH MỤC (n)` → danh sách dòng (chấm màu, tên ellipsis, số tiền, `%`, thanh tiến độ **cùng màu chấm**) → dòng gợi ý.
- **Danh sách ĐẦY ĐỦ (khác màn `01` cắt top 5)**: dùng lại **đúng** `_breakdown` của màn `01`, chỉ bỏ `.take(5)` ⇒ dòng **"Khác"** ở màn `02` **chỉ** chứa tiền chi `categoryId == null` (không còn nuốt phần ngoài top 5). `Σ tiền = tổng chi`, `Σ% = 100` (`percentSplit` trên toàn bộ dòng). `rank` = chỉ số sau sắp xếp, "Khác" = `-1`.
- **Màu định tính LẶP CHU KỲ** (chốt 2026-09-12, 1B): `chartPalette[rank % 5]`, "Khác" = `chartPalette[5]` — danh mục thứ 6 trở đi **trùng màu** hạng trước, **không** thêm màu, **không** coral, **không** dùng `Category.color`. Xem [[Design system]].
- **Chip kỳ là NHÃN TĨNH** (chốt 2026-09-12, 2A): không bấm được, không mũi tên/menu chọn kỳ — khác biệt **cố ý** với mockup `02`; nhãn = `Ngày 12/09/2026` · `Tuần 07/09–13/09/2026` (**năm theo ngày cuối kỳ**) · `Tháng 9/2026` · `Năm 2026`, ghép danh từ kỳ **đã có bản dịch** với chuỗi ngày không phụ thuộc ngôn ngữ ⇒ **0 khóa dịch mới cho phần này**.
- **Số liệu dựng từ BẢN CHỤP RAM** (`ReportController.categoryDetail()`, trả `null` khi chưa nạp): màn `02` **không** đọc DB, không state mới, không cache ⇒ hai màn **không thể lệch số**, không thêm phép đọc nào. Đứng trong màn `02` số liệu **không tự nhảy** (kế thừa PBI 22).
- **Drill-down**: chạm **dòng** hoặc **lát cắt** → `TxnSearchFilter` (Chi + `dateStart = range.start` + `dateEnd = range.end − 1 ngày` + `categoryIds = {id cha}` + sort mới nhất) → **`popUntil(isFirst)`** (màn `02` là route đè shell, **khác** màn `01`) → `onSelectTab(1)`. Dòng **"Khác" `onTap: null`**.
- **Trạng thái rỗng TOÀN MÀN**: `total == 0` ⇒ chỉ thông điệp (`hasAnyTxn` ? "Chưa có chi tiêu nào trong kỳ này" : "Chưa có giao dịch nào trong kỳ này"), **không** vẽ vòng tròn/tiêu đề nhóm rỗng.
- **Không schema, không migration, không dependency**: giữ drift **v7**, tái dùng `fl_chart` `PieChart` sẵn có; **không** đụng `app_shell.dart`, `sora_colors.dart`, `pubspec.yaml`. Thêm **7 khóa dịch EN** (dùng lại `'Xem tất cả'`, `'Khác'`, `'Tổng chi'`, 4 danh từ kỳ, 2 thông điệp rỗng).
- Bàn giao test: sửa `report_view_test` / `report_screen_test` / `dark_theme_smoke_test` + mới `report_category_detail_screen_test`; `flutter analyze` sạch, **683 test xanh** (1 test đỏ **có sẵn** ở `transactions_dao_test`, không liên quan).

## [2026-09-13] implement | Quét hóa đơn AI — PBI 24 **chặng 1**
- **Chặng 1 xong** (Pha 1→5 + Polish, `.specify/specs/24/tasks.md`): FAB → **bottom sheet "Thêm giao dịch"** `scan-01` → màn chụp `scan-02` → xử lý `scan-03` (tiền xử lý ảnh → OCR ML Kit Latin offline → **bộ luật**) → xác nhận `scan-04` (6 trường sửa tay, **không tự lưu**) → lưu giao dịch `source = aiScan` + ảnh + 1 dòng `scan_sessions`. Kèm **kiểm tra cấu hình máy `scan-10`** (Tier A/B/C, lưu kết quả, tự đo lại sau 30 ngày) + **mục Cài đặt `scan-11`**.
- **Schema v8**: cột `transactions.source` (`textEnum<TxnSource>` default `manual`, migration chỉ `addColumn`, **không seed** — dòng cũ = `manual`) + bảng mới **`scan_sessions`** (`image_path`, `raw_text`, `parsed_json`, `engine`, `transaction_id` nullable, `created_at`). Ghi một lần quét = **một `db.transaction()`** (bù `balance` ví + insert transactions + insert scan_sessions). Màn `02` **không** đọc bảng này để tính số liệu.
- **Rule mới đáng nhớ**: **ảnh chỉ ghi vào kho ở nhánh lưu** (`<appDocuments>/receipts/<millis>.jpg`) — huỷ giữa luồng không để lại file; **không bao giờ tự lưu** giao dịch quét (SC-004); cảnh báo trùng chỉ **mềm** (±24h, cùng số tiền, không chặn); **Tier C không bao giờ chặn luồng**.
- **Bộ luật trích xuất**: số tiền ưu tiên dòng từ khoá **tổng** rồi mới tới **số lớn nhất cuối** (tránh lấy dòng mặt hàng); loại số thập phân `12.5` và số < 1000 không nhóm nghìn; ngày không đọc được ⇒ `now` + **độ tin cậy thấp**; danh mục gợi ý từ **từ điển tĩnh ~50 khoá** (bỏ dấu, khớp chuỗi con) → chỉ resolve vào danh mục **đang hoạt động**. Tiền xử lý ảnh thuần Dart: `bakeOrientation` → resize maxSide 2000 → contrast 1.15/saturation 0 → JPEG q88, chạy trong `compute`; **không crop theo biên** (R4 — crop đoán sai mất dòng tổng).
- **Lệch doc đã ghi nhận**: doc §7 xếp *gợi ý danh mục theo từ điển* vào GĐ2 — chặng 1 làm sớm (từ điển **tĩnh**, **không** có `merchant_category_mapping` học theo người dùng của doc §4); doc §4 gợi ý `scan_history` — thực tế dùng `scan_sessions` (chưa có màn Scan History). **Chặng 2** (Tier A/B: Gemini Nano qua AICore, Gemma 3n E2B tải ~1.8GB, `LlmExtractor` timeout 10s + fallback bộ luật, quản lý model) **chưa thi công** — cần thiết bị thật, emulator luôn Tier C; nút Tier A/B hiện **vô hiệu + chú thích "Chưa khả dụng trong bản này"**.
- **🪤 Bẫy build đã gặp (chặn `flutter build apk --release`)**: R8/minify fail `Missing class com.google.mlkit.vision.text.japanese/korean…` — plugin `google_mlkit_text_recognition` tham chiếu **cả** bộ nhận diện Nhật/Hàn/Trung trong nhánh `initialize` dù app chỉ đóng gói bộ Latin. Fix: **`android/app/proguard-rules.pro`** với `-dontwarn com.google.mlkit.vision.text.**` + khai `proguardFiles(...)` ở `buildTypes.release`. Trước fix, release build **fail**; debug không lộ vì không minify.
- **Kích thước APK đo được (T062)**: fat `app-release.apk` **96.0 MB**; split-per-abi arm64 **35.3 MB** / v7a **29.3 MB** / x86_64 **37.3 MB**. ML Kit đóng góp **~12.5 MB/ABI** (`libmlkit_google_ocr_pipeline.so` 11.1 MB arm64 + model assets 1.42 MB dùng chung) — **cao hơn dự kiến 4–6 MB** của rủi ro 2 trong plan, vì đóng gói nguyên pipeline OCR chứ không chỉ model Latin. Nên phát hành bằng **split-per-abi / appbundle**, không dùng fat APK.
- **Nền tảng**: iOS min **15.5** (Podfile + 3 chỗ `IPHONEOS_DEPLOYMENT_TARGET`) + 3 khóa quyền camera/thư viện/mic; Android `CAMERA` + `<uses-feature required="false">`; kênh native **`sora_thu_chi/device_probe`** (Android `DeviceProbeChannel.kt`, iOS `DeviceProbeChannel.swift`) — **thiếu đăng ký là màn `scan-10` treo ở "Đang kiểm tra…"**.
- Cập nhật wiki: [[Giao dịch]] (nguồn `aiScan`, `scan_sessions`, luồng quét, điểm vào sheet), [[Hồ sơ & Bảo mật]] (mục Cài đặt quét AI + màn `scan-10` + 4 key `AppSettings`), [[Design system]] (bottom sheet, **màn chụp nền tối cố định**, chỉ báo độ tin cậy teal/xám/coral), [[Stack kỹ thuật]] (5 package + seam/kênh native/schema v8 + proguard), [[Lộ trình phát triển]] (định vị PBI 24, chặng 2 còn lại), `index.md`, `log.md`.
- Test: **789 xanh** (+8 test mới ở smoke theme tối cho `scan-04`/`scan-10`, sửa `device_check_screen_test`), `flutter analyze` **sạch**; **1 test đỏ có sẵn** ở `transactions_dao_test` (nhãn danh mục seed — đã đỏ từ trước PBI 24, file không bị đụng).
- ⚠ **QA tay emulator nhóm A–L chưa chạy** (T044/T049 của `.specify/specs/24/tasks.md` còn mở) — cần người dùng chạy trên máy Android; chặng 2 (nhóm M/N) cần thiết bị thật đủ điều kiện Tier A/B.

## [2026-09-13] implement | Quét hóa đơn AI — PBI 24 **chặng 2** (Tier A/B)
- **Chặng 2 xong** (Pha 6, `.specify/specs/24/tasks.md` T052–T058): **Tier A** = Gemini Nano qua **ML Kit GenAI Prompt API** (`com.google.mlkit:genai-prompt:1.0.0-beta4`, model do AICore hệ thống quản lý — **không tải về app**); **Tier B** = **Gemma 3n E2B** `.litertlm` qua `flutter_gemma` 1.8.1 + `flutter_gemma_litertlm` 1.6.3. Cả hai đi qua **cùng seam `ReceiptExtractor`** (`LlmExtractor`), màn xác nhận không biết engine nào đã chạy.
- **Fallback là lưới an toàn (FR-011/SC-009)**: timeout **10 giây**, model ném lỗi, hoặc trả về không đọc được JSON ⇒ tự gọi **bộ luật** trong cùng seam → người dùng **luôn** ra màn xác nhận, không phải chụp lại, không thấy lỗi kỹ thuật; phiên quét ghi `engine = ruleBased`.
- **Rule mới đáng nhớ**: **Tier B chỉ chạy khi `modelBytes > 0`** (đã tải xong) — chưa tải thì `ScanSettings.effectiveEngine` tự rơi về Chế độ cơ bản thay vì gọi model không tồn tại. **Tải model thất bại/bị huỷ ⇒ KHÔNG bật AI** (FR-012), người dùng ở lại Chế độ cơ bản. LLM **không** trả vùng chữ ⇒ nhánh AI **không khoanh vùng** ảnh gốc ở màn xác nhận (nhánh hợp lệ của FR-029).
- **Kênh native dùng chung, không dựng kênh thứ hai**: `GenAiChannel.kt` cắm thêm method `genAiGenerate` vào chính `sora_thu_chi/device_probe` (R5/R18) — chạy trên `Dispatchers.Main` với `kotlinx-coroutines-android`. **minSdk Android nâng lên 26** (yêu cầu của GenAI Prompt API).
- **Gỡ trạng thái trung gian**: nút Tier A/B ở `scan-10` không còn "vô hiệu + Chưa khả dụng trong bản này"; Tier A bật ngay, Tier B có **tiến trình tải %** + lựa chọn "Dùng chế độ cơ bản" (huỷ), lỗi thì đổi thành "Thử lại". Cài đặt thêm 3 hàng: **Dung lượng model** (chỉ hiện khi đã tải) · **Xoá model** (về Chế độ cơ bản, giao dịch đã lưu không bị ảnh hưởng) · **Kiểm tra cập nhật model**.
- ⚠ **QUYẾT ĐỊNH MỞ — nguồn phân phối model Tier B**: repo HuggingFace đang trỏ (`kGemmaModelUrl` trong `lib/data/platform/gemma_model_manager.dart`) là **gated**, cần token ⇒ lượt tải sẽ thất bại và người dùng ở lại Chế độ cơ bản (an toàn, không vỡ luồng). Phải chốt nguồn công khai (tự host / repo không gated) **trước khi phát hành** Tier B.
- ⚠ **QA tay nhóm M/N (T058) CHƯA kiểm chứng được**: cần thiết bị thật đủ điều kiện (Tier A: Pixel 8+/S24+ có AICore; Tier B: tải/xoá model thật) — emulator luôn Tier C. Đã phủ bằng test tự động: chọn engine theo tier, fallback khi lỗi/timeout/JSON hỏng, tải thất bại ⇒ không bật AI, xoá model ⇒ về Chế độ cơ bản. **Còn phải QA trên máy thật**: AICore thật + `engine = geminiNano` + đo < 10s; tiến trình tải thật, ngắt mạng giữa chừng, dung lượng hiển thị đúng, xoá model giải phóng dung lượng.
- Cập nhật wiki: [[Giao dịch]] (chọn engine + fallback), [[Hồ sơ & Bảo mật]] (Tier A/B chọn chế độ, 3 hàng model, quyết định mở nguồn model), [[Stack kỹ thuật]] (2 nhóm package mới, seam `ScanModelManager`, minSdk 26), [[Lộ trình phát triển]] (chặng 2 xong, phần chưa QA được, quyết định mở), `index.md`, `log.md`.
- Test: **809 xanh** (thêm `llm_extractor_test.dart` 11 ca + ca mới ở `device_check_screen_test`/`settings_screen_test`/`scan_settings_test`), `flutter analyze` **sạch**, `flutter build apk --debug` **pass** (Kotlin GenAI + flutter_gemma biên dịch được); **1 test đỏ có sẵn** ở `transactions_dao_test` (nhãn danh mục seed — đỏ từ trước PBI 24, file không bị đụng).
- **QA tay emulator nhóm A–L (T044/T049) đã đạt** — người dùng chạy trên máy Android.

## [2026-09-13] fix | Màn `scan-10` luôn hiện "0 GB — Không đạt" ở Bộ nhớ RAM
- **Nguyên nhân**: `device_info_plus` trả `physicalRamSize` theo **megabyte** (Android: `memoryInfo.totalMem / 1048576L`; iOS: cùng đơn vị), nhưng `PlatformDeviceProbe` quy đổi như thể là **byte** (`~/ (1024*1024*1024)`) ⇒ máy 8GB đọc thành `8192 / 1.07e9 = 0` GB. Sai trên **mọi** thiết bị (emulator lẫn máy thật) nên không lộ ra ở test nào cũ.
- **Sửa**: `gbFromMb(int megabytes) => megabytes ~/ 1024` (đổi tên khỏi `_toGb` để không còn gợi ý đơn vị sai) + test `test/device_probe_platform_test.dart`.
- Hệ quả đã hết: RAM giờ hiện đúng GB → `classifyTier` xét ngưỡng RAM ≥ 4GB bằng số thật (trước đây luôn trượt ngưỡng này, kéo Tier B về Tier C oan).

## [2026-09-13] fix | Màn Cài đặt: giá trị trailing không còn căn phải
- **Nguyên nhân**: `_SettingsRow` dùng `Row([Expanded(label), SizedBox, Flexible(trailing)])` — Flutter chia không gian cho **mọi** flex child theo tỉ lệ flex, nên nhãn `Expanded` (tight) và giá trị `Flexible` (loose) chia đôi hàng; `Flexible` chỉ chiếm intrinsic width nên khoảng dư **không** dồn về phải ⇒ giá trị nằm giữa hàng (đo được `VND` ở x ≈ 450/800 thay vì 780). Đây là hệ quả của lần thêm `Flexible` để chống tràn cỡ chữ lớn ở chặng 1 PBI 24.
- **Sửa**: `Row(mainAxisAlignment: MainAxisAlignment.spaceBetween, children: [Flexible(label), SizedBox, Flexible(trailing)])` — child cuối luôn sát mép phải, mà cả hai vế vẫn co được nên giữ nguyên khả năng chống tràn (SC-014). Test: `settings_screen_test` ca "Giá trị trailing căn sát mép phải hàng".
- **Không dính ở chỗ khác**: `_itemRow` của màn Tiện ích dùng `Expanded` cho **khối** label (Column) + trailing không-flex ⇒ trailing vẫn sát phải đúng.

## [2026-09-13] update | PBI 25 — nhận diện thương hiệu: logo launcher + splash
- **Nguồn mới**: `docs/logo/logo-concepts-sora-thu-chi.md` (mục "Phương án chốt triển khai" vừa bổ sung) + `.specify/specs/25/` (spec/plan/contracts `brand-assets.md`).
- **[[Design system]]**: thêm §"Nhận diện thương hiệu — logo & splash (PBI 25)": Concept A dùng cho **cả icon launcher và splash** (lệch gợi ý "Concept C cho icon" của doc gốc — chủ ý của người dùng), biến thể **vòng viền trắng `0.03×D`** (nếu thiếu thì nửa teal chìm vào nền teal) + **nền icon teal đặc** (iOS cấm PNG trong suốt; Android mask lộ nền launcher), tỉ lệ chốt (coin/icon `0.68`, coin/khung splash `0.88`), splash 2 tầng cùng cỡ `140dp`, trần 5 giây, không hiện lại khi resume.
- **Sửa claim lỗi thời**: mục "Phạm vi" theme trước đây nói *màn PIN/boot đọc `SoraColors.of(context)`* — nay splash khởi động là **ngoại lệ cố ý** dùng màu bất biến `AppColors.teal`/`white` (giống màn chụp `scan-02`). Đã viết lại thành "2 ngoại lệ cố ý".
- **[[Lộ trình phát triển]]**: thêm ghi chú PBI 25 (PBI thuần trình bày, schema giữ v8) + **⚠ quyết định mở**: độ rõ của icon ở cỡ launcher nhỏ nhất chưa QA mắt trên máy thật; đổi được bằng 1 hằng số trong `tool/gen_brand_assets.dart`.
- **Ghi nhận khác biệt có chủ ý**: splash kết thúc → màn **thiết lập PIN** khi chưa đặt (khóa app bắt buộc lần đầu, PBI 3), không phải Tổng quan.

## [2026-09-13] fix | PBI 25 — 2 lỗi nhận diện chỉ lộ khi QA trên emulator
- **Bối cảnh**: người dùng báo icon launcher vẫn là icon mặc định của Flutter. Kiểm APK (`aapt2 dump badging/xmltree`) thì **APK đúng** — nguyên nhân là bản **cài cũ** trên máy (10:15, trước khi sửa); cài lại là thấy icon mới. Nhưng khi QA tiếp trên emulator Android 16 thì lộ **2 lỗi thật** mà test tự động không bắt được.
- **Lỗi 1 — `VectorDrawable` bỏ qua path đường tròn dạng rút gọn** `M cx-r,cy a r,r 0 1 0 2r,0 a r,r 0 1 0 -2r,0 Z`: path ra **rỗng** ⇒ mất **cả nửa teal lẫn vòng viền trắng** ở icon launcher và splash native (trên nền teal, splash trông như "nửa hình tròn bị khuyết" — đúng thứ FR-016/SC-008 cấm). **Sửa**: dùng hai cung bán nguyệt tuyệt đối `A r,r 0 0 1 …`. Phần raster (PNG) không bị ảnh hưởng.
- **Lỗi 2 — Android 12+ mask icon splash**: `windowSplashScreenAnimatedIcon` bị scale lên ~277dp rồi cắt theo đường tròn ~192dp (emulator 420dpi). Bản `0.88` khung vượt mask ⇒ mất vòng viền **và** logo to hơn tầng Flutter ~1,6 lần (nhảy hình, FR-010). **Sửa**: thêm `splash_logo_masked.xml` (coin `0.44` khung) chỉ dùng cho `values-v31`; đo lại logo hiện ra **122,7dp** ≈ tầng Flutter **123,2dp** (`0.88 × 140dp`).
- **Đã ghi vào**: `contracts/brand-assets.md` §3 + **§7 mới** (ghi chú thi công 2 giới hạn trên), `test/brand_assets_test.dart` (chặn tái phát: cấm `a … 0 1 0 …` trong vector, buộc `values-v31` trỏ `splash_logo_masked`), [[Design system]] (ràng buộc Android 12+), `.specify/specs/25/tasks.md` (kết quả QA A–E).
- **Chưa xong**: QA tay các bước F–O (tự tắt 5s, dark/light, đổi ngôn ngữ, xoay, cỡ chữ lớn, resume, cài đè).

## [2026-09-13] update | PBI 26 — màn `03` So sánh kỳ (Báo cáo)
- **Nguồn mới**: `.specify/specs/26/` (spec/plan/research/data-model/quickstart) + mockup `docs/report/man-hinh-03-so-sanh-ky.svg`. Nghiệp vụ không đổi — spec chỉ **làm rõ** luồng đã có trong doc §3.3/§3.4/§4.
- **[[Báo cáo]]**: thêm §"Màn `03` — So sánh kỳ" (điểm vào icon vùng tiêu đề + **vô hiệu hoá kèm `SnackBar`** khi chưa từng có thu/chi trước kỳ; cặp chip: kỳ chính **nhãn tĩnh** vs kỳ đối chiếu **bấm được** ⇄ "cùng kỳ năm trước"; nút hoán đổi **chỉ đổi chỗ**, không tính lại theo chế độ; `LineChart` 2 đường liền/đứt; màn dựng từ **bản chụp RAM**, cặp kỳ là **state cục bộ**). Thêm 6 luật 16–21 vào "Quy tắc nghiệp vụ" (kỳ đối chiếu giải bằng số học ngày + kẹp 29/02 → 28/02; badge % đánh giá **theo từng chỉ số** + màu theo **ý nghĩa**; hoán đổi đối xứng; câu Nhận xét ghép 2 mệnh đề + `@ref` **suy từ so sánh ngày** + danh mục cha tăng mạnh nhất; trạng thái rỗng **2 mức**). Sửa claim lỗi thời: schema **v7 → v8** và ghi chú "kỳ đang chọn trong controller dùng chung" nay có **3** màn đọc.
- **[[Lộ trình phát triển]]**: màn `03` xong (PBI 26) — còn lại của module chỉ là `04` Xuất báo cáo + bộ lọc nâng cao (kèm biểu đồ xu hướng **một kỳ** ở màn `01`, dùng lại insight cho thông báo cuối kỳ, toggle danh mục con).
- **[[Design system]]**: thêm §"So sánh kỳ — màn `03`": cặp cột tỉ lệ `Container` cao `64 × v / max` (**không** dùng `BarChart` cho 2 cột tĩnh), cột kỳ chính = màu loại giao dịch / kỳ đối chiếu `dotEmpty` alpha `.5`, badge % theo ý nghĩa, `LineChart` + `dashArray [4, 3]` (lần đầu dùng trong repo), và **khác biệt cố ý** với mockup: thẻ dùng token `softCardBg`/`coralLightBg` thay cặp nền trắng + viền `#EFEFEF` (repo không có token cho cặp đó), chip đối chiếu **bấm được** nên thêm mũi tên chỉ báo.
- **QA tay emulator nhóm A–O ĐÃ ĐẠT (2026-09-13)** — người dùng chạy theo `quickstart.md`; mọi luật cũng đã phủ bằng test tự động (883 pass + 1 đỏ **có sẵn** ở `transactions_dao_test`, không tăng). **iOS chưa QA** (PBI không đụng native/config ⇒ Android là đủ).

## [2026-09-13] update | PBI 27 — màn `04` Xuất báo cáo (Báo cáo)
- **Nguồn mới**: `.specify/specs/27/` (spec/checklist/plan/research/data-model/quickstart/tasks) + mockup `docs/report/man-hinh-04-xuat-bao-cao.svg`. Nghiệp vụ không đổi — spec **làm rõ** §3.7/§3.8/§4/§6/§8 của `docs/report/bao-cao-thong-ke-giai-phap.md`.
- **[[Báo cáo]]**: thêm §"Màn `04` — Xuất báo cáo" (điểm vào **icon thứ hai** trên vùng tiêu đề, **luôn** bấm được khác nút so sánh; bộ lọc **riêng của màn, sống trong phiên**; 5 mục + hộp tóm tắt + **dòng cảnh báo hiển thị sẵn**; nút vô hiệu khi 0 giao dịch; 3 định dạng **một đợt**) + 8 luật mới (22–29: bộ lọc riêng & đọc một lần; VÀ/HOẶC/rỗng-không-giới-hạn; khoảng nửa mở + **bộ chọn ngày chặn chéo, không có nhánh lỗi**; danh mục cha gồm con dùng chung `effectiveCategoryIds`; **tag chỉ soi cột `tags`**; danh sách tệp **đủ mọi loại** và **không** group-keep transfer; số tiền ghi số có dấu/không đơn vị; tên tệp ASCII + nội dung 3 định dạng). Sửa claim lỗi thời: kỳ đang chọn nay có **bốn** màn đọc; thêm giới hạn đã biết "tệp ra ngoài app **không** còn được bảo vệ" (cảnh báo cố ý, không phải bug) + **iOS chưa QA** luồng chia sẻ; bỏ màn `04` khỏi mục "Ngoài phạm vi".
- **Ghi nhận lệch có lý do**: (1) biểu đồ trong PDF **vẽ lại** bằng widget của `pdf` (cặp cột Thu/Chi ≤ 6 khoảng con + thanh ngang phân bổ), **không** phải ảnh chụp màn `01` — hai widget biểu đồ màn `01` là private, chụp ảnh cần `toImage()` offscreen (khó test/flaky); số liệu đi qua **đúng** `reportTotals`/`reportBreakdown` nên SC-004 vẫn 0 đ. (2) Chip danh mục hiện **tối đa 3** + "+N khác" (ngưỡng hằng số, mockup chia theo chỗ trống). (3) Lọc tag **chỉ cột `tags`**, không soi ghi chú như màn Giao dịch.
- **[[Lộ trình phát triển]]**: GĐ2 "Xuất báo cáo" nay đã xong; còn lại của module chỉ là **bộ lọc nâng cao xuyên suốt ba màn `01/02/03`** (+ biến thể line một kỳ, insight cho thông báo cuối kỳ, toggle danh mục con...). Thêm **⚠ CHỜ QA TAY A–P** cho màn `04` (mở tệp thật bằng ứng dụng, tên tệp khi chia sẻ, huỷ bảng chia sẻ, chữ tiếng Việt trong PDF) + khuyến nghị QA iOS vì có plugin native.
- **[[Design system]]**: thêm §"Xuất báo cáo — màn `04`": chip chọn **fill teal** bo `15` cao `30`; thẻ định dạng **viền `2px` teal + `check_circle`** (chưa chọn `1px` `divider`); ô ngày/tag nền `softCardBg` bo `8`; **dòng cảnh báo `coralLightBg` + icon coral** (coral đúng ngữ nghĩa cảnh báo); nút xuất teal bo `8` cao `46`, vô hiệu hoá = alpha `0.4`; **ô `trailing` của `ScreenHeader` nay rộng theo nội dung** (trước cố định `48px`) để chứa 2 nút.
- **[[Stack kỹ thuật]]**: thêm 3 thư viện `pdf`/`excel_community`/`share_plus` + lý do **không** dùng `excel` gốc (xung đột `archive` với `image ^4.9.2` của PBI 25), **không** `csv`/`printing`; font Roboto nhúng `assets/fonts/` (font mặc định của `pdf` không có dấu tiếng Việt); mục mới §"Xuất báo cáo" ghi 2 cái bẫy: `rootBundle` **không** dùng được trong isolate ⇒ nạp font ở main isolate; `XFile.fromData` **bỏ `name`** trên Android/iOS ⇒ phải dùng `ShareParams.fileNameOverrides`.
- **Kiểm thử**: 3 file test mới (`report_export_test`, `report_export_writers_test`, `report_export_screen_test`) + mở rộng `report_screen_test` + `dark_theme_smoke_test`; thêm `archive` vào **dev_dependencies** để test mở lại `.xlsx` (không thêm package mới vào cây — vốn đã là phụ thuộc bắc cầu). `flutter analyze` sạch; chỉ còn **1 test đỏ có sẵn** `transactions_dao_test` (PBI 11), **không tăng**.

## [2026-09-13] update | PBI 28 — màn "Thông báo & nhắc nhở" (Cài đặt)
- **Nguồn mới**: `.specify/specs/28/` (spec/checklist/plan/research/data-model/quickstart/tasks) + mockup `docs/notification/01-cai-dat-thong-bao.svg`; nghiệp vụ gốc `docs/notification/notification-solution.md` (§2 bảng 5 loại + nguyên tắc "mỗi loại một công tắc độc lập", §3.1 data model, §4 mockup `01`). Spec đã đóng 3 điểm `[CẦN LÀM RÕ]` bằng chốt **1A/2A/3A** (2026-09-13).
- **[[Hồ sơ & Bảo mật]]**: thêm §"Màn Thông báo & nhắc nhở" — điểm vào Cài đặt nhóm KHÁC (ngay sau "Tiện ích & Cá nhân hóa"), `SubPageScaffold`; **5 nhóm/8 hàng**, mỗi hàng **đúng một** điều khiển (**6 công tắc + 2 chevron no-op**); mặc định FR-008 (20:30 + cờ "chỉ nhắc nếu chưa ghi"; 80%/100%; 3 ngày; **nhắc mục tiêu tắt**; tuần/tháng 20:00); **1 row JSON `notificationPrefs` (16 trường)** trong `AppSettings` v5 — **schema giữ v8**, 0 dependency, 0 plugin; **ghi mặc định ngay lần mở đầu** (`load` rồi `save` luôn khối vừa đọc, idempotent); parse tolerant từng trường; `copyWith` chỉ chạm 1 loại ⇒ tắt/bật lại **giữ tham số**, tắt cả 6 vẫn hợp lệ, **không** có công tắc "bật/tắt tất cả"; ghi bám đuôi `_saveTail`.
- **Chốt phạm vi ghi rõ trong wiki**: **Q1=A** — đợt này **chỉ ghi nhận cấu hình**, **không engine**, **không bắn thông báo**, **không xin quyền**; **Q2=A** — 2 hàng chevron (*Ngưỡng cảnh báo*, *Nhắc trước*) chạm **không mở gì**, là điểm nối cho PBI chỉnh tham số; **Q3=A** — MỤC TIÊU TIẾT KIỆM & GIAO DỊCH ĐỊNH KỲ (chưa có module) vẫn là **công tắc thật**, không lộ "chưa hỗ trợ".
- **Lệch doc đã ghi nhận**: **không** dựng bảng `NotificationRule`/`NotificationLog` (§3.1 doc — phục vụ engine + Trung tâm thông báo, ngoài phạm vi; row JSON là nguồn migrate khi engine ra đời); **không lưu** danh sách ngày trong tuần / chu kỳ + mốc % của mục tiêu (per-mục-tiêu, GĐ3) / thứ của tổng kết tuần / ngưỡng riêng từng ngân sách; đường kẻ chỉ trong nhóm (mockup vẽ không nhất quán). Thêm trường sau này không phá dữ liệu cũ nhờ parse tolerant.
- **[[Lộ trình phát triển]]**: GĐ2 "Nhắc nhở" nay **chỉ màn cấu hình đã xong (PBI 28)** — thêm khối ⚠ liệt kê phần còn nguyên: **engine bắn thông báo** (chọn plugin, lịch, chống trùng, nội dung, quyền/channel), **màn `02`–`04`** của doc (đích của 2 hàng chevron), **Trung tâm thông báo** + `NotificationLog`, **cấu hình per-mục-tiêu**; **chưa QA tay nhóm A–K**.
- **[[Design system]]**: thêm §"Thông báo & nhắc nhở — màn cài đặt": **2 sắc icon teal/coral trong CÙNG một màn** (lần đầu áp quy tắc coral ở cấp **hàng**: 2 hàng NGÂN SÁCH dùng `coralLightBg`/`coralOnNeutral`), khuôn hàng `36px` + `Expanded` + trailing ngoài, divider chỉ trong nhóm, `Switch` Material **không** `switchTheme`, map icon theo nhóm, giờ `HH:mm` 24h qua **`formatClock`** (hàm mới trong `date_label.dart`, `formatTimeLabel` gọi lại — hành vi PBI 21 giữ nguyên).
- **Kiểm thử**: 3 file test mới (`notification_prefs_test` 22 ca — 17 luật, `notification_store_drift_test` 5 ca, `notification_settings_screen_test` 14 ca US1/US2/chất lượng) + 1 fake + sửa `settings_screen_test` (thêm hàng + 2 ca điểm vào) + `dark_theme_smoke_test`. **`flutter analyze` sạch**; **1004 pass + 1 test đỏ CÓ SẴN** `transactions_dao_test` (PBI 11) — baseline trước PBI là 960 pass, **số đỏ không tăng**. Grep `lib/`: **0** tham chiếu plugin thông báo/quyền.
- **Bẫy đã gặp**: khoá dịch **`'Ngưỡng cảnh báo'` đã tồn tại** ở nhóm Ngân sách (PBI 21, bản dịch `'Alert thresholds'`) ⇒ **tái dùng**, không thêm (thêm là lỗi `equal_keys_in_const_map`); khoá dịch = chuỗi tiếng Việt nên một chuỗi chỉ có một bản dịch.
- **QA tay emulator nhóm A–K ĐÃ ĐẠT (2026-09-13)** — người dùng chạy theo `quickstart.md` (điểm vào & khung màn; đối chiếu mockup `01`; công tắc độc lập & giữ tham số; mặc định & lưu bền qua kill app + khởi động lại thiết bị; 2 hàng chevron im lặng; 0 thông báo & 0 lần hỏi quyền; tắt hết không ảnh hưởng chức năng khác; English; chế độ Tối; cỡ chữ lớn/màn nhỏ; 2 nhóm chưa có module). **iOS chưa QA** (PBI không đụng native/config ⇒ Android là đủ). PBI 28 **hoàn tất 28/28 task**.

## [2026-09-13] update | PBI 29 — màn `02` "Nhắc nhập giao dịch" (Thông báo & nhắc nhở)
- **Nguồn mới**: `.specify/specs/29/` (spec/checklist/plan/research/data-model/quickstart/tasks) + mockup `docs/notification/02-cau-hinh-nhac-nhap-giao-dich.svg`; nghiệp vụ gốc `docs/notification/notification-solution.md` (§2 loại 1 "1 lần/ngày, theo các ngày trong tuần đã chọn", §3.1 `config.weekdays`, §4 mockup `02`). Spec đã đóng 3 điểm `[CẦN LÀM RÕ]` bằng chốt **Q1=B** (điểm vào = **vùng tiêu đề/dòng phụ hàng công tắc**), **Q2=A** (nút "Lưu thay đổi", back **bỏ** thay đổi, không hỏi lại), **Q3=A** (mặc định **cả 7 ngày**).
- **[[Hồ sơ & Bảo mật]]**: thêm §"Màn `02` Nhắc nhập giao dịch" — **hai vùng chạm trên MỘT hàng** (cụm icon + tiêu đề/dòng phụ mở màn `02`; **công tắc ngoài mọi `InkWell`**; hàng này **không** thêm chevron, 2 hàng chevron cũ chỉ mất phản hồi mực ở icon); bố cục **3 nhãn nhóm + 1 hàng công tắc + nút Lưu ghim đáy** (THỜI GIAN NHẮC 2 trục ▲/▼ quay vòng `%24`/`%60` · LẶP LẠI VÀO CÁC NGÀY 7 chip tròn `36px` · hàng công tắc "chỉ nhắc nếu chưa ghi" · XEM TRƯỚC THÔNG BÁO thẻ tĩnh + giờ động); **ngữ nghĩa bản nháp** (mở màn **chỉ đọc**, back **bỏ** thay đổi, chỉ ghi khi bấm Lưu, ghi lỗi bỏ qua rồi vẫn pop); row `notificationPrefs` nay **17 khoá** (`dailyWeekdays`) — **schema giữ v8**; **bất biến "luôn ≥1 ngày" đặt ở tầng MODEL** (`toggleDay` trả **chính object cũ** khi tắt ngày bật cuối cùng); parse tolerant tập ngày (thiếu khoá/sai kiểu/`[]`/phần tử lạ → cả 7 ngày; lọc + bỏ trùng + **sắp tăng**; **không bao giờ** ghi `[]`); **dòng phụ màn `01` nay theo tập ngày** (đủ 7 → `"@giờ mỗi ngày"`; thiếu → `"@giờ vào @ngày"` nén dải ≥3, **Chủ Nhật luôn liệt kê riêng** ⇒ `T2–T7, CN`); 2 hàm dùng chung `dayLabel`/`daysLabel` ở `date_label.dart`, nhãn ngày **literal trước `.tr`** (`T2…CN` → `Mon…Sun`). **Sửa claim lỗi thời** ở §PBI 28 ("không lưu các ngày trong tuần của nhắc hàng ngày" nay **đã bổ sung**).
- **Ghi nhận 5 lệch có lý do**: (1) **`Navigator.push`/`pop`, không `Get.to`/`Get.back`** như plan — repo **không** dùng `Get.to` ở màn nào (GetX chỉ cho state/DI/i18n) và test pump `MaterialApp` thường; (2) **thêm bước đọc lại store khi về màn `01`** (plan không nói — thiếu thì dòng phụ giữ giá trị cũ sau khi Lưu, trái kịch bản 13/14); (3) **Chủ Nhật không gộp vào dải nén** (nén thuần sẽ ra `T2–CN`, chốt đúng ví dụ spec là `T2–T7, CN`); (4) **`FittedBox(scaleDown)` bọc khối thời gian** — ở `textScaleFactor 2.0` nội dung ~369px trong 280px khả dụng (tràn `89px`); (5) **không** dựng bảng `NotificationRule` (giữ nguyên quyết định PBI 28 — bảng đó phục vụ engine).
- **[[Lộ trình phát triển]]**: GĐ2 "Nhắc nhở" nay **màn cấu hình `01` (PBI 28) + màn `02` (PBI 29) đã xong**; khối ⚠ còn lại thu hẹp: engine bắn thông báo · **màn `03`–`04`** của doc (đích 2 hàng chevron no-op — trước đây ghi `02`–`04`, nay **màn `02` đã làm**) · Trung tâm thông báo + `NotificationLog` · cấu hình per-mục-tiêu (GĐ3); thêm **⚠ CHỜ QA TAY nhóm A–M** của màn `02` (hai vùng chạm; **DB cũ PBI 28 thiếu khoá `dailyWeekdays`**; `[]`/`[0,9,"x"]` sửa tay; dòng phụ nén dải; **0 thông báo & 0 lần hỏi quyền**; English; Tối; cỡ chữ lớn).
- **[[Design system]]**: thêm §"Nhắc nhập giao dịch — màn `02`": khối chọn giờ/phút **tự dựng** (mũi tên `IconButton` ±1 quay vòng, ô giá trị đang chọn `26 w600` `tealOnNeutral` trên nền teal alpha `0.10`, **pad 2 chữ số**) thay trục cuộn; **`FittedBox(scaleDown)`** chống tràn ở cỡ chữ lớn (không đổi hình ở cỡ chuẩn); **chip tròn `36px` tự vẽ** (không `ChoiceChip`) chọn = **fill teal bất biến** + nhãn **clamp `1.4×`**, `Wrap` cho phép xuống hàng; thẻ xem trước **viền `divider`** + vòng `28px` chữ `'S'` (**không** dùng ảnh logo PBI 25 — lệch hệ màu trên nền nhạt); **hai vùng chạm trên một hàng** (lần đầu trong app, sửa **một chỗ** ở `_itemRow` dùng chung); nút chính ghim đáy giữ nguyên khuôn 5 màn form.
- **Kiểm thử**: 1 file mới (`daily_reminder_config_screen_test` 17 ca) + sửa 4 file (`notification_prefs_test` +6 nhóm luật tập ngày; `date_label_test` +2 nhóm; `notification_settings_screen_test` +7 ca hai vùng chạm/dòng phụ; `dark_theme_smoke_test` +1 màn `02`). **`flutter analyze` sạch; 1052 pass + 1 test đỏ CÓ SẴN** `transactions_dao_test` (PBI 11) — baseline PBI 28 là 1004 pass, **số đỏ không tăng**. Grep `lib/`: **0** tham chiếu plugin thông báo/quyền (FR-014 giữ nguyên).
- **Bẫy đã gặp**: (a) **`daysLabel` gộp cả Chủ Nhật** vào dải nếu viết nén thuần (test bắt được ngay — 3 ca đỏ); (b) **thiếu `modulo: 60`** cho trục phút (mặc định 24 ⇒ phút nhảy sai; `flutter analyze` bắt qua `unused_element_parameter`); (c) **thiếu pad 2 chữ số** ⇒ lân cận `0` hiện `'0'` thay vì `'00'` (test quay vòng bắt được); (d) **pump lại cùng loại widget trong một test không đổi `State`** ⇒ `initState` không chạy lại, phải `pumpWidget(SizedBox())` giữa các lượt (tiền lệ PBI 28).

### [2026-09-13] QA — PBI 29 màn `02` Nhắc nhập giao dịch: nhóm A–M ĐẠT
- Người dùng chạy tay trên emulator theo `.specify/specs/29/quickstart.md` §2 — **đạt toàn bộ A–M**: hai vùng chạm hàng nhắc hàng ngày (tiêu đề/dòng phụ/icon mở màn, **công tắc thì không**); đối chiếu mockup `02`; mũi tên giờ/phút ±1 + quay vòng, thẻ xem trước đổi ngay; 7 chip ngày, chip cuối **không** tắt được; bản nháp (back **bỏ** thay đổi, chỉ ghi khi bấm Lưu); mặc định/lưu bền + **DB cũ PBI 28 thiếu khoá `dailyWeekdays`**; dòng phụ màn `01` nén dải; **0 thông báo & 0 lần hỏi quyền**; không ảnh hưởng dữ liệu/5 loại nhắc khác; English; theme Tối; cỡ chữ lớn/màn hẹp.
- **iOS chưa QA** (PBI không đụng native/config ⇒ Android là đủ). PBI 29 **hoàn tất 25/25 task**; cập nhật ⚠ trong [[Lộ trình phát triển]] + [[Hồ sơ & Bảo mật]].

## [2026-09-13] update | PBI 30 — Trung tâm thông báo (màn `03`) + chuông màn Tổng quan
- **Nguồn mới**: `.specify/specs/30/` (spec/checklist/plan/research/data-model/quickstart/tasks) + mockup `docs/notification/03-trung-tam-thong-bao.svg`; nghiệp vụ gốc `docs/notification/notification-solution.md` (§1 hai tầng, §2 bảng 5 loại + deep link, §3.1 `NotificationLog`, §5 điểm vào "biểu tượng chuông ở màn hình Tổng quan"). Spec đã đóng 3 điểm `[CẦN LÀM RÕ]` bằng chốt **Q1=A** (đợt này **không** có engine ghi lịch sử), **Q2=A** (không thao tác hàng loạt/xoá), **Q3=A** (trần **200** mục, dọn mục cũ nhất).
- **[[Hồ sơ & Bảo mật]]**: thêm §"Trung tâm thông báo (màn `03`)" — điểm vào **chuông ở vùng tiêu đề màn Tổng quan** (khuôn `ScreenHeader.trailing`, nút tròn 48px, **luôn** bấm được); màn `SubPageScaffold` + back + **bánh răng → màn `01`**, không bottom nav/FAB; **2 tab "Tất cả"/"Chưa đọc"** lọc **trong bộ nhớ**; nhóm **HÔM NAY / TUẦN NÀY / TRƯỚC ĐÓ** (nhóm rỗng không vẽ); mục = chấm teal 8px (chỉ chưa đọc) + vòng 36px theo loại + tiêu đề + mô tả + nhãn thời gian; **2 trạng thái rỗng khác câu**; **chạm mục = đánh dấu đã đọc TRƯỚC rồi mới điều hướng** (thêm giao dịch / chi tiết ngân sách theo `relatedId` / `popUntil` + tab Báo cáo; `recurringDue`+`goalReminder`+`budgetAlert` thiếu `relatedId` ⇒ **im lặng**, vẫn đánh dấu); **chấm chưa đọc** hiện ⟺ có ≥1 mục chưa đọc, lỗi đọc **nuốt** (`_unread = 0`), cập nhật **ngay khi quay về** (đếm lại **sau** `await push`); state **cục bộ** ở `DashboardScreen` — **không** GetX controller/stream. Ghi **6 lệch có lý do** (bảng thật tên `notifications` không phải `NotificationLog`, không `payload` JSON; "TUẦN NÀY" = **7 ngày gần nhất** không cắt theo tuần lịch; nhãn thời gian **giờ tuyệt đối** `HH:mm`; chấm chuông dùng `coral` + viền trắng; `DashboardScreen` đổi sang `StatefulWidget`; nâng schema v8→v9 kéo theo 4 file test drift đổi số khẳng định). **Sửa claim lỗi thời** ở §PBI 28 ("Trung tâm thông báo + `NotificationLog` ngoài phạm vi" nay **đã có**, nhưng tên bảng là `notifications`).
- **[[Stack kỹ thuật]]**: drift nay **schema v9** (thêm bảng `notifications`); thêm §"Lịch sử thông báo — seam & bảng drift": seam **mới** `NotificationHistoryStore` (3 phương thức, **cố ý không nới** `NotificationStore` PBI 28; bỏ `unreadCount` — đếm bằng Dart; không `markAllRead`/`delete`), **trần 200 cưỡng chế ở TẦNG GHI** (`append` chèn rồi `delete(id.isNotIn(top-200))` theo `created_at DESC, id DESC`), **một chiều ở tầng SQL** (`WHERE read_at IS NULL`), migration `from < 9` thuần `createTable` **không seed**, **0 dependency mới / 0 plugin thông báo**. Ghi chú: test DAO drift **chạy thật** trên host này (có sqlite native) nên trần 200 được kiểm thật, khác mấy test drift phải skip-guard.
- **[[Design system]]**: thêm §"Trung tâm thông báo — màn `03` + chuông Tổng quan": **2 tab tự vẽ** (`Container` + `Border(bottom)` ⇒ gạch chân **rộng theo chữ**, không `TabBar`) **bọc `FittedBox(scaleDown)`** (cỡ chữ 2.0 tràn `52px`); mục = chấm 8px (đã đọc **vẫn chiếm chỗ**) + vòng 36px + `Divider` thụt lề 20; **coral vẫn chỉ ở `budgetAlert`** trong 5 loại; nhãn thời gian `HH:mm` / **tên thứ đầy đủ** (`weekdayName` mới ở `date_label.dart`, literal trước `.tr`) / `dd/MM`; **chấm `coral` + viền trắng `2px` 9px trên chuông nền teal — ngoại lệ cố ý** (DS **không** có token đỏ; muốn đỏ thật ⇒ thêm 1 hằng số, đổi 1 dòng).
- **[[Lộ trình phát triển]]**: GĐ2 "Nhắc nhở" nay **`01` (PBI 28) + `02` (PBI 29) + Trung tâm thông báo (PBI 30)** đã xong; khối ⚠ thu hẹp còn **engine bắn thông báo · màn `03`–`04` của doc · cấu hình per-mục-tiêu (GĐ3) · màn `04` mẫu thông báo đẩy · ghim/tìm kiếm/xoá mục · đưa lịch sử vào backup JSON (GĐ3)**; thêm **⚠ CHỜ QA TAY nhóm A–N** (`.specify/specs/30/quickstart.md` §2 — nhóm có dữ liệu phải **chèn tay** DB theo §1.2 vì app **không** seed; **iOS chưa QA**).
- **Kiểm thử**: 3 file mới (`app_notification_test` 15 ca hàm thuần; `notification_history_store_drift_test` 8 ca — trần 200/thứ tự/một chiều/không đụng bảng khác; `notification_center_screen_test` 19 ca màn) + 1 fake mới + sửa 6 file (`widget_test` +5 ca chuông/chấm, `dark_theme_smoke_test` +1 màn, 4 file drift đổi `schemaVersion` 8→9). **`flutter analyze` sạch; 1100 pass + 1 test đỏ CÓ SẴN** `transactions_dao_test` (PBI 11) — baseline PBI 29 là 1052 pass, **số đỏ không tăng**.
- **Bẫy đã gặp**: (a) **hàng tab tràn `52px`** ở cỡ chữ 2.0 (2 nhãn không co được) ⇒ **`FittedBox(scaleDown)`** (khuôn `_HeaderSummary` màn Báo cáo); (b) **số học dọn trần sai 1 đơn vị** trong test — trần dọn **sau mỗi** `append` nên 205 lần chèn chỉ mất **5** mục cũ nhất (test đỏ ngay); (c) **pump lại cùng loại widget trong CÙNG một test không dựng lại `State`** ⇒ `initState` không chạy lại, chấm đỏ giữ giá trị cũ — phải **tách thành 2 test** (tiền lệ PBI 28/29); (d) `createTable` của drift **idempotent** (test migration cũ dựng DB ở version mới rồi hạ `user_version` vẫn xanh) nên **không** cần `DROP TABLE notifications` ở test hạ cấp.

### [2026-09-13] QA — PBI 30 Trung tâm thông báo: nhóm A–N ĐẠT
- Người dùng chạy tay trên emulator theo `.specify/specs/30/quickstart.md` §2 — **đạt toàn bộ A–N**: chuông vùng tiêu đề Tổng quan (1 chạm mở Trung tâm; chấm hiện/mất theo mục chưa đọc; chấm mất ngay khi đọc mục cuối rồi back); trạng thái rỗng 2 câu; đối chiếu mockup `03` (app bar + 2 tab gạch chân + chấm/vòng icon + nhãn nhóm + đường kẻ thụt lề); đọc/chưa đọc — lọc tab, lưu bền qua đóng app, **không** có nút đọc-tất-cả/xoá; nhóm & nhãn thời gian (`HH:mm` / tên thứ / `dd/MM`, mục 8 ngày ⇒ TRƯỚC ĐÓ); điều hướng khi chạm (thêm giao dịch, chi tiết ngân sách, tab Báo cáo, 2 loại im lặng); trần 200 mục; **0** thay đổi dữ liệu nghiệp vụ + **0** thay đổi cấu hình màn `01`/`02`; **0** thông báo bắn ra & **0** lần hỏi quyền; dữ liệu cũ nguyên vẹn khi nâng v8→v9; English; theme Tối; cỡ chữ lớn/màn hẹp. Nhóm có dữ liệu **chèn tay** DB theo §1.2 (app không seed).
- **iOS chưa QA** (PBI 0 plugin native mới ⇒ rủi ro thấp). PBI 30 **hoàn tất 32/32 task**; cập nhật ⚠ trong [[Lộ trình phát triển]] + [[Hồ sơ & Bảo mật]].

## [2026-09-13] update | PBI 31 — Engine bắn thông báo đẩy (đóng ⚠ treo từ PBI 28)
- **Nguồn mới**: `.specify/specs/31/` (spec/checklist/plan/research/data-model/quickstart/tasks 55 task) + mockup `docs/notification/04-mau-thong-bao-day.svg`; nghiệp vụ gốc `docs/notification/notification-solution.md` (§1 **không FCM** · §2 bảng 5 loại + tần suất + deep link · §3.2 cơ chế lên lịch · §3.3 quyền & giới hạn nền tảng) + mockup `01-cai-dat-thong-bao.svg`. Spec đã chốt **Q1=A** (3 loại có dữ liệu mới có engine) · **Q2=C** (quyền hỏi ở lần đầu mở màn `01`) · **Q3=A** (chặn chỉ khi đang ở **đúng** màn liên quan).
- **[[Hồ sơ & Bảo mật]]**: thêm §"Engine thông báo đẩy — đã triển khai PBI 31" (dài nhất page): 3 loại + bảng chống trùng/đích điều hướng; **vì sao lịch nhắc hàng ngày là "một-lần cho từng mốc trong cửa sổ 30 ngày"**; **cơ chế GHI BÙ** bản ghi Trung tâm qua bảng sổ (điểm kỹ thuật cốt lõi — hệ điều hành không chạy Dart khi app đóng); **không bắn bù**; Q3 cho ngân sách + tổng kết; **soft-ask 1 lần + dòng trạng thái quyền** + exact alarm lùi inexact; **snapshot theo ngôn ngữ lúc bắn**; 3 điểm nối fire-and-forget (kèm luật "chuyển khoản **có** tính là đã ghi hôm nay nhưng **không** vào ngân sách"); engine **chỉ đọc** nghiệp vụ/**chỉ ghi** 3 chỗ; **cấu hình native lần đầu sau PBI 25**; **7 lệch có chủ ý**; kiểm thử + 4 bẫy đã gặp. **Sửa claim lỗi thời** ở §PBI 30 mục Q1 ("chưa có engine ghi lịch sử" → nay đã có, engine là **nguồn ghi thứ hai** qua đúng `append`/`markRead`).
- **[[Stack kỹ thuật]]**: drift nay **schema v10** (thêm bảng sổ `notification_ledger`); 3 dependency mới ghi vào bảng thư viện (`flutter_local_notifications` + `timezone` + `flutter_timezone`, nêu rõ **vì sao package thứ 3 là bắt buộc**); thêm §"Engine thông báo đẩy — 2 seam mới + sổ drift": 2 seam (`NotificationPresenter` bọc hệ điều hành, `NotificationLedger` bọc sổ — **cố ý không có `delete`**), bất biến nằm trong SQL, **FNV-1a 32-bit tự viết** (vì `String.hashCode` không ổn định giữa bản SDK), cấu hình native, **3 bẫy** (`AndroidNotificationChannel` không có `color`; exact alarm "im lặng"; `hide NotificationLedger` do trùng tên lớp), nâng schema kéo theo **5** file test drift đổi số.
- **[[Lộ trình phát triển]]**: GĐ2 nay có **engine bắn thông báo (PBI 31) đã xong**; thêm bullet "ENGINE BẮN THÔNG BÁO" (3 loại · ghi bù · quyền · **⚠ QA tay A–P chưa chạy** · iOS chưa QA); **gạch bỏ 3 claim cũ đã hết đúng** (engine "chưa có PBI", "Trung tâm thông báo chưa có PBI", "vẫn còn nguyên: engine") theo quy ước sửa claim lỗi thời; khối "còn lại của module" nay chỉ còn màn `03`–`04` của doc · cấu hình per-mục-tiêu · ghim/tìm/xoá lịch sử · backup lịch sử · **2 loại chưa có module**.
- **[[Design system]]**: thêm §"Thông báo hệ điều hành — kênh, icon, dòng trạng thái quyền": coral **chỉ** ở kênh `sora_budget`; **icon nhỏ Android buộc là drawable đơn sắc** (vì sao: hệ điều hành chỉ đọc kênh alpha); dòng trạng thái quyền `softCardBg` + lý do **cố ý không dùng coral**; soft-ask dùng `AlertDialog` mặc định.
- **Kiểm thử**: **5 file test mới** (`notification_schedule_test` 19 · `notification_content_test` 11 · `notification_engine_test` 53 · `notification_ledger_drift_test` 10) + **2 fake mới** + sửa `notification_settings_screen_test` (+7 ca US4), `FakeNotificationStore` (+2 hàm, `asked` mặc định `true`), 5 file drift đổi `schemaVersion` 9→10. **`flutter analyze` sạch; 1200 pass + 1 test đỏ CÓ SẴN** `transactions_dao_test` (PBI 11) — baseline PBI 30 là 1100 pass, **số đỏ không tăng**. **`flutter build apk --release` xanh** (xác nhận desugaring + proguard không chặn plugin — bẫy proguard ML Kit PBI 24 **không** lặp lại).
- **Bẫy đã gặp**: (a) **trùng tên lớp** — bảng drift cũng tên `NotificationLedger` như seam ⇒ impl phải `import 'db/app_database.dart' hide NotificationLedger`; (b) **`AndroidNotificationChannel` không có tham số `color`** ở FLN v22 ⇒ màu coral chuyển sang `AndroidNotificationDetails.color`; (c) **soft-ask làm đỏ 21 test cũ** của màn `01` (hộp thoại chặn chạm công tắc) ⇒ `FakeNotificationStore.asked` mặc định `true`; (d) `reconcile()` cuốn **cả 3 loại** ⇒ assertion kiểu `ledger.all()`/`history.stored` phải **lọc theo `kind`** (9 ca đỏ vì khẳng định cả sổ); (e) **`ReportScreen` KHÔNG bọc thành `StatefulWidget`** như plan: màn sống trong `IndexedStack` nên `initState` không phản ánh "đang xem" ⇒ khai báo hiện diện ở `AppShell._onTabSelected` (ghi thành **lệch 7** ở quickstart §5).
- **⚠ CÒN LẠI**: **QA tay nhóm A–P CHƯA CHẠY** (`.specify/specs/31/quickstart.md` §2 — 16 nhóm + 6 lệnh `adb`/`sqlite3`; **bắt buộc gỡ app rồi cài lại** vì có plugin native mới — bẫy "0 thông báo, không lỗi" của PBI 24/27); **iOS chưa QA** (đợt này **có** cấu hình native).
- **Sửa trong lúc thi công (Q3 tổng kết)**: bản đầu chặn **mọi** mốc tổng kết đang chờ khi vào tab Báo cáo ⇒ **ghé tab một lần là mất luôn tổng kết tuần này** (mốc cùng khoá đã bị chặn nên lần cuốn sau bỏ qua). Sửa thành **chỉ chặn mốc rơi vào ĐÚNG NGÀY HÔM NAY** (mốc duy nhất có thể bắn trong lúc đang xem) + thêm 1 test hồi quy ("ghé tab ngày thường ⇒ không huỷ mốc sắp tới").

## [2026-09-15] update | PBI 36 — Quét ảnh thông báo giao dịch ngân hàng
- **Nguồn**: `.specify/specs/36/` (spec/plan/tasks/research/data-model/quickstart, 22 task). Vấn đề gốc: AI Scan (PBI 24) chỉ hiểu hóa đơn cửa hàng, luôn mặc định **chi** — quét ảnh SMS/app/email thông báo ngân hàng ra sai loại GD và có nguy cơ lấy nhầm **số dư** làm số tiền giao dịch.
- **[[Giao dịch]]**: thêm §"Nhận diện ảnh thông báo ngân hàng (PBI 36)" trong mục Quét hóa đơn AI — cùng luồng/engine cũ, **không thêm điểm vào**. `RuleBasedExtractor` phân nhánh bằng `looksLikeBankNotification` (đếm ≥2 từ khoá ngân hàng, tổng quát không theo ngân hàng cụ thể) → `parseBankNotification` (file mới `bank_notif_parser.dart`) thay `parseReceipt`. Nhánh AI dùng chung 1 prompt `buildScanPrompt` (đổi tên từ `buildReceiptPrompt`) tự đọc ngữ cảnh trả `type`, không cần bước phát hiện riêng. Field mới `ScanExtraction.typeNeedsReview` (mặc định `false`, chỉ thêm khoá JSON vào `parsed_json`, **không đổi schema DB** — vẫn v10) bật cờ khi không đủ căn cứ suy thu/chi ⇒ màn `scan-04` hiện "Kiểm tra lại" dưới ô Loại giao dịch.
- **Quyết định kỹ thuật đáng chú ý**: số tiền giao dịch chọn theo **số đầu tiên** xuất hiện (ngược với hóa đơn — chọn **số lớn nhất**), vì số dư ngân hàng luôn ≥ số tiền giao dịch đơn lẻ; loại hẳn dòng "số dư" khỏi ứng viên (khác hóa đơn chỉ loại dòng ngày/ĐT/MST). "Chuyển tiền thành công" không rõ ghi nợ/có vẫn luôn suy **chi** (tiền rời tài khoản nguồn), không tự gán "chuyển khoản nội bộ" của app dù người nhận là ai (FR-004).
- **Kiểm thử**: 3 file test mới (`scan_result_test` 4 ca field mới; `bank_notif_parser_test` 15 ca trên 5 fixture SMS/app/email; `receipt_extractor_test` 2 ca dispatcher) + mở rộng `scan_confirm_screen_test` (+3 ca hint "Kiểm tra lại") + `llm_extractor_test` (+6 ca `buildScanPrompt`/`type`). Refactor `parseTxnDate` (đổi từ `_parseDate` private) dùng chung cho cả 2 bộ luật hóa đơn/ngân hàng. `flutter analyze` sạch; **1316 pass**, không hồi quy (baseline trước PBI 36 là 1294 pass).
- **⚠ CÒN LẠI**: QA tay nhóm A–E theo `.specify/specs/36/quickstart.md` chưa chạy (cần emulator/thiết bị thật — nhóm F chỉ khi có thiết bị hỗ trợ Tier A/B thật).
