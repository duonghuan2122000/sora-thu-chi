# Log cập nhật wiki

## [2026-09-10] implement | Màn "Ngôn ngữ" (Tiếng Việt / English) + dịch toàn bộ nhãn tĩnh (PBI 19) — nguồn .specify/specs/19
- Cập nhật [[Hồ sơ & Bảo mật]] thêm § **"Màn 'Ngôn ngữ' (03) — đã triển khai PBI 19"**: hàng "Ngôn ngữ" màn `01` **bỏ no-op** → push màn con `03` (`LanguageScreen`, `SubPageScaffold` teal back, không bottom nav) — **2 card đúng thứ tự Tiếng Việt → English**: vòng tròn mã `VI`/`EN` + endonym đậm + dòng phụ tên ngôn ngữ kia + radio tự dựng; **4 chuỗi tên/dòng phụ là hằng số cố định** (R8, chốt theo mockup `03`), chỉ tiêu đề app bar + ghi chú chân màn đi qua `.tr`. Sửa claim cũ "còn 4 hàng no-op" → **còn 3** (Định dạng & Tiền tệ, Tìm kiếm toàn cục, Quản lý Tag). Phạm vi PBI = dịch **toàn bộ** nhãn tĩnh của mọi màn hiện có (không dịch dần).
- Ghi chốt nghiệp vụ: **2 ngôn ngữ `vi` (mặc định) + `en`**, *không* có "theo ngôn ngữ hệ thống" (khác theme); lưu **1 row `('locale','vi'|'en')`** trong `AppSettings` v5 — **không migration, không seed**; key vắng = `vi`, chuỗi lạ ⇒ `vi` (parse an toàn); ghi chỉ upsert key `locale` (**độc lập** `themeMode`/2 công tắc). **Tên ví KHÔNG dịch** kể cả ví mẫu (chốt R7 — ví là dữ liệu, không có cờ "do app tạo"); **định dạng ngày/số không đổi** theo ngôn ngữ (§2.2).
- Cập nhật [[Stack kỹ thuật]] viết lại § **"Đa ngôn ngữ (i18n) — rule & cơ chế đã thi công"**: **khóa dịch LÀ chuỗi tiếng Việt đang hiển thị**; bản đồ `SoraTranslations` chỉ có **nhánh `'en'`**, không nhánh `'vi'`, không `fallbackLocale` (thiếu nhánh/khóa hoặc `Get.locale == null` ⇒ `.tr` trả khóa — nhờ vậy 436 test cũ giữ nguyên). Root: `GetMaterialApp(translations, locale, supportedLocales [vi,en], 3 delegate)` + **`flutter_localizations` (chỉ SDK, không package bên thứ ba)**. **Bài học đo được:** hạ `locale:` là **không đủ** — route đang mở không tự rebuild (probe `LEAF BUILDS before=1 after=1`), nên `LocaleController.setLocale` gọi `Get.updateLocale()` (reassemble toàn cây, giữ state + stack) và gọi **bắn-và-quên** (`await` làm test treo). Seam pattern PBI 17/18: `locale_prefs.dart` (hằng khóa + parse + `kLanguageOptions`) + `LocaleStore`/`DriftLocaleStore`/`ensureLocale*`/`FakeLocaleStore` + `SoraApp.localeStore?`. Thêm rule: test `sora_translations_test.dart` quét literal trước `.tr/.trArgs/.trParams` — bắt lỗi *thiếu khóa*, **không** bắt lỗi *quên gắn `.tr`* (phải QA tay).
- Cập nhật [[Danh mục]] thêm § **"Tên danh mục theo ngôn ngữ (PBI 19 — rule)"**: dịch **ở tầng hiển thị** — 15 tên `CategorySource` thành khóa `'en'` (`Ăn uống`→`Food & Drink`, `Khác`→`Other`…), chỗ render gọi `category.name.tr`; khớp khóa ⇒ dịch, **đã đổi tên/tự tạo ⇒ nguyên văn** (tự động thoả "không dịch dữ liệu người dùng"). **DB không đổi**, không cột `slug`, schema v4 giữ nguyên; snapshot `transactions.category` dùng cùng khóa; **không** dịch chỗ dùng làm `ValueKey`.
- Cập nhật [[Ví & Tài khoản]] thêm § **"Ngôn ngữ hiển thị (rule, PBI 19)"**: tên ví không dịch (kể cả ví mẫu) nhưng **nhãn loại ví có** dịch; hậu tố `(đã ẩn)` dịch riêng rồi ghép; số dư giữ `42.500.000 đ`.
- Cập nhật [[Lộ trình phát triển]]: ghi chú PBI 19 xong; **đóng phần "đa ngôn ngữ" của quyết định mở #7** (danh sách ngôn ngữ đã chốt `vi` + `en`); còn màn con `04`–`07`, hiệu ứng 2 công tắc, theme màu tùy biến, tablet, ngôn ngữ thứ ba/gói tải ngoài.
- Kỹ thuật: schema drift **giữ v5** (chỉ thêm row runtime, không migration, không `build_runner`); dịch ~36 file lib qua `.tr`/`trParams`. **472 test pass** (36 test mới của PBI 19), `flutter analyze` **sạch**; **1 test đỏ có sẵn từ trước, không liên quan** (`transactions_dao_test` — đối chiếu `git stash`: đỏ cả trên HEAD sạch). ⚠ Còn **T029 QA emulator thủ công** (quickstart nhóm A–L, đặc biệt nhóm K quét sót nhãn tĩnh — cách duy nhất bắt lỗi "quên gắn `.tr`") chưa chạy.

## [2026-09-10] implement | Màn "Giao diện" Sáng/Tối/Theo hệ thống + dark mode toàn app (PBI 18) — nguồn .specify/specs/18
- Cập nhật [[Hồ sơ & Bảo mật]] thêm § **"Màn 'Giao diện' (02) — đã triển khai PBI 18"**: hàng "Giao diện" màn `01` **bỏ no-op** → push màn con `02`; màn `02` (`ThemeScreen`) `SubPageScaffold` teal back + tiêu đề, không bottom nav, **3 card đúng thứ tự Sáng – Tối – Theo hệ thống** (vòng nền nhạt + icon minh họa, tên đậm + dòng phụ, radio **tự dựng** không `RadioListTile`; hàng chọn tô nền nhạt + vòng icon viền teal; ghi chú chân màn nguyên văn svg). Sửa claim cũ "5 hàng no-op" → **còn 4** (Ngôn ngữ, Định dạng & Tiền tệ, Tìm kiếm toàn cục, Quản lý Tag).
- Ghi chốt nghiệp vụ: 3 lựa chọn ánh xạ thẳng **`ThemeMode` Flutter** (không tạo enum riêng); lưu **1 row `('themeMode','light'|'dark'|'system')`** trong `AppSettings` v5 — **không migration, không seed**, row ghi write-through khi chọn; **key vắng = "Theo hệ thống"**, chuỗi lạ cũng về `system` (parse an toàn); nhãn thống nhất `system`→**"Theo hệ thống"** (màn `01` bỏ chữ cũ "Hệ thống"). Hàng "Giao diện" màn `01` trailing **reactive** → đổi ở màn `02` rồi back vẫn đúng.
- Cập nhật [[Design system]] thêm § **"Giao diện Sáng / Tối / Theo hệ thống"**: bộ đôi theme qua **`ThemeExtension<SoraColors>`** (không thêm dependency); nguyên tắc tách token **bất biến** (`AppColors`: teal fill `#0F6E56`, trắng **on-brand**, coral fill, bảng màu danh mục/ví, avatar) vs **đổi theo theme** (`SoraColors`: nền/card/chữ/kẻ ngang/dot/vòng nền nhạt/glyph teal-coral trên nền trung tính) + **bảng token light↔dark** đầy đủ (nền `#121212`, surface `#1E1E1E`, card `#242420`, chữ `#F2F2F0`/`#A8A8A3`, kẻ `#3A3A36`, glyph teal `#3FA98A` / coral `#E8734C`). Rule nhấn: **`white` 2 vai trò** (nền → token đổi; trên teal → giữ trắng) — VD ô tìm kiếm pill màn `05` giữ "đảo sáng cố định"; **light giữ nguyên giá trị** nên giao diện sáng không đổi. Phạm vi: mọi màn/chrome + màn PIN/boot, **trừ** ảnh hóa đơn/avatar, nội dung người nhập, màu nhận diện danh mục/ví. Cập nhật §Triển khai trong code: token màu nay **tách 2 file**.
- Cập nhật [[Stack kỹ thuật]] thêm § **"Giao diện Sáng/Tối — cơ chế"**: `ThemeController` (GetX, `Rx<ThemeMode>`) đăng ký gốc `SoraApp` + `GetMaterialApp(theme/darkTheme/themeMode)` → **đổi ngay toàn cây, không restart** — **lệch gợi ý doc §1.2** dùng `Get.changeThemeMode()` (lý do: tránh cơ chế ngầm khó bơm seam test). "Theo hệ thống" dựa sẵn `ThemeMode.system` của Material, **không** tự viết `didChangePlatformBrightness`. Nạp lúc boot mặc định `system`, cổng boot nền trống nên chấp nhận nháy khung đầu. Seam `ThemeStore`/`DriftThemeStore`/`ensureThemeStore()`/`FakeThemeStore` + `SoraApp.themeStore?` (pattern PBI 17); ghi nối đuôi.
- Cập nhật [[Lộ trình phát triển]]: ghi chú PBI 18 xong + đóng phần "Light/Dark mode" của **quyết định mở #7** (mặc định chưa chọn = "Theo hệ thống"); còn màn con `03`–`07`, hiệu ứng 2 công tắc, theme màu tùy biến, tablet.
- Schema drift **giữ v5** (chỉ thêm row runtime, **không migration, không `build_runner`**). Refactor token màu ~26 file widget. 436 test (27 mới của PBI 18) — `flutter analyze` **sạch**; **1 test đỏ có sẵn từ trước, không liên quan** (`transactions_dao_test` "addTransaction: income/expense…" — đã đối chiếu bằng `git stash`: đỏ cả trên HEAD sạch). QA emulator quickstart A–G **pass** (user kiểm thủ công 2026-09-10).

## [2026-09-06] implement | Màn Tiện ích & Cá nhân hóa — danh sách (PBI 17) — nguồn .specify/specs/17
- Cập nhật [[Hồ sơ & Bảo mật]] thêm § **"Màn Tiện ích & Cá nhân hóa (danh sách) — đã triển khai PBI 17"**: entry Cài đặt nhóm KHÁC → "Tiện ích & Cá nhân hóa" (dưới "Danh mục"), `SubPageScaffold` teal back, không bottom nav; 3 nhóm/8 hàng mockup `01` (HIỂN THỊ / TRẢI NGHIỆM / DỮ LIỆU & TÌM KIẾM; hàng = vòng `tealLightBg` + icon teal + tên + dòng phụ + trailing). Ghi rõ: **2 công tắc thật** (Ẩn số dư tắt / Máy tính bật) nhớ trạng thái bằng bảng drift key-value **`AppSettings` schema v5** (key `hideBalance`/`amountCalculatorEnabled`, key vắng = mặc định domain, row ghi write-through khi bật/tắt); hàng Widget = switch "bật" câm + dialog hướng dẫn ghim theo nền tảng (**không lưu**); 5 hàng điều hướng no-op chờ màn con `02`–`07`; hàng Quản lý Tag dòng phụ **mô tả** không số/tag giả (SC-008). Cấu trúc code: domain `UtilitiesPrefs` + seam `UtilitiesStore`/`DriftUtilitiesStore` + `ensureUtilitiesStore()` (GetX singleton) + `FakeUtilitiesStore`. **Chưa kéo hiệu ứng công tắc** (che số dư/đổi bàn phím = PBI sau).
- Cập nhật §Bảo mật mở rộng (Privacy mode): công tắc "Ẩn số dư" **đã lưu được** (PBI 17), hiệu ứng che số tiền vẫn PBI sau.
- Cập nhật [[Lộ trình phát triển]]: ghi chú giai đoạn — module Tiện ích màn `01` **đã MVP (PBI 17)**; quyết định #6/#7 ghi rõ còn màn con `02`–`07` + hiệu ứng công tắc chưa gắn GĐ; hàng Widget màn hình chính là hướng dẫn ghim (chưa có widget quick-add thật).
- Schema drift **v4 → v5** (bảng `AppSettings`, migration `from < 5` thuần tạo bảng, **không seed** — R8); chạy `build_runner` đúng 1 lần. PBI cài đặt sau chỉ **thêm row**, không thêm migration. 409 test pass; QA emulator quickstart bước 1–9 **pass** (user kiểm thủ công 2026-09-06).

## [2026-09-06] implement | Màn sắp xếp danh mục (PBI 16) — nguồn .specify/specs/16
- Cập nhật [[Danh mục]] §Sắp xếp & hiển thị + §Màn hình + thêm §Màn `04`: `04-sap-xep-danh-muc.svg` ✅ **đã triển khai** (`CategorySortScreen`). Entry = màn `01` icon **"Sắp xếp"** (no-op PBI 13 → kích hoạt) mở theo **đúng tab** (`initialType`); `SubPageScaffold` app bar teal back + tiêu đề "Sắp xếp danh mục" + nút **"Xong"**; `ReorderableListView.builder` liệt kê **cha cấp 1 đúng loại** — dòng `CategoryRow` + **tay cầm trái** (`ReorderableDragStartListener`, kéo chỉ từ tay cầm), `showChevron: false`; ẩn/hệ thống vẫn hiện + sắp được.
- Ghi quyết định: **ghi ngay khi thả** qua seam **`reorderCategories(orderedIds)`** (thêm vào "4 seam ghi" — đánh `sort_order = 0..n−1` cho đủ cha cấp 1 đúng loại trong **1 transaction**; con cấp 2 & loại kia bất biến — thứ tự nhóm con giữ nguyên); helper thuần **`moveCategoryAt`** (`core/category/category_sort.dart`) theo `ReorderableListView.onReorderItem` (SDK mới đã trừ `newIndex` khi kéo xuống — **lệch** R7 research vốn mô tả `onReorder` trừ thủ công); "Xong"/back pop không xác nhận (đã ghi khi thả), về màn `01` reload lặng → thứ tự mới hiện ngay. `CategoryRow` +2 param optional `leading`/`showChevron` (default giữ → màn `01`/`03` không đổi). Không đổi schema drift v4, không `build_runner`.
- Cập nhật [[Lộ trình phát triển]] ghi chú MVP: module Danh mục xong `01`–`04`; còn xóa/gộp + sắp con trong nhóm. 386 test pass; QA emulator quickstart A–C core pass (mở từ icon đúng mockup, kéo–thả ghi ngay, "Xong"/back về đúng tab + mở lại thứ tự bền vững; B/D/E/F/G phủ bằng widget test).

## [2026-09-06] implement | Màn danh sách danh mục con (PBI 15) — nguồn .specify/specs/15
- Cập nhật [[Danh mục]] §Màn hình: `03` ✅ **đã triển khai** (`CategoryChildListScreen`) — mô tả: tự dựng `Scaffold`+`AppBar` teal (tiêu đề 2 dòng chạm được = **tên cha + "Danh mục con"**, `FittedBox`+`InkWell` giữa back và "+"); list chỉ **con trực tiếp cùng loại** của cha đang xem theo `sortOrder` ổn định (dòng `CategoryRow`, không dòng phụ đếm con), con ẩn vẫn hiện đúng vị trí + mờ + "Đã ẩn", cuối list hàng **"+ Thêm danh mục con"** (`Expanded` chống tràn cỡ chữ lớn); empty phòng thủ giữ 2 điểm thêm; không bottom nav/số tiền. Nạp **1 loại** khi mở + cha hiển thị **derive** từ list; sau **mỗi pop form reload lặng**.
- Ghi quyết định: **vùng tiêu đề = điểm sửa danh mục cha có con** (loại khóa + ô cha không mở sheet — PBI 14 để lại); chạm con → Sửa con; thêm nhanh con preset cha qua param **`initialParentId`** ở `02` (Sửa `category.parentId` vẫn thắng; không truyền = hành vi cũ); con **đổi cha rời nhóm cũ**; màn `01` nối dòng CÓ con → push màn `03` + **reload lặng 2 loại** khi về (số con/tên cha phản ánh, đúng tab). Dòng danh mục trích thành **`CategoryRow`** (`core/widgets/category_row.dart`) dùng chung `01`/`03`. Không đổi schema drift v4, không `build_runner`.
- Cập nhật [[Lộ trình phát triển]] ghi chú MVP: module Danh mục xong `01`+`02`+`03`; còn `04` (sắp xếp) + xóa/gộp. 369 test pass; QA emulator quickstart A–H **pass** (thủ công, giới hạn seed: chỉ "Ăn uống" có con — cha loại khác tự tạo qua form).

## [2026-09-05] implement | Màn thêm/sửa danh mục (PBI 14) — nguồn .specify/specs/14
- Cập nhật [[Danh mục]] §Màn hình: `02` ✅ **đã triển khai** (`CategoryFormScreen`) — Thêm: loại mặc định theo tab lúc FAB, đổi được; Sửa: prefill 100% + luật khóa (đổi loại chỉ khi gốc sạch `canChangeType` = chưa gd + không con + đang gốc; danh mục **có con** không đổi cha/loại — phải luôn gốc giữ cây 2 cấp; con cùng loại cha). Tên chặn trống/trùng nhóm `(type, parentId)` **gồm bản ẩn** (bỏ chính nó khi Sửa). Xếp **cuối nhóm** khi Thêm/đổi nhóm (cùng nhóm giữ `sortOrder`); switch ẩn (Thêm tắt / Sửa theo `isHidden`); cờ `_saving` chặn lưu trùng; back trước lưu không ghi. `03` (DS con, entry dòng CÓ con) + `04` (sắp xếp) vẫn PBI sau.
- Ghi **3 seam ghi/đếm**: `insertCategory`/`updateCategory` (ghi literal, `isSystem` Sửa giữ / Thêm false, không tự validate/tính order) + `categoryHasTransactions` (nguồn khóa loại) — drift **v4 giữ nguyên**, không `build_runner`. Module thuần `category_form.dart` + presets icon/màu `category_presets.dart` (15 khóa + 13 ARGB = 5 màu seed + 8 token `AppColors.category*`; bất biến test seed ⊆ palette). Màn `01` nối điểm vào FAB → Thêm / dòng không con → Sửa + **refresh lặng 2 loại** sau pop.
- Cập nhật [[Lộ trình phát triển]] ghi chú MVP: module Danh mục xong `01` + `02`; còn `03`/`04` + xóa/gộp. 353 test pass; QA emulator quickstart A–I pass (entry sửa con/có con chưa mở tay — phủ widget test pump trực tiếp). Không đổi schema drift v4.

## [2026-09-05] implement | Màn danh sách danh mục (PBI 13) + sync schema v4 Danh mục (backlog PBI 11) — nguồn .specify/specs/13, /11
- Cập nhật [[Danh mục]] §Mô hình: sửa claim lệch (wiki cũ ghi `id(UUID)/created_at/updated_at`) → đúng drift schema **v4**: `categories` `id(int autoincrement), name(≤30), type(income|expense), icon, color(ARGB), parent_id(null=cha), sort_order, is_system, is_hidden`; giao dịch tham chiếu `transactions.category_id` (nullable) + `category` text **snapshot** tên. **Backlog PBI 11 chưa sync giờ đã ghi** (trước PBI 13 mới chạm bảng này).
- Cập nhật §Seed data: sửa thiếu "Khác" (nay đủ **4 cha thu** gồm Khác + 8 cha chi + 3 con Ăn uống), khớp `CategorySource.all`.
- Thêm §Màn `01` + đổi bảng Màn hình sang trạng thái: `01` ✅ **đã triển khai** (PBI 13 `CategoryListScreen`), `02/03/04` ⏳ PBI sau. Mô tả: entry Cài đặt nhóm KHÁC sau "Quản lý ví"; StatefulWidget nạp **2 loại 1 lúc** + chuyển tab lọc local (không GetX); hiện **cả danh mục ẩn** + nhãn "Đã ẩn"/mờ; "N danh mục con" **gồm con ẩn**; empty thật vs **ẩn-toàn-bộ không empty giả**; FAB/dòng/icon "Sắp xếp" no-op có ripple chờ `02/03/04`; mỗi lần vào reload.
- Ghi 2 seam đọc phân vai: `categories({type})` (đang hoạt động, picker PBI 11) vs `categoriesIncludingHidden({type})` (gồm ẩn, màn quản lý PBI 13) + module thuần `category_list.dart` (`topLevelParents`/`childrenOf` sort ổn định).
- Cập nhật [[Lộ trình phát triển]] ghi chú MVP: module Danh mục màn `01` xong, còn `02/03/04`. Không đổi schema drift v4 (PBI 13 chỉ đọc, không `build_runner`); 314 test pass; QA emulator A–F pass (G qua widget test — chưa tạo ẩn bằng UI).

## [2026-09-05] implement | Tìm kiếm & Lọc giao dịch (PBI 12) — nguồn .specify/specs/12
- Cập nhật [[Giao dịch]] §Tìm/lọc/sắp xếp: mô tả **đã triển khai** màn `05` (chip 4 loại, 5 bộ lọc nâng cao, bản nháp + Áp dụng/back giữ tập cũ, bỏ dấu tiếng Việt tự viết, AND, summary `N/Tổng` với transfer/adjustment ngoài tổng, group-keep transfer khi lọc ví).
- Ghi 2 lựa chọn hiển thị **user đã chốt** (R4 sort tiền → list phẳng; R5 đang lọc → ẩn card tháng, thay thanh `N kết quả · Tổng` + Bỏ lọc) + kỹ thuật: lọc trong bộ nhớ (cache mỗi load), filter sống `TransactionController` bền qua tab, không đổi schema.
- ⚠ `docs/transaction` chưa chép phần triển khai này (raw chỉ đọc — nghiệp vụ tinh chỉnh từ mô tả đặc tả); PBI 12 vẫn còn T011 QA emulator thủ công.

## [2026-09-03] rule | Khóa app = mã PIN bắt buộc lần đầu (PBI 3 đã implement) — nguồn .specify/specs/3
- Cập nhật [[Hồ sơ & Bảo mật]] §Khóa app: chốt **PIN bắt buộc ngay lần đầu mở app** + luôn có hiệu lực (lệch `docs/auth §2.1` "bật/tắt tùy chọn" — quyết định user, đã chốt) — ghi chi tiết: thiết lập 2 lần khớp, PIN 4 số cố định, PIN yếu cảnh báo-cho phép, hash SHA-256 có muối (`crypto`) + `lock_state` JSON trong flutter_secure_storage, mở khóa mỗi boot/resume về đúng màn, chống dò ladder 30s→1p→5p→15p giữ khi thoát app, sinh trắc & đổi/quên PIN & độ dài 4/6 ngoài đợt. Numpad hàng cuối trái để trống (chưa sinh trắc).
- Cập nhật [[Lộ trình phát triển]]: ghi chú MVP — khóa PIN đã xong PBI 3 (sinh trắc để sau).
- Cập nhật [[Stack kỹ thuật]]: thêm `crypto` (SHA-256) + mô tả key secure storage của khóa PIN.
- ⚠ `docs/auth/chi-tiet-quan-ly-tai-khoan-nguoi-dung.md §2.1` (và doc tính năng tổng phần lần đầu/Onboarding) **chưa đồng bộ** với quyết định bắt buộc — cần cập nhật tài liệu nghiệp vụ theo (raw sources chỉ đọc, sửa tay ngoài wiki).

## [2026-09-03] bootstrap | Dựng wiki từ toàn bộ docs/ (7 doc markdown + mockup SVG)
- Tạo schema CLAUDE.md + index + 9 page (5 entity, 4 concept).
- Raw = `../docs/` trỏ thẳng, không copy.
- Gom các quyết định mở vào [[Lộ trình phát triển]].

## [2026-09-03] rule | Cấu hình style tập trung 1 nơi khi triển khai app
- Thêm mục "Triển khai trong code (rule)" vào [[Design system]]: màu/size/typography config tại 1 nơi (AppTheme + token), widget không nhúng hex/số cứng rải rác. Quyết định của user (chưa nằm trong `docs/`).

## [2026-09-03] rule | Đa ngôn ngữ dùng GetX Translations
- Thêm mục "Đa ngôn ngữ (i18n) — rule" vào [[Stack kỹ thuật]]: chuỗi UI qua key i18n từ lúc dựng UI, cấu hình root `GetMaterialApp(translations/locale/fallbackLocale)`, mặc định + fallback tiếng Việt, không dịch data user, tách i18n khỏi format số tiền/ngày.
- Mở quyết định #7 trong [[Lộ trình phát triển]]: nhóm §12 Tiện ích chưa gắn GĐ; đa ngôn ngữ cần chốt danh sách ngôn ngữ. Quyết định của user (cơ chế GetX Translations) + nguồn gốc doc §12.

## [2026-09-12] implement | Ngân sách phạm vi 2a — PBI 20 (màn `01` Tổng quan + `02` Thêm/Sửa)
- Cập nhật [[Ngân sách]]: viết lại theo **trạng thái đã triển khai** — bảng drift **`budgets` schema v6** chỉ **6 cột** (`id, category_id, amount, period, is_recurring, start_date`), migration thuần tạo bảng **không seed**; 10 cột của doc §3.1 (`scope/wallet_ids/currency/period_start_rule/end_date/alert_thresholds/rollover_unused/status/created_at/updated_at`) **chưa có hiệu lực**.
- Chốt kỹ thuật đáng nhớ: **bỏ hẳn bảng `budget_period_snapshots`** — "đã chi / % / còn lại / ngày còn lại" là đại lượng **tính lại mỗi lần nạp màn** (doc §9 cho phép khi dữ liệu nhỏ) ⇒ sửa `amount` không thể hồi tố kỳ trước, giao dịch Chi bị sửa/xoá tự khớp lại.
- Rule ghi rõ: chỉ **Chi** (Thu/transfer loại); khớp `category_id` = chính nó **hoặc con trực tiếp** (không cháu, không suy đoán theo tên cho dòng `< v4`); `transaction_date ∈ [start, end)` **end độc quyền**; **Tuần bắt đầu Thứ Hai**, Tháng/Năm dương lịch; **chồng lấn** = cùng `category_id` + cùng `period` + khoảng **hiệu lực** giao nhau (lặp lại → ∞, không lặp lại → đúng kỳ chứa `start_date`).
- Trạng thái `active/ended/invalid` **suy ra**, không lưu cột; `ended`/`invalid` **không** vào thẻ tổng; danh sách sắp **giảm dần theo %**; bộ chọn kỳ chỉ điều khiển dòng chu kỳ Tháng (Tuần/Năm giữ kỳ riêng + nhãn chu kỳ).
- Ghi nhận ngoại lệ có lý do: màn `01` là **màn cấp tab** (điểm vào từ tab Báo cáo) nên **có bottom nav thật** + FAB dùng chung + callback đổi tab từ `AppShell`; màn `02` vẫn là sub-page không bottom nav. Seam: mở rộng `WalletRepository` 3 method + module thuần `core/budget/*`, **không** thêm repository/controller.
- Giới hạn đã biết ghi vào page: picker danh mục tái dùng (PBI 11) **không chọn được cha có con** ⇒ UI chưa tạo được ngân sách cho danh mục cha (luật gộp con vẫn đúng ở tầng tính toán).
- Cập nhật [[Design system]]: **đóng ⚠ mốc 80–99%** — thanh coral `#D85A30` `alpha 0.6` + % coral (giữ hệ 2 màu, không thêm token mới).
- Cập nhật [[Lộ trình phát triển]]: ghi PBI 20 xong (2a + màn `01`/`02`), **đóng quyết định mở #2**, còn lại 2b/2c + màn `03`.
- QA emulator A–N: phần lớn ĐẠT; bắt được **1 lỗi dịch** (hàng "Ngưỡng cảnh báo" màn `02` thiếu `.tr` ⇒ ở English sót "80% và 100%") — đã sửa + thêm khẳng định chống hồi quy. Nhóm không tái hiện được trên emulator (E/F/L/N một phần) phủ bằng test tự động; chi tiết ở `.specify/specs/20/quickstart.md`.

## [2026-09-12] implement | Ngân sách màn `03` Chi tiết + 2c mức tối thiểu — PBI 21
- Cập nhật [[Ngân sách]]: **schema v7** — `budgets` thêm cột `is_archived` (bool default `false`), migration = `addColumn` có default (bọc `from >= 6 && from < 7` để không đụng nhánh `createTable` của DB cũ hơn), **không seed**. `budgets()` **giữ nguyên hợp đồng** (trả cả ngân sách đã lưu trữ) — **lọc ở tầng view duy nhất**: `buildBudgetOverview` bỏ qua dòng `isArchived` khỏi cả danh sách lẫn thẻ tổng.
- Ghi **màn `03` là điểm vào mới**: chạm dòng ở màn `01` mở Chi tiết (FR-001 thay hành vi cũ mở thẳng form `02`; form nay vào từ nút "Chỉnh sửa"). App bar **2 dòng** qua tham số `subtitle` mới của `SubPageScaffold` (additive, `toolbarHeight 68`) + nút đổi kỳ góc phải.
- Rule mới đáng nhớ: **một phép lọc duy nhất** `budgetPeriodTransactions()` — `budgetSpent` cộng lại chính danh sách đó, màn `03` hiển thị `take(5)` ⇒ tổng danh sách không thể lệch số "đã dùng" (SC-005); **đừng viết bộ lọc thứ hai**.
- Công thức ghi rõ + **⚠ lệch 1 ngày có chủ ý**: màn `03` tính ngày còn lại **gồm hôm nay** (`clamp(today − start + 1, 0, total)`, tháng 9 ngày 12 → **18 ngày**, khớp mockup `03`); màn `01` giữ `end − today` → 19. PBI 21 **không** đồng bộ màn `01` (ngoài phạm vi, có test riêng). Cảnh báo nhịp = `!ended && %dùng > %ngày`, **chỉ một băng**, ẩn khi kỳ đã kết thúc.
- Biểu đồ: `fl_chart` `BarChart` **dùng thật lần đầu**; cột "Dự kiến" = `amount` **hiện tại** cho **mọi** kỳ (sửa giới hạn → mốc đổi, số thực tế kỳ trước **không** hồi tố); luôn **3 kỳ gần nhất tính đến kỳ đang xem** (kỳ đang xem là cột cuối), không vượt quá kỳ chứa `start_date` ⇒ ngân sách 1–2 kỳ vẫn vẽ được. Bộ chọn kỳ chỉ liệt kê **từ kỳ `start_date` đến kỳ hiện tại**; không lặp lại ⇒ đúng 1 lựa chọn.
- **Lưu trữ một chiều, chưa có nơi xem lại** (giữ nguyên chốt 2026-09-12): chỉ ghi `is_archived = true` qua `updateBudget(copyWith)` — **không** thêm method repository, **không** đụng giao dịch nào. 2 nút "Chỉnh sửa"/"Lưu trữ" hiện ở **mọi kỳ** và **cả khi danh mục đã bị xoá**.
- Cập nhật [[Design system]]: cột "Dự kiến" một **màu trung tính** (`dotEmpty` alpha 0.5) cho mọi kỳ + **đường mốc nét đứt** `dashArray [3,3]`; cột "Thực tế" teal/coral theo kết quả kỳ; nhãn kỳ đang xem in đậm.
- Cập nhật [[Lộ trình phát triển]]: **2c đóng ở mức tối thiểu** (so sánh 3 kỳ + tốc độ tiêu tính từ dữ liệu, không snapshot); còn lại **2b** + phần còn lại của 2c (dự báo tới hết kỳ, băng "chi chậm hơn", ngưỡng cảnh báo tuỳ chỉnh) + một PBI riêng nếu muốn **xem lại ngân sách đã lưu trữ**.
- Cập nhật [[Stack kỹ thuật]]: `fl_chart` từ "khai báo nhưng chưa dùng" → **đã dùng thật** (BarChart màn `03`).
- Test: 597 xanh (thêm `test/budget_detail_test.dart` + `test/budget_detail_screen_test.dart`; sửa 4 file test cũ + `FakeWalletRepository`). **Bẫy `trParams`**: tham số **không** được chứa `%` (`'Vượt @p%'` phải truyền `p='107'`).
- ⚠ QA emulator **mới chạy một phần** (đường vàng + đổi kỳ + "Xem tất cả" khớp số); nhóm H/I/L/M và đối chiếu số tính tay (SC-003/SC-012) **còn lại cho người dùng** — chi tiết ở `.specify/specs/21/quickstart.md`. Trong đó **Lưu trữ chưa thao tác tay** vì nó làm thay đổi dữ liệu ngân sách trên emulator.

## [2026-09-12] qa | PBI 21 — QA tay emulator nhóm A–N ĐẠT
- Người dùng chạy trọn A–N trên emulator Android ⇒ đạt toàn bộ, gồm các nhóm Claude không tự chạy (H Chỉnh sửa, I Lưu trữ, L giao dịch thay đổi, M English/cỡ chữ/dark mode, K danh mục bị xóa) và đối chiếu số tính tay SC-003/SC-012. **T033 hoàn tất — PBI 21 xong trọn 35/35.** Chi tiết ở `.specify/specs/21/quickstart.md`.

## [2026-09-12] implement | Báo cáo màn `01` Tổng quan — PBI 22
- **Page mới [[Báo cáo]]**: module Báo cáo — màn `01` Tổng quan đã xong; `02` Chi tiết theo Danh mục / `03` So sánh Kỳ / `04` Xuất báo cáo còn lại cho PBI sau.
- **Không schema riêng**: chỉ **đọc** `transactions` + `categories` (schema giữ **v7**), **không** migration, **không** chạm `lib/data/db/*`, **không** `build_runner`. Mọi số liệu là **đại lượng tính toán**; **không** bảng tổng hợp/cache (`report_monthly_summary` mà doc §2.2/§7 gợi ý **đã bỏ** — lần thứ hai đi ngược đề xuất snapshot, sau PBI 21).
- **Rule mới đáng nhớ**: **một phép lọc duy nhất** `reportTotals(transactions, range)` loại `transfer` + `adjustment` — 2 số tổng, 6 cột, vòng tròn phân bổ và top đều đi qua nó (SC-003/SC-004); **đừng viết bộ lọc thứ hai**. Kỳ **chỉ có chuyển khoản** = kỳ rỗng.
- **Mốc kỳ**: nửa mở `[start, end)`, **Tuần bắt đầu Thứ Hai**, Tháng/Năm **số học ngày** (không cộng `Duration`); giao dịch đúng ngày `end` **không** thuộc kỳ; **giao dịch ngày tương lai trong kỳ vẫn tính**. Biểu đồ = **6 đơn vị liền trước kết thúc ở kỳ đang chọn**.
- **Gộp danh mục cha + nhóm "Khác"**: khóa nhóm `parentId ?? id`, tên/icon/màu lấy từ **cha**; top 5 + "Khác" (phần ngoài top 5 **và** tiền chi `categoryId == null`) xếp **cuối**, chỉ hiện khi có dư; danh mục **ẩn vẫn tính**; con trỏ tới cha đã xoá ⇒ nhóm theo chính nó. **Chia % theo phần dư lớn nhất** (`percentSplit`) ⇒ `Σ%` = **đúng 100**. Đồng hạng sắp theo **tên đã bỏ dấu** (`normalizeSearch`) — so code-unit thuần sẽ xếp "Nhà" trước "Ăn".
- **Trạng thái rỗng theo KỲ ĐANG CHỌN** (không xét cả cửa sổ 6 đơn vị): không thu/chi ⇒ **cả 3 thẻ** "Chưa có giao dịch nào trong kỳ này"; **chỉ có Thu** ⇒ biểu đồ **vẫn vẽ đủ 6 đơn vị**, riêng thẻ phân bổ + thẻ top "Chưa có chi tiêu nào trong kỳ này".
- **Drill-down**: chạm một danh mục (lát cắt **hoặc** dòng chú giải) → đặt `TxnSearchFilter` (Chi + `dateStart = range.start` + `dateEnd = range.end − 1 ngày` + `categoryIds = {id cha}` + sort ngày mới nhất) rồi `onSelectTab(1)` — màn Báo cáo **là** tab nên **không** `popUntil`. Bộ lọc PBI 12 tự mở rộng cha → con nên tổng khớp. Nhóm **"Khác" không có đích** ⇒ không chạm được.
- **Nạp lại**: `AppShell._onTabSelected` gọi `load()` khi chọn tab 2 (bám khuôn tab Giao dịch) + `_openAddTransaction` nạp lại controller báo cáo **khi** đang đứng ở tab Báo cáo (FAB hiện trên mọi tab — SC-008). Đổi kỳ **không** đọc DB (tính lại từ RAM — SC-007).
- **Kiểu dùng chung**: `DateRange` (nửa mở) **tách sang `lib/core/date_range.dart`**, `budget_view.dart` **re-export** ⇒ **0 import phải sửa** ở module Ngân sách.
- Cập nhật [[Design system]]: **token mới `SoraColors.chartPalette`** (6 màu **đổi theo theme**, phần tử cuối cho "Khác") — bảng màu **định tính** của vòng tròn theo doc §5, **tuyệt đối không coral** (coral = chi tiêu/cảnh báo), gán cố định **theo hạng** chứ không lấy `Category.color`; cột biểu đồ + 2 số tổng thì ngược lại vẫn dùng token teal/coral.
- Cập nhật [[Lộ trình phát triển]]: MVP "Báo cáo cơ bản" **màn `01` xong**; liệt kê phần còn lại (3 màn + bộ lọc nâng cao, trong đó **nút biểu tượng lịch mockup `01` chưa hiển thị**) và giới hạn **chưa quy đổi đa tiền tệ**.
- Cập nhật [[Stack kỹ thuật]]: `fl_chart` nay **đã dùng cả `BarChart` lẫn `PieChart`**; ghi thêm ghi chú "đã bỏ snapshot/cache 2 lần, chỉ thêm khi đo thấy chậm".
- **Bẫy đã gặp khi thi công**: (1) `ReportScreen` build sẵn offstage trong `IndexedStack` ⇒ đừng render `CircularProgressIndicator` khi **chưa** nạp (vòng lặp animation vô hạn làm `pumpAndSettle` timeout ở mọi test pump shell) — chỉ quay spinner khi `isLoading`; (2) test pump cả app (`pin_flow_test`) **phải** đăng ký `FakeWalletRepository` vì tab Báo cáo nay đọc DB khi chọn tab, thiếu fake sẽ khởi tạo drift/sqlite native và treo.
- Test: **657 xanh** (+3 file mới `report_view/controller/screen_test.dart`; sửa `pin_flow_test`, `dark_theme_smoke_test`), `flutter analyze` sạch; **1 test đỏ có sẵn** ở `transactions_dao_test` (đã đỏ từ trước PBI 22, không liên quan).
- ⚠ **QA tay emulator nhóm A–K chưa chạy** (T039/T040 của `.specify/specs/22/tasks.md` còn mở) — cần người dùng chạy trên máy Android.

## [2026-09-12] qa | PBI 22 — QA tay emulator nhóm A–K ĐẠT
- Người dùng chạy trọn **A–K** trên emulator Android ⇒ **đạt toàn bộ, không lệch cần sửa**, gồm các nhóm Claude không tự chạy (C chạm cột hiện số tiền, E drill-down sang màn Giao dịch đã lọc, G đổi kỳ, H 3 thẻ rỗng, I dữ liệu đổi → số liệu đổi, J English/chế độ Tối, K cỡ chữ lớn/màn nhỏ/số hàng tỉ/tắt mạng). **T039/T040 hoàn tất — PBI 22 xong trọn 42/42.** Chi tiết ở `.specify/specs/22/quickstart.md`.
