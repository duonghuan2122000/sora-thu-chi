# Danh sách Task: Màn hình danh sách danh mục

**Mã PBI**: 13
**Nguồn**: plan.md, spec.md, data-model.md, research.md, quickstart.md

## Pha 1: Setup

*(Không có task — PBI 13 **không thêm dependency/pubspec mới**, **không đổi schema drift** (schemaVersion giữ **4**, không chạy `build_runner` — plan §Ngữ cảnh kỹ thuật, research R1/kết luận & data-model §Phạm vi). Tính năng **chỉ đọc**: thêm đúng 1 module thuần `category_list.dart` + 1 màn `category_list_screen.dart` + 1 method đọc `categoriesIncludingHidden` (interface + drift + fake) + 1 param `floatingActionButton` cho `SubPageScaffold` + 1 hàng "Danh mục" trong Cài đặt. Không tạo `contracts/` — app nội bộ offline, không API/CLI (bám PBI 7–12).)*

## Pha 2: Foundational

*(Hạ tầng bắt buộc xong trước user story: module thuần `category_list.dart` (tách cấp 1 + đếm con gồm ẩn) + unit test; seam đọc `categoriesIncludingHidden` (interface + `DriftWalletRepository` + fake kèm seed tùy chọn); param `floatingActionButton` cho `SubPageScaffold` — theo plan.md "Cấu trúc dự án dự kiến", research R1/R4/R7 và data-model §Seam đọc bổ sung/§Projection.)*

- [X] T001 Tạo `app/sora_thu_chi/lib/core/category/category_list.dart`: module **thuần** (không đọc DB/state — nhận `List<Category>` đầu vào, data-model §Bất biến, R7) gồm — `List<Category> topLevelParents(List<Category> list)` = các danh mục `isParent` (`parentId == null`), sắp **tăng `sortOrder`, ổn định** (trùng sortOrder giữ thứ tự danh sách gốc — FR-004); `List<Category> childrenOf(List<Category> list, int parentId)` = danh mục có `parentId == parentId` thuộc cùng `list` — **gồm cả con đang ẩn** (FR-005 edge), sắp `sortOrder`; không lọc danh mục cha đang ẩn khỏi kết quả (màn quản lý hiện cả ẩn — FR-006); màn dùng `childrenOf(...).length` cho dòng phụ "N danh mục con" (rỗng = không hiện dòng phụ); dựa domain `Category.isParent`/`parentId` (đã có, không đổi); mọi id `null` cha không bỏ lọt (SC-002); file dùng lại cho màn danh sách con `03` PBI sau
- [X] T002 [P] Tạo `app/sora_thu_chi/test/category_list_test.dart`: unit **thuần** bơm `List<Category>` tự dựng (không sqlite) — `topLevelParents` chỉ giữ cấp 1 (cha ẩn vẫn giữ — không lọc ẩn, SC-004) + sắp đúng `sortOrder` tăng + **ổn định khi trùng sortOrder** (FR-004); `childrenOf` trả đúng con của 1 cha, **đếm gồm con đang ẩn** (FR-005/edge), không con → rỗng (dòng phụ bỏ trống), con của cha A không lẫn con cha B; 2 danh mục cùng tên khác cha/type xử lý độc lập không gộp (edge, data-model §Projection 4); phụ thuộc T001
- [X] T003 Sửa `app/sora_thu_chi/lib/data/wallet_repository.dart` (interface): thêm method đọc — `Future<List<Category>> categoriesIncludingHidden({required CategoryType type})` (R1) — doc: trả **toàn bộ** danh mục thuộc `type` (cha cấp 1 **và** con cấp 2, đang hoạt động **và** đang ẩn — cần con để đếm dòng phụ FR-005), sắp tăng `sortOrder` (không đảm bảo cha trước con — module `category_list` tự tách); **khác** `categories({type})` hiện có (chỉ đang hoạt động, cho picker PBI 11) — giữ nguyên method cũ, không đổi ngữ nghĩa (không hồi quy PBI 11/12)
- [X] T004 [P] Sửa `app/sora_thu_chi/lib/data/wallet_repository_drift.dart`: implement `categoriesIncludingHidden` — `@override` thêm method (bám `categories()` hiện có tại dòng `categories({required CategoryType type})`): `select(_db.categories)` **không lọc** `is_hidden` (khác SQL hiện tại có `c.isHidden.equals(false)`), lọc `type` trong Dart (cột enum converter — bảng nhỏ local), map `CategoryRow` → `Category` qua `_toCategory` (đã có), sắp `sortOrder`; **không** cần `build_runner`/`.g.dart` mới (bảng `categories` v4 đã có — plan §Rủi ro, schema giữ 4); phụ thuộc T003 (chữ ký method)
- [X] T005 [P] Sửa `app/sora_thu_chi/test/fakes/fake_wallet_repository.dart`: thêm — (1) `categoriesIncludingHidden({type})`: đọc bộ danh mục fake của `type` **không lọc ẩn** (cha + con), sắp `sortOrder`, parity drift (R1/data-model §Fake); (2) constructor nhận thêm named `List<Category>? categoriesSeed` (mặc định `null` → giữ `CategorySource.all` — **additive**, test cũ picker PBI 11/filter PBI 12 không đổi hành vi — data-model §Seed & fake, R11); khi có seed dùng seed thay `CategorySource.all` cho mọi method đọc danh mục (kể cả `categories({type})` cũ); phụ thuộc T003 (chữ ký method)
- [X] T006 [P] Sửa `app/sora_thu_chi/lib/core/widgets/sub_page_scaffold.dart`: thêm param optional `Widget? floatingActionButton` → truyền xuống `Scaffold.floatingActionButton` (R4); màn phụ đã dùng (`wallet_*`, thêm giao dịch, lọc…) **không đổi** vì param mới default null; Cài đặt (sub-page) & PBI Danh mục sau tái dùng

## Pha 3: User Story 1 - Màn danh sách danh mục (Ưu tiên: P1)

**Mục tiêu**: Màn con "Danh mục" theo bố cục `docs/category/01-danh-sach-danh-muc.svg` (spec acceptance 1–8, FR-001…011) — mở từ hàng **"Danh mục"** trong Cài đặt (nhóm KHÁC, sau "Quản lý ví"); app bar teal tiêu đề "Danh mục" + back trái + icon **sắp xếp** phải, **không** bottom nav; hàng tab tự dựng **Chi tiêu / Thu nhập** (mặc định Chi tiêu, chọn teal + gạch chân teal); thân là danh sách **danh mục cấp 1** của tab đang xem — mỗi dòng vòng tròn nền nhạt phái sinh từ `category.color` (alpha ~0.14) + icon `categoryIcon` màu đầy đủ, **tên**, dòng phụ **"N danh mục con"** khi có con (đếm gồm con ẩn) / để trống khi không, chevron phải; danh mục **ẩn** vẫn hiện đúng vị trí + nhãn "Đã ẩn"/mờ (không rút khỏi màn quản lý, không tạo empty giả khi ẩn-toàn-bộ); tab không còn cấp 1 nào → empty hướng dẫn; FAB "+" (teal, góc phải dưới qua param `SubPageScaffold`); 3 điểm vào (dòng / FAB / icon sắp xếp) **no-op có ripple** — không lỗi/treo vì màn đích (thêm/sửa `02`, con `03`, sắp xếp `04`) ở PBI sau; nạp **2 loại 1 lúc** lúc mở, chuyển tab lọc local (FR-004/SC-003, không đọc DB lại); mỗi lần vào từ Cài đặt push route mới → reload (FR-008/SC-006); không vỡ cỡ chữ lớn/safe area.
**Tiêu chí kiểm thử độc lập**: `flutter test test/category_list_test.dart test/category_list_screen_test.dart test/settings_screen_test.dart` pass — seam bơm `FakeWalletRepository` (+ `categoriesSeed` tùy chọn) render đúng bố cục mockup `01` + tab Chi/Thu + dòng icon/tên/dòng phụ/chevron + ẩn + empty + no-op 3 điểm vào + cỡ chữ lớn & safe area; hàng Cài đặt điều hướng đúng; không sqlite native (R11).

- [X] T007 [US1] Tạo `app/sora_thu_chi/lib/screens/category_list_screen.dart`: `StatefulWidget` (R2) — seam `final WalletRepository? repository` (mặc định `ensureWalletRepository()` trong `initState`, pattern `CategoryPickerScreen` PBI 11 / `SearchFilterScreen` PBI 12) + `_loading`/`_error`/`_expense`/`_income` state + enum tab `CategoryType` (mặc định `expense`); `initState` **nạp 1 lần** `Future.wait([categoriesIncludingHidden(expense), categoriesIncludingHidden(income)])` giữ cache suốt vòng đời màn, chuyển tab **lọc local không đọc DB lại** (R3/FR-004); lỗi đọc → thông báo + nút "Thử lại" (load lại); dựng trên `SubPageScaffold(title: 'Danh mục', actions: [IconButton(icon sắp xếp) onPressed no-op], floatingActionButton: FAB)`: **FAB** `Icons.add` tròn nền `AppColors.teal` icon trắng `onPressed: () {}` (R4/R8, mockup); body bọc `SafeArea(top: false)`: loading → spinner trung tâm / error → thông báo + Thử lại / thành công → `Column`: **(1) hàng tab tự dựng** (R6): 2 `InkWell` nhãn "Chi tiêu"/"Thu nhập" xếp **trái từ lề** — tab chọn chữ teal đậm + gạch chân teal bo 1.5 cao 3px dưới chữ, tab kia chữ `AppColors.tabInactive`, vạch chia ngang `AppColors.listDivider` dưới hàng; mặc định Chi tiêu; **(2) `Expanded(ListView)`** các danh mục cấp 1 của tab qua `topLevelParents` (module T001): mỗi dòng = `InkWell(onTap: () {})` (no-op có ripple — R8, FR-007/SC-008) chứa `Row`: leading bubble tròn 36px nền `Color(category.color).withValues(alpha: ~0.14)` + `Icon(categoryIcon(category.icon), color: Color(category.color), size ~20)` (R5, bám mockup — màu là **dữ liệu** `category.color`, không hardcode), giữa cột `Expanded`: tên (`maxLines 1`, `TextOverflow.ellipsis` — FR-010) + dòng phụ khi `childrenOf(...).length > 0` → "N danh mục con" (`N` gồm con ẩn — R7/FR-005) còn không → để trống; danh mục **ẩn** → làm mờ icon/tên (opacity/xám) + nhãn "Đã ẩn" cạnh tên (R5/FR-006/SC-004); trailing chevron `Icons.chevron_right` `AppColors.tabInactive`; **empty state** (FR-009): `topLevelParents` rỗng → thông báo hướng dẫn thêm danh mục (không lỗi); trường hợp mọi cấp 1 của tab đều ẩn → các dòng ẩn vẫn hiện (không empty giả — edge, data-model §Projection 3); không số tiền; toàn màn cuộn được + `ListView` padding dưới tránh FAB che + không vỡ cỡ chữ lớn/safe area (FR-011/SC-007); phụ thuộc T001 + T003 (repo) + T006 (SubPageScaffold FAB)
- [X] T008 [P] [US1] Tạo `app/sora_thu_chi/test/category_list_screen_test.dart`: widget test **bơm `FakeWalletRepository`** (constructor + `categoriesSeed` khi cần case ẩn/con lạ — R11, không sqlite): (a) bố cục mockup `01` — `SubPageScaffold` AppBar title "Danh mục" + icon back + icon sắp xếp phải, tab "Chi tiêu" chọn (chữ teal + gạch chân), tab "Thu nhập" mờ, FAB "+" teal phải dưới, **không** bottom nav (acceptance 1, FR-001/003); (b) mặc định tab Chi tiêu hiện 8 dòng cha chi (Ăn uống…Giáo dục) đúng thứ tự seed, dòng "Ăn uống" dòng phụ **"3 danh mục con"**, dòng không con (VD "Di chuyển") **không** có dòng phụ, mỗi dòng đủ icon bubble + tên + chevron (acceptance 2, FR-004/005/SC-002); (c) chạm tab "Thu nhập" → chỉ 4 danh mục thu (Lương/Thưởng/Đầu tư/Khác) không lẫn chi; chạm lại "Chi tiêu" → danh sách chi cũ đủ 8 dòng đúng vị trí/thứ tự (acceptance 3, FR-003/004, SC-003); (d) bơm seed có danh mục **ẩn**: dòng ẩn vẫn hiện đúng vị trí + nhãn "Đã ẩn"/mờ khác biệt; mọi cấp 1 của 1 tab đều ẩn → không empty giả (acceptance 4, FR-006/SC-004); con ẩn vẫn tính vào "N danh mục con" của cha (edge, FR-005); (e) tab không còn cấp 1 nào (seed loại đó rỗng) → empty hướng dẫn, không lỗi (acceptance edge, FR-009); (f) chạm 1 dòng / FAB "+" / icon sắp xếp → không crash/không rời màn/không push route (acceptance 5/6/7, FR-002/007/SC-008); (g) `textScaleFactor` cao + safe area → cuộn tới dòng cuối + không `RenderFlex overflow`, chevron & FAB không cắt (acceptance 8, FR-010/011/SC-007); phụ thuộc T007
- [X] T009 [US1] Sửa `app/sora_thu_chi/lib/screens/settings_screen.dart`: thêm hàng **"Danh mục"** (chevron `AppColors.tabInactive`) ngay dưới "Quản lý ví" trong nhóm `KHÁC` (R9 — plan chốt vị trí); thêm seam `VoidCallback? onManageCategoryTap` vào constructor (mặc định null); method `_openManageCategory(context)` theo pattern `_openManageWallet`: có callback → gọi callback (test bơm — không push); không → `Navigator.push(MaterialPageRoute(builder: (_) => CategoryListScreen()))` (FR-001/SC-001); giữ nguyên các hàng/section/`_ProfileBlock` khác (không hồi quy); phụ thuộc T007 (class `CategoryListScreen`)
- [X] T010 [P] [US1] Sửa `app/sora_thu_chi/test/settings_screen_test.dart`: cập nhật kỳ vọng **số hàng/thứ tự** nhóm KHÁC (hiện giả định 4 hàng: Tiền tệ mặc định/Đổi mã PIN/Mở khóa sinh trắc học/Quản lý ví → thêm "Danh mục" sau "Quản lý ví" — plan §Rủi ro, chủ đích additive); **thêm case**: tap hàng "Danh mục" → `Navigator.push` mở `CategoryListScreen` (register `FakeWalletRepository` qua `Get.put` như shell — màn đọc repo qua `ensureWalletRepository`) + back về; bơm `onManageCategoryTap` → callback được gọi, **không** push màn (seam — bám case `onManageWalletTap`); giữ nguyên case cũ không hồi quy; phụ thuộc T009 + T007

## Pha cuối: Polish & Cross-cutting

- [X] T011 Chạy `flutter analyze` (sạch 0 warning) + `flutter test` toàn bộ tại `app/sora_thu_chi/` (test PBI 2–12 cũ + PBI 13 mới pass — **đặc biệt** chạy lại test PBI 5 (`settings_screen_test` row-order), PBI 11 (`category_picker_screen_test`, `categories({type})` không đổi), PBI 12 (`search_filter_screen_test`), màn `wallet_*`/thêm giao dịch dùng `SubPageScaffold` param mới optional — khẳng định additive không hồi quy); xác nhận **không** cần `build_runner`/`.g.dart` mới (schema v4 giữ nguyên); đối chiếu danh sách "Không đổi" plan.md — `core/category/category.dart`, `category_source.dart`, `category_icon.dart`, `data/db/app_database*.dart`, `wallet_*` screens/controller, màn PIN/boot, `category_picker_screen.dart`, `add_transaction_screen.dart`, `transaction_*`, `app_shell.dart`, `wallet_deps.dart`/`transaction_deps.dart` không thay đổi; ghi nhận quyết định hiển thị (R5 bubble nhạt phái sinh màu, R6 tab tự dựng, R8 no-op có ripple, R9 hàng KHÁC sau Quản lý ví) vào trạng thái PBI
- [X] T012 Kiểm chứng thủ công emulator theo `.specify/specs/13/quickstart.md` nhóm A–G: mở từ Cài đặt → "Danh mục" — app bar teal + back + icon sắp xếp, tab Chi tiêu chọn, không bottom nav, FAB (A); tab Chi tiêu 8 cha đúng thứ tự + "Ăn uống" "3 danh mục con" + dòng không con không dòng phụ (B); tab Thu nhập 4 thu không lẫn chi, về Chi tiêu giữ danh sách (C); chạm dòng/FAB/icon sắp xếp → không crash/treo/no route, ripple hiện (D); back → ghi giao dịch chi (PBI 11) → mở lại "Danh mục" không lỗi, hiển thị đúng (E); cỡ chữ lớn + vùng an toàn → không vỡ, tên dài ellipsis, chevron & FAB không che, cuộn tới cuối (F); trạng thái ẩn chỉ verify qua widget test (chưa tạo ẩn bằng UI — ghi rõ quickstart G); ghi kết quả từng mục vào trạng thái PBI
- [X] T013 Cập nhật wiki tri thức qua skill `sora-wiki` + append `wiki-knowledge/log.md`: entity **Danh mục** (schema v4 `Categories` + `category_id` — **backlog PBI 11 chưa sync**, plan §Kiểm tra sau thiết kế ⚠️) + màn quản lý list `01` (danh sách cấp 1 theo tab, đếm con gồm ẩn, ẩn hiện + "Đã ẩn"/mờ, empty thật vs ẩn-toàn-bộ, FAB/no-op điểm vào, entry từ Cài đặt nhóm KHÁC) + ghi nhận "không đổi schema drift v4" (PBI 13 chỉ đọc)

## Sơ đồ phụ thuộc

```text
Foundational:
  T001 category_list.dart (thuần: topLevelParents + childrenOf gồm ẩn)
    └──▶ T002 [P] category_list_test.dart
  T003 wallet_repository.dart (interface + categoriesIncludingHidden)
    ├──▶ T004 [P] wallet_repository_drift.dart (impl, không build_runner)
    └──▶ T005 [P] fake_wallet_repository.dart (+ method + categoriesSeed additive)
  T006 [P] sub_page_scaffold.dart (+ floatingActionButton param)   [độc lập]
US1:
  T001 + T003 + T006 ──▶ T007 category_list_screen.dart ──▶ T008 [P] category_list_screen_test.dart
  T007 ──▶ T009 settings_screen.dart (hàng "Danh mục" KHÁC + seam onManageCategoryTap → push) ──▶ T010 [P] settings_screen_test.dart
Polish:
  T011 analyze + full test (không hồi quy PBI 2–12) ──▶ T012 QA emulator (quickstart A–G) ──▶ T013 wiki sync (PBI 13 + backlog PBI 11)
```

Thứ tự thực thi chính: `T001 → T003 → T007 → T009 → T011 → T012 → T013` (test `[P]` T002 chạy sau T001; T004/T005 sau T003; T006 độc lập chạy bất kỳ trước T007; T008 sau T007; T010 sau T009). Mấu chốt: module thuần **T001** + seam đọc **T003** + param **T006** xong trước khi dựng màn **T007**; **T009** là bước cuối US1 vì cần `CategoryListScreen` đã tồn tại để nối điều hướng từ Cài đặt; **không có** `build_runner` (schema giữ v4) nên thứ tự thoải mái như PBI 12.

## Ví dụ chạy song song

```text
# Foundational — sau T001 (module nền xong) / T003 (interface — móc chuỗi repo):
T002 [P] category_list_test.dart      T006 [P] sub_page_scaffold.dart (param FAB, độc lập)

# Sau T003:
T004 [P] wallet_repository_drift.dart (impl)      T005 [P] fake_wallet_repository.dart (+ seed)

# T006 sub_page_scaffold.dart [P] — độc lập, chạy bất kỳ lúc nào trước T007.

# US1 — test màn chạy song song việc nối Cài đặt (sau file tương ứng):
T008 [P] [US1] category_list_screen_test.dart (sau T007)
T010 [P] [US1] settings_screen_test.dart       (sau T009, khác file T008)
```

## Chiến lược triển khai

- **MVP đề xuất**: User Story 1 (toàn bộ PBI 13 — đặc tả là một lát cắt "màn danh sách danh mục" duy nhất, không chia user story P1/P2/P3). Foundational dựng phần logic trình bày thuần + seam đọc gồm cả ẩn (`categoriesIncludingHidden` + seed fake additive) + param FAB `SubPageScaffold` làm tiền đề bắt buộc; US1 ghép `category_list_screen` (màn `01`) + nối hàng Cài đặt lên để ra giá trị end-to-end (FR-001).
- **Thứ tự giao hàng tăng dần**: Foundational (module `category_list` thuần + unit test chứng minh tách cấp 1/đếm con gồm ẩn/sort ổn định; seam đọc interface → drift + fake — mọi logic test trên `FakeWalletRepository`/hàm thuần, không sqlite native) → US1 màn `01` (screen + widget test) → nối Cài đặt (hàng "Danh mục" + seam + widget test) → Polish (analyze/test toàn cục không hồi quy + QA emulator quickstart A–G + sync wiki).
