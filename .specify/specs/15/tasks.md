# Danh sách Task: Màn hình danh sách danh mục con

**Mã PBI**: 15
**Nguồn**: plan.md, spec.md, data-model.md, research.md, quickstart.md

## Pha 1: Setup

*(Không có task — PBI 15 **không thêm dependency/pubspec mới**, **không đổi schema drift** (schemaVersion giữ **4**, không chạy `build_runner` — plan §Ngữ cảnh kỹ thuật, research kết luận & data-model §Phạm vi), **không thêm seam repository** (màn `03` đọc qua `categoriesIncludingHidden` sẵn có + lọc `childrenOf` thuần; ghi qua màn `02` PBI 14). Không tạo `contracts/` — app nội bộ offline, không API/CLI (bám PBI 7–14).)*

## Pha 2: Foundational

*(Hạ tầng dùng chung bắt buộc xong trước khi dựng màn `03` & nối màn `01`: trích widget `CategoryRow` (R4) + param `initialParentId` cho form `02` (R5) — theo plan.md "Cấu trúc dự án dự kiến", research R4/R5 và data-model §Thay đổi nhỏ ở tầng giao diện.)*

- [X] T001 [P] Tạo `app/sora_thu_chi/lib/core/widgets/category_row.dart`: widget chia sẻ **`CategoryRow`** (R4, data-model §Thay đổi nhỏ) — `{required Category category, String? subtitle, required VoidCallback onTap}`; **sao chép nguyên trạng bố cục `_row` hiện tại của màn `01`** (PBI 13) để màn `01` test hiện có không vỡ: vòng tròn nền nhạt alpha `category.color` chứa `Icon(categoryIcon(category.icon))` màu nhận diện (mờ opacity khi `category.isHidden`), tên `category.name` (ellipsis — không cắt chevron), **nhãn "Đã ẩn"** khi `isHidden`, dòng phụ `subtitle` chỉ render khi `!= null`, chevron `›` phải; `ValueKey('category-row-${category.id}')` (pattern màn `01`); không nhận trạng thái/khiến khác — onTap do caller truyền (màn `01` chọn hướng Sửa/màn con, màn `03` mở Sửa con); không hex cứng (dùng `AppColors`, màu dòng = dữ liệu `category.color`)
- [X] T002 [P] Sửa `app/sora_thu_chi/lib/screens/category_form_screen.dart`: thêm param **`int? initialParentId`** (R5, data-model §Thay đổi nhỏ — PBI 14 để lại cho màn `03`): constructor `{CategoryType? initialType, int? initialParentId, Category? category}`; trong `initState` đổi dòng khởi tạo cha thành `_parentId = widget.category?.parentId ?? widget.initialParentId;` (chế độ **Sửa** `category.parentId` vẫn thắng; **Thêm** dùng preset; không truyền `initialParentId` → `null` = hành vi cũ "danh mục gốc" — không đổi behavior test PBI 14); **không** đổi validator/khóa/save-plan/`_switchType` (khi đổi loại cha về null — giữ nguyên R5/giả định spec "form tự vận hành theo ràng buộc")

## Pha 3: User Story 1 - Màn hình danh sách danh mục con (Ưu tiên: P1) 🎯 MVP

**Mục tiêu**: Màn con "Danh mục con" theo bố cục `docs/category/03-danh-muc-con.svg` (spec acceptance 1–10, FR-001…012) — mở từ màn danh sách danh mục (PBI 13) khi chạm **danh mục có con**; app bar teal tự dựng (không qua `SubPageScaffold` — cần tiêu đề 2 dòng chạm được, R3): nút back (trái), tiêu đề = khối **tên danh mục cha + dòng phụ "Danh mục con"** (bọc `FittedBox` + `InkWell` nằm giữa back và "+"), action **"+"** (phải); body `ListView` liệt kê **con trực tiếp cùng loại của cha đang xem** theo `sortOrder` ổn định (mỗi dòng `CategoryRow`: bubble icon+màu, tên, chevron, **không** dòng phụ đếm con — con cấp 2 không có con; con **ẩn** vẫn hiện đúng vị trí + mờ + nhãn "Đã ẩn"); cuối danh sách hàng **"+ Thêm danh mục con"**; **không** bottom nav, **không** số tiền; empty-state phòng thủ khi không còn con (vẫn giữ 2 điểm thêm); đa cỡ chữ/safe area không tràn/cắt.
**Tiêu chí kiểm thử độc lập**: `flutter test test/category_child_list_screen_test.dart test/category_list_screen_test.dart test/category_form_screen_test.dart` pass — bơm `FakeWalletRepository` (store biến đổi PBI 14): màn `03` render đúng mockup (chỉ con của cha đang xem — không lẫn cha khác/loại khác, thứ tự ổn định, con ẩn hiện + "Đã ẩn"), chạm con → Sửa prefill + loại khóa, "+"/hàng Thêm → form preset cha (lưu con cuối nhóm), chạm vùng tiêu đề → Sửa cha (khóa loại + không mở sheet cha, đổi tên → tiêu đề mới), con đổi cha → rời nhóm cũ, back → màn `01` đúng tab + số con cha cập nhật, empty phòng thủ, cỡ chữ lớn không overflow; không sqlite native (R10).

- [X] T003 [US1] Tạo `app/sora_thu_chi/lib/screens/category_child_list_screen.dart`: `StatefulWidget` **`CategoryChildListScreen`** (pattern `CategoryListScreen` PBI 13, R2) — params `{required Category parent, WalletRepository? repository}` (mặc định `ensureWalletRepository()` trong `initState`); `_load()`: nạp **1 loại** `categoriesIncludingHidden(type: parent.type)` → `_list`, cha hiển thị **derive** `_parent = _list.firstWhere(id == parent.id)` (không giữ object tĩnh — tiêu đề tươi sau đổi tên cha, FR-009); body tự dựng `Scaffold` + `AppBar` chuẩn (hưởng `AppBarTheme` teal/foreground trắng/`centerTitle: false` — R3): `leading` mặc định (BackButton tự hiện vì route đẩy), `title` = `FittedBox(scaleDown)` bọc `InkWell` (vùng nằm giữa back và "+", **không** đè 2 nút — FR-003/edge) bọc `Column` [tên cha `white` 16 w600, "Danh mục con" `white` ~85% cỡ 11]; `actions` = `IconButton` "+" tooltip "Thêm danh mục con" (FR-008); chạm vùng tiêu đề → `Navigator.push` `CategoryFormScreen(category: _parent, repository: repo)` — cha **có con** nên form tự khóa loại + không mở sheet cha (R3); body = `SafeArea` + `ListView` (cuộn được, FR-012): từng con = `CategoryRow(category: con, subtitle: null, onTap: push `CategoryFormScreen(category: con)`) — con cấp 2 → loại readonly, vẫn đổi cha/bỏ cha (R7, FR-010) — + `Divider` + hàng cuối **"Thêm danh mục con"** (icon + teal) → push `CategoryFormScreen(initialType: parent.type, initialParentId: parent.id, repository: repo)` preset (R5, FR-008/acceptance 6 — ô cha hiện tên cha, người dùng chỉ nhập tên/icon/màu); **sau mỗi pop form** (dù lưu hay back) → `_reloadSilent()` đọc lại 1 loại + derive lại `_parent` (R8, FR-009/010 — thêm/sửa/ẩn phản ánh ngay, con đổi cha biến mất, tên cha mới lên tiêu đề); khi `childrenOf(_list, _parent.id)` rỗng → empty-state phòng thủ hướng dẫn "Chưa có danh mục con" + **vẫn giữ** "+" và hàng "Thêm danh mục con" (R9, không lỗi); không FAB/bottom nav/số tiền; phụ thuộc T001 (`CategoryRow`) + T002 (`initialParentId`)
- [X] T004 [US1] Sửa `app/sora_thu_chi/lib/screens/category_list_screen.dart` (R1/R4): **(1)** thay block `_row` cũ bằng dùng chung `CategoryRow` — mỗi dòng cấp 1: `subtitle: childrenOf(_list, c.id).isEmpty ? null : '<N> danh mục con'` (chỉ khi có con — FR-005), `onTap` theo nhánh dưới; **(2)** `onTap` dòng: danh mục **có con** (`childrenOf(...)` không rỗng) → bỏ no-op cũ → `Navigator.push` `CategoryChildListScreen(parent: c, repository: _repository)` (R1 — FR-001, acceptance 1); **không con** → giữ mở form Sửa (PBI 14 — không đổi); **(3)** sau `await push` màn con trả về → `_reloadSilent()` nạp lại lặng cả 2 loại (helper sẵn có — số con/tên cha phản ánh khi về, FR-011/acceptance 8/10; route giữ `_tab` → back về đúng tab); FAB "+" (thêm gốc) giữ nguyên; không hồi quy PBI 13/14 (bố cục dòng/ẩn/đếm giữ nhờ `CategoryRow` trích nguyên trạng — R4); phụ thuộc T001 + T003 (class `CategoryChildListScreen`)
- [X] T005 [P] [US1] Tạo `app/sora_thu_chi/test/category_child_list_screen_test.dart`: widget test **bơm `FakeWalletRepository`** (store biến đổi — assert ghi qua store, không sqlite; seed cha-con bằng `withCategories`/`insertCategory`, R10): **(a)** bố cục mockup `03` — pump màn con của cha có con ("Ăn uống" 3 con): app bar back + tiêu đề "Ăn uống" + "Danh mục con" + nút "+", **không** bottom nav / số tiền, danh sách đúng 3 con đúng thứ tự, mỗi dòng bubble icon+màu + tên + chevron, **không** dòng phụ "N danh mục con" (acceptance 1); **(b)** chỉ con của cha đang xem — seed thêm con của cha khác ("Nhà ở") + 1 danh mục thu nhập → không lẫn (acceptance 2/SC-003); **(c)** thứ tự `sortOrder` ổn định; **(d)** con ẩn → hiện đúng vị trí + nhãn "Đã ẩn" + mờ, không loại (acceptance 4); **(e)** chạm dòng con → push `CategoryFormScreen` chế độ Sửa prefill + ô loại readonly (khóa, `_canChangeType` false — con cùng loại cha, acceptance 5) — dùng `find.descendant` scope trong màn con (màn `01` đè dưới giữ state, pattern helper `_formText` PBI 14); **(f)** chạm "+" (app bar) → form Thêm với ô cha hiện tên cha đang xem + loại preset; chạm hàng "Thêm danh mục con" → tương tự; lưu tên hợp lệ → `insertCategory` vào store cuối nhóm + quay về màn con thấy dòng mới cuối danh sách (acceptance 6/7 — không refresh tay); **(g)** sửa con **đổi cha** sang cha khác (hoặc bỏ cha) → lưu → con không còn trong danh sách cha cũ (FR-010/acceptance 7); **(h)** chạm **vùng tiêu đề** → form Sửa cha: ô loại khóa (có con), ô cha không mở sheet, đổi tên cha → lưu → tiêu đề màn con hiện tên mới + danh sách con giữ nguyên (acceptance 9, FR-003/FR-009); **(i)** empty-state phòng thủ — pump trực tiếp repo không còn con → hiện hướng dẫn + vẫn có "+"/hàng Thêm, không lỗi (edge R9); **(j)** cỡ chữ hệ thống lớn → không overflow, tiêu đề/không che (FR-012); phụ thuộc T003 (+ T001/T002)
- [X] T006 [P] [US1] Sửa `app/sora_thu_chi/test/category_form_screen_test.dart` (R10): thêm case **Thêm với `initialParentId`** — pump `CategoryFormScreen(initialType: expense, initialParentId: id-cha-có-con, repository: fake)`: ô "Danh cha (tùy chọn)" hiển thị **tên cha preset** (acceptance 6 — không cần chọn lại cha), nhóm so trùng tên tính theo con của cha đó (thêm trùng tên con cùng cha → bị chặn), lưu tên hợp lệ → `insertCategory` vào store với `parentId == id-cha`, `sortOrder` = cuối nhóm con cha đó (acceptance 7); case không truyền `initialParentId` → hành vi cũ ("Không có — là danh mục gốc") giữ nguyên (không hồi quy PBI 14); phụ thuộc T002
- [X] T007 [US1] Sửa `app/sora_thu_chi/test/category_list_screen_test.dart` (R1/R10): thay case cũ **"dòng có con → no-op"** (PBI 13) bằng — chạm dòng **có con** ("Ăn uống") → push `CategoryChildListScreen` với `parent` = dòng đó (acceptance 1, FR-001); thêm case **back từ màn con** → về màn `01` đúng tab đang mở (vd "Chi tiêu") + số danh mục con của cha cập nhật sau thao tác trong màn con (fake biến đổi — acceptance 8/10, FR-011); giữ nguyên các case cũ khác (tab/đếm con/ẩn/empty/FAB→Thêm/dòng không con→Sửa/refresh sau pop form… — không hồi quy PBI 13/14, `CategoryRow` trích nguyên trạng giữ bố cục test hiện có); phụ thuộc T004

## Pha cuối: Polish & Cross-cutting

- [X] T008 Chạy `flutter analyze` (sạch 0 warning) + `flutter test` toàn bộ tại `app/sora_thu_chi/` (test PBI 2–14 cũ + PBI 15 mới pass — **đặc biệt** chạy lại test màn `01`/form sau khi trích `CategoryRow` (R4) và thêm param `initialParentId` (T002): `category_list_screen_test`, `category_form_screen_test` — khẳng định trích nguyên trạng + param mặc định `null` không hồi quy PBI 13/14); xác nhận **không** cần `build_runner`/`.g.dart` mới (schema v4 giữ nguyên — plan §Rủi ro); đối chiếu danh sách "Không đổi" plan.md — `core/category/*` (category, category_list, category_form, category_source, category_presets), `data/*` (repository, drift, db, deps), `theme/*`, `category_picker_screen.dart`, `add_transaction_screen.dart`, `transaction_*`, `wallet_*`, màn PIN/boot, `app_shell.dart`, `settings_screen.dart` không thay đổi; ghi nhận quyết định (R1 entry màn `01` có con → màn `03` + reload lặng, R3 vùng tiêu đề = sửa cha tự dựng AppBar, R4 `CategoryRow` dùng chung, R5 preset cha qua `initialParentId`, R7 con đổi cha rời nhóm cũ, R8 reload 2 tầng, R9 empty phòng thủ) vào trạng thái PBI
- [X] T009 Kiểm chứng thủ công emulator theo `.specify/specs/15/quickstart.md` nhóm A–H: Cài đặt → Danh mục (màn `01`) → chạm **"Ăn uống"** (cha có con) → màn con: app bar teal + back trái + tiêu đề 2 dòng "Ăn uống"/"Danh mục con" + "+" phải, không bottom nav/số tiền, đúng 3 con seed thứ tự ổn định, mỗi dòng bubble + tên + chevron, cuối list hàng "+ Thêm danh mục con", mở < 1 s (A); tạo cha-có-con khác qua form → vào màn con chỉ thấy con của cha đang xem không lẫn (B); tên dài ~30 ký tự không tràn/cắt chevron, list cuộn (C); ẩn con → vẫn hiện đúng vị trí + mờ + "Đã ẩn", bỏ ẩn lại được (D); chạm con → Sửa prefill + ô loại khóa readonly (con cùng loại cha), đổi tên/icon/màu lưu phản ánh ngay, **đổi cha con** sang cha khác → về không còn trong nhóm cũ + màn `01` số con cha cập nhật, back trước lưu không đổi (E); "+"/hàng Thêm → màn Thêm cha preset "Ăn uống" + loại Chi tiêu, chỉ nhập tên → lưu về cuối nhóm, thêm liên tiếp không trùng (F); chạm **vùng tiêu đề** (giữa back và "+") → Sửa cha: loại khóa + ô cha không mở, đổi tên → tiêu đề mới + danh sách con giữ nguyên, back → màn `01` đúng tab + tên/số con mới; chạm sát back → quay lại (không mở Sửa cha), chạm "+" → Thêm (không mở Sửa cha) (G); chuyển hết con sang cha khác → empty hướng dẫn + vẫn thêm được, cỡ chữ lớn nhất/safe area không vỡ/che (H); ghi rõ giới hạn seed (chỉ "Ăn uống" có con; cha loại khác tự tạo theo quickstart B) vào trạng thái PBI; ghi kết quả từng mục — ✅ QA thủ công A–H pass (user xác nhận, 2026-09-06).
- [X] T010 Cập nhật wiki tri thức qua skill `sora-wiki` + append `wiki-knowledge/log.md`: entity **Danh mục** (plan §Kiểm tra ⚠️) — màn `03` danh sách danh mục con đã triển khai (nội dung/bố cục mockup `03`: chỉ con trực tiếp cùng loại của cha đang xem theo `sortOrder`, con ẩn vẫn hiện + dấu phân biệt; entry từ màn `01` — chạm cha có con; **vùng tiêu đề = điểm sửa danh mục cha** có con — khóa loại + không đổi cha; chạm con → sửa con, con đổi cha rời nhóm cũ; thêm nhanh con preset cha qua `initialParentId` — 2 điểm thêm "+"/hàng cuối); refresh lặng 2 tầng màn sau pop (không refresh tay); `CategoryRow` widget dùng chung `01`/`03`; schema v4 giữ nguyên không build_runner; ngoài phạm vi: màn sắp xếp `04`, xóa/gộp danh mục (PBI sau)

## Sơ đồ phụ thuộc

```text
Foundational:
  T001 [P] core/widgets/category_row.dart (CategoryRow — trích nguyên trạng từ màn 01)
  T002 [P] screens/category_form_screen.dart (thêm param int? initialParentId)
US1:
  T001 + T002 ──▶ T003 category_child_list_screen.dart (màn 03: đọc 1 loại + dựng + reload lặng)
                    ├──▶ T005 [P] [US1] category_child_list_screen_test.dart
  T002 ──▶ T006 [P] [US1] category_form_screen_test.dart (case Thêm initialParentId)
  T003 (+T001) ──▶ T004 category_list_screen.dart (dùng CategoryRow; dòng có con → push màn 03; reload lặng)
                    └──▶ T007 [US1] category_list_screen_test.dart (thay case no-op cũ + back)
Polish:
  T008 analyze + full test (không hồi quy PBI 2–14) ──▶ T009 QA emulator (quickstart A–H) ──▶ T010 wiki sync
```

Thứ tự thực thi chính: `T001 → T002 → T003 → T004 → T007 → T008 → T009 → T010` (test `[P]` chạy sau code tương ứng: T005 sau T003, T006 sau T002; T007 sau T004). Mấu chốt: **T001 `CategoryRow`** (màn `01` lẫn `03` dùng) + **T002 `initialParentId`** (form `02`) là 2 mảnh độc lập xong trước dựng màn **T003**; **T004** nối điểm vào màn `01` là bước cuối US1 (cần màn `03` T003 đã tồn tại); **không có** `build_runner`/seam/schema mới (schema v4 giữ nguyên) nên thứ tự thoải mái như PBI 12/13/14.

## Ví dụ chạy song song

```text
# Foundational — 2 mảnh độc lập (khác file, không chặn nhau):
T001 [P] core/widgets/category_row.dart     T002 [P] screens/category_form_screen.dart (initialParentId)

# US1 — 2 test màn độc lập (sau code tương ứng, khác file):
T005 [P] [US1] category_child_list_screen_test.dart (sau T003)
T006 [P] [US1] category_form_screen_test.dart        (sau T002)

# T007 list_screen_test chạy sau T004 (màn 01 đã nối màn 03) — tuần tự không song song [P].
```

## Chiến lược triển khai

- **MVP đề xuất**: User Story 1 (toàn bộ PBI 15 — đặc tả là một lát cắt "màn danh sách danh mục con" duy nhất, không chia user story P1/P2/P3; spec không gán mức ưu tiên theo story). Foundational dựng 2 mảnh tái dùng — `CategoryRow` trích từ màn `01` (R4) + param `initialParentId` ở form `02` (R5) — làm tiền đề bắt buộc; US1 ghép `CategoryChildListScreen` (màn `03`, thuần đọc + dựng) + nối điểm vào màn `01` (dòng có con → màn con; dòng không con giữ Sửa) + reload lặng 2 tầng để ra giá trị end-to-end (FR-001/FR-009/FR-011).
- **Thứ tự giao hàng tăng dần**: Foundational (widget `CategoryRow` chung + param form — không đổi hành vi màn `02`, test PBI 14 giữ) → US1 màn `03` (screen + widget test phủ bố cục/chỉ-con-đúng-cha/con-ẩn/thêm-nhanh-preset/sửa-cha-qua-tiêu-đề/đổi-cha-con-rời-nhóm/empty phòng thủ) → nối màn `01` (dòng có con → màn con + refresh sau pop + widget test) → Polish (analyze/test toàn cục không hồi quy + QA emulator quickstart A–H + sync wiki).
