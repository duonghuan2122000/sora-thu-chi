# Danh sách Task: Thay đổi giao diện Sáng / Tối / Theo hệ thống

**Mã PBI**: 18
**Nguồn**: plan.md, spec.md, data-model.md, research.md, quickstart.md

## Pha 1: Setup

*(Không có task — PBI 18 **không thêm dependency/pubspec mới** (research R2/R4 — dùng `ThemeMode` Flutter + `ThemeExtension<SoraColors>`, không package), **không tạo `contracts/`** (app nội bộ offline — plan §Giai đoạn 1), **không migration drift** (thêm 1 row `themeMode` vào bảng `AppSettings` v5 sẵn có, không đổi schema — data-model §Không bảng mới).)*

## Pha 2: Foundational

*(Nền tảng theme — chưa thay đổi hiển thị ở giao diện sáng: domain `ThemeMode` + hằng khóa, seam `ThemeStore`, `ThemeController`, bộ 2 theme qua `ThemeExtension<SoraColors>` (bản light **giữ nguyên giá trị** `AppColors` cũ → 409 test hiện hữu không vỡ), gốc `SoraApp` nối `darkTheme` + `themeMode` reactive (mặc định `system` — chưa có cách đổi nên hiển thị giữ như cũ). Theo plan.md "Cấu trúc dự án dự kiến", research R1/R2/R4/R8/R10, data-model §Row mới + §Domain. Các màn hiện có chưa cần sửa ở pha này — vẫn đọc `AppColors` const (giá trị light).)*

- [X] T001 [P] Tạo `app/sora_thu_chi/lib/core/theme/theme_mode.dart`: module domain thuần (không đọc DB/UI) — hằng `const kKeyThemeMode = 'themeMode'` (data-model §Row mới — convention PBI 17); `String themeModeToStorage(ThemeMode mode)` → `'light'`/`'dark'`/`'system'` khớp tên enum; `ThemeMode themeModeFromStorage(String? raw)` — khớp 3 chuỗi hợp lệ, **null/ngoài 3 chuỗi → `ThemeMode.system`** (an toàn, không ném — data-model §Domain, bám `UtilitiesPrefs.fromSettings`); `String themeModeLabel(ThemeMode mode)` → `light`→"Sáng", `dark`→"Tối", `system`→"Theo hệ thống" (R10 — tên thống nhất spec §Giả định); doc comment ngắn (nguồn giá trị duy nhất xuyên suốt là `ThemeMode` Flutter, không tạo enum riêng)
- [X] T002 [P] Tạo `app/sora_thu_chi/lib/core/theme/theme_store.dart`: seam đọc/ghi trừu tượng **`ThemeStore`** (bám pattern `UtilitiesStore` PBI 17 — màn & seam chỉ phụ thuộc interface để test bơm fake, research R8) với `Future<ThemeMode?> load()` (row vắng/key lạ → trả `null` = mặc định `system` do controller tự xử) và `Future<void> save(ThemeMode mode)`; doc comment ngữ nghĩa (lưu 1 row `themeMode` trong `AppSettings`; row chỉ ghi khi user đổi — write-through)
- [X] T003 Tạo `app/sora_thu_chi/lib/core/theme/theme_controller.dart`: `GetxController` **`ThemeController`** (bám `PinController`) — constructor `ThemeController(this._store)`; field `final ThemeMode _initial = ThemeMode.system;` + `final Rx<ThemeMode> mode = ThemeMode.system.obs;` + `Future<void> _saveTail = Future.value();`; `Future<void> load()` — đọc `_store.load()`, nếu trả `ThemeMode` khác null → `mode.value = mode` (nối đuôi/không ghi); `void setMode(ThemeMode m)` — nếu `m == mode.value` → return (tránh ghi thừa, plan §Rủi ro — không vòng lặp), ngược lại `mode.value = m;` rồi xếp `_store.save(m)` qua `_saveTail` (chọn nhanh liên tiếp → save cuối đè — lần chạm cuối là trạng thái cuối, trường hợp biên spec); **phụ thuộc: T001 + T002**
- [X] T004 Tạo `app/sora_thu_chi/lib/data/theme_store_drift.dart`: class **`DriftThemeStore implements ThemeStore`** (bám `DriftUtilitiesStore` PBI 17 — cùng bảng `AppSettings`, schema v5 đã có, không đụng migration) — `load()`: `_db.select(_db.appSettings)..where((t) => t.key.equals(kKeyThemeMode))` lấy row → trả `themeModeFromStorage(row?.value)` (vắng → null → `system`); `save(mode)`: `_db.into(_db.appSettings).insertOnConflictUpdate(AppSettingsCompanion.insert(key: kKeyThemeMode, value: themeModeToStorage(mode)))` — **chỉ upsert 1 key `themeMode`, không xoá row cài đặt khác** (data-model §Row mới); **phụ thuộc: T001 + app_database.dart (bảng `AppSettings` v5)**
- [X] T005 Tạo `app/sora_thu_chi/lib/data/theme_deps.dart`: hàm **`ensureThemeStore()`** (bám `ensureUtilitiesStore` — `utilities_deps.dart`): `Get.isRegistered<ThemeStore>()` → `Get.find` (test bơm fake trước → trả fake, không tạo drift, R8); ngược lại tạo `DriftThemeStore(AppDatabase())` + `Get.put`; hàm **`ensureThemeController()`**: `Get.isRegistered<ThemeController>()` → find; ngược lại `Get.put(ThemeController(ensureThemeStore()))`; **phụ thuộc: T004 + T003**
- [X] T006 [P] Tạo `app/sora_thu_chi/test/fakes/fake_theme_store.dart`: fake bộ nhớ implements **`ThemeStore`** (bám `FakeUtilitiesStore`) — `ThemeMode? _mode` (mặc định null = chưa từng đổi); `load()` trả `_mode`; `save(mode)` gán `_mode = mode`; getter `ThemeMode? get storedMode` (widget test assert trạng thái store sau chọn); constructor tùy chọn `FakeThemeStore({this.initialMode})`; **phụ thuộc: T002** (song song T003–T005 — khác file)
- [X] T007 Tạo `app/sora_thu_chi/lib/theme/sora_colors.dart`: **`SoraColors extends ThemeExtension<SoraColors>`** (bộ 2 theme qua context — research R4): các field token **"đổi theo theme"** với giá trị **light = nguyên giá trị `AppColors` hiện có** (409 test so màu hằng cũ vẫn xanh), **dark** theo plan §Ánh xạ token / doc `docs/tool/giai-phap-tien-ich-ca-nhan-hoa.md` §1.2 — `static const SoraColors light` = `background` #FFFFFF, `surface` #FFFFFF (card phân lớp), `softCardBg` #F1EFE8, `textPrimary` #1A1A1A, `textSecondary` #6B6B6B, `tabInactive` #9B9B9B, `listLabel` #5F5E5A, `divider` #E0E0E0, `listDivider` #EFEFEF, `dotEmpty` #B4B2A9, `tealLightBg` #E1F5EE, `coralLightBg` #FAECE7, `tealLightText` #CDE9DF, `tealOnNeutral` #0F6E56 (glyph teal trên nền trung tính — thay `AppColors.teal` ở ngữ cảnh icon/chữ, không thay fill), `coralOnNeutral` #D85A30 (glyph coral trên nền trung tính); `static const SoraColors dark` = `background` #121212, `surface` #1E1E1E, `softCardBg` #242420, `textPrimary` #F2F2F0, `textSecondary` #A8A8A3, `tabInactive` #A8A8A3, `listLabel` #B4B4AE, `divider` #3A3A36, `listDivider` #2E2E2A, `dotEmpty` #6E6D66, `tealLightBg` #17332C, `coralLightBg` #3A2518, `tealLightText` #CDE9DF (giữ, sáng nhẹ nếu đối chiếu cần), `tealOnNeutral` #3FA98A, `coralOnNeutral` #E8734C (glyph sáng hơn trên nền tối — FR-009); triển khai `copyWith` + `lerp` (mọi field) + `static SoraColors of(BuildContext)` = `Theme.of(context).extension<SoraColors>() ?? SoraColors.light` (fallback light khi theme không đăng ký extension → test cũ pump `AppTheme.themeData`/MaterialApp thường không crash — R4); doc comment từng field ghi rõ vai trò semantic + **token `AppColors` cũ mà nó thay thế** + **on-brand giữ `AppColors.white` không nằm ở đây** (2 vai trò `white` — plan §Rủi ro)
- [X] T008 Sửa `app/sora_thu_chi/lib/theme/app_theme.dart`: (a) `themeData` (light) — giữ `scaffoldBackgroundColor` nền sáng + app bar teal/foreground trắng như cũ (không đổi hành vi) nhưng **đăng ký `extensions: [SoraColors.light]`**; (b) thêm **`static ThemeData get darkThemeData`** — `ColorScheme.fromSeed(seedColor: AppColors.teal, brightness: Brightness.dark)` + `scaffoldBackgroundColor: SoraColors.dark.background` + `appBarTheme` giữ teal fill/`AppColors.white` foreground + `extensions: [SoraColors.dark]`; **phụ thuộc: T007**
- [X] T009 Sửa `app/sora_thu_chi/lib/app.dart` (R2/R3/R8): `SoraApp` thêm seam `final ThemeStore? themeStore;` (bơm test — mặc định null → `ensureThemeStore()`); trong `initState`: `final store = widget.themeStore ?? ensureThemeStore();` rồi nếu `!Get.isRegistered<ThemeController>()` → `Get.put(ThemeController(store))`, kế đó `Get.find<ThemeController>().load()` (mặc định `system` trước khi load xong — R3; home là `PinGate` nền trống nên chấp nhận nháy khung đầu); `build`: `GetMaterialApp` thêm `darkTheme: AppTheme.darkThemeData` + `themeMode: Obx(() => Get.find<ThemeController>().mode.value)` (GetX reactive — đổi Rx → toàn cây rebuild theo theme mới ngay, FR-005/SC-004); **phụ thuộc: T003 + T005 + T008**
- [X] T010 [P] Tạo `app/sora_thu_chi/test/theme_mode_test.dart`: unit test thuần `theme_mode.dart` (không fake/widget/sqlite): `themeModeToStorage` 3 giá trị; `themeModeFromStorage` — null/''/rỗng/`'LIGHT'`/`'auto'` → `system` (không ném), `'light'`→light…; round-trip; `themeModeLabel` — system→"Theo hệ thống", light→"Sáng", dark→"Tối"; **phụ thuộc: T001** (độc lập, chạy sớm được)
- [X] T011 Tạo `app/sora_thu_chi/test/theme_store_drift_test.dart` + `app/sora_thu_chi/test/theme_controller_test.dart`: (a) drift in-memory (bám `utilities_store_drift_test.dart` — helper mở `AppDatabase(NativeDatabase.memory())`, host thiếu sqlite → `markTestSkipped`): chưa có row `themeMode` → `load()` trả null; round-trip `save`→`load` đúng; save đè nhiều lần → giá trị cuối; save không xoá row `hideBalance`/key lạ khác trong `AppSettings` (chèn row lạ trước rồi save); (b) controller dùng `FakeThemeStore`: `mode` mặc định system; `load()` khi store có light → mode=light; `setMode` đổi Rx + store `storedMode` ghi; `setMode` cùng giá trị → không ghi thừa (store giữ); `setMode` liên tiếp nhanh → `storedMode` cuối = lần cuối; **phụ thuộc: T003 + T004 + T006**
- [X] T012 Sửa `app/sora_thu_chi/test/pin_flow_test.dart` (+ `test/widget_test.dart` nếu có pump `SoraApp`): mọi chỗ pump `SoraApp(store: fakePin)` thêm `themeStore: FakeThemeStore()` (sau T009 `SoraApp.initState` tạo `ThemeController` + gọi `load()` — không bơm fake sẽ khởi tạo `DriftThemeStore`/sqlite native, vỡ test; R8); giữ nguyên assert PIN (không đổi hành vi — default system = light ở môi trường test); **phụ thuộc: T009 + T006**

## Pha 3: User Story 1 - Giao diện Sáng / Tối / Theo hệ thống (Ưu tiên: P1) 🎯 MVP

**Mục tiêu**: Đưa dark mode thành hiện thực end-to-end — (a) **mọi màn/chrome sau mở khóa + màn PIN/boot** chuyển sang đọc token qua `SoraColors.of(context)` (giao diện sáng **giữ nguyên giá trị** → không hồi quy light; giao diện tối đọc được, không vùng "chìm" — FR-005/FR-009/SC-007); (b) **màn con "Giao diện"** (`02`) theo `docs/tool/02-giao-dien.svg` + kích hoạt hàng "Giao diện" màn `01` (bỏ no-op, trailing reactive) + chọn → áp ngay toàn app + nhớ qua restart (FR-001…008). Đặc tả là **một** feature duy nhất không chia US2/US3 (spec không gán ưu tiên theo story; plan §Kiến trúc: đổi theme = themeMode đổi → Theme mới → widget đọc token tự rebuild). Nhóm thứ tự: chrome dùng chung trước → màn theo module (mỗi nhóm chạy full test light không hồi quy) → màn 02 + hàng 01 + test → smoke dark.
**Tiêu chí kiểm thử độc lập**: `flutter analyze` sạch + `flutter test` toàn bộ pass với mọi test light cũ giữ kết quả; `flutter test test/theme_screen_test.dart test/dark_theme_smoke_test.dart test/utilities_screen_test.dart` pass — bơm `ThemeController(FakeThemeStore)`: màn 02 đủ 3 card đúng thứ tự + radio mặc định system + chọn → controller Rx đổi + store ghi + app đổi theme ngay; hàng "Giao diện" màn 01 hiện "Theo hệ thống" và push màn 02; smoke đại diện các màn ở theme tối không overflow/không "chìm".

**Bước A — Chrome dùng chung (màu theo theme trước khi màn dùng):**

- [X] T013 [P] [US1] Sửa `app/sora_thu_chi/lib/core/widgets/sub_page_scaffold.dart` + `app/sora_thu_chi/lib/core/widgets/screen_header.dart`: chuyển token "đổi theo theme" sang `SoraColors.of(context)` — nền body/app bar: giữ fill teal + `AppColors.white` chữ/icon **on-brand** (không đổi), nền thân/card chuyển `SoraColors.background`/`surface`, text → `textPrimary`/`textSecondary`, phân cách → `divider`; bỏ `const` nơi cần `context`; giá trị light giữ y; **phụ thuộc: T007**; sau khi sửa chạy `flutter analyze` + `flutter test test/pin_flow_test.dart test/widget_test.dart` (không hồi quy)
- [X] T014 [P] [US1] Sửa `app/sora_thu_chi/lib/core/widgets/app_bottom_nav_bar.dart` + `app/sora_thu_chi/lib/core/app_shell.dart`: nền bottom nav/shell đổi theo theme (`background`/`surface`), icon tab active teal giữ fill teal thương hiệu, tabInactive → `SoraColors.tabInactive`, nền FAB/ví tròn giữ teal fill + `AppColors.white` glyph on-brand; **phụ thuộc: T007**; chạy `flutter analyze` + test màn Tổng quan/Giao dịch liên quan
- [X] T015 [P] [US1] Sửa `app/sora_thu_chi/lib/core/widgets/category_row.dart` + `app/sora_thu_chi/lib/core/widgets/category_icon.dart` + `app/sora_thu_chi/lib/core/widgets/amount_keypad.dart`: text/nền phím/vòng icon → `SoraColors.of(context)` (`textPrimary`/`textSecondary`/`surface`/`softCardBg`/`divider`…); **bảng màu danh mục/ví là màu nhận diện — không đổi** (R4/FR-009); icon teal trên nền trung tính → `tealOnNeutral`; **phụ thuộc: T007**; chạy `flutter analyze` + test category/list liên quan
- [X] T016 [P] [US1] Sửa `app/sora_thu_chi/lib/core/boot_gate.dart` + toàn bộ màn PIN (`app/sora_thu_chi/lib/screens/pin/pin_lock_screen.dart`, `pin_setup_screen.dart`, `widgets/pin_dots.dart`, `widgets/pin_keypad.dart`): không để nền trắng chói ở giao diện tối — nền màn/khay phím/chữ theo `SoraColors.of(context)` (`background`/`surface`/`textPrimary`/`textSecondary`/`dotEmpty`/`tealOnNeutral`…); giữ màu teal nút xác nhận/logo on-brand; **phụ thuộc: T007**; chạy `flutter analyze` + `flutter test test/pin_flow_test.dart test/pin_controller_test.dart`

**Bước B — Màn theo module (sáng giữ nguyên, tối đọc được):**

- [X] T017 [P] [US1] Sửa màn ví `app/sora_thu_chi/lib/screens/wallet_list_screen.dart`, `wallet_form_screen.dart`, `wallet_detail_screen.dart`: token nền/card/chữ/phân cách/số tiền/vòng icon → `SoraColors.of(context)` (giữ nguyên giá trị light); màu chọn cho ví trong form là **màu nhận diện — không đổi**; số tiền giữ format `…đ` (không liên quan theme); **phụ thuộc: T007**; chạy `flutter analyze` + `flutter test test/wallet_*_screen_test.dart test/wallet_controller_test.dart test/wallets_dao_test.dart`
- [X] T018 [P] [US1] Sửa màn danh mục `app/sora_thu_chi/lib/screens/category_list_screen.dart`, `category_form_screen.dart`, `category_picker_screen.dart`, `category_child_list_screen.dart`, `category_sort_screen.dart`: token nền/card/chữ/label/phân cách/vòng icon/radio-chấm → `SoraColors.of(context)`; **bảng màu preset danh mục không đổi** (màu nhận diện — FR-009); nút chọn màu hiển thị đúng màu người dùng chọn (không đổi theo theme); **phụ thuộc: T007**; chạy `flutter analyze` + `flutter test test/category_*_screen_test.dart test/category_*.dart` (list/form/sort/picker)
- [X] T019 [P] [US1] Sửa màn giao dịch `app/sora_thu_chi/lib/screens/transaction_screen.dart`, `transaction_detail_screen.dart`, `add_transaction_screen.dart`, `search_filter_screen.dart`, `wallet_transfer_screen.dart`: token → `SoraColors.of(context)`; giữ quy ước **thu = teal / chi = coral** (fill & chữ — nhận diện không đổi, FR-009); số tiền thu/chi giữ màu teal/coral đúng ngữ cảnh và format `…đ`; ảnh hóa đơn/receipt & nội dung người nhập **không đổi màu** (dùng nguyên asset/Text không tint); **phụ thuộc: T007**; chạy `flutter analyze` + `flutter test test/transaction_*_test.dart test/search_filter_screen_test.dart test/add_transaction_screen_test.dart test/wallet_transfer_screen_test.dart test/transactions_dao_test.dart`
- [X] T020 [P] [US1] Sửa `app/sora_thu_chi/lib/screens/dashboard_screen.dart` + `app/sora_thu_chi/lib/screens/report_screen.dart`: nền/card/chữ/divider/vòng icon/biểu đồ (màu nền của chart tooltip/axis) → `SoraColors.of(context)`; giữ thu=teal/chi=coral trong số liệu và biểu đồ (nhận diện); **phụ thuộc: T007**; chạy `flutter analyze` + test Tổng quan/Báo cáo nếu có
- [X] T021 [P] [US1] Sửa `app/sora_thu_chi/lib/screens/settings_screen.dart`: nền/card nhóm/chữ/label/divider/hàng → `SoraColors.of(context)` (bám cách màn `01` settings hiện dùng token); avatar/ảnh hồ sơ không tint; **phụ thuộc: T007**; chạy `flutter analyze` + `flutter test test/settings_screen_test.dart`

**Bước C — Màn 02 "Giao diện" + kích hoạt hàng màn 01 + test:**

- [X] T022 [US1] Tạo `app/sora_thu_chi/lib/screens/theme_screen.dart`: `StatelessWidget` **`ThemeScreen`** — màn con shell (FR-001): `SubPageScaffold(title: 'Giao diện', child: SafeArea(top: false, child: ListView(...)))` (padding đáy đủ — FR-010), **không** bottom nav; seam `final ThemeController? controller;` (default `Get.find<ThemeController>()` — test bơm qua Get); thân liệt kê **3 card đúng thứ tự Sáng – Tối – Theo hệ thống** đối chiếu `02-giao-dien.svg` (R6): mỗi card hàng chọn được **tô nền nhạt khi đang chọn** + vòng icon có viền teal; cấu trúc hàng = `Container` bo 10 + `InkWell` toàn hàng → `Row`: vòng nền nhạt (`SoraColors.tealLightBg`/`coralLightBg` theo lựa chọn) chứa icon minh họa (Sáng = `Icons.wb_sunny`, Tối = `Icons.dark_mode`, Theo hệ thống = `Icons.devices`) màu `tealOnNeutral`, giữa là `Expanded` cột tên đậm (`textPrimary`) + dòng phụ (`textSecondary` — Sáng: "Nền trắng, chữ tối"; Tối: "Nền tối, chữ sáng, đỡ mỏi mắt ban đêm"; Theo hệ thống: "Tự đổi theo cài đặt điện thoại"), cuối là **radio tự dựng** (R6 — không dùng `RadioListTile` Material): vòng tròn viền xám nhạt (`SoraColors.dotEmpty`) chưa chọn / chấm teal fill + dấu tích trắng khi chọn; chân màn ghi chú xám `SoraColors.textSecondary` "Thay đổi được áp dụng ngay lập tức, không cần khởi động lại ứng dụng." (lấy nguyên văn svg); trạng thái chọn đọc **`Obx(() => controller.mode.value)`** — chọn 1 hàng → `controller.setMode(mode)` (Rx đổi → toàn app đổi ngay, card tự tô đúng theme mới — FR-005); chống tràn cỡ chữ lớn/màn nhỏ (tên `Expanded`, icon vòng kích thước cố định, ListView cuộn — FR-010/SC-008); **phụ thuộc: T003 + T013** (cần `SubPageScaffold` đã theo theme)
- [X] T023 [US1] Sửa `app/sora_thu_chi/lib/screens/utilities_screen.dart` (R7/R10): (a) đổi màu token trong màn sang `SoraColors.of(context)` (`tealLightBg`→`SoraColors.tealLightBg`, icon hàng `teal`→`SoraColors.tealOnNeutral`, `textPrimary`/`textSecondary`/`tabInactive`/`listDivider`, nền màn qua `SubPageScaffold`/`background`) — giá trị light giữ nguyên (đã sửa chrome T013); (b) hàng **"Giao diện"**: bỏ no-op — `trailing` đổi từ tĩnh `_trailingValue('Hệ thống')` thành **reactive** đọc `Get.find<ThemeController>()` qua `Obx(() => _trailingValue(themeModeLabel(controller.mode.value)))` → hiện "Sáng"/"Tối"/"Theo hệ thống" đúng lựa chọn hiện hành, cập nhật ngay khi đổi dù đang ở màn 02 (màn 01 vẫn mounted dưới route — FR-007); `onTap` hàng → `Navigator.push(MaterialPageRoute(builder: (_) => const ThemeScreen()))` (FR-001, kích hoạt điểm vào no-op PBI 17); (c) 4 hàng điều hướng còn lại (Ngôn ngữ, Định dạng & Tiền tệ, Tìm kiếm toàn cục, Quản lý Tag) **giữ no-op**; doc comment class cập nhật (hàng Giao diện đã kích hoạt, dark mode áp dụng cho màn); import `theme_screen.dart`, `core/theme/*`; **phụ thuộc: T022 + T003**
- [X] T024 [US1] Sửa `app/sora_thu_chi/test/utilities_screen_test.dart` (R7/R10 — thay đổi hành vi **đúng spec**, plan §Rủi ro không phải hồi quy): thêm helper đăng ký GetX — `Get.reset(); Get.put<ThemeController>(ThemeController(FakeThemeStore())); addTearDown(Get.reset);` trước mỗi pump (màn giờ đọc controller qua Obx — nếu thiếu sẽ `Get.find` ném); **cập nhật case cũ**: case "trailing mặc định" đổi assert `Text('Hệ thống')` → `Text('Theo hệ thống')` (R10); case "(g) 5 hàng no-op" rút bớt "Giao diện" (giờ mở màn) còn 4 hàng no-op; **thêm case mới**: (1) chạm hàng "Giao diện" → push `ThemeScreen` (`find.byType(ThemeScreen)`) + back → về màn Tiện ích (FR-001); (2) trailing hiện "Theo hệ thống" lần đầu; (3) set mode qua controller (fake store initialMode `ThemeMode.dark` + `ThemeController(fake).load()`) → trailing hiện "Tối" — phản ánh lựa chọn; (4) khi controller mode `dark`, chạm "Tối"... assert trailing đổi; giữ nguyên mọi case switch/2 công tắc/widget-dialog/cỡ chữ (đã có); **phụ thuộc: T023 + T006**
- [X] T025 [US1] Tạo `app/sora_thu_chi/test/theme_screen_test.dart`: widget test màn 02 bơm controller (helper `Get.put(ThemeController(FakeThemeStore()))`, wrap `MaterialApp(theme: AppTheme.themeData, darkTheme: AppTheme.darkThemeData, themeMode: ThemeMode.system)`) — **(a)** bố cục mockup `02` (acceptance 1–2/FR-001–002): `SubPageScaffold` app bar back + tiêu đề "Giao diện", **không** bottom nav, đủ **3** card đúng thứ tự Sáng → Tối → Theo hệ thống, mỗi card có icon + tên + dòng phụ đúng mockup + radio, chân màn ghi chú "…áp dụng ngay lập tức…"; **(b)** radio mặc định (acceptance 3/FR-004): chưa đổi → card "Theo hệ thống" chọn (chấm teal), không hàng thứ 2 chọn; **(c)** chọn "Tối" (acceptance 4/FR-005): chạm hàng → radio Tối chọn + Sáng/Theo hệ thống bỏ chọn + controller `mode.value == ThemeMode.dark` + `fake.storedMode == dark` (save gọi) — assert qua `tester.widget`/state; **(d)** áp ngay toàn app: sau khi chạm, assert theme hiện tại của MaterialApp là dark (`Theme.of(element).brightness == Brightness.dark` — Rx đổi themeMode → rebuild); **(e)** persist + mở lại (acceptance 5/FR-006): tạo `FakeThemeStore(initialMode: dark)` + pump lại → radio Tối chọn sẵn; **(f)** chọn nhanh liên tiếp các hàng → radio cuối = lần chạm cuối, `storedMode` cuối đúng (biên spec); **(g)** cỡ chữ lớn + màn nhỏ (textScaleFactor 2.0, surface 360x640) → cuộn tới card cuối không overflow (`takeException()` null — FR-010/SC-008); **phụ thuộc: T022 + T006**
- [X] T026 [US1] Sửa `app/sora_thu_chi/test/settings_screen_test.dart` **chỉ nếu có case đẩy thật `UtilitiesScreen`** (case dùng callback `onManageUtilitiesTap` thì không đổi): bơm helper đăng ký `ThemeController(FakeThemeStore())` trước khi push để màn Tiện ích đọc controller không ném; giữ nguyên mọi case profile/cỡ chữ/Danh mục/quản lý ví; **phụ thuộc: T023 + T006** (test sau khi utilities nối controller)
- [X] T027 [US1] Tạo `app/sora_thu_chi/test/dark_theme_smoke_test.dart`: smoke đại diện các nhóm màn ở **theme tối** (R5/SC-007 — không overflow, không "chìm", đọc được): helper pump từng màn với `MaterialApp(theme: AppTheme.themeData, darkTheme: AppTheme.darkThemeData, themeMode: ThemeMode.dark)` + register `ThemeController(FakeThemeStore())` khi màn cần; pump ít nhất: `SubPageScaffold`/màn con (theme_screen/utilities), màn danh sách + form (category_list hoặc wallet_list), màn giao dịch (transaction_screen), `DashboardScreen` nếu pump được độc lập (nếu cần nhiều store/controller → chỉ chọn màn self-contained + assert `tester.takeException()` null sau khi cuộn, text chính xuất hiện — không đơn thuần nền trắng); ghi rõ trong test là smoke (không phủ mọi nhánh logic — bổ sung case dark cụ thể vào test module sẵn có khi cần, plan §Cấu trúc "bổ sung case dark nếu cần"); **phụ thuộc: T013–T021** (sau khi các màn đã migrate)

## Pha 4: Polish & Cross-cutting

- [X] T028 [P] Dọn `app/sora_thu_chi/lib/theme/app_colors.dart`: sau khi mọi màn đã đọc `SoraColors.of(context)`, **xóa token "đổi theo theme" đã hết tham chiếu** (kiểm bằng grep từng tên: `white` dùng làm nền, `textPrimary`, `textSecondary`, `divider`, `listDivider`, `listLabel`, `tabInactive`, `dotEmpty`, `tealLightBg`, `coralLightBg`, `tealLightText` — trừ chỗ còn là on-brand/fill thì giữ); chỉ giữ màu **bất biến** theo plan (teal fill thương hiệu, `white` on-brand khi đặt trên teal/FAB/chữ trắng trên nền màu, coral fill chi/cảnh báo, `avatarBg`, bảng màu danh mục/ví) + ghi chú "hằng light nay sống ở `SoraColors.light`"; nếu còn token chưa hết tham chiếu do để sót → **không xóa vội**, quay lại migrate màn đó (task này đảm bảo không còn file widget đọc `AppColors.white`/textPrimary… làm nền ở dark); chạy `flutter analyze` xác nhận không còn import thừa
- [X] T029 Chạy `flutter analyze` (0 warning) + `flutter test` toàn bộ tại `app/sora_thu_chi/` (test PBI 2–17 cũ + PBI 18 mới pass — khẳng định **light không hồi quy**: mọi suite cũ giữ kết quả vì SoraColors.light = giá trị cũ; đặc biệt chạy lại `settings_screen_test`/`utilities_screen_test` sau đổi nhãn "Theo hệ thống" + bỏ no-op hàng Giao diện, và `pin_flow_test`/`widget_test` sau khi SoraApp bơm FakeThemeStore — T012); xác nhận **không** cần chạy `build_runner` (không đổi schema drift v5 — chỉ thêm row runtime, plan §Rủi ro); đối chiếu danh sách "Không đổi" plan.md — `data/*` (trừ 2 file theme mới), `core/category/*`/`core/transaction/*`/`core/wallet/*` (domain thuần), bảng màu preset không thay đổi; ghi nhận quyết định (R1 row `themeMode`, R2 controller gốc MaterialApp, R4 SoraColors light giữ giá trị + dark theo doc §1.2, R5 refactor mọi màn + PIN/boot, R6 radio tự dựng, R7 hàng reactive, R9 Material tự theo system, R10 tên "Theo hệ thống") vào trạng thái PBI
- [X] T030 Kiểm chứng thủ công emulator theo `.specify/specs/18/quickstart.md` bước A–G: A hàng "Giao diện" màn 01 hiện "Theo hệ thống" ▸ → mở màn con đúng `02-giao-dien.svg` (app bar teal + back + tiêu đề "Giao diện", không bottom nav, 3 card + ghi chú chân màn); B radio "Theo hệ thống" chọn sẵn lần đầu; C chạm "Tối" → radio Tối chọn, cả app nền tối ngay không restart, rà vài màn (Giao dịch/ví/danh mục) không "chìm", số tiền vẫn `1.234.000 đ`, teal/coral phân biệt, ảnh hóa đơn/avatar không đổi màu → chọn "Sáng" về sáng; D chọn Tối → back hàng 01 hiện "Tối"; E tắt hẳn app mở lại vẫn tối + radio Tối chọn; F chọn "Theo hệ thống", đổi điện thoại sáng/tối → app tự đổi; G chạm nhanh liên tiếp → radio cuối = lần chạm cuối; cỡ chữ lớn + màn nhỏ cuộn tới card cuối không vỡ; chọn mới rồi tắt hẳn app ngay → mở lại giữ; ghi kết quả từng bước vào trạng thái PBI
- [X] T031 Cập nhật wiki tri thức qua skill `sora-wiki` + append `wiki-knowledge/log.md`: entity **Hồ sơ & Bảo mật / Cài đặt** (hoặc page phù hợp nhất) + **Lộ trình** — màn con "Giao diện" **đã triển khai** (thay mục "PBI sau" còn trống): màn `02` (`SubPageScaffold` tiêu đề "Giao diện", 3 card Sáng/Tối/Theo hệ thống — icon trong vòng nhạt + tên + dòng phụ + radio tự dựng, ghi chú chân màn); hàng "Giao diện" màn `01` đã kích hoạt → push màn 02, trailing reactive hiện "Sáng"/"Tối"/"Theo hệ thống" (thay "Hệ thống" cũ — tên thống nhất); **cơ chế dark toàn app**: `ThemeController` (GetX, `Rx<ThemeMode>`) đăng ký gốc `SoraApp` + `GetMaterialApp` `theme/darkTheme/themeMode` (Obx) → chọn đổi theme **ngay không restart**; bộ 2 theme qua `ThemeExtension<SoraColors>` (light giữ nguyên giá trị token cũ — không vỡ test, dark theo doc §1.2 — nền `#121212`/card `#242420`/chữ `#F2F2F0`, giữ teal fill & glyph teal/coral sáng hơn `#3FA98A`/`#E8734C`; mọi màn/chrome + PIN/boot đọc `SoraColors.of(context)`); lưu 1 row `themeMode` trong bảng `AppSettings` v5 (key vắng = system — không migration, schema giữ v5); "Theo hệ thống" tự đổi nhờ Material (`ThemeMode.system`), không tự viết observer; ngoài phạm vi: màn con 03–07, i18n, theme màu tùy biến, tablet

## Sơ đồ phụ thuộc

```text
Foundational (không đổi hiển thị light):
  T001 [P] core/theme/theme_mode.dart        (hằng kKeyThemeMode + parse/label — thuần)
  T002 [P] core/theme/theme_store.dart       (abstract ThemeStore — seam)
  T001 + T002 ──▶ T003 core/theme/theme_controller.dart (Rx<ThemeMode> load/setMode tail)
  T001 + app_database v5 ──▶ T004 data/theme_store_drift.dart (DriftThemeStore — 1 row 'themeMode')
  T003 + T004 ──▶ T005 data/theme_deps.dart  (ensureThemeStore/ensureThemeController)
  T002 ──▶ T006 [P] test/fakes/fake_theme_store.dart
  T007 theme/sora_colors.dart (ThemeExtension .light/.dark + of(context))  ← đọc AppColors light cũ
  T007 ──▶ T008 theme/app_theme.dart (+ darkThemeData + đăng ký ext light/dark)
  T003 + T005 + T008 ──▶ T009 app/app.dart (darkTheme + themeMode Obx + seam themeStore + init controller)
  T001 ──▶ T010 [P] theme_mode_test.dart
  T003+T004+T006 ──▶ T011 theme_store_drift_test.dart + theme_controller_test.dart
  T009 + T006 ──▶ T012 pin_flow_test/widget_test (bơm FakeThemeStore vào SoraApp)
US1 (dark E2E):
  T007 ──▶ Bước A chrome: T013 [P] sub_page_scaffold+screen_header · T014 [P] app_bottom_nav_bar+app_shell
           · T015 [P] category_row+category_icon+amount_keypad · T016 [P] boot_gate + pin (lock/setup/dots/keypad)
  T007 ──▶ Bước B màn: T017 [P] wallet_* · T018 [P] category_* · T019 [P] transaction_*+search+add+transfer
           · T020 [P] dashboard+report · T021 [P] settings
  T003+T013 ──▶ T022 [US1] theme_screen.dart (màn 02)
  T022+T003 ──▶ T023 [US1] utilities_screen.dart (màu SoraColors + hàng Giao diện reactive + push)
  T023+T006 ──▶ T024 [US1] utilities_screen_test.dart · T023 ──▶ T026 [US1] settings_screen_test (nếu cần)
  T022+T006 ──▶ T025 [US1] theme_screen_test.dart
  T013–T021 ──▶ T027 [US1] dark_theme_smoke_test.dart
Polish:
  T028 dọn AppColors (chỉ còn màu bất biến) ──▶ T029 analyze + full test (light không hồi quy)
  ──▶ T030 QA emulator (quickstart A–G) ──▶ T031 wiki sync (sora-wiki)
```

Thứ tự thực thi chính: `T001 → T002 → T003 → T004 → T005 → T006 → T007 → T008 → T009 → T012 → (Bước A chrome T013–T016) → (Bước B màn T017–T021) → T022 → T023 → T024/T025/T026 → T027 → T028 → T029 → T030 → T031`. Mấu chốt: **Foundational dựng engine thuần trước** (domain + seam + controller + store drift + deps + fake — T001–T006, không đụng màn nào), **SoraColors là mắt xích trung tâm** (T007 — mọi task migrate sau phụ thuộc nó); **T009 app.dart nối themeMode** là bước bắt buộc kèm T012 sửa root test (bơm FakeThemeStore, không sqlite native — R8); **Bước A chrome xong trước Bước B** (màn dùng chung chrome đã theo theme); **T022 màn 02 + T023 hàng 01** là lát cắt E2E cuối của story (cần chrome T013 + controller); **chỉ 1 bộ theme light giữ giá trị cũ** nên test light không vỡ, rủi ro chính là refactor để sót token → T028 dọn + T029 full suite bắt lỗi; **không migration drift** (schema v5 giữ nguyên, thêm row lúc runtime — không chạy build_runner).

## Ví dụ chạy song song

```text
# Foundational — 4 mảnh độc lập (khác file, không chặn nhau):
T001 [P] core/theme/theme_mode.dart            (domain thuần — T003/T004/T010 nối sau)
T002 [P] core/theme/theme_store.dart           (seam — T003/T005/T006 nối sau)
T006 [P] test/fakes/fake_theme_store.dart      (sau T002 — song song T003–T005)
T007     theme/sora_colors.dart                (độc lập về file, nhưng mọi migrate đợi nó — làm sớm)

# US1 Bước A — chrome theo theme, 4 nhóm khác file chạy cùng lúc (sau T007):
T013 [P] [US1] sub_page_scaffold + screen_header
T014 [P] [US1] app_bottom_nav_bar + app_shell
T015 [P] [US1] category_row + category_icon + amount_keypad
T016 [P] [US1] boot_gate + pin (lock/setup/dots/keypad)

# US1 Bước B — màn theo module, 5 nhóm khác file chạy cùng lúc (sau T007):
T017 [P] [US1] wallet_*    T018 [P] [US1] category_*    T019 [P] [US1] transaction_* + search/add/transfer
T020 [P] [US1] dashboard + report                        T021 [P] [US1] settings

# Test PBI 18 (sau code tương ứng, khác file):
T010 [P] theme_mode_test.dart (sau T001) · T011 theme_controller_test + theme_store_drift_test (sau T003/T004/T006)
T025 [US1] theme_screen_test.dart (sau T022) · T024 [US1] utilities_screen_test (sau T023) · T027 [US1] dark_theme_smoke (sau T013–T021)
```

## Chiến lược triển khai

- **MVP đề xuất**: User Story 1 (toàn bộ PBI 18 — đặc tả là một lát cắt "Giao diện Sáng/Tối/Theo hệ thống" duy nhất, không chia user story P1/P2/P3; spec không gán mức ưu tiên theo story). Foundational dựng engine nền — domain `ThemeMode` + hằng (T001), seam `ThemeStore` (T002), `ThemeController` (T003), store drift row `themeMode` (T004) + deps (T005) + fake (T006), **bộ 2 theme `SoraColors`** light/dark (T007) + `darkThemeData` (T008) — làm tiền đề bắt buộc; US1 ghép chrome/màn theo theme (không vùng "chìm" ở tối — SC-007) rồi màn 02 + hàng 01 để ra giá trị end-to-end (FR-001/FR-005/FR-006).
- **Thứ tự giao hàng tăng dần**: Foundational (engine theme + bộ 2 màu — **không đổi hành vi hiển thị nào**, 409 test cũ giữ) → Bước A chrome dùng chung theo theme → Bước B màn theo module (mỗi nhóm chạy full test light không hồi quy — bản light giữ nguyên giá trị token, test so màu hằng cũ vẫn xanh) → Màn 02 "Giao diện" + kích hoạt hàng màn 01 (trailing reactive "Theo hệ thống" + push) + widget test + smoke dark → Polish (dọn `AppColors` chỉ còn màu bất biến + analyze/full test toàn cục + QA emulator quickstart A–G + sync wiki).
- **Lưu ý rủi ro chính**: refactor màu ~26 file (plan §Rủi ro) — giảm thiểu bằng (1) light giữ nguyên giá trị (SoraColors.light = AppColors cũ) nên mọi test light soi được hồi quy; (2) `SoraColors.of(context)` có fallback `.light` khi theme không đăng ký extension (test pump MaterialApp thường không crash); (3) mỗi nhóm màn chạy analyze + test module sau khi sửa; (4) `white` 2 vai trò (nền sáng vs on-brand) — phân loại theo từng chỗ dùng, không đổi tên máy móc; (5) màn 02 + smoke dark bắt lỗi "chìm" trước khi QA.
