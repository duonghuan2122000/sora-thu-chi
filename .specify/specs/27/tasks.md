# Danh sách Task: Xuất báo cáo (màn 04 Báo cáo)

**Mã PBI**: 27
**Nguồn**: spec.md, plan.md, data-model.md, research.md, quickstart.md
**Ghi chú phạm vi**: chỉ **màn 04 (Xuất báo cáo)** — **không** schema mới (giữ drift **v8**), không migration, không `build_runner`; **CÓ** sửa `pubspec.yaml` (lần đầu: +3 dependency + asset font); **CÓ** 1 lần đổi tên private→public ở `transaction_filter.dart` (PBI 12, không đổi hành vi); không sửa `sora_colors.dart`/`app_colors.dart`, không đụng `app_shell.dart`/`AppBottomNavBar`/`ReportController`/`report_view.dart`/`WalletRepository`.
**Mốc baseline**: **883 pass + 1 test đỏ có sẵn** (`test/transactions_dao_test.dart`, PBI 11) — mục tiêu **không tăng** số test đỏ.

## Định dạng task

`- [ ] [MãTask] [P?] [Story?] Mô tả kèm đường dẫn file`

- `[P]`: có thể chạy song song (khác file, không phụ thuộc task chưa xong)
- `[Story]`: bắt buộc ở pha User Story (VD `[US1]`), không dùng ở Setup/Foundational/Polish
- Mọi đường dẫn tính từ `app/sora_thu_chi/` (trừ `.specify/`, `wiki-knowledge/`)

---

## Pha 1: Setup

- [X] T001 Chạy `flutter pub get` + `flutter analyze` + `flutter test` tại `app/sora_thu_chi/` để chốt baseline (**883 pass + 1 test đỏ có sẵn** ở `test/transactions_dao_test.dart`) — **không** sửa gì ở bước này
- [X] T002 Thêm 3 dependency vào `app/sora_thu_chi/pubspec.yaml` rồi `flutter pub get`: `pdf: ^3.13.0`, `excel_community: ^2.4.0` (**không** dùng `excel` — xung đột `archive` với `image ^4.9.2` của PBI 25), `share_plus: ^13.3.0`; **không** thêm `csv`/`printing`/`syncfusion` (research R8). Nếu resolver từ chối ⇒ dừng, báo lại (rủi ro 1 plan.md)
- [X] T003 Chạy `flutter build apk --debug` để phát hiện sớm yêu cầu native của `share_plus` (`minSdk` Android, `Info.plist` iOS) — nếu phải sửa `android/app/build.gradle`/`ios/Runner/Info.plist` thì ghi lại **chính xác** thay đổi vào cuối file này (rủi ro 9 plan.md)

## Pha 2: Foundational

*(Bắt buộc xong trước mọi user story: 1 hàm dùng chung nâng public + 31 khóa dịch + 4 thực thể logic. Không schema, không migration — data-model §1)*

- [X] T004 Đổi `Set<int> _effectiveCategoryIds(...)` → `Set<int> effectiveCategoryIds(...)` (public) tại `lib/core/transaction/transaction_filter.dart` và cập nhật **1** call site nội bộ trong `filterTransactions` — **không** đổi hành vi, không đổi chữ ký nào khác (research R3, FR-006)
- [X] T005 [P] Thêm **31 khóa dịch EN** vào nhánh `en` của `lib/core/locale/sora_translations.dart` (mục "Báo cáo (PBI 27) — màn Xuất báo cáo `04`"), đúng danh sách research R17: `'Xuất báo cáo'`, `'Xuất'`, `'KHOẢNG THỜI GIAN'`, `'Từ ngày'`, `'Đến ngày'`, `'VÍ'`, `'DANH MỤC'`, `'TAG'`, `'Nhập tag để lọc (VD: #dulich)'`, `'ĐỊNH DẠNG XUẤT'`, `'Có biểu đồ'`, `'Bảng dữ liệu'`, `'Dữ liệu thô'`, `'@count giao dịch • @from – @to'`, `'Định dạng: @format (@hint)'`, `'@count khác'`, `'Chọn danh mục'`, `'Bộ lọc hiện không có giao dịch nào'`, `'Tệp xuất ra không còn được app bảo vệ. Hãy cẩn thận khi chia sẻ.'`, `'Đang tạo tệp…'`, `'Đã tạo tệp'`, `'Không tạo được tệp báo cáo'`, `'Không đọc được dữ liệu để xuất báo cáo'`, `'Báo cáo thu chi'`, `'Chênh lệch'`, `'Danh sách giao dịch'`, `'Tổng hợp'`, `'Phân bổ chi theo danh mục'`, `'Dòng tiền'`, `'Số tiền'`, `'Loại'` — **tái dùng** (không thêm) `'Tất cả'`, `'Huỷ'`, `'Thử lại'`, `'Thu'`, `'Chi'`, `'Chuyển khoản'`, `'Điều chỉnh số dư'`, `'Khác'`, `'Tổng thu'`, `'Tổng chi'`, `'Ghi chú'`, `'Ngày'`, `'Ví'`, `'Danh mục'`; **không** dịch `PDF`/`Excel`/`CSV` (FR-020, R17)
- [X] T006 Tạo `lib/core/report/report_export.dart` với **khung thực thể** (chưa có hàm): `enum ExportFormat { pdf, excel, csv }` + extension cho `label` (`PDF`/`Excel`/`CSV`, **không** dịch), `hint` (`Có biểu đồ`/`Bảng dữ liệu`/`Dữ liệu thô`, **có** dịch), `extension` (`pdf`/`xlsx`/`csv`), `mimeType`; `class ReportExportFilter` (bất biến: `start`, `end`, `walletIds`, `categoryIds`, `tag`) + `fromRange(DateRange)`, `withStart`, `withEnd`, `toggleWallet`, `toggleCategory`, `clearWallets`, `clearCategories`, `withTag` (**không** `copyWith` tổng quát); `class ExportTxn` (`date`, `typeLabel`, `category`, `wallet`, `amount` **có dấu**, `note`, `tags`); `class ReportExportData` (`filter`, `range`, `transactions`, `rows`, `income`, `expense`, `allocation`, `fileName` + getter `count`/`isEmpty`/`balance`) — data-model §1.1–§1.5, FR-003…FR-009
- [X] T007 Cổng chốt Foundational: chạy `flutter analyze` + `flutter test` tại `app/sora_thu_chi/` → **0 lỗi analyze**, test vẫn **883 pass + 1 đỏ có sẵn** (xác nhận T004 không làm vỡ test PBI 12, T005 không làm vỡ `test/sora_translations_test.dart`) trước khi vào user story

## Pha 3: User Story 1 — Mở màn Xuất báo cáo, lọc và xem hộp tóm tắt (Ưu tiên: P1) 🎯 MVP

**Mục tiêu**: từ màn Tổng quan Báo cáo, chạm **1 lần** vào biểu tượng **Xuất báo cáo** trên vùng tiêu đề → màn **04** mở ra (app bar teal + back + tiêu đề "Xuất báo cáo", **không** bottom nav/FAB) với khoảng thời gian **kế thừa đúng kỳ đang xem**; 5 mục KHOẢNG THỜI GIAN / VÍ / DANH MỤC / TAG / ĐỊNH DẠNG XUẤT hoạt động; **hộp tóm tắt** cập nhật ngay theo bộ lọc + định dạng; dòng **cảnh báo** hiển thị sẵn; bộ lọc 0 giao dịch ⇒ nút xuất **vô hiệu hoá** kèm thông báo; back về màn 01 **giữ nguyên kỳ**.
**Tiêu chí kiểm thử độc lập**: ở kỳ Tháng 9/2026 có dữ liệu → chạm icon → màn 04 đúng mockup `docs/report/man-hinh-04-xuat-bao-cao.svg` (6 khối, 2 trường ngày `01/09/2026`–`30/09/2026`, thẻ PDF đang chọn); đếm tay số giao dịch khớp **chính xác** số ở hộp tóm tắt cho mọi tổ hợp lọc (chỉ ngày / chỉ ví / chỉ danh mục / chỉ tag / tổ hợp); bộ lọc rỗng ⇒ nút mờ + thông báo; back → màn 01 vẫn Tháng 9/2026.

- [X] T008 [US1] Thêm hàm thuần `List<Transaction> filterForExport(List<Transaction> all, ReportExportFilter filter, List<Category> categories)` vào `lib/core/report/report_export.dart`: **VÀ** giữa 4 nhóm (luật 1), **HOẶC** trong nhóm (luật 2), nhóm rỗng/tag trắng = không giới hạn (luật 3); khoảng nửa mở `!date.isBefore(start) && date.isBefore(end)` (**bao gồm** ngày đầu và ngày cuối — luật 4); danh mục chọn cha ⇒ gồm con qua `effectiveCategoryIds` (luật 6), giao dịch không `categoryId` khớp theo **tên bỏ dấu** (luật 7), transfer/adjustment chỉ khớp khi nhóm danh mục **không** được chọn (luật 8); tag: bỏ `#` đầu + `normalizeSearch` hai vế rồi kiểm **chứa** (luật 9–10); giữ **mọi** loại giao dịch, **không** group-keep hai vế chuyển khoản (luật 11–12); sắp **tăng dần theo `date`** rồi `id` (luật 13) — FR-004…FR-008, FR-015, data-model luật 1–13, R3/R4/R5/R7
- [X] T009 [US1] Thêm hàm thuần `ReportExportData buildReportExport({required List<Transaction> transactions, required List<Category> categories, required Map<int, String> walletNames, required ReportExportFilter filter, required ExportFormat format})` + `String exportFileName(DateTime start, DateTime end, ExportFormat format)` vào `lib/core/report/report_export.dart`: `range = DateRange(start: filter.start, end: filter.end)`; `transactions` = `filterForExport(...)`; `rows` = bản chiếu 1-1 (`typeLabel` từ `TxnType.label`, `category` = tên snapshot hoặc **rỗng** với transfer/adjustment, `wallet` = tra `walletNames` hoặc rỗng, `tags` thô); `income`/`expense` lấy **nguyên** từ `reportTotals(transactions, range)`; `allocation` lấy **nguyên** từ `reportBreakdown(transactions:, categories:, range:)` (top 5 + "Khác"); tên tệp ASCII `bao-cao-thu-chi_<yyyyMMdd>-<yyyyMMdd>.<ext>` với ngày cuối = `range.end − 1 ngày` — FR-016, FR-019, FR-024, data-model §1.4–§1.5 + luật 14–16, 23–25, R6/R14
- [X] T010 [P] [US1] Viết `test/report_export_test.dart` (phần US1, thuần — không widget): biên ngày (giao dịch đúng ngày đầu và ngày cuối **có** mặt, ngày ngoài khoảng không vào), nhiều ví (HOẶC), chọn danh mục cha ⇒ có cả con, giao dịch không `categoryId` khớp theo tên bỏ dấu, tag (`dulich`/`#dulich`/`DULICH`/`du lịch` cùng kết quả; tag chỉ có trong ghi chú **không** khớp), VÀ giữa các nhóm + nhóm rỗng không giới hạn, transfer/adjustment **có** trong danh sách nhưng **không** vào `income`/`expense`, thứ tự dòng tăng dần theo ngày, `income`/`expense`/`allocation` khớp kết quả gọi thẳng `reportTotals`/`reportBreakdown`, tên tệp đúng mẫu + ngày cuối trừ 1 + đuôi theo định dạng, `count`/`isEmpty`/`balance` — FR-004…FR-008/FR-015/FR-016/FR-024, SC-003/SC-004/SC-009/SC-010/SC-013
- [X] T011 [US1] Tạo khung màn `lib/screens/report_export_screen.dart`: `StatefulWidget` `ReportExportScreen({this.share})` (**chưa** dùng `share` ở story này); `initState` gọi **một lần** `ensureWalletRepository()` để lấy `allTransactions()`, `loadAll()`, `categories(type: income)`, `categories(type: expense)` và giữ **bản chụp** trong state (`List<Transaction> _all`, `Map<int, String> _walletNames`, `List<Category> _categories`) — đổi bộ lọc **không** đọc lại (FR-026); state `_filter` = `ReportExportFilter.fromRange(reportPeriodRange(ensureReportController().period.value, ensureReportController().now))` (kế thừa **đúng** kỳ đang xem — Ngày/Tuần(Thứ Hai→Chủ Nhật)/Tháng/Năm) + `_format = ExportFormat.pdf`; `SubPageScaffold(title: 'Xuất báo cáo'.tr)` (app bar teal + back sẵn có, **không** bottom nav/FAB); thân là `ListView` (cuộn tới được nút xuất — FR-022); nhánh **loading** (`CircularProgressIndicator`) / **error** (`'Không đọc được dữ liệu để xuất báo cáo'.tr` + nút `'Thử lại'.tr` như khuôn PBI 21) — FR-002/FR-003/FR-018/FR-026/FR-027, data-model §4, R2/R4
- [X] T012 [US1] Dựng `_SectionTitle('KHOẢNG THỜI GIAN')` + `_DateFields` trong `lib/screens/report_export_screen.dart`: hai `InkWell` nền `colors.softCardBg` bo `8`, nhãn `'Từ ngày'`/`'Đến ngày'` + giá trị `dd/MM/yyyy` (kèm `ValueKey('export-date-from')`/`('export-date-to')`); chạm → `showDatePicker` với **chặn chéo**: trường "Từ ngày" truyền `lastDate = đến ngày`, trường "Đến ngày" truyền `firstDate = từ ngày` ⇒ **không** có nhánh validate/lỗi (FR-004 + chốt 2026-09-13); chọn xong ⇒ `setState` + dựng lại `ReportExportData` — FR-003/FR-004, R4, rủi ro 5 plan.md
- [X] T013 [US1] Dựng `_SectionTitle('VÍ')` + `_ChipRow` cho ví trong `lib/screens/report_export_screen.dart`: `Wrap` chip bo `15` cao `30`; chip `'Tất cả'` **đang chọn** khi `walletIds.isEmpty` (nền `AppColors.teal` + chữ trắng), chạm ⇒ `clearWallets()`; mỗi ví một chip theo `walletsByDisplayOrder`, chạm ⇒ `toggleWallet(id)` (**chọn nhiều** cùng lúc — HOẶC), tên dài cắt ellipsis (`maxLines: 1` + `TextOverflow.ellipsis`) — FR-005, SC-010, R15
- [X] T014 [US1] Dựng `_SectionTitle('DANH MỤC')` + `_ChipRow` cho danh mục trong `lib/screens/report_export_screen.dart`: chip `'Tất cả'` + tối đa **3** chip danh mục **cha đang hoạt động** (cả `income` lẫn `expense`, theo `sortOrder`; danh mục **ẩn** không vào chip — biên spec); phần vượt gộp thành chip `'@count khác'` (`ValueKey('export-more-categories')`) mở `showModalBottomSheet` tiêu đề `'Chọn danh mục'.tr` liệt kê **đầy đủ** danh mục cha với ô chọn nhiều (`toggleCategory`); `'Tất cả'` ⇒ `clearCategories()` — FR-006, SC-010, R15
- [X] T015 [US1] Dựng `_SectionTitle('TAG')` + ô nhập + `_SectionTitle('ĐỊNH DẠNG XUẤT')` + 3 `_FormatCard` trong `lib/screens/report_export_screen.dart`: `TextField` hint `'Nhập tag để lọc (VD: #dulich)'.tr` → `withTag(text)` (`ValueKey('export-tag-field')`); 3 thẻ ngang hàng (`Row` + `Expanded`), mỗi thẻ = biểu tượng (bubble `tealLightBg`/`softCardBg`) + `format.label` (**không** dịch) + `format.hint.tr`; thẻ **đang chọn**: viền `2 px` `AppColors.teal` + dấu chọn tròn góc phải trên, còn lại viền `1 px` `colors.divider`; chạm ⇒ `setState(_format = ...)` (**loại trừ nhau**, mặc định `pdf`) — FR-007/FR-009, SC-001, R17
- [X] T016 [US1] Dựng `_SummaryBox` + `_EmptyFilterNotice` + `_PrivacyWarning` + nút `'Xuất báo cáo'` trong `lib/screens/report_export_screen.dart`: hộp tóm tắt nền `colors.softCardBg` bo `10` — dòng 1 `'@count giao dịch • @from – @to'`, dòng 2 `'Định dạng: @format (@hint)'`; khi `data.isEmpty` ⇒ `_EmptyFilterNotice` (`'Bộ lọc hiện không có giao dịch nào'.tr`) + nút **`onPressed: null`** (vô hiệu hoá — FR-017); dòng cảnh báo **luôn hiển thị sẵn** ngay trên nút: nền `colors.coralLightBg` bo `10` + `Icons.warning_amber_rounded` coral + `'Tệp xuất ra không còn được app bảo vệ. Hãy cẩn thận khi chia sẻ.'.tr` (**không** hộp thoại xác nhận — FR-028); nút teal cao `≥ 44`, **chưa** nối hành động xuất ở story này (`ValueKey('export-button')`) — FR-010/FR-011/FR-017/FR-018/FR-028, SC-001/SC-006, R16
- [X] T017 [US1] Thêm điểm vào tại `lib/screens/report_screen.dart`: `ScreenHeader.trailing` đổi từ `_CompareButton(...)` thành `Row(mainAxisSize: MainAxisSize.min, children: [_CompareButton(...), _ExportButton(onTap: () => _openExport(context))])` — nút tròn `48 px` icon `Icons.ios_share`, chữ `AppColors.white`, `Tooltip('Xuất'.tr)` (**không** dịch nhãn `'Xuất báo cáo'` ở đây dùng tiêu đề màn), `ValueKey('report-export-entry')`, **luôn** bấm được (khác nút so sánh — FR-001 + chốt); `_openExport` → `Navigator.of(context).push(MaterialPageRoute<void>(builder: (_) => const ReportExportScreen()))`; giữ nguyên `_CompareButton` và `bottom: _HeaderSummary(...)` — FR-001/FR-002, SC-002, R1
- [X] T018 [P] [US1] Viết `test/report_export_screen_test.dart` (phần US1): pump màn 04 với `FakeWalletRepository.withCategories(...)` (như `test/report_comparison_screen_test.dart`) + `ReportController.load(now: DateTime(2026, 9, ...))` qua `ensureReportController()` (`Get.reset()` trong `tearDown`) — app bar teal + tiêu đề "Xuất báo cáo", **không** bottom nav; hai trường ngày mặc định **đúng kỳ đang xem** (Tháng/Năm/Tuần/Ngày); đổi ví/danh mục/tag/ngày ⇒ hộp tóm tắt cập nhật đúng số + đúng chuỗi khoảng ngày; chip `'Tất cả'` xoá hết lựa chọn; chip `'+N khác'` mở sheet đủ danh mục; đổi định dạng ⇒ chỉ 1 thẻ chọn + dòng 2 đổi; bộ lọc 0 giao dịch ⇒ nút **vô hiệu hoá** + có thông báo + **không** có tệp nào được tạo; dòng cảnh báo hiện **sẵn** khi chưa bấm xuất; kỳ **rỗng** ⇒ màn vẫn mở đủ 5 mục — FR-003…FR-010/FR-017/FR-018/FR-028, SC-003/SC-006/SC-007
- [X] T019 [P] [US1] Mở rộng `test/report_screen_test.dart`: có `report-export-entry` trên vùng tiêu đề; chạm → `find.byType(ReportExportScreen)` xuất hiện; ca kỳ **rỗng** (không có thu/chi) ⇒ icon xuất **vẫn** mở được màn 04 (khác nút so sánh bị vô hiệu hoá); back ⇒ về màn 01 — FR-001/FR-018, SC-002

## Pha 4: User Story 2 — Xuất tệp CSV và mở bảng chia sẻ (Ưu tiên: P2)

**Mục tiêu**: chạm **"Xuất báo cáo"** (định dạng CSV) → màn hiện trạng thái "Đang tạo tệp…", UI vẫn phản hồi, rồi **bảng chia sẻ của hệ thống** mở ra với tệp CSV tên `bao-cao-thu-chi_<yyyyMMdd>-<yyyyMMdd>.csv` — tệp chứa **đủ mọi** giao dịch khớp bộ lọc (kể cả chuyển khoản + điều chỉnh), dòng tiêu đề + số thuần đọc được bằng bảng tính; huỷ bảng chia sẻ hoặc lỗi ⇒ màn giữ nguyên bộ lọc/định dạng, hiện thông báo dễ hiểu.
**Tiêu chí kiểm thử độc lập**: xuất CSV cho khoảng có cặp chuyển khoản + điều chỉnh ⇒ 3 dòng đó **có mặt**; số dòng tệp **bằng đúng** số ở hộp tóm tắt (0 sai lệch); ghi chú chứa `,`/`"`/xuống dòng mở bằng bảng tính **không** lệch cột; huỷ chia sẻ ⇒ bộ lọc còn nguyên.

- [X] T020 [US2] Tạo `lib/core/report/export_share.dart`: `typedef ShareExport = Future<void> Function({required String fileName, required Uint8List bytes, required String mimeType});` + `ShareExport defaultShareExport` dùng `SharePlus.instance.share(ShareParams(files: [XFile.fromData(bytes, name: fileName, mimeType: mimeType)]))` — **không** ghi tệp tạm, **không** `path_provider` (research R12, FR-011)
- [X] T021 [US2] Thêm hàm thuần `Uint8List buildCsvBytes(List<ExportTxn> rows)` vào `lib/core/report/report_export.dart`: dòng tiêu đề `'Ngày'`,`'Loại'`,`'Danh mục'`,`'Ví'`,`'Số tiền'`,`'Ghi chú'`,`'Tag'` (đã dịch); mỗi dòng một giao dịch; ngày `dd/MM/yyyy`; số tiền ghi **số có dấu**, **không** phân tách nghìn, **không** đơn vị; ô chứa `,`/`"`/xuống dòng ⇒ bọc `"…"` + nhân đôi `"` bên trong (RFC 4180); **BOM UTF-8** ở đầu tệp; **không** kèm phần tổng hợp/hình ảnh — FR-014/FR-019/FR-024, data-model luật 17–21 + 26–29, R8/R13
- [X] T022 [US2] Nối nút xuất trong `lib/screens/report_export_screen.dart`: thêm hàm **top-level** `Uint8List _buildBytes(_ExportPayload payload)` (switch theo `ExportFormat`; story này chỉ nhánh `csv` → `buildCsvBytes`); `_export()` chạy `compute(_buildBytes, payload)` (payload = `ReportExportData` + bytes asset, **không** đọc asset trong isolate — R11), đặt `_busy = true` (nút chuyển `'Đang tạo tệp…'.tr` + khoá bấm lặp, phần còn lại của màn vẫn phản hồi — FR-023), xong gọi `widget.share ?? defaultShareExport` với `fileName: data.fileName`, `mimeType: format.mimeType`; bọc `try/catch` ⇒ `SnackBar('Không tạo được tệp báo cáo'.tr)` + **giữ nguyên** `_filter`/`_format`/`_busy = false` (FR-025) — FR-011/FR-023/FR-025, SC-008/SC-014, R11/R12
- [X] T023 [P] [US2] Bổ sung vào `test/report_export_test.dart` (phần US2): `buildCsvBytes` có **BOM UTF-8** ở đầu (không lặp BOM), đúng dòng tiêu đề, số dòng = `rows.length`, ô chứa `,`/`"`/xuống dòng được escape đúng (đọc lại bằng parser tối thiểu trong test ⇒ không lệch cột), số tiền có dấu + không đơn vị, chữ tiếng Việt có dấu giữ nguyên, danh sách **có** dòng transfer/adjustment — FR-014/FR-015/FR-019/FR-024, SC-003/SC-013
- [X] T024 [P] [US2] Bổ sung vào `test/report_export_screen_test.dart` (phần US2): bơm `ShareExport` **giả** ghi lại tham số ⇒ chạm nút xuất gọi seam **đúng 1 lần** với `fileName` khớp mẫu `bao-cao-thu-chi_20260901-20260930.csv` + `mimeType` `text/csv` + `bytes` không rỗng; bấm **2 lần** liên tiếp nhanh ⇒ seam chỉ được gọi 1 lần (`_busy`); seam **ném lỗi** ⇒ có `SnackBar` + `_filter`/`_format` **giữ nguyên** + nút bật lại; ca `share` trả về bình thường ⇒ không treo — FR-011/FR-023/FR-025, SC-008/SC-014

## Pha 5: User Story 3 — Xuất tệp Excel 2 trang (Ưu tiên: P3)

**Mục tiêu**: chọn thẻ **Excel** → bấm xuất → tệp `.xlsx` có **2 sheet**: "Tổng hợp" (tổng thu, tổng chi, chênh lệch + phân bổ chi theo danh mục) và "Giao dịch" (đúng bộ cột CSV); mở được bằng bảng tính, chữ tiếng Việt không lỗi font, cột số tiền là **số** cộng được.
**Tiêu chí kiểm thử độc lập**: giải nén bytes bằng `archive` trong test ⇒ có `xl/workbook.xml` + 2 sheet đúng tên + ô tiếng Việt đọc lại đúng; số liệu sheet "Tổng hợp" khớp **0 đ** màn 01 cùng khoảng.

- [X] T025 [US3] Tạo `lib/core/report/report_export_writers.dart` với `Uint8List buildXlsxBytes(ReportExportData data)` dùng `excel_community`: sheet `'Tổng hợp'` (3 số tổng hợp dùng `formatMoney` + bảng `allocation` với tên danh mục + số tiền + %) và sheet `'Giao dịch'` (dòng tiêu đề + mỗi dòng một giao dịch, cùng bộ cột R13); cột **số tiền** ghi **ô số** (không kèm `đ`); **không** phụ thuộc `Widget`/`BuildContext`/asset (gọi được trong `compute()`) — FR-013/FR-019, data-model luật 20, 22, 27, R8/R13
- [X] T026 [US3] Nối nhánh `excel` vào `_buildBytes` trong `lib/screens/report_export_screen.dart` (chọn `buildXlsxBytes` khi `_format == ExportFormat.excel`) — **không** đổi luồng `compute`/seam đã có ở T022 — FR-011/FR-013
- [X] T027 [P] [US3] Viết `test/report_export_writers_test.dart` (phần US3): `buildXlsxBytes` không rỗng; giải nén bằng `archive` ⇒ có `xl/workbook.xml`, **đúng 2** sheet tên `Tổng hợp`/`Giao dịch`, đọc lại một ô tiếng Việt có dấu đúng nguyên văn; sheet "Giao dịch" có đủ 7 cột tiêu đề + số dòng = `rows.length` — FR-013/FR-014, SC-005 (kiểm **cấu trúc**; "mở bằng bảng tính thật" chỉ QA tay nhóm I)

## Pha 6: User Story 4 — Xuất tệp PDF có biểu đồ (Ưu tiên: P3)

**Mục tiêu**: định dạng **mặc định PDF** xuất được: trang đầu có tiêu đề + khoảng thời gian + tổng thu/tổng chi/chênh lệch + **biểu đồ dòng tiền** (≤ 6 cột Thu/Chi) + **biểu đồ phân bổ chi theo danh mục** (thanh ngang) + bảng top danh mục; tiếp theo là **danh sách giao dịch** phân trang; chữ tiếng Việt có dấu hiển thị đúng (font nhúng).
**Tiêu chí kiểm thử độc lập**: bytes bắt đầu bằng `%PDF`; số liệu trong tệp khớp **0 đ** màn 01 cùng khoảng; khoảng nhiều năm vẫn xuất được và số cột biểu đồ **≤ 6**; chữ có dấu không vỡ khi mở bằng trình đọc PDF (QA tay nhóm H).

- [X] T028 [US4] Thêm font PDF vào repo: tải `Roboto-Regular.ttf` + `Roboto-Bold.ttf` (Apache-2.0, ~515 KB/file, nguồn `googlefonts/roboto-2/src/hinted/`) vào `app/sora_thu_chi/assets/fonts/` rồi khai báo `assets/fonts/` trong mục `flutter: assets:` của `app/sora_thu_chi/pubspec.yaml` + `flutter pub get`; chạy lại `flutter test test/brand_assets_test.dart` (tiền lệ PBI 25) để xác nhận asset có mặt trong `flutter test` (rủi ro 8 plan.md) — FR-024/SC-005, R9
- [X] T029 [US4] Thêm hàm thuần `List<({DateRange range, int income, int expense})> exportCashflowBuckets(List<Transaction> transactions, DateRange range)` vào `lib/core/report/report_export.dart`: `n = min(6, số ngày của range)`, chia khoảng thành `n` phần **bằng số học ngày** (không mất ngày nào, khoảng cuối kết thúc đúng `range.end`), mỗi phần tính `income`/`expense` bằng `reportTotals` (transfer/adjustment tự bị loại) — FR-012, data-model luật 26, 29, R10
- [X] T030 [US4] Thêm `Uint8List buildPdfBytes({required ReportExportData data, required ByteData fontRegular, required ByteData fontBold})` vào `lib/core/report/report_export_writers.dart`: `pw.Document` + `pw.MultiPage` + `pw.ThemeData.withFont(base:, bold:)`; trang đầu = tiêu đề `'Báo cáo thu chi'` + khoảng thời gian + 3 số `formatMoney` + **biểu đồ dòng tiền** vẽ bằng `pw.Container`/`pw.Row` (cặp cột Thu teal / Chi coral theo `exportCashflowBuckets` — **tối đa 6**) + **biểu đồ phân bổ** dạng **thanh ngang tỉ lệ** theo `data.allocation` (số tiền + %) + bảng top danh mục; tiếp theo = **danh sách giao dịch** (`pw.Table`/`pw.TableHelper`, phân trang tự động của `MultiPage`); thuần Dart (không `Widget`/`BuildContext`/asset) — FR-012/FR-019, data-model luật 20, 22, 26, R10 (**lệch có lý do** so với FR-012: biểu đồ **vẽ lại**, không phải ảnh chụp — ghi ở plan mục "Ngoại lệ có lý do" #2)
- [X] T031 [US4] Nối nhánh `pdf` vào `lib/screens/report_export_screen.dart`: nạp 2 font **một lần ở main isolate** (`initState`/lazy `rootBundle.load('assets/fonts/Roboto-Regular.ttf')` + `...Bold.ttf` → `ByteData`) rồi truyền bytes vào payload của `compute` (**không** gọi `rootBundle` trong isolate — bẫy R11); `_buildBytes` nhánh `pdf` gọi `buildPdfBytes` — FR-012/FR-023, R11, rủi ro 3 plan.md
- [X] T032 [P] [US4] Bổ sung vào `test/report_export_writers_test.dart` (phần US4): `buildPdfBytes` (với 2 font nạp từ asset trong test) bắt đầu bằng `%PDF`, không rỗng, **không** ném lỗi với dữ liệu nhiều năm (khoảng 2019→2026); `exportCashflowBuckets` trả **≤ 6** phần, phần đầu bắt đầu đúng `range.start` và phần cuối kết thúc đúng `range.end`, tổng `income`/`expense` các phần **bằng** `reportTotals` toàn khoảng, kỳ 1 ngày ⇒ đúng 1 phần — FR-012, data-model luật 26, 29, SC-004

## Pha cuối: Polish & Cross-cutting

- [X] T033 [P] Thêm ca smoke **theme tối** cho màn 04 vào `test/dark_theme_smoke_test.dart`: pump màn Xuất báo cáo (có dữ liệu) ở chế độ Tối → không overflow, token tối thật sự áp cho nền/chip/thẻ định dạng/hộp tóm tắt/dòng cảnh báo — FR-021, SC-011
- [X] T034 Chạy `flutter analyze` + `flutter test` toàn bộ tại `app/sora_thu_chi/` → **0 lỗi analyze**, **không tăng** số test đỏ (chỉ còn test đỏ **có sẵn** đã ghi ở T001)
- [ ] T035 QA tay trên emulator theo `quickstart.md` nhóm **A–P** (điểm vào & khung màn, bố cục mockup `04`, khoảng thời gian, ví/danh mục/tag, tổ hợp nhóm + hộp tóm tắt, PDF, Excel/CSV mở bằng bảng tính, đủ dòng & loại transfer khỏi con số, bộ lọc rỗng, tiến trình/lỗi/huỷ chia sẻ, vòng đời & cô lập bộ lọc, Tối + English, bố cục cực trị, hiệu năng) — ghi lại kết quả từng nhóm. **Ghi rõ trong bàn giao**: QA **Android** là đủ cho phần lớn nhóm, nhưng PBI này đụng plugin native (`share_plus`) ⇒ **nên** chạy thêm H1/H2/I1/I4 trên **iOS** nếu có máy; nếu bỏ qua phải ghi rõ "iOS chưa QA"
- [X] T036 Tick checklist `.specify/specs/27/checklists/requirements.md` sau khi thi công xong (**giữ nguyên nội dung** đã duyệt)
- [X] T037 Đồng bộ wiki theo skill `sora-wiki`: cập nhật `wiki-knowledge/entity/` **Báo cáo** (màn `04`: điểm vào icon thứ hai trên vùng tiêu đề, bộ lọc riêng của màn sống trong phiên, khoảng mặc định kế thừa kỳ đang xem, luật VÀ/HOẶC, danh sách tệp đủ mọi loại giao dịch nhưng con số tổng hợp loại transfer/adjustment, 3 định dạng + tệp ra ngoài app không còn được bảo vệ), `wiki-knowledge/concept/` **Lộ trình phát triển** (màn `04` xong; còn bộ lọc nâng cao xuyên suốt ba màn Báo cáo) và **Design system** (chip chọn fill teal, dòng cảnh báo dùng `coralLightBg`, thẻ định dạng viền teal + dấu chọn) + append `wiki-knowledge/log.md` + cập nhật `wiki-knowledge/index.md`
- [X] T038 Commit PBI 27 theo skill `git-commit` (Conventional Commits, message tiếng Việt có dấu, **không** trailer `Co-Authored-By`)

---

## Sơ đồ phụ thuộc

```text
Setup (T001–T003)  ← T002 cần T001 (baseline) · T003 cần T002 (dependency đã cài)
   ↓
Foundational (T004–T007)  ← chặn MỌI user story: effectiveCategoryIds + 31 khóa dịch + 4 thực thể
   ↓
US1 P1 (T008–T019)  ← MVP: hàm lọc/dựng + khung màn 04 + 5 mục + hộp tóm tắt + cảnh báo + điểm vào màn 01
   ├──→ US2 P2 (T020–T024)  seam chia sẻ + CSV + nút xuất (cần khung màn + `ReportExportData` của US1)
   ├──→ US3 P3 (T025–T027)  Excel 2 sheet (cần `ReportExportWriters` + nhánh `_buildBytes` của US2)
   └──→ US4 P3 (T028–T032)  PDF + font (cần khung `_buildBytes`/seam của US2; độc lập với US3 ngoài `report_export_writers.dart`)
   ↓
Polish (T033–T038)  ← T033 độc lập; T034–T037 sau khi US1–US4 xong; T038 cuối cùng
```

- **US1 là cổng**: US2–US4 đều cần khung màn 04 + `ReportExportData` đã dựng; US2 là cổng của US3/US4 (đường `compute` + seam chia sẻ + `_buildBytes`).
- **US3 và US4 chạy song song được** trên lý thuyết (khác hàm trong cùng file `report_export_writers.dart` ⇒ thực tế **tuần tự**); US4 **không** phụ thuộc US3.
- Trong Foundational: T004 và T006 **tuần tự** với nhau không bắt buộc (khác file) — nhưng tất cả phải xong trước T008; T005 [P] chạy song song với T004/T006.
- Trong US1: T008 → T009 (cùng file `report_export.dart`, T009 gọi T008); T011 → T012…T016 (cùng file màn 04, tuần tự); T017 độc lập (file `report_screen.dart`); T010/T018/T019 [P] (3 file test khác nhau).
- Trong Polish: T033 [P] (file test theme) chạy cùng lúc với T034–T037.

## Ví dụ chạy song song

```text
# Pha 2 — Foundational (khác file):
T004                              lib/core/transaction/transaction_filter.dart
T005 [P]                          lib/core/locale/sora_translations.dart
T006                              lib/core/report/report_export.dart

# Pha 3 — US1 (3 file khác nhau):
T017 [US1]                        lib/screens/report_screen.dart
T018 [P] [US1]                    test/report_export_screen_test.dart
T019 [P] [US1]                    test/report_screen_test.dart

# Pha 4 — US2 (2 file test khác nhau):
T023 [P] [US2]                    test/report_export_test.dart
T024 [P] [US2]                    test/report_export_screen_test.dart

# Pha cuối:
T033 [P]                          test/dark_theme_smoke_test.dart
```

## Chiến lược triển khai

- **MVP đề xuất: chỉ User Story 1** (T001–T019) — sau pha này màn 04 đã *dùng được*: mở từ icon vùng tiêu đề (luôn bấm được), khoảng mặc định đúng kỳ đang xem, 5 mục lọc + 3 thẻ định dạng + hộp tóm tắt cập nhật + dòng cảnh báo + nút vô hiệu hoá khi rỗng (SC-001/SC-002/SC-003/SC-006/SC-007/SC-009/SC-010, trừ phần ra tệp).
- **Giao hàng tăng dần**: US1 → US2 (chốt SC-003/SC-013 + đường chia sẻ) → US3 (chốt SC-005 phần Excel) → US4 (chốt SC-004/SC-005 phần PDF) → Polish (theme tối, QA tay A–P, wiki, commit). Mỗi pha kết thúc bằng `flutter analyze` + test xanh.
- **PBI chỉ giao được khi US1–US4 xong**: spec chốt Q2 = **cả 3 định dạng trong một đợt**, không chia chặng ⇒ các mốc US2/US3 là **mốc thi công**, **không** phải bản phát hành. Giữa các mốc, định dạng chưa nối nhánh sẽ không ra tệp — **không** thêm nhánh tạm/rơi-về-CSV để "che" trạng thái đó.
- **Thứ tự bắt buộc**: Foundational xong trước US1 (T007 là cổng chốt); US2 sau US1; US3/US4 sau US2; Polish sau khi cả 4 story xong.
- **Rủi ro cần để mắt khi thi công**: (1) `excel_community` là **fork** — kiểm ngay bằng T027 + QA nhóm I, nếu không đạt ⇒ phương án B tự viết XLSX bằng `archive` (R8); (2) `share_plus` có thể **bỏ `name`** của `XFile.fromData` ⇒ tên tệp sai (FR-024) — kiểm ở T024 + QA H2, dự phòng ghi tệp tạm bằng `path_provider`; (3) `rootBundle` **không** dùng được trong isolate ⇒ font **phải** nạp ở main isolate (T031); (4) **bộ chọn ngày chặn chéo** dễ cài ngược chiều — luật 5 + test/T012; (5) test đỏ **có sẵn** `transactions_dao_test` dễ bị nhầm là lỗi mới — mốc baseline ghi ở T001; (6) **lệch có chủ ý** so với mockup `04`: thêm **dòng cảnh báo** (FR-028) và chip danh mục giới hạn **3** + "+N khác" (R15) — SC-001 vẫn đạt.
