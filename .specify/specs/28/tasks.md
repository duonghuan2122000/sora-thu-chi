# Danh sách Task: Cài đặt Thông báo & nhắc nhở (màn Cài đặt)

**Mã PBI**: 28
**Nguồn**: spec.md, plan.md, data-model.md, research.md, quickstart.md
**Ghi chú phạm vi**: chỉ **màn Thông báo & nhắc nhở** + **1 hàng điểm vào** ở màn Cài đặt — **không** schema mới (giữ drift **v8**), **không** migration, **không** `build_runner`, **không** sửa `pubspec.yaml` (0 dependency), **không** plugin thông báo/quyền (FR-012/R14). **Không** sửa `utilities_screen.dart`, `app_database.dart`, `sora_colors.dart`, `app_theme.dart`, `app_shell.dart`, `pubspec.yaml`; **không** sửa `transactions`/`wallets`/`categories`/`budgets`/`scan_sessions` (data-model luật 17). Dữ liệu duy nhất ghi: **1 row** `AppSettings(key='notificationPrefs')`.
**Mốc baseline**: **960 pass + 1 test đỏ có sẵn** (`test/transactions_dao_test.dart`, từ PBI 11) — mục tiêu **không tăng** số test đỏ.

## Định dạng task

`- [ ] [MãTask] [P?] [Story?] Mô tả kèm đường dẫn file`

- `[P]`: có thể chạy song song (khác file, không phụ thuộc task chưa xong)
- `[Story]`: bắt buộc ở pha User Story (VD `[US1]`), không dùng ở Setup/Foundational/Polish
- Mọi đường dẫn tính từ `app/sora_thu_chi/` (trừ `.specify/`, `wiki-knowledge/`)

---

## Pha 1: Setup

*PBI này **không có gì để khởi tạo**: 0 dependency mới, 0 asset, 0 cấu hình native, 0 migration (plan.md §Ngữ cảnh kỹ thuật). Setup chỉ gồm việc chốt baseline.*

- [X] T001 Chạy `flutter pub get` + `flutter analyze` + `flutter test` tại `app/sora_thu_chi/` để chốt baseline (**960 pass + 1 test đỏ CÓ SẴN** ở `test/transactions_dao_test.dart`, tồn tại từ PBI 11 — **không** phải hồi quy của PBI 28) và xác nhận `pubspec.yaml` **không** cần sửa — không code gì ở bước này (quickstart §5)

## Pha 2: Foundational

*(Bắt buộc xong trước mọi user story: tầng prefs thuần + seam + impl drift + DI + 23 khoá dịch + 2 file test tầng thấp. Không schema, không migration — data-model §1)*

- [X] T002 [P] Tạo `lib/core/notification/notification_prefs.dart`: hằng `kKeyNotificationPrefs = 'notificationPrefs'`; `class NotificationPrefs` **bất biến** đúng **16 trường** data-model §1.1 (`dailyEnabled`/`dailyHour`/`dailyMinute`/`dailyOnlyIfNoTxnToday`; `budgetEnabled`/`budgetEarlyPercent`/`budgetOverPercent`; `recurringEnabled`/`recurringDaysBefore`; `goalEnabled`; `weeklyEnabled`/`weeklyHour`/`weeklyMinute`; `monthlyEnabled`/`monthlyHour`/`monthlyMinute`) + `static const defaults` = **nguyên** bộ FR-008 (20:30 + cờ "chỉ nhắc nếu chưa ghi" = bật; 80%/100%; 3 ngày; **`goalEnabled = false`**; tuần/tháng 20:00); `copyWith` **chỉ** đổi trường được truyền; `toSettings()` = `{kKeyNotificationPrefs: jsonEncode(json)}` + `fromSettings(Map<String, String>)` parse **tolerant, không bao giờ ném**: JSON rác/`null`/mảng → **cả bộ mặc định**; thiếu khoá/sai kiểu/ngoài miền (giờ ∉ 0–23, phút ∉ 0–59, % ∉ 0–100, ngày ∉ 0–30) → mặc định **của riêng trường đó**; giá trị ghi ra **luôn** đã clamp — data-model §1.1/§1.3 + luật 1–6, 11–13, FR-007/FR-008/FR-010, research R1/R3/R4
- [X] T003 [P] Tạo `lib/core/notification/notification_store.dart`: seam nội bộ `abstract class NotificationStore { Future<NotificationPrefs> load(); Future<void> save(NotificationPrefs prefs); }` + doc ngắn (load: key vắng → cả bộ mặc định; save: upsert **1 row**, **không** xoá row khác) — data-model §1.4, research R5
- [X] T004 [P] Sửa `lib/core/date_label.dart`: thêm `String formatClock(int hour, int minute) => '${_two(hour)}:${_two(minute)}';` và cho `formatTimeLabel(DateTime d)` **gọi lại** `formatClock(d.hour, d.minute)` — hành vi `formatTimeLabel` **giữ nguyên** (PBI 21 đang dùng); chạy `flutter test test/date_label_test.dart` — research R9, FR-009/luật 15
- [X] T005 [P] Thêm **23 khoá dịch EN** vào nhánh `_en` của `lib/core/locale/sora_translations.dart` (mục mới "PBI 28 — màn Thông báo & nhắc nhở"), đúng danh sách research R10: `'Thông báo & nhắc nhở'`, `'NHẮC NHỞ HÀNG NGÀY'`, `'NGÂN SÁCH'`, `'GIAO DỊCH ĐỊNH KỲ'`, `'MỤC TIÊU TIẾT KIỆM'`, `'TỔNG KẾT TỰ ĐỘNG'`, `'Nhắc nhập giao dịch hằng ngày'`, `'Cảnh báo vượt ngân sách'`, `'Ngưỡng cảnh báo'`, `'Nhắc hóa đơn sắp đến hạn'`, `'Nhắc trước'`, `'Nhắc đóng góp mục tiêu'`, `'Tổng kết cuối tuần'`, `'Tổng kết cuối tháng'`, `'@giờ mỗi ngày'`, `' · chỉ nhắc nếu chưa ghi'`, `'Khi đạt @sớm% và khi vượt @vượt%'`, `'Sớm: @sớm% · Vượt mức: @vượt%'`, `'Tiền điện, tiền nhà, trả nợ...'`, `'@n ngày trước hạn thanh toán'`, `'Theo chu kỳ đã đặt cho từng mục tiêu'`, `'Chủ nhật hằng tuần, @giờ'`, `'Ngày cuối tháng, @giờ'`; **tái dùng** (không thêm) `'Không đọc được cài đặt.'` + `'Thử lại'` (đã có); giờ/số/`%` **không** dịch — FR-015, research R10 (rủi ro 1 plan.md: chạy `flutter test test/sora_translations_test.dart` ngay sau khi thêm)
- [X] T006 [P] Tạo `lib/data/notification_store_drift.dart`: `DriftNotificationStore(AppDatabase)` bám **nguyên khuôn** `DriftUtilitiesStore` — `load()` = `select(appSettings)` → map `{key: value}` → `NotificationPrefs.fromSettings`; `save()` = **đúng 1** `insertOnConflictUpdate(AppSettingsCompanion.insert(key: kKeyNotificationPrefs, value: …))`, **không** vòng lặp 16 row, **không** xoá row nào khác; **không** `build_runner` (dùng lại `AppSettingsCompanion` đã sinh ở v8) — data-model §1.3 + luật 9, FR-007/FR-018, research R1
- [X] T007 [P] Tạo `lib/data/notification_deps.dart`: `ensureNotificationStore()` — nếu `Get.isRegistered<NotificationStore>()` → `Get.find` (test đăng ký fake trước ⇒ trả fake), ngược lại `Get.put(DriftNotificationStore(AppDatabase()))`; doc lý do (đúng **1** connection drift trên cùng file sqlite) — bám `ensureUtilitiesStore`, research R12
- [X] T008 [P] Tạo `test/fakes/fake_notification_store.dart`: bản bộ nhớ của `NotificationStore` (`storedPrefs` cho phép khởi tạo trạng thái có sẵn **và** assert sau khi chạm công tắc) — bám `FakeUtilitiesStore`, dùng cho mọi widget test (không cần sqlite native) — research R13
- [X] T009 [P] Tạo `test/notification_prefs_test.dart` (thuần Dart, không widget/binding) phủ **17 luật** data-model §2: mặc định của cả 16 trường; parse tolerant (chuỗi rác, `null`, mảng, JSON thiếu khoá → chỉ trường thiếu nhận mặc định, bool≠bool/number≠number, giờ 24, phút 60, % 150, ngày âm, chuỗi rỗng) **không ném**; round-trip `fromSettings(toSettings(p)) == p`; `copyWith(xEnabled:)` **chỉ** đổi trường của loại đó (15 trường còn lại nguyên) + **giữ nguyên tham số** khi tắt/bật lại (không reset mặc định); tắt **cả 6** công tắc vẫn là prefs hợp lệ; `formatClock(20, 5) == '20:05'` + `formatClock(0, 0) == '00:00'` — FR-006…FR-010, SC-003/SC-005
- [X] T010 [P] Tạo `test/notification_store_drift_test.dart` (khuôn `test/utilities_store_drift_test.dart`, DB in-memory + **skip-guard** host thiếu sqlite native): `db.schemaVersion == 8`; key `notificationPrefs` vắng → cả bộ mặc định; round-trip save→load đúng 16 trường; chèn sẵn `hideBalance`/`amountCalculatorEnabled`/`scanEnabled` rồi `save` ⇒ các row đó **còn nguyên** (`rows.length` đúng) — data-model luật 7–9, FR-007/FR-018, quickstart §6
- [X] T011 **Cổng chốt Foundational**: chạy `flutter analyze` + `flutter test` tại `app/sora_thu_chi/` → **0 lỗi analyze**, test **không tăng** số đỏ (chỉ còn `transactions_dao_test`); riêng `test/sora_translations_test.dart` (T005 đủ khoá), `test/date_label_test.dart` (T004 không đổi hành vi cũ) và `test/notification_prefs_test.dart` phải **xanh** trước khi vào user story

## Pha 3: User Story 1 — Mở màn từ Cài đặt, xem đúng mockup và đổi công tắc (Ưu tiên: P1) 🎯 MVP

**Mục tiêu**: từ tab **Cài đặt**, chạm **1 lần** hàng "Thông báo & nhắc nhở" → màn mở ra (app bar teal + back + tiêu đề, **không** bottom nav/FAB) đủ **5 nhóm / 8 hàng** đúng mockup `docs/notification/01-cai-dat-thong-bao.svg`: mỗi hàng có icon tròn + tiêu đề + dòng phụ đọc **từ cấu hình đã nạp** (lần đầu = bộ mặc định FR-008), **2 hàng nhóm NGÂN SÁCH** coral còn **6 hàng** teal, mỗi hàng **đúng một** điều khiển (**6 công tắc** + **2 chevron**); chạm một công tắc ⇒ **chỉ hàng đó** đổi trạng thái trong phiên, 5 công tắc còn lại giữ nguyên; chạm 2 hàng chevron ⇒ **0** màn mới, **0** thông báo lỗi; back về Cài đặt.
**Tiêu chí kiểm thử độc lập**: mở màn lần đầu (DB trống) → đếm đúng 5 nhãn nhóm theo thứ tự + 8 hàng + 6 `Switch` + 2 `chevron_right`, không hàng nào có cả hai; dòng phụ đúng 8 chuỗi ở quickstart B5; soi decoration của vòng tròn icon ⇒ 2 hàng Ngân sách coral, 6 hàng teal; tắt "Cảnh báo vượt ngân sách" → chỉ `Switch` đó đổi `value`, hàng "Ngưỡng cảnh báo" vẫn hiện `Sớm: 80% · Vượt mức: 100%`; chạm 2 hàng chevron → `find.byType(...)` không thêm route, không `SnackBar`/`AlertDialog`.

- [X] T012 [US1] Tạo khung màn `lib/screens/notification_settings_screen.dart`: `StatefulWidget` `NotificationSettingsScreen({NotificationStore? store})`; field `late final NotificationStore _store` (mặc định `ensureNotificationStore()` khi `store == null`, đặt trong `initState`), `NotificationPrefs _prefs = const NotificationPrefs()`, `_loading`, `_error`; `_load()` gọi `_store.load()` → `setState(_prefs)` (nhánh lỗi ⇒ `_error`, thân màn hiện `'Không đọc được cài đặt.'.tr` + `OutlinedButton` `'Thử lại'.tr` như khuôn `UtilitiesScreen`); `SubPageScaffold(title: 'Thông báo & nhắc nhở'.tr)` + `SafeArea(top: false)` + `ListView(padding: EdgeInsets.only(bottom: 48))` — **không** bottom nav, **không** FAB — FR-002, research R5/R7 (chưa gọi `save` — sang US2)
- [X] T013 [US1] Viết các widget private trong cùng file (viết lại theo khuôn `utilities_screen.dart`, **không** nâng thành widget dùng chung): `_SectionLabel` (nhãn nhóm viết hoa, `colors.tabInactive`, 13 w600, padding `(20, 24, 20, 8)`); `_itemRow({colors, icon, iconBg, iconColor, name, subtitle, trailing, onTap})` — vòng tròn **36 px** nền `iconBg` + glyph **20 px** `iconColor`, cột giữa `Expanded` (tiêu đề `maxLines: 1` + `TextOverflow.ellipsis`, **dòng phụ wrap tự nhiên**, không `maxLines`), `trailing` **ngoài** `Expanded`; `_switchRow(...)` → `trailing: Switch(value:, onChanged:)` dùng màu `ColorScheme` (**không** thêm `switchTheme`); `_navRow(...)` → `trailing: Icon(Icons.chevron_right, color: colors.tabInactive)`; `_rows(colors, rows)` chèn `Divider(color: colors.listDivider, height: 1)` **giữa** các hàng **trong** nhóm (không sau hàng cuối, không trước nhãn nhóm) — FR-004/FR-005/FR-017, research R7/R8
- [X] T014 [US1] Dựng đủ **5 nhóm / 8 hàng** đúng thứ tự data-model §1.2 trong `lib/screens/notification_settings_screen.dart`: `NHẮC NHỞ HÀNG NGÀY` → `Nhắc nhập giao dịch hằng ngày` (`Icons.notifications_none`, teal, công tắc ← `_prefs.dailyEnabled`); `NGÂN SÁCH` → `Cảnh báo vượt ngân sách` (`Icons.warning_amber_rounded`, **coral**, công tắc ← `budgetEnabled`) + `Ngưỡng cảnh báo` (coral, **chevron**); `GIAO DỊCH ĐỊNH KỲ` → `Nhắc hóa đơn sắp đến hạn` (`Icons.event_outlined`, teal, công tắc ← `recurringEnabled`) + `Nhắc trước` (teal, **chevron**); `MỤC TIÊU TIẾT KIỆM` → `Nhắc đóng góp mục tiêu` (`Icons.track_changes`, teal, công tắc ← `goalEnabled`); `TỔNG KẾT TỰ ĐỘNG` → `Tổng kết cuối tuần` + `Tổng kết cuối tháng` (`Icons.pie_chart_outline`, teal, công tắc ← `weeklyEnabled`/`monthlyEnabled`); sắc lấy **token đã có** (`tealLightBg`/`tealOnNeutral` cho 6 hàng, `coralLightBg`/`coralOnNeutral` cho **đúng 2** hàng Ngân sách) — **không** hex cứng, **không** sửa `sora_colors.dart`; **không** công tắc "bật/tắt tất cả" — FR-003/FR-004/FR-005/FR-014, SC-001, research R8
- [X] T015 [US1] Dựng **8 dòng phụ** đọc từ `_prefs` (không hằng cứng trong widget) bằng `.trParams` + `formatClock(...)`: `'@giờ mỗi ngày'` (giờ = `formatClock(_prefs.dailyHour, _prefs.dailyMinute)`) **+** `' · chỉ nhắc nếu chưa ghi'` **chỉ khi** `_prefs.dailyOnlyIfNoTxnToday` (ghép 2 khoá, không nối chuỗi vào giữa một khoá); `'Khi đạt @sớm% và khi vượt @vượt%'`; `'Sớm: @sớm% · Vượt mức: @vượt%'`; `'Tiền điện, tiền nhà, trả nợ...'`; `'@n ngày trước hạn thanh toán'`; `'Theo chu kỳ đã đặt cho từng mục tiêu'`; `'Chủ nhật hằng tuần, @giờ'`; `'Ngày cuối tháng, @giờ'` — FR-009/FR-011/FR-015, data-model luật 14–16, quickstart B5, research R10
- [X] T016 [US1] Nối hành vi **trong phiên**: `void _setPrefs(NotificationPrefs next) => setState(() => _prefs = next);` + **6 handler** `_toggleDaily/_toggleBudget/_toggleRecurring/_toggleGoal/_toggleWeekly/_toggleMonthly` mỗi handler `=> _setPrefs(_prefs.copyWith(<đúng 1 trường>: v))` (**không** handler nào chạm 2 loại ⇒ không có cơ chế "bật/tắt tất cả"); 2 hàng chevron truyền `onTap` = **handler rỗng** (`() {}`) ⇒ có phản hồi mực nhưng **0** route mới, **0** `SnackBar`/`AlertDialog`, **0** khung "sắp có" — FR-006/FR-010/FR-011, SC-003/SC-008, data-model luật 11–12, research R11
- [X] T017 [P] [US1] Viết `test/notification_settings_screen_test.dart` (phần US1) với `test/fakes/fake_notification_store.dart`: mở màn (DB trống) ⇒ đủ **5 nhãn nhóm** đúng thứ tự + **8 tiêu đề hàng** + **8 dòng phụ** đúng nguyên văn quickstart B5; `find.byType(Switch)` = **6** và `find.byIcon(Icons.chevron_right)` = **2**, **không** hàng nào có cả hai (soi theo từng `_itemRow`); soi `Container.decoration` của vòng tròn icon ⇒ **2 hàng Ngân sách** dùng `coralLightBg`/`coralOnNeutral`, **6 hàng còn lại** teal; tắt "Cảnh báo vượt ngân sách" ⇒ chỉ `Switch` đó đổi + dòng phụ "Ngưỡng cảnh báo" **vẫn** `Sớm: 80% · Vượt mức: 100%`; 5 `Switch` còn lại **không đổi**; chạm 2 hàng chevron ⇒ **không** route mới, **không** `SnackBar`/`AlertDialog`, không ném; app bar có tiêu đề "Thông báo & nhắc nhở" + **không** có `BottomNavigationBar`/FAB; nhánh lỗi: store `load()` ném ⇒ hiện `'Không đọc được cài đặt.'` + nút `'Thử lại'` hoạt động — FR-002…FR-011, SC-001/SC-003/SC-008
- [X] T018 [US1] Sửa `lib/screens/settings_screen.dart`: thêm seam `final VoidCallback? onManageNotificationsTap;` vào constructor (khuôn 3 seam đã có) + hàm `_openManageNotifications(context)` (callback có ⇒ gọi callback, **không** push — khuôn `_openManageUtilities`) + **1** `_SettingsRow(label: 'Thông báo & nhắc nhở'.tr, onTap: …, trailing: Icon(Icons.chevron_right, color: colors.tabInactive))` đặt **ngay sau** hàng "Tiện ích & Cá nhân hóa" trong nhóm **KHÁC**; cập nhật doc comment đầu class — **không** đụng nhóm "QUÉT HÓA ĐƠN AI" — FR-001, SC-002, research R6
- [X] T019 [P] [US1] Sửa `test/settings_screen_test.dart`: thêm `'Thông báo & nhắc nhở'` vào danh sách hàng đúng **thứ tự** (sau "Tiện ích & Cá nhân hóa"), tổng số hàng của 2 nhóm đầu tăng đúng 1; chạm hàng ⇒ `find.byType(NotificationSettingsScreen)` xuất hiện (đăng ký `FakeNotificationStore` qua `Get.put` để **không** mở drift — khuôn `_registerUtilitiesStore`); back ⇒ về màn Cài đặt; bơm `onManageNotificationsTap` ⇒ callback được gọi và **không** push màn — FR-001, SC-002

## Pha 4: User Story 2 — Lưu bền: ghi mặc định lần mở đầu, giữ tham số khi tắt/bật lại (Ưu tiên: P2)

**Mục tiêu**: mọi thay đổi công tắc **được ghi ngay** vào `AppSettings`; **lần mở màn đầu tiên** trên máy chưa từng cấu hình ⇒ bộ mặc định FR-008 **được lưu lại ngay** (không chờ người dùng thao tác); thoát app / khởi động lại thiết bị ⇒ màn hiện **đúng** trạng thái đã đặt (**kể cả tắt hết** — không tự bật lại); tắt một loại rồi **bật lại** ⇒ thấy **đúng tham số cũ** (không reset về mặc định); bật/tắt liên tiếp nhanh ⇒ trạng thái cuối cùng thắng (save cũ không đè save mới).
**Tiêu chí kiểm thử độc lập**: mở màn với `FakeNotificationStore()` rỗng ⇒ `store.storedPrefs == NotificationPrefs.defaults` ngay sau lần mở đầu; chạm công tắc ⇒ `storedPrefs` cập nhật; mở lại màn **mới** với `storedPrefs` đã đổi ⇒ UI đúng trạng thái đã đặt; tắt `dailyEnabled` rồi bật lại ⇒ `dailyHour/dailyMinute/dailyOnlyIfNoTxnToday` vẫn 20/30/true; tắt cả 6 rồi mở lại ⇒ cả 6 vẫn tắt.

- [X] T020 [US2] Thêm đường ghi bền vào `lib/screens/notification_settings_screen.dart`: field `Future<void> _saveTail = Future<void>.value();`; trong `_load()` **sau** `setState(_prefs)` gọi `_saveTail = _saveTail.then((_) => _store.save(_prefs))` — ghi lại **nguyên khối vừa đọc** (idempotent ⇒ FR-007 + kịch bản 6 "mặc định được ghi ngay lần mở đầu" thoả mà **không** thêm phương thức seam, **không** side-effect ẩn trong `load()`); `_setPrefs(next)` nối tiếp `setState` bằng `_saveTail = _saveTail.then((_) async { try { await _store.save(next); } catch (_) {/* lần chạm sau ghi lại toàn trạng thái mới nhất */} })` — ghi bám đuôi để bật/tắt liên tiếp không bị save cũ đè — FR-007/FR-010, data-model luật 8–10, 12, research R2/R5, rủi ro 4 plan.md
- [X] T021 [P] [US2] Bổ sung vào `test/notification_settings_screen_test.dart` (phần US2): mở màn lần đầu với `FakeNotificationStore()` rỗng ⇒ `storedPrefs` **bằng đúng** `NotificationPrefs.defaults` (16 trường) **ngay** sau khi màn dựng xong, **không** cần chạm gì; chạm 1 công tắc ⇒ `storedPrefs` phản ánh đúng trường đó + 15 trường còn lại nguyên; mở **màn mới** với `FakeNotificationStore(storedPrefs: …)` đã đổi ⇒ UI hiện đúng trạng thái + đúng dòng phụ đã lưu (SC-006); tắt `dailyEnabled` rồi bật lại ⇒ tham số 20:30 + cờ chưa-ghi **giữ nguyên**; **tắt cả 6** ⇒ `storedPrefs` vẫn là prefs hợp lệ, không công tắc nào tự bật lại khi mở lại; bật/tắt liên tiếp 4 lần nhanh ⇒ store giữ **trạng thái cuối** (0 lần bị đè ngược) — FR-006/FR-007/FR-008/FR-010/FR-013, SC-003/SC-004/SC-005/SC-006, quickstart C/D/K

## Pha cuối: Polish & Cross-cutting

- [X] T022 [P] Thêm ca smoke **theme tối** cho màn mới vào `test/dark_theme_smoke_test.dart`: pump `NotificationSettingsScreen(store: FakeNotificationStore())` dưới `darkApp(...)` ⇒ không overflow, không ném, `Theme.of(context).brightness == Brightness.dark`, token tối **thật sự** áp cho nền/nhãn nhóm/vòng tròn icon (cả teal lẫn coral)/công tắc bật-tắt — FR-016, SC-010 (rủi ro 6 plan.md: nếu công tắc thiếu tương phản ⇒ ghi lại, **không** tự thêm `switchTheme` mà báo để QA lại 2 công tắc PBI 17/24)
- [X] T023 [P] Bổ sung vào `test/notification_settings_screen_test.dart` (phần chất lượng hiển thị): (a) **English** — `Get.addTranslations(SoraTranslations().keys)` + `Get.locale = const Locale('en')` (khôi phục trong `tearDown`, khuôn `test/budget_overview_screen_test.dart`) ⇒ tiêu đề app bar + **5 nhãn nhóm** + **8 tiêu đề hàng** + các dòng phụ dạng mô tả là **tiếng Anh**, **0** nhãn tĩnh tiếng Việt sót, còn `20:30`/`80%`/`100%`/`3`/`20:00` **giữ nguyên định dạng**; (b) **cỡ chữ 2.0 + màn 360×640** (`platformDispatcher.textScaleFactorTestValue = 2.0` + `setSurfaceSize`, khuôn `test/utilities_screen_test.dart:361`) ⇒ không `overflow`/không exception, tiêu đề hàng dài **không** đè công tắc/chevron, `Scrollable` cuộn tới được **hàng cuối** ("Tổng kết cuối tháng") — FR-015/FR-017, SC-010/SC-011
- [X] T024 Chạy `flutter analyze` + `flutter test` toàn bộ tại `app/sora_thu_chi/` → **0 lỗi analyze**, test pass **tăng**, số test đỏ **không tăng** (chỉ còn `transactions_dao_test` có sẵn — T001); kiểm thêm bằng grep trong `lib/`: **0** tham chiếu plugin thông báo/quyền (`flutter_local_notifications`, `requestPermission`, `NotificationChannel`) — FR-012/SC-007, research R14
- [ ] T025 QA tay trên emulator theo `quickstart.md` nhóm **A–K** (điểm vào & khung màn; đối chiếu mockup `01`; công tắc độc lập & giữ tham số; mặc định & lưu bền qua kill app + khởi động lại thiết bị; 2 hàng chevron im lặng; **không** bắn thông báo & **không** xin quyền; tắt hết không ảnh hưởng chức năng khác; English; chế độ Tối; cỡ chữ lớn/màn nhỏ; 2 nhóm chưa có module) — ghi lại kết quả **từng nhóm**. Bàn giao ghi rõ: QA **Android** đủ cho mọi nhóm; PBI **không** có plugin native mới ⇒ iOS chạy nhanh A/B/D nếu có máy, nếu bỏ qua phải ghi "iOS chưa QA"
- [X] T026 Tick checklist `.specify/specs/28/checklists/requirements.md` sau khi thi công xong (**giữ nguyên nội dung** đã duyệt)
- [X] T027 Đồng bộ wiki theo skill `sora-wiki`: cập nhật `wiki-knowledge/entity/` **Hồ sơ & Bảo mật** (màn "Thông báo & nhắc nhở": điểm vào nhóm KHÁC, 5 nhóm/8 hàng, 6 công tắc độc lập + 2 hàng chevron no-op, lưu **1 row JSON** `notificationPrefs` trong `AppSettings` — schema giữ **v8**, mặc định ghi ngay lần mở đầu, tham số giữ nguyên khi tắt/bật lại, **chưa** có engine bắn thông báo), `wiki-knowledge/concept/` **Lộ trình phát triển** (màn cài đặt thông báo xong — PBI 28; ⚠ còn engine bắn thông báo + màn `02`–`04` của doc + trung tâm thông báo) và **Design system** (2 sắc icon theo nhóm teal/coral trong cùng một màn) + append `wiki-knowledge/log.md` + cập nhật `wiki-knowledge/index.md`
- [X] T028 Commit PBI 28 theo skill `git-commit` (Conventional Commits, message tiếng Việt có dấu, **không** trailer `Co-Authored-By`)

---

## Sơ đồ phụ thuộc

```text
Setup (T001)  ← chỉ chốt baseline, không code
   ↓
Foundational (T002–T011)  ← chặn MỌI user story: NotificationPrefs + seam + impl drift + DI + formatClock + 23 khoá dịch + fake + 2 test tầng thấp
   ↓
US1 P1 (T012–T019)  ← MVP: khung màn + 5 nhóm/8 hàng + dòng phụ + công tắc (trong phiên) + hàng chevron no-op + điểm vào màn Cài đặt
   ↓
US2 P2 (T020–T021)  ← cần khung màn + 6 handler của US1 (ghi bền vào store + fake test)
   ↓
Polish (T022–T028)  ← T022/T023 độc lập; T024–T027 sau khi US1+US2 xong; T028 cuối cùng
```

- **Foundational là cổng**: không story nào bắt đầu trước T011 (US1 cần `NotificationPrefs` để dựng dòng phụ + `NotificationStore`/fake để pump test).
- **US2 phụ thuộc US1** (cùng file màn + cần 6 handler `copyWith` đã có ở T016); US2 **không** phụ thuộc T018/T019 (điểm vào màn Cài đặt).
- Trong Foundational: T002–T010 **khác file** ⇒ chạy song song được; **thực tế** T006/T008/T009/T010 đọc API do T002/T003 sinh ra nên viết sau — T004/T005 **hoàn toàn độc lập**.
- Trong US1: T012 → T013 → T014 → T015 → T016 là **cùng một file** ⇒ **tuần tự**; T017 [P] (file test) và T018 (file `settings_screen.dart`) chạy song song với nhóm trên; T019 [P] sau T018.

## Ví dụ chạy song song

```text
# Pha 2 — Foundational (khác file):
T004 [P]                          lib/core/date_label.dart
T005 [P]                          lib/core/locale/sora_translations.dart
T002 [P]                          lib/core/notification/notification_prefs.dart
T003 [P]                          lib/core/notification/notification_store.dart

# Pha 3 — US1 (khác file):
T017 [P] [US1]                    test/notification_settings_screen_test.dart
T018 [US1]                        lib/screens/settings_screen.dart
T019 [P] [US1]                    test/settings_screen_test.dart

# Pha cuối:
T022 [P]                          test/dark_theme_smoke_test.dart
T023 [P]                          test/notification_settings_screen_test.dart (cùng file với T021 ⇒ thực tế tuần tự)
```

## Chiến lược triển khai

- **MVP đề xuất: chỉ User Story 1** (T001–T019) — sau pha này màn **đã đúng mockup và dùng được trong phiên**: tới nơi bằng 1 lần chạm, đủ 5 nhóm/8 hàng, dòng phụ đọc cấu hình, 6 công tắc đổi được (chưa mất khi đóng app ở mốc này), 2 hàng chevron im lặng (SC-001/SC-002/SC-003/SC-008).
- **Giao hàng tăng dần**: US1 → US2 (chốt SC-004/SC-005/SC-006: mặc định ghi ngay lần mở đầu + lưu bền + giữ tham số) → Polish (SC-007/SC-009/SC-010/SC-011 + QA tay + wiki + commit). Mỗi pha kết thúc bằng `flutter analyze` + `flutter test` xanh.
- **Mốc giữa US1 và US2 không phải bản phát hành**: ở mốc US1, cấu hình **chưa** được ghi xuống DB — **không** thêm nhánh tạm/nơi lưu thứ hai để "che" trạng thái đó; T020 là task duy nhất mở đường ghi.
- **Thứ tự bắt buộc**: T011 (cổng Foundational) → T012 → … → T016 → T020 → T024 → T025 → T027 → T028.
- **Rủi ro cần để mắt khi thi công**: (1) **thiếu bản dịch EN** ⇒ `sora_translations_test` đỏ — thêm khoá **cùng lúc** với màn (T005 + T024, rủi ro 1 plan.md); (2) test đỏ **có sẵn** `transactions_dao_test` dễ bị nhầm là lỗi mới — mốc baseline ghi ở T001 (rủi ro 2); (3) **JSON hỏng trong DB** làm trắng màn ⇒ parse tolerant từng trường + test riêng (T009, rủi ro 3); (4) **ghi bất đồng bộ** khi bật/tắt nhanh ⇒ `_saveTail` (T020 + test T021, rủi ro 4); (5) **cỡ chữ lớn** làm tiêu đề đè công tắc ⇒ `maxLines: 1` + ellipsis + dòng phụ wrap trong `Expanded` (T013 + test T023, rủi ro 5); (6) **2 hàng chevron bị hiểu là lỗi QA** — chốt Q2=A, kiểm đúng hành vi **im lặng** (quickstart §4.2); (7) **2 nhóm chưa có module** (Giao dịch định kỳ, Mục tiêu tiết kiệm) phải lưu được **y như mọi hàng**, không ghi "sắp có"/không vô hiệu hoá công tắc (Q3=A, quickstart K).
