# Danh sách Task: Màn sắp xếp danh mục

**Mã PBI**: 16
**Nguồn**: plan.md, spec.md, data-model.md, research.md, quickstart.md

## Pha 1: Setup

*(Không có task — PBI 16 **không thêm dependency/pubspec mới** (`ReorderableListView` là Material built-in — plan §Ngữ cảnh kỹ thuật, research R1), **không đổi schema drift** (schemaVersion giữ **4**, không chạy `build_runner` — data-model §Migration, research R10), **không tạo `contracts/`** — app nội bộ offline, không API/CLI (bám PBI 7–15).)*

## Pha 2: Foundational

*(Hạ tầng bắt buộc xong trước khi dựng màn sắp xếp `04`: helper thuần `moveCategoryAt` (R7), mở rộng `CategoryRow` 2 param optional (R5), seam ghi `reorderCategories` xuyên interface + drift + fake (R3) — theo plan.md "Cấu trúc dự án dự kiến", research R3/R5/R7 và data-model §Ghi mới.)*

- [X] T001 [P] Tạo `app/sora_thu_chi/lib/core/category/category_sort.dart`: module thuần **`moveCategoryAt`** (R7, research §Kết luận) — hàm top-level `List<Category> moveCategoryAt(List<Category> list, int oldIndex, int newIndex)` **không đọc DB**: sao chép list đầu vào → **điều chỉnh `newIndex`** theo đúng ngữ nghĩa `ReorderableListView` (`if (newIndex > oldIndex) newIndex -= 1;`) → `removeAt(oldIndex)` rồi `insert(newIndex, removed)` → trả list mới; **không** mutate list gốc (dữ liệu bất biến — tránh setState cùng tham chiếu); không phụ thuộc file nào khác ngoài `category.dart` (`Category`)
- [X] T002 [P] Sửa `app/sora_thu_chi/lib/core/widgets/category_row.dart`: mở rộng `CategoryRow` thêm **2 param optional** (R5) — `Widget? leading` (render **trước** bubble tròn: đặt vào đầu `Row` trước `Container` bubble) và `bool showChevron = true` (điều khiển `Icon(Icons.chevron_right, ...)` cuối dòng — `if (showChevron)`); **default giữ nguyên** → chevron vẫn hiện, không leading → màn `01`/`03` (PBI 13/15) cùng test cũ **không đổi**; cập nhật doc comment class (dùng chung màn `01`/`03` + mới màn sắp xếp `04`: màn `04` truyền `leading:` tay cầm kéo–thả, `showChevron: false`, `onTap: () {}`); **không** hex cứng — màu dòng vẫn là dữ liệu `category.color` qua `AppColors`
- [X] T003 Sửa `app/sora_thu_chi/lib/data/wallet_repository.dart`: thêm khai báo **seam ghi mới** (R3) `Future<void> reorderCategories({required List<int> orderedIds});` — `orderedIds` = danh sách **đủ id danh mục cha cấp 1 của đúng một loại** theo thứ tự mới sau kéo–thả; doc comment ghi rõ ngữ nghĩa (ghi `sort_order = 0..n−1` cho từng id theo thứ tự list, atomic một transaction; con cấp 2 & cha loại kia bất biến — data-model §Ghi mới, research R4); **không** sửa các seam đọc/ghi cũ (`categories`, `categoriesIncludingHidden`, `updateCategory`, `insertCategory`… — research R10)
- [X] T004 Sửa `app/sora_thu_chi/lib/data/wallet_repository_drift.dart`: implement **`reorderCategories`** (R3, bám mẫu `performTransfer` — data-model §Ghi mới): `await db.transaction(() async { for (var i = 0; i < orderedIds.length; i++) { await (db.categories.updateWhere((t) => t.id.equals(orderedIds[i]), (t) => t.copyWith(sortOrder: Value(i))); } });` — ghi atomic `sort_order = index` (0..n−1); **không** chạy `build_runner` (không đổi schema — `.g.dart` giữ nguyên, chỉ thêm method trong class repository thuần)
- [X] T005 Sửa `app/sora_thu_chi/test/fakes/fake_wallet_repository.dart`: cài **`reorderCategories`** khớp interface (R3) — với mỗi id trong `orderedIds` (theo thứ tự list) đặt `sortOrder = index` (0..n−1) cho phần tử tương ứng trong `_categories` (đổi trong list store, giữ nguyên phần tử khác — bất biến nhóm con & loại kia); `categoriesStored` getter (đã có, dòng ~98) để widget test assert thứ tự store sau kéo; ghi chú `@override`

## Pha 3: User Story 1 - Màn sắp xếp danh mục (Ưu tiên: P1) 🎯 MVP

**Mục tiêu**: Màn con "Sắp xếp danh mục" theo bố cục `docs/category/04-sap-xep-danh-muc.svg` (spec acceptance 1–9, FR-001…009) — mở từ màn danh sách danh mục (PBI 13) khi chạm **icon "Sắp xếp"** trên app bar; `SubPageScaffold` app bar teal: back (trái), tiêu đề **"Sắp xếp danh mục"**, nút **"Xong"** (phải); body `ReorderableListView.builder` liệt kê **danh mục cha cấp 1 của đúng loại tab đang mở** theo `sortOrder` hiện tại; mỗi dòng `CategoryRow` + **tay cầm kéo–thả** trái (drag chỉ từ tay cầm), không chevron, dòng **ẩn** vẫn hiện + nhãn "Đã ẩn"; **thả → ghi ngay** qua `reorderCategories`; "Xong"/back → pop về màn danh sách **đúng tab** + thứ tự mới hiện ngay; **không** tab riêng, **không** bottom nav, **không** số tiền, **không** danh mục con.
**Tiêu chí kiểm thử độc lập**: `flutter test test/category_sort_test.dart test/category_sort_screen_test.dart test/category_list_screen_test.dart` pass — bơm `FakeWalletRepository` (store có `reorderCategories` + `categoriesStored` để assert): màn `04` render đúng mockup (chỉ cha cấp 1 đúng `initialType` theo thứ tự seed — không con/loại kia, dòng ẩn hiện + "Đã ẩn"), mỗi dòng có tay cầm, kéo tay cầm → `reorderCategories` gọi + UI đổi thứ tự, "Xong"/back → màn `01` đúng tab + thứ tự mới; unit `moveCategoryAt` phủ biên index-math; không sqlite native (R10).

- [X] T006 [US1] Tạo `app/sora_thu_chi/lib/screens/category_sort_screen.dart`: `StatefulWidget` **`CategorySortScreen`** (pattern `CategoryChildListScreen` PBI 15, R2/R8) — params `{required CategoryType initialType, WalletRepository? repository}` (mặc định `ensureWalletRepository()` trong `initState` — seam test); `initState` gọi `_load()`: nạp **1 loại** `categoriesIncludingHidden(type: initialType)` → `_rows = topLevelParents(list)` (cha cấp 1 cùng loại, theo thứ tự seed — R6/FR-003); trạng thái loading spinner / lỗi + "Thử lại" (`_load` lại) / empty phòng thủ (defensive — hiện thông báo, không lỗi; bình thường không xảy ra — spec Giả định) đúng pattern màn `01`; body = `SubPageScaffold(title: 'Sắp xếp danh mục', actions: [TextButton 'Xong' → `Navigator.pop(context)`], child: SafeArea(top: false, child: ...))` — back hệ thống cũng pop (FR-008), **không** xác nhận/hủy khi rời (đã ghi khi thả — R8); danh sách = `ReorderableListView.builder(buildDefaultDragHandles: false, itemCount: _rows.length, itemBuilder: (ctx, i) => CategoryRow(key: ValueKey('sort-row-${c.id}'), category: _rows[i], leading: ReorderableDragStartListener(index: i, child: Icon(Icons.drag_handle, color: AppColors.tabInactive)), showChevron: false, onTap: () {}), proxyDecorator: bọc dòng nổi nhẹ (Material elevation — SC-002 dòng đang kéo), onReorder: (o, n) { setState(() => _rows = moveCategoryAt(_rows, o, n)); _persist(); })` (R1/R2/R7) — autoscroll gần mép có sẵn; `_persist()`: `await _repository.reorderCategories(orderedIds: _rows.map((c) => c.id).toList())` ghi **ngay khi thả** (FR-006/R3) — bắt lỗi → gọi `_load()` đồng bộ lại về thứ tự DB (R7, không dialog phức tạp); phụ thuộc T001 (`moveCategoryAt`) + T002 (`CategoryRow` leading/showChevron) + T003/T004/T005 (seam ghi)
- [X] T007 [P] [US1] Tạo `app/sora_thu_chi/test/category_sort_test.dart`: unit test thuần **`moveCategoryAt`** (R7) — **không** cần fake/widget/sqlite, bơm `List<Category>` seed tưởng tượng (id tuần tự): kéo **lên** (`oldIndex 3 → newIndex 0` → phần tử về đầu, các phần tử dạt xuống đúng), kéo **xuống** (`oldIndex 0 → newIndex 3` — assert đã trừ `newIndex` về 2 theo ngữ nghĩa ReorderableListView: phần tử chèn sau 2 phần tử còn lại), kéo **qua nhiều dòng** (index cách xa), **thả cuối** (`oldIndex 1 → newIndex == length` → về cuối), **không đổi khi `oldIndex == newIndex`** (list giữ nguyên — acceptance biên "thả về đúng vị trí cũ"), **không mutate** list gốc (so sánh tham chiếu/list trước sau); phụ thuộc T001
- [X] T008 [P] [US1] Tạo `app/sora_thu_chi/test/category_sort_screen_test.dart`: widget test **bơm `FakeWalletRepository`** (seed bằng `withCategories`/`insertCategory` — store biến đổi, assert ghi qua `categoriesStored`, R10): **(a)** bố cục mockup `04` — pump `CategorySortScreen(initialType: expense, repository: fake)` với seed cha chi tiêu ["Ăn uống"(có 2 con seed), "Di chuyển", "Nhà ở", "Hóa đơn"]: app bar back + tiêu đề "Sắp xếp danh mục" + nút "Xong" phải, **không** tab/bottom nav/số tiền, đúng 4 cha theo thứ tự seed (acceptance 1/FR-002); **(b)** **chỉ cha cấp 1 đúng `initialType`** — seed thêm cha thu nhập + con của "Ăn uống" → không lẫn (chỉ thấy 4 cha chi tiêu, con không xuất hiện — acceptance 3/5, FR-003/004); **(c)** dòng ẩn (seed "Di chuyển" `isHidden: true`) → vẫn hiện đúng vị trí + nhãn "Đã ẩn" (acceptance 4, FR-004); **(d)** mỗi dòng có **tay cầm** drag_handle trái (find icon); **(e)** **kéo tay cầm** dòng kề bằng gesture (`tester.drag` / `timedDrag` trên handle) → assert `reorderCategories` gọi với `orderedIds` đúng thứ tự mới (fake lộ `categoriesStored` — R2/R7; kịch bản kéo xa/tự cuộn QA tay quickstart C4 — plan §Rủi ro, không ép trong widget test); **(f)** "Xong" → `Navigator.pop`; empty phòng thủ (fake không cha loại đó → hiện hướng dẫn, không lỗi — spec Giả định); **(g)** cỡ chữ hệ thống lớn → không overflow (FR-009); dùng `find.descendant` scope trong màn con (màn `01` đè dưới giữ state — pattern PBI 15) khi pump cả stack; phụ thuộc T006
- [X] T009 [US1] Sửa `app/sora_thu_chi/lib/screens/category_list_screen.dart` (R9): thay `onPressed: () {}` no-op của icon `Icons.sort` (dòng ~130, từ PBI 13) bằng — `onPressed: () async { await Navigator.push(context, MaterialPageRoute(builder: (_) => CategorySortScreen(initialType: _tab, repository: _repository))); if (!mounted) return; await _reloadSilent(); }` — mở màn `04` theo **đúng tab đang mở** (`_tab`) → khi pop về giữ nguyên tab (acceptance 7/FR-008) + `_reloadSilent()` nạp lại lặng cả 2 loại → thứ tự mới hiện ngay (FR-007, không làm mới tay — helper sẵn có PBI 15); import `category_sort_screen.dart`; **không** đụng tab/dòng/FAB/FAB thêm/reload khác; cập nhật doc comment dòng icon (hết "PBI sau no-op"); phụ thuộc T006 (class `CategorySortScreen`)
- [X] T010 [US1] Sửa `app/sora_thu_chi/test/category_list_screen_test.dart` (R9/R10): thay case cũ **"icon sort no-op"** (PBI 13) bằng — chạm icon `Icons.sort` → push `CategorySortScreen` với `initialType` = tab đang mở (acceptance 1, FR-001 — kiểm tra qua class find trong stack, `_tab` đang "Chi tiêu" → chỉ cha chi tiêu); thêm case **back từ màn sắp xếp** (bơm fake có `reorderCategories` + `categoriesStored`; kéo 1 dòng trong màn `04` hoặc pre-order store rồi vào–về) → về màn `01` đúng tab + cha hiển thị **thứ tự mới** không cần làm mới tay (acceptance 7, FR-007/008); giữ nguyên các case cũ khác (tab/đếm con/ẩn/empty/FAB→Thêm/dòng→Sửa hay màn con/refresh sau pop… — không hồi quy PBI 13/14/15); phụ thuộc T009

## Pha cuối: Polish & Cross-cutting

- [X] T011 Chạy `flutter analyze` (sạch 0 warning) + `flutter test` toàn bộ tại `app/sora_thu_chi/` (test PBI 2–15 cũ + PBI 16 mới pass — **đặc biệt** chạy lại `category_list_screen_test`, `category_child_list_screen_test`, `category_form_screen_test` sau khi mở rộng `CategoryRow` (T002, default giữ nguyên) và `category_list_screen_test` sau khi nối entry (T009): khẳng định không hồi quy PBI 13/14/15); xác nhận **không** cần `build_runner`/`.g.dart` mới (schema v4 giữ nguyên — plan §Rủi ro); đối chiếu danh sách "Không đổi" plan.md — `core/category/*` (category, category_list, category_form, category_source, category_presets), `core/widgets/category_icon.dart`, `data/db/*`, `wallet_deps.dart`, `transaction_deps.dart`, `theme/*`, `category_picker_screen.dart`, `add_transaction_screen.dart`, `category_child_list_screen.dart`, `category_form_screen.dart`, `transaction_*`, `wallet_*`, màn PIN/boot, `app_shell.dart`, `settings_screen.dart` không thay đổi; ghi nhận quyết định (R1 `ReorderableListView` built-in, R2 kéo chỉ bằng tay cầm `ReorderableDragStartListener`, R3 seam `reorderCategories` atomic, R4 đánh lại cha cấp 1 đúng loại 0..n−1, R5 `CategoryRow` +leading/+showChevron, R6 nạp 1 loại lúc mở, R7 helper thuần `moveCategoryAt` + ghi lỗi → reload lại, R8 `SubPageScaffold` + "Xong" pop, R9 entry icon màn `01` → push theo `_tab` + reload lặng, R10 không đổi schema/seam đọc cũ) vào trạng thái PBI
- [X] T012 Kiểm chứng thủ công emulator theo `.specify/specs/16/quickstart.md` nhóm A–H: Cài đặt → Danh mục (màn `01`) → tab **Chi tiêu** → chạm icon ⣿ Sắp xếp góc phải app bar → màn "Sắp xếp danh mục": app bar teal + back trái + tiêu đề + nút "Xong" phải, không tab/bottom nav/số tiền, đúng cha chi tiêu thứ tự hiện tại, mỗi dòng tay cầm trái + bubble icon màu + tên, đối chiếu trực quan `04-sap-xep-danh-muc.svg`, mở < 1 s (A); từ tab Thu nhập → chỉ cha thu nhập không lẫn; con của "Ăn uống" không xuất hiện (B); kéo tay cầm "Di chuyển" lên đầu → thả dòng dạt ra + đứng yên vị trí mới, chạm "Xong" → về đúng tab Chi tiêu thứ tự mới hiện ngay, vào lại giữ nguyên, kéo vượt màn hình tự cuộn không bật ngược, kéo nhanh nhiều dòng không lệch/trùng, loại 1 cha không lỗi (C); ẩn 1 cha qua form Sửa → vẫn hiện + "Đã ẩn" + kéo bình thường, kéo không đổi ẩn/hiện/cha (D); cha có con kéo lên/xuống → sang màn `03` con giữ nguyên thứ tự/liên kết; thêm giao dịch → picker liệt kê cha thứ tự mới; sắp cha loại kia không ảnh hưởng loại này (E); chưa kéo chạm "Xong"/back → về không đổi không cảnh báo, thả đúng vị trí cũ không ghi thừa (F); cỡ chữ lớn + màn nhỏ cuộn tới cuối không tràn/cắt tay cầm/icon/tên (G); xác nhận luồng khóa app chung (H); ghi kết quả từng mục vào trạng thái PBI
- [X] T013 Cập nhật wiki tri thức qua skill `sora-wiki` + append `wiki-knowledge/log.md`: entity **Danh mục** + **Lộ trình** (plan §Kiểm tra theo hiến pháp ⚠️) — màn `04` sắp xếp danh mục **đã triển khai** (thay mục "PBI sau" còn trống): bố cục mockup `04` (`SubPageScaffold` tiêu đề "Sắp xếp danh mục" + "Xong"; chỉ **cha cấp 1 đúng loại** tab đang mở theo `sortOrder`, không tab riêng/không con); entry từ màn `01` — icon Sắp xếp (no-op PBI 13 → kích hoạt); kéo–thả qua tay cầm (`ReorderableListView` + `ReorderableDragStartListener`, chỉ kéo trong cùng nhóm cha cùng loại, không tạo cha-con/trộn loại); **`sort_order` thành dữ liệu ghi được khi thả** — seam `reorderCategories` đánh lại `0..n−1` cho cha cấp 1 đúng loại (atomic; con & loại kia bất biến — "thứ tự theo nhóm con" giữ nguyên); danh mục ẩn/hệ thống vẫn hiện + sắp được; "Xong"/back pop không xác nhận; mọi nơi dùng thứ tự (danh sách, picker nhanh, chú thích biểu đồ sau) đọc cùng nguồn `sortOrder`; schema v4 giữ nguyên không build_runner; ngoài phạm vi: sắp danh mục con trong nhóm cha–con, sắp tự động, thao tác cấu trúc cây (PBI sau)

## Sơ đồ phụ thuộc

```text
Foundational:
  T001 [P] core/category/category_sort.dart   (moveCategoryAt — thuần)
  T002 [P] core/widgets/category_row.dart     (CategoryRow +leading +showChevron, default giữ nguyên)
  T003 data/wallet_repository.dart            (seam ghi reorderCategories — interface)
     ├──▶ T004 data/wallet_repository_drift.dart (implement drift, transaction atomic)
     └──▶ T005 test/fakes/fake_wallet_repository.dart (implement fake + categoriesStored để assert)
US1:
  T001 + T002 + T003/T004/T005 ──▶ T006 category_sort_screen.dart (màn 04: nạp 1 loại + kéo–thả + ghi khi thả)
                                    ├──▶ T008 [P] [US1] category_sort_screen_test.dart
  T001 ──▶ T007 [P] [US1] category_sort_test.dart (unit moveCategoryAt — độc lập, chạy sớm được)
  T006 ──▶ T009 category_list_screen.dart (icon Sắp xếp no-op → push màn 04 theo _tab + reload lặng)
           └──▶ T010 [US1] category_list_screen_test.dart (thay case no-op cũ + back đúng tab/thứ tự mới)
Polish:
  T011 analyze + full test (không hồi quy PBI 2–15) ──▶ T012 QA emulator (quickstart A–H) ──▶ T013 wiki sync
```

Thứ tự thực thi chính: `T001 → T002 → T003 → T004 → T005 → T006 → T009 → T010 → T011 → T012 → T013` (test `[P]` chạy sau code tương ứng: T007 sau T001, T008 sau T006; T010 sau T009). Mấu chốt: **3 mảnh Foundational độc lập đầu** (T001 helper thuần, T002 `CategoryRow` mở rộng default-an-toàn, T003 seam interface) xong trước — T004/T005 là implement seam theo interface T003; **T006 màn `04`** ghép tất cả Foundational (kéo–thả + ghi); **T009 nối điểm vào màn `01`** là bước cuối US1 (cần màn `04` đã tồn tại); **không có** `build_runner`/schema/dependency mới (schema v4 giữ nguyên) nên thứ tự thoải mái như PBI 15.

## Ví dụ chạy song song

```text
# Foundational — 3 mảnh độc lập (khác file, không chặn nhau):
T001 [P] core/category/category_sort.dart (moveCategoryAt)
T002 [P] core/widgets/category_row.dart   (CategoryRow +leading/+showChevron)
T003     data/wallet_repository.dart       (seam interface — T004/T005 nối sau)

# US1 — 2 test màn độc lập (sau code tương ứng, khác file):
T007 [P] [US1] category_sort_test.dart          (sau T001 — unit thuần, không cần chờ screen)
T008 [P] [US1] category_sort_screen_test.dart   (sau T006)

# T010 list_screen_test chạy sau T009 (màn 01 đã nối màn 04) — tuần tự không song song [P].
```

## Chiến lược triển khai

- **MVP đề xuất**: User Story 1 (toàn bộ PBI 16 — đặc tả là một lát cắt "màn sắp xếp danh mục" duy nhất, không chia user story P1/P2/P3; spec không gán mức ưu tiên theo story). Foundational dựng 3 mảnh nền — helper thuần `moveCategoryAt` (R7), mở rộng `CategoryRow` default-an-toàn (R5), seam ghi `reorderCategories` (R3) — làm tiền đề bắt buộc; US1 ghép `CategorySortScreen` (màn `04`, nạp 1 loại + kéo–thả + ghi ngay khi thả) + nối điểm vào màn `01` (icon Sắp xếp → màn theo `_tab` + reload lặng) để ra giá trị end-to-end (FR-001/FR-006/FR-007/FR-008).
- **Thứ tự giao hàng tăng dần**: Foundational (helper + `CategoryRow` + seam — không đổi hành vi màn `01`/`02`/`03`, test PBI 13/14/15 giữ) → US1 màn `04` (screen + widget test phủ bố cục/chỉ-cha-đúng-loại/dòng-ẩn/kéo-tay-cầm-ghi + unit test index-math) → nối màn `01` (icon → màn + refresh sau pop + widget test) → Polish (analyze/test toàn cục không hồi quy + QA emulator quickstart A–H + sync wiki).
