# Danh sách Task: Tìm kiếm & lọc giao dịch

**Mã PBI**: 12
**Nguồn**: plan.md, spec.md, data-model.md, research.md, quickstart.md

## Pha 1: Setup

*(Không có task — PBI 12 **không thêm dependency/pubspec mới**, **không đổi schema drift** (schemaVersion giữ **4**, không chạy `build_runner` — plan §Ngữ cảnh kỹ thuật & research R1/kết luận), không thêm method repository (repository/DAOs/fakes hiện có đã đủ: `allTransactions`/`loadAll`/`categories({type})`). Tính năng **chỉ đọc** — mọi logic lọc là module thuần mới + widget màn form + nối controller/màn danh sách.)*

## Pha 2: Foundational

*(Hạ tầng thuần bắt buộc xong trước user story: module lọc `transaction_filter.dart` (domain `TxnSearchFilter` + normalize bỏ dấu + luật khớp AND + group-keep transfer + summary + sort) + unit test; nối state bộ lọc vào `TransactionController` (activeFilter/filtered/cache, additive không hồi quy) + test controller — theo plan.md "Cấu trúc dự án dự kiến" và research R1–R13, data-model §Domain/§Luật khớp/§Bất biến.)*

- [X] T001 Tạo `app/sora_thu_chi/lib/core/transaction/transaction_filter.dart`: module **thuần** (không đọc DB/state, R1) gồm — enum `TxnTypeFilter { all, income, expense, transfer }` (nhãn Tất cả/Thu/Chi/Chuyển khoản — **không có adjustment**, R12) + `SortOption { dateNewest, dateOldest, amountAsc, amountDesc }` (nhãn Ngày mới nhất/Ngày cũ nhất/Số tiền tăng dần/Số tiền giảm dần, mặc định dateNewest) + `DatePreset { today, thisWeek, thisMonth, all, custom }` (Hôm nay/Tuần này/Tháng này/Toàn bộ + tùy chỉnh, R11); class **bất biến** `TxnSearchFilter` — field `keyword ''`, `type all`, `datePreset thisMonth`, `dateStart/dateEnd` (ngày đầu/cuối **tháng hiện tại** giải theo `now` anchor, data-model §Domain — mặc định FR-005), `categoryIds Set<int>` rỗng, `walletId null`, `amountMin/amountMax null`, `sort dateNewest`; `bool get hasAnyCondition`; `TxnSearchFilter reset()` (FR-014) + factory mặc định giải preset → cặp ngày `[start 00:00, hôm sau dateEnd)` theo `now`; `String normalizeSearch(String)` — lowercase + **bỏ dấu tiếng Việt** bằng bảng ánh xạ ký tự (ă â đ ê ô ơ ư + nguyên âm 5 dấu, không thêm package — R9); `List<Transaction> filterTransactions(...)` luật khớp **AND** từng tiêu chí (data-model §Luật khớp): loại (adjustment chỉ trong `all`), ngày trong `[dateStart, endExclusive)`, danh mục `categoryId ∈ (chọn ∪ con của cha chọn)` **hoặc** fallback tên khi `categoryId == null` (so `t.category` text — giữ lịch sử, R7), ví `walletId`, tiền theo **`abs(amount)` bao biên** (bỏ trống một đầu → bỏ ràng buộc, R10), từ khóa substring trên `normalize(note+category+tags)` (R9); **bất biến group-keep**: transfer cùng `transferGroupId` giữ **cả 2 vế** nếu chính nó hoặc vế cùng nhóm khớp (R2); `class FilteredTxSummary { int count; int signedTotal; }` — `count` = số **dòng hiển thị sau gộp** transfer, `signedTotal` = Σ thu − Σ chi.abs (transfer/adjustment trong count ngoài total, R3/FR-011); hàm sort cho rows sau gộp: ngày mới/cũ nhất (controller giữ nhóm qua `groupDisplayRows`) và **tiền phẳng** theo `abs` đúng hướng, trùng → ngày mới nhất trước rồi `sortId` tăng (ổn định, R4) — phụ thuộc domain `Transaction`/`TxnType` (đã có) + `transaction_list.dart` `buildDisplayRows/TxnRow` khi tính summary; mọi chữ `null` id không bỏ lọt (SC-002)
- [X] T002 [P] Tạo `app/sora_thu_chi/test/transaction_filter_test.dart`: unit **thuần** bơm tham số (không sqlite, R11) — `normalizeSearch`: "an uong" ↔ "Ăn uống", hoa/thường, chữ có dấu các loại → bỏ hết (SC-004); khớp từ khóa trên note/category/tags riêng & chuỗi dài; chip loại: Thu/Chi/Chuyển khoản giữ đúng loại, adjustment **chỉ** trong `all` (R12); **AND nhiều tiêu chí** đồng thời loại+ngày+danh mục+ví+tiền+từ khóa không thừa/thiếu (SC-002); chọn cha gộp đủ con + chọn con chỉ đúng con (SC-008); fallback tên dòng `category_id == null`; group-keep: lọc ví A → cặp transfer A→B giữ đủ 2 vế (R2); preset date theo `now` anchor: today/thisWeek (thứ Hai đầu tuần)/thisMonth/all/custom + custom chặn start>end; khoảng tiền abs **bao biên** + trống một đầu + `min > max` vô hiệu không trả kết quả sai (R10); sort ngày mới/cũ nhất (nhóm) vs tiền **phẳng** tie-break id; `FilteredTxSummary`: count sau gộp + total thu − chi, transfer/adjustment trong count **ngoài** total (R3); `reset()` về đúng mặc định FR-014; phụ thuộc T001
- [X] T003 Sửa `app/sora_thu_chi/lib/core/transaction/transaction_controller.dart`: thêm **additive** (không hồi quy 248 test cũ — R13): `final Rxn<TxnSearchFilter?> activeFilter` (null = không lọc, hành vi PBI 9), `final Rxn<FilteredTxView?> filtered` (view phụ — class nhỏ `FilteredTxView { List<DayGroup>? groups; List<TxnRow>? flatRows; FilteredTxSummary summary; }` đặt cùng controller), cache `List<Transaction> _all` + `Map<int,String> _names` + index danh mục cho mở rộng cha→con/fallback tên từ lần `load()` gần nhất, `DateTime now` (anchor lần nạp, mặc định giờ thật — deterministic test, data-model bất biến 8); `void setFilter(TxnSearchFilter f)` — gán `activeFilter` + **tính lại trên cache không đọc DB** (R6): `filterTransactions(_all, f, …)` → `buildDisplayRows` → `FilteredTxSummary` → sort ngày giữ nhóm `groupDisplayRows` vào `groups` **hoặc** sort tiền phẳng vào `flatRows`; `void clearFilter()` — `activeFilter = null` + `filtered = null` (Bỏ lọc → toàn bộ, FR-013); sửa `load()` — vẫn **luôn** dựng `data` không-lọc cho test cũ + khi `activeFilter == null` xanh, nếu đang có `activeFilter != null` → dựng `filtered` từ dữ liệu mới nạp (bộ lọc còn hiệu lực qua ra/vào tab — SC-007); giữ nguyên `TransactionView.data`/error/isLoading/retry; phụ thuộc T001 (filterTransactions/summary/sort)
- [X] T004 [P] Tạo `app/sora_thu_chi/test/transaction_controller_filter_test.dart`: widget/unit controller bơm `FakeWalletRepository` (không sqlite) — `setFilter` sinh `filtered` đúng tập/sort/summary trên cache; `clearFilter` về null; bộ lọc **giữ nguyên** qua `load()` lặp lại (ra/vào tab — SC-007) và khi dữ liệu thay đổi (thêm giao dịch mới rồi load → view lọc làm mới không nhân đôi — data-model bất biến 7); đường dữ liệu không-lọc (`data`) không đổi khi chưa `setFilter`; phụ thuộc T003

## Pha 3: User Story 1 - Luồng Tìm kiếm & Lọc giao dịch (Ưu tiên: P1)

**Mục tiêu**: Màn con "Tìm kiếm & Lọc" theo bố cục `docs/transaction/05-tim-kiem-loc.svg` (spec acceptance 1–10, FR-001…018) — mở từ **icon lọc** màn "Giao dịch" (no-op PBI 9 → thật), toàn màn hình (app bar teal chứa ô tìm kiếm pill + back, **không** bottom nav); ô tìm kiếm so khớp ghi chú/tên danh mục/tag **bỏ dấu tiếng Việt**; hàng chip loại Tất cả/Thu/Chi/Chuyển khoản (chọn teal, adjustment chỉ dưới Tất cả); nhãn "BỘ LỌC NÂNG CAO" + 5 dòng bộ lọc (Khoảng thời gian preset+custom chặn ngày vô lý / Danh mục chọn nhiều gộp con thích ứng loại chip / Ví một hoặc tất cả / Khoảng số tiền Từ–Đến dùng `AmountKeypad` abs bao biên chặn min>max / Sắp xếp theo 4 option) — mỗi dòng mở bottom sheet; dòng tóm tắt "N kết quả · Tổng: X đ" **live** (debounce ~250ms khi gõ); nút "Đặt lại" (về mặc định) + "Áp dụng" (pop về màn Giao dịch hiển thị đúng tập đã lọc AND kèm chỉ báo "N kết quả · Tổng: X đ" + "Bỏ lọc", ẩn card "Thu/Chi tháng này"); back chưa Áp dụng giữ tập cũ; sort ngày giữ nhóm, sort tiền **phẳng**; empty 0 kết quả không lỗi; không vỡ cỡ chữ lớn/safe area.
**Tiêu chí kiểm thử độc lập**: `flutter test test/transaction_filter_test.dart test/search_filter_screen_test.dart test/transaction_controller_filter_test.dart test/transaction_screen_test.dart test/widget_test.dart` pass — seam bơm `FakeWalletRepository` render đúng bố cục mockup `05` + summary live khi gõ/chip, lọc danh mục/ví/tiền/ngày, Đặt lại/Áp dụng/back giữ tập cũ, màn danh sách nhánh lọc (chỉ báo + sort phẳng + Bỏ lọc), 0 kết quả empty, cỡ chữ lớn/safe area không overflow; không sqlite native (R11/R14).

- [X] T005 [US1] Tạo `app/sora_thu_chi/lib/screens/search_filter_screen.dart`: `StatefulWidget` (R6/R10/R11) — seam `final WalletRepository? repository` (default null → `ensureWalletRepository()` trong `initState`, pattern PBI 11) + tham số `TxnSearchFilter? initial` (mặc định = filter đang áp dụng của controller **hoặc** mặc định Tháng này khi chưa áp dụng — FR-015) + `DateTime? now` anchor; `initState` **nạp 1 lần** `allTransactions() + loadAll()` (build cache raw + tên ví + danh mục active thu/chi để mở rộng cha) cho summary live (không đọc DB lại từng thao tác — R1); dựng trên **Scaffold riêng** (màn con đè shell, không bottom nav — FR-001): AppBar teal leading back (FR-015), title là **ô tìm kiếm pill** trắng (icon kính lúp + `TextField` placeholder "Tìm kiếm giao dịch...", debounce ~250ms bằng `Timer` hủy khi dispose — R9); body cuộn được: hàng **chip loại cuộn ngang** 4 nút Tất cả/Thu/Chi/Chuyển khoản (chọn = teal chữ trắng, khác nền trắng viền `#E0E0E0` — FR-004/R12, adjustment chỉ trong Tất cả); nhãn "BỘ LỌC NÂNG CAO"; **5 dòng bộ lọc** mỗi dòng icon tròn + nhãn + giá trị hiện tại + chevron, chạm mở `showModalBottomSheet` (pattern PBI 8): **(1) Khoảng thời gian** — preset Hôm nay/Tuần này/Tháng này/Toàn bộ + "Tùy chọn…" mở 2 `showDatePicker` ràng buộc chặn bắt đầu sau kết thúc, hiển thị "dd/MM/yyyy - dd/MM/yyyy" hoặc "Toàn bộ", mặc định Tháng này (FR-005/R11); **(2) Danh mục** — sheet danh sách multi-select (checkbox + icon tròn `category.color` + tên, cha có con chú thích "(gồm con)") nguồn **theo loại chip** (Tất cả → thu+chi, Thu → thu, Chi → chi — FR-006/R7), chip Chuyển khoản → dòng vô hiệu + bỏ chọn cũ; row hiện chip đã chọn + ô "+ Thêm"; **(3) Ví** — sheet dòng "Tất cả các ví" + từng ví `walletsByDisplayOrder` (gồm ví ẩn nhãn "(đã ẩn)" để xem lịch sử — FR-007/R8); **(4) Khoảng số tiền** — 2 ô "Từ … đ"/"Đến … đ" chạm mở sheet dùng lại `AmountKeypad` (PBI 11) trả `int?` (null = bỏ giới hạn đầu đó), khớp **`abs` bao biên**; `min > max` → dòng báo đỏ "Số tiền tối thiểu không được lớn hơn tối đa" + **vô hiệu Áp dụng** (FR-008/R10); **(5) Sắp xếp theo** — sheet single 4 option (FR-009); dòng tóm tắt "N kết quả · Tổng: X đ" **live** khi gõ/chip (đếm dòng sau gộp transfer, total thu − chi, transfer/adjustment ngoài total — FR-003/011/R3; 0 → "0 kết quả"); chân màn `bottomNavigationBar` (pattern `SubPageScaffold`) hai nút: **"Đặt lại"** (trắng viền) reset nháp về mặc định không pop (FR-014) + **"Áp dụng"** (teal, bo 8 cao 44) → `Navigator.pop(draft)`; back/X → `pop(null)` giữ tập cũ (FR-015/R6); lỗi đọc → thông báo + "Thử lại"; toàn màn cuộn được + nút chân không cắt khi cỡ chữ lớn/safe area (SC-011); phụ thuộc T001 + tái dùng `AmountKeypad`/`formatMoney`/`categoryGlyph`/`walletsByDisplayOrder`/`SubPageScaffold`
- [X] T006 [P] [US1] Tạo `app/sora_thu_chi/test/search_filter_screen_test.dart`: widget test **bơm `FakeWalletRepository`** (constructor, không sqlite — seed dòng mẫu theo case trong test, không sửa fake — R11): (a) bố cục mockup `05`: AppBar back + ô tìm pill "Tìm kiếm giao dịch...", chip "Tất cả" chọn (teal), nhãn "BỘ LỌC NÂNG CAO", 5 dòng, tóm tắt, Đặt lại/Áp dụng, **không** bottom nav (acceptance 1/SC-009); (b) gõ từ khóa → summary **cập nhật sau pump ≥250ms**, "an uong" tìm thấy "Ăn uống" bỏ dấu (acceptance 2, SC-004); (c) chip Chi/Thu/Chuyển khoản → summary + nguồn danh mục đổi (Chi → chỉ chi; Chuyển khoản → dòng Danh mục vô hiệu) (acceptance 3, FR-006); (d) picker Danh mục multi + chọn cha/"+ Thêm"/X chip (acceptance 9, SC-008); (e) sheet Ví chọn một ví/"Tất cả" (FR-007); (f) sheet tiền: trống một đầu giữ giới hạn đầu kia, `min>max` báo đỏ + Áp dụng vô hiệu (acceptance G/FR-008); (g) date preset Hôm nay/Toàn bộ/Tùy chọn chặn start>end (FR-005); (h) "Đặt lại" về mặc định (Tháng này/chip Tất cả/...) (acceptance 7, FR-014); (i) "Áp dụng" → `pop` trả filter đúng điều kiện; back/không Áp dụng → `pop(null)` giữ tập cũ (acceptance 8, FR-015); (j) `textScaleFactor` cao + safe area → cuộn tới chân màn, Đặt lại/Áp dụng không cắt, không `RenderFlex overflow` (acceptance 10, SC-011); phụ thuộc T005
- [X] T007 [US1] Sửa `app/sora_thu_chi/lib/screens/transaction_screen.dart`: đổi `_FilterButton` (no-op PBI 9 — plan §Rủi ro hồi quy có chủ đích) → `onTap` `await Navigator.push<TxnSearchFilter>(MaterialPageRoute(builder: (_) => SearchFilterScreen(now: controller.now)))`, kết quả non-null → `controller.setFilter(filter)` (Áp dụng) — null giữ nguyên (back, FR-015); body `Obx` thêm nhánh đọc `controller.activeFilter`/`filtered`: khi **null** → giữ nguyên nhánh cũ PBI 9 (`_TransactionList` với card "Thu/Chi tháng này"); khi **có filter** → thay card tháng bằng **thanh chỉ báo lọc** đầu danh sách (icon lọc + "N kết quả · Tổng: X đ" theo `formatMoney` số âm tự có `−` + nút "Bỏ lọc" → `controller.clearFilter()` về toàn bộ — FR-013/R5) rồi vẽ: sort ngày (`dateNewest`/`dateOldest`) → nhóm `DayGroup` header như PBI 9 (mới nhất/cũ nhất lên đầu — R4); sort tiền (`amountAsc`/`amountDesc`) → danh sách **phẳng** (không header ngày) xếp theo `abs` (R4); dòng `TxnRow` giữ nguyên cấu trúc/màu thu teal/chi coral/transfer trung tính, chạm mở chi tiết (PBI 10) & FAB ghi giao dịch mới hoạt động (FR-012); tập khớp rỗng → trạng thái riêng "Không có giao dịch khớp bộ lọc." + gợi ý "Bỏ lọc để xem toàn bộ" (khác empty "Chưa có giao dịch nào." khi không lọc — FR-016/R14); phụ thuộc T005 (class `SearchFilterScreen`) + T003 (controller activeFilter/filtered/setFilter/clearFilter/now)
- [X] T008 [P] [US1] Sửa `app/sora_thu_chi/test/transaction_screen_test.dart`: cập nhật case icon lọc (d) — trước kỳ vọng no-op (PBI 9) → nay chạm mở màn `SearchFilterScreen` (assert AppBar ô tìm/chip, bơm `FakeWalletRepository` Get.put + bộ lọc mặc định khi chưa áp dụng); **thêm nhóm test mới** nhánh lọc (bơm fake + `controller.setFilter` hoặc bấm Áp dụng qua fake): thanh chỉ báo `N kết quả · Tổng: X đ` thay card "Thu/Chi tháng này", danh sách chỉ tập khớp đúng sắp xếp; sort **Số tiền giảm dần** → list phẳng không header ngày, khoản lớn trước; **Ngày cũ nhất** → nhóm cũ nhất lên đầu; "Bỏ lọc" → về toàn bộ + card tháng hiện lại (FR-013); 0 kết quả → empty khớp có hint Bỏ lọc, không lỗi (FR-016); chạm dòng vẫn mở chi tiết + FAB còn (FR-012); giữ nguyên các case cũ khi **không** lọc (không hồi quy — plan §Rủi ro); phụ thuộc T007
- [X] T009 [P] [US1] Sửa `app/sora_thu_chi/test/widget_test.dart`: chạy lại & cập nhật nếu có kỳ vọng icon lọc no-op (PBI 9/2) → nay chạm icon lọc ở tab Giao dịch mở màn lọc thật (bơm `FakeWalletRepository` Get.put như shell); không bơm sâu luồng lọc tại đây (đã cover ở transaction_screen_test) — mục tiêu shell còn đúng: nav tab/PIN/boot/FAB không hồi quy; phụ thuộc T007

## Pha cuối: Polish & Cross-cutting

- [X] T010 Chạy `flutter analyze` (sạch 0 warning) + `flutter test` toàn bộ tại `app/sora_thu_chi/` (test PBI 2–11 cũ + PBI 12 mới pass — **đặc biệt** chạy lại test PBI 6/8/9/10/11 do `transaction_controller.dart` thêm state & `transaction_screen.dart` đổi icon lọc no-op → màn lọc thật, khẳng định additive không hồi quy trên đường dữ liệu không-lọc); xác nhận **không** cần `build_runner`/`.g.dart` mới (schema v4 giữ nguyên); đối chiếu danh sách "Không đổi" plan.md — `wallet_*` screens/controller, màn PIN/boot, `transaction_list.dart`/`transaction_detail*.dart`, `add_transaction_screen.dart`/`category_picker_screen.dart`, `app_shell.dart`, `app_database*.dart`, `wallet_repository*`, `fake_wallet_repository.dart` không thay đổi; ghi nhận quyết định hiển thị user chốt (R4 sort tiền phẳng, R5 thay card bằng thanh lọc) vào trạng thái PBI
- [X] T011 Kiểm chứng thủ công emulator theo `.specify/specs/12/quickstart.md` nhóm A–I: mở màn lọc từ icon lọc — bố cục mockup `05` + mặc định Tháng này/chip Tất cả/Ví tất cả/Ngày mới nhất (A); gõ "ăn trưa" summary live + "an uong" bỏ dấu (B); chip Chi + tiền 0–100.000 + từ khóa AND (C); preset & tùy chọn thời gian + picker chặn start>end (D); danh mục cha gộp con theo loại chip, Chuyển khoản vô hiệu dòng Danh mục (E); lọc ví Vietcombank → transfer gộp 1 dòng (F); tiền trống biên + min>max báo đỏ vô hiệu Áp dụng (G); Áp dụng → card tháng ẩn thay thanh `N kết quả · Tổng` + Bỏ lọc, sort tiền phẳng / ngày cũ nhất, bộ lọc còn qua ra/vào tab, back giữ tập cũ, Đặt lại về mặc định, 0 kết quả empty không lỗi (H); cỡ chữ lớn + safe area hai nút chân không cắt (I); ghi kết quả vào trạng thái PBI

## Sơ đồ phụ thuộc

```text
Foundational:
  T001 transaction_filter.dart (domain thuần: enum + TxnSearchFilter + normalize + filterTransactions + summary + sort)
    ├──▶ T002 [P] transaction_filter_test.dart
    └──▶ T003 transaction_controller.dart (activeFilter/filtered/cache/setFilter/clearFilter) ──▶ T004 [P] transaction_controller_filter_test.dart
US1:
  T001 ──▶ T005 search_filter_screen.dart (màn form 05) ──▶ T006 [P] search_filter_screen_test.dart
  T003 + T005 ──▶ T007 transaction_screen.dart (icon lọc thật + nhánh list đã lọc) ──▶ T008 [P] transaction_screen_test.dart (sửa d + nhóm mới)
                                                                                  └──▶ T009 [P] widget_test.dart (cập nhật kỳ vọng icon lọc)
Polish:
  T010 analyze + full test (không hồi quy PBI 2–11) ──▶ T011 QA emulator (quickstart A–I)
```

Thứ tự thực thi chính: `T001 → T003 → T005 → T007 → T010 → T011` (test `[P]` T002 chạy sau T001, T004 sau T003, T006 sau T005, T008/T009 sau T007). Mấu chốt: module thuần **T001 xong trước** mọi thứ (controller/màn lọc đều tiêu thụ `filterTransactions`/`FilteredTxSummary`/sort); **T007** là màn cuối vì ghép cả `SearchFilterScreen` lẫn state controller; **không có** `build_runner` (schema giữ v4) nên thứ tự thoải mái hơn PBI 7/11.

## Ví dụ chạy song song

```text
# Foundational — sau T001 (file nền xong, khác file độc lập):
T002 [P] transaction_filter_test.dart      T003 [P] transaction_controller.dart

# Sau T003:
T004 [P] transaction_controller_filter_test.dart

# US1 — test màn chạy song song việc nối màn danh sách (sau file tương ứng):
T006 [P] [US1] search_filter_screen_test.dart (sau T005)
T008 [P] [US1] transaction_screen_test.dart   (sau T007)
T009 [P] [US1] widget_test.dart               (sau T007, khác file T008)
```

## Chiến lược triển khai

- **MVP đề xuất**: User Story 1 (toàn bộ PBI 12 — đặc tả là một lát cắt "luồng Tìm kiếm & Lọc" duy nhất, không chia user story P1/P2/P3). Foundational dựng phần logic thuần (module `transaction_filter` + unit test; state controller additive) làm tiền đề bắt buộc; US1 ghép `search_filter_screen` (màn form `05`) + sửa `transaction_screen` (icon lọc thật + nhánh danh sách đã lọc + Bỏ lọc) lên để ra giá trị end-to-end.
- **Thứ tự giao hàng tăng dần**: Foundational (module lọc thuần + unit test chứng minh luật AND/group-keep/summary/sort; controller nối state + test — mọi logic test trên `FakeWalletRepository`/hàm thuần, không sqlite native) → US1 màn lọc `05` (screen + widget test) → nối màn Giao dịch (icon lọc + nhánh list lọc + test) → widget_test → Polish (analyze/test toàn cục không hồi quy + QA emulator quickstart A–I).
