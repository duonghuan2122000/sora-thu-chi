# Danh sách Task: Màn hình thêm giao dịch & chọn danh mục

**Mã PBI**: 11
**Nguồn**: plan.md, spec.md, data-model.md, research.md, quickstart.md

## Pha 1: Setup

*(Không có task — PBI 11 **không thêm dependency/pubspec mới**: drift `^2.34.4`, drift_dev & build_runner đã khai báo từ PBI 7. Đây là bản **bump schema drift v3 → v4** (bảng `Categories` + cột `transactions.category_id`) do người dùng chốt (research R1/R2) — mọi thay đổi schema/seed/migration đặt ở Foundational, `build_runner` tái sinh `app_database.g.dart` chạy ngay trong task T008 (bắt buộc trước khi `flutter run`/DAO test, như PBI 7–10).)*

## Pha 2: Foundational

*(Hạ tầng bắt buộc xong trước user story: domain `Category`/`CategoryType` + `Transaction.categoryId`; hằng `CategorySource` seed danh mục mặc định; schema v4 `Categories` + cột `category_id` + seed/migration + gán id cho giao dịch seed khớp tên; repository + 2 method (`categories`/`addTransaction` trong 1 `db.transaction()` bù balance); hàm thuần `add_form`; map `categoryIcon` + widget `amount_keypad`; fake mở rộng; test thuần/DAO — theo plan.md "Cấu trúc dự án dự kiến" và research R1–R11.)*

- [X] T001 [P] Tạo `app/sora_thu_chi/lib/core/category/category.dart`: enum `CategoryType { income, expense }` + domain `Category` — field `id` (int; `CategorySource` dùng id tạm 0), `name`, `type`, `icon` (chuỗi khóa → IconData qua map UI), `color` (int ARGB, bắt buộc), `parentId` (int?, null = cha), `sortOrder`, `isHidden`, `isSystem`, getter `bool get isParent => parentId == null` (data-model §Domain `Category`; R1/R4) — additive, không đụng file khác
- [X] T002 [P] Sửa `app/sora_thu_chi/lib/core/transaction/transaction.dart`: thêm field **`int? categoryId`** (constructor default `null`, additive — data-model §Thay đổi trên domain, R2) — tham chiếu `categories.id` (transfer/adjustment/dòng seed không khớp → null); giữ nguyên mọi field/sort/constructor hiện có (test PBI 6/8/9/10 compile nguyên)
- [X] T003 Tạo `app/sora_thu_chi/lib/core/category/category_source.dart`: hằng danh sách seed danh mục mặc định (pattern `WalletSource`, R4) — 8 cha **chi** (Ăn uống/Di chuyển/Nhà ở/Hóa đơn/Mua sắm/Giải trí/Sức khỏe/Giáo dục, `sortOrder` 0..7) + 4 cha **thu** (Lương/Thưởng/Đầu tư/**Khác**, `sortOrder` 0..3) + 3 con của **Ăn uống** (Cà phê/Ăn ngoài/Đi chợ, `sortOrder` 0..2), mỗi phần tử `Category(id: 0, ..., isSystem: true, isHidden: false)`; **màu/icon theo bảng data-model §Seed** (con thừa hưởng màu cha `#3D8C77`, icon riêng; thu "Khác" màu `#9B9B9B` icon `category`) — nguồn cho drift seed & fake (acceptance 3/4, R4); phụ thuộc T001
- [X] T004 [P] Tạo `app/sora_thu_chi/lib/core/transaction/add_form.dart`: module **thuần** (không đọc DB/state, R6/R9) gồm — `int appendAmountDigit(int current, int digit, {int maxDigits = 12})` (bỏ số 0 đầu, chặn tràn quá 12 chữ số), `int backspaceAmount(int current)` (= `current ~/ 10`), `List<String> missingRequiredFields({required int amount, Category? category, Wallet? wallet, DateTime? date})` (trả tên trường thiếu: `'amount'` khi `amount <= 0`, `'category'`, `'wallet'`, `'date'` — rỗng = hợp lệ), `bool isDirty({...})` (số tiền > 0 / đã chọn category / note khác rỗng / ngày ≠ hiện tại / type khác expense — data-model §View) — unit test ở T012
- [X] T005 [P] Tạo `app/sora_thu_chi/lib/core/widgets/category_icon.dart`: hàm `IconData categoryIcon(String key)` map khóa → IconData Material (restaurant, directions_car, home, receipt, shopping_bag, sports_esports, medical_services, school, payments, redeem, show_chart, **category**, local_cafe, restaurant_menu, shopping_cart) + fallback `Icons.category` khi key lạ (data-model §Map icon; R5); đặt gần `categoryGlyph` hiện có — `categoryGlyph(name)` (PBI 9, theo tên) giữ nguyên cho dòng lịch sử chưa có `category_id`
- [X] T006 [P] Tạo `app/sora_thu_chi/lib/core/widgets/amount_keypad.dart`: widget numpad số tiền bám mockup `02` (R6) — lưới phím tròn viền (hàng `1..9`; hàng cuối trái phím `,` màu nhạt **no-op** VND số nguyên, giữa `0`, phải icon backspace); callback `onDigit(int)`/`onBackspace()`; dùng token `AppColors` (không hex cứng widget); `const`, tái dùng ở màn thêm (T017)
- [X] T007 Sửa `app/sora_thu_chi/lib/data/db/app_database.dart`: **bump `schemaVersion` 3 → 4**; thêm bảng `Categories` (`@DataClassName('CategoryRow')`: id autoIncrement, name, type `textEnum<CategoryType>()`, icon, color int, parentId int nullable, sortOrder default 0, isSystem/isHidden bool default false — data-model §Bảng drift mới, R1); thêm cột `transactions.categoryId` (`integer().nullable()`, R2); thêm `_seedCategories()` (chèn cha trước lấy id theo tên → chèn con gán `parent_id`, chỉ chạy khi bảng rỗng tránh duplicate khi upgrade nhiều bước — plan §Rủi ro); sửa `_seedSampleTransactions` gán `category_id` khi tên `category` text khớp danh mục mặc định **cùng type** (map tên→id; tên lạ `'Xăng xe'`/`'Thu nhập khác'`/`'Bán đồ cũ'` & transfer → null, R4); `onCreate`: `createAll()` → `_seedCategories()` → `_seedSampleWallets()` → `_seedSampleTransactions()`; `onUpgrade`: giữ nhánh `from < 2`/`from < 3`, thêm nhánh `if (from < 4)` `createTable(categories)` + `addColumn(transactions, transactions.categoryId)` + `_seedCategories()` (+ với DB `from < 2` mới tạo `transactions` trong nhánh cũ thì seed giao dịch gán luôn `category_id`; DB `from ≥ 2` đã có giao dịch → giữ `category_id` null, không mất dữ liệu — data-model §Migration); **giữ nguyên** số dòng/giá trị seed giao dịch (không hồi quy PBI 6/8/9/10); phụ thuộc T001 (CategoryType) + T003 (CategorySource)
- [X] T008 Chạy `dart run build_runner build --delete-conflicting-outputs` tại `app/sora_thu_chi/` tái sinh `app_database.g.dart` (schema v4 — bảng `Categories` + cột `categoryId`) (commit); bắt buộc sau T007, trước `flutter run`/DAO test (quickstart §Cách chạy)
- [X] T009 [P] Sửa `app/sora_thu_chi/lib/data/wallet_repository.dart` (interface): thêm — `Future<List<Category>> categories({required CategoryType type})` (trả danh mục đang hoạt động `isHidden == false` thuộc `type`, gồm cha & con; UI tự tách bằng `parentId` — R3/R5) + `Future<void> addTransaction({required int walletId, required TxnType type, required int amount, required Category category, required DateTime date, String note = ''})` (chỉ nhận `type` income/expense, `amount > 0`, `category.type` khớp `type`, `category.id > 0` — R3); không thêm layer (plan §Kiểm tra sau thiết kế)
- [X] T010 Sửa `app/sora_thu_chi/lib/data/wallet_repository_drift.dart`: implement 2 method — `categories` = `select` filter đúng `type` + `isHidden false`, sắp `sortOrder`, map `CategoryRow` → `Category` (`_toCategory`); `addTransaction` trong **1 `db.transaction()`**: đọc ví `wallet_id` → update `wallets.balance` ± `amount` (`income` + / `expense` −) → insert `TransactionsCompanion` với `amount` **có dấu theo loại** (`income` `+x` / `expense` `−x`, khớp quy ước signed seed/PBI 8), `categoryId: category.id`, `category: category.name`, `note`, `transactionDate`; `transferGroupId` null, `tags`/`receiptImage`/`location` `''` (R3, data-model §Repository); sửa `_toTransaction` map thêm `categoryId: r.categoryId` (bỏ sót → màn đọc DB thật luôn thấy null); phụ thuộc T008 (`.g.dart` v4) + T009
- [X] T011 [P] Sửa `app/sora_thu_chi/test/fakes/fake_wallet_repository.dart`: thêm `categories({type})` (trả danh mục active từ seed `CategorySource`, filter đúng type + `!isHidden`) + `addTransaction` (ghi 1 dòng vào list đang giữ với `amount` có dấu theo loại + bù balance ví fake tương ứng, parity drift — R3/R11); `_toTransaction`/dòng dựng tay map `categoryId` (field mới default null additive — test cũ không phá); tất cả màn/controller test vẫn chạy fake (R11)
- [X] T012 [P] Tạo `app/sora_thu_chi/test/add_form_test.dart`: unit thuần bơm tham số — `appendAmountDigit` (bắt đầu `0` → ghi đè số mới, đủ 12 chữ số → chặn tràn giữ nguyên), `backspaceAmount` (`0` → `0`, xóa từng số), `missingRequiredFields` (thiếu riêng từng trường → đúng danh sách; đủ → rỗng; amount `0` → có `'amount'`), `isDirty` (từng điều kiện: tiền > 0 / chọn category / note rỗng khác rỗng / ngày khác now / đổi type) (plan §Cấu trúc test, R6/R9); phụ thuộc T004
- [X] T013 [P] Tạo `app/sora_thu_chi/test/category_source_test.dart`: kiểm tra seed hợp lệ — tên **duy nhất** trong phạm vi cha + type; **≤ 2 cấp** (con có `parentId` trỏ đúng cha cùng type, cha không có `parentId`); màu ARGB hợp lệ & icon key **đủ** (mọi key đều có entry trong `categoryIcon`, không rơi fallback — import T005); con thừa hưởng đúng type cha; sortOrder tuần tự 0..n trong từng nhóm (data-model §Seed, R4); phụ thuộc T003 + T005
- [X] T014 Sửa `app/sora_thu_chi/test/transactions_dao_test.dart`: drift `NativeDatabase.memory()` (giữ skip-guard nếu host thiếu sqlite, như PBI 7–10) — (1) fresh schema v4: seed categories (bảng rỗng → đủ 12 cha + 3 con đúng type/`parent_id`), `allTransactions` map `category_id` khớp tên (dòng `'Ăn uống'`/`'Mua sắm'`/`'Lương'` → id đúng), tên lạ → null; (2) migration v3→v4: dựng DB cũ schema v3 → nâng lên v4 → bảng `categories` + cột có sẵn, giao dịch cũ `category_id` null không mất dữ liệu; (3) `addTransaction` drift: income bù `+balance` + dòng `+x` đúng ví/ngày/`category_id` + tên, expense bù `−balance` + dòng `−x`; không phá case migration v1→v2/v2→v3 (data-model §Migration; R3); phụ thuộc T008 + T010

## Pha 3: User Story 1 - Màn hình thêm giao dịch & chọn danh mục (Ưu tiên: P1)

**Mục tiêu**: Màn "Thêm giao dịch" + luồng con "Chọn danh mục" theo đúng bố cục `docs/transaction/02-them-giao-dich.svg` & `03-chon-danh-muc.svg` (spec acceptance 1–9, FR-001…016) — toàn màn hình (app bar teal X/check, **không** bottom nav), segmented **Chi | Thu | Chuyển khoản** (default Chi; tab Chuyển khoản mở luồng PBI 8), nhập số tiền bằng numpad tùy chỉnh (phân tách nghìn + `đ`, accent chi coral / thu teal), 4 trường Danh mục (bắt buộc → màn chọn danh mục: lưới cha 4 cột đúng loại + khoan xuống "DANH MỤC CON", ô "Thêm mới" no-op) / Ví (pre-select ví mặc định, sheet ví hoạt động gồm thẻ) / Ngày giờ (mặc định now) / Ghi chú (tùy chọn), nút "Lưu giao dịch" — ghi đúng loại/dấu/bù balance trong 1 `db.transaction()`, chống trùng khi bấm 2 lần (cờ `_saving`), báo lỗi tại đúng trường, xác nhận rời khi dirty, empty ví không kẹt; mở từ FAB "+" danh sách giao dịch (PBI 9), sau lưu làm mới danh sách + card "Thu/Chi tháng này" (FR-013/SC-006); không vỡ với cỡ chữ lớn/safe area, không lộ qua màn khóa (PBI 3).
**Tiêu chí kiểm thử độc lập**: `flutter test test/add_transaction_screen_test.dart test/category_picker_screen_test.dart test/widget_test.dart` pass — seam bơm `FakeWalletRepository` render đúng bố cục mockup + numpad `1.250.000 đ`, lọc thu/chi, drill cha→con chọn đúng, báo lỗi tại trường, dialog xác nhận rời, 2 lần Lưu → 1 dòng, empty/1 ví, FAB mở màn thêm + shell refresh; không sqlite native.

- [X] T015 [US1] Tạo `app/sora_thu_chi/lib/screens/category_picker_screen.dart`: `StatefulWidget` (R5/R11) — seam `final WalletRepository? repository` (default null → `ensureWalletRepository()` trong `initState`), nhận `CategoryType type`; dựng trên `Scaffold` AppBar teal back icon + title "Chọn danh mục" (màn con đè shell, không bottom nav); body cuộn được: **lưới 4 cột** danh mục **cha** đang hoạt động của `type` (nạp `repository.categories(type)`, tách cha = `parentId == null` sắp sortOrder) — mỗi ô icon tròn nền `Color(category.color)` + glyph `categoryIcon(category.icon)` trắng + tên dưới (token — màu là **dữ liệu** `Categories`, không styling cứng); chạm cha **có con** → hiện vùng **"DANH MỤC CON: <TÊN CHA>"** (nhãn viết hoa xám + đường kẻ `#EFEFEF`, mockup `03`) hiện các con dạng cell chọn được ngay dưới lưới cha (cùng màn, cuộn được — R5, không route con); chạm cha **không con** → chọn luôn; chọn (cha đơn hoặc con) → `Navigator.pop(context, Category)`; **ô "Thêm mới"** cuối lưới (nét đứt, dấu `+`) → **no-op không lỗi/treo** (module Danh mục sau — spec ngoài phạm vi); **empty state** (không danh mục loại đang chọn) → thông báo gọn + hướng dẫn, không lỗi (edge); lỗi đọc categories → thông báo + nút "Thử lại"; không vỡ bố cục khi cỡ chữ lớn/safe area (FR-015/SC-008)
- [X] T016 [P] [US1] Tạo `app/sora_thu_chi/test/category_picker_screen_test.dart`: widget test **bơm `FakeWalletRepository`** (constructor, không sqlite) — (a) đang loại Chi → lưới chỉ danh mục chi (Ăn uống…Giáo dục), **không thấy** Lương/Thưởng; đổi Thu → ngược lại (acceptance 3/4, SC-004); (b) cha có con **Ăn uống** → chạm → hiện vùng "DANH MỤC CON: ĂN UỐNG" (Cà phê/Ăn ngoài/Đi chợ), chạm **Ăn ngoài** → `pop` trả `Category` tên "Ăn ngoài" (acceptance 3); (c) cha không con **Nhà ở** → chọn ngay (FR-006); (d) ô "Thêm mới" chạm → no-op không crash/không rời màn (spec ngoài phạm vi); (e) type không có danh mục → empty state thông báo không lỗi (edge); (f) `textScaleFactor` cao + safe area → cuộn tới ô cuối không `RenderFlex overflow` (acceptance 9, FR-015); phụ thuộc T015
- [X] T017 [US1] Viết lại `app/sora_thu_chi/lib/screens/add_transaction_screen.dart` (hiện là stub SubPageScaffold — thay bằng StatefulWidget đầy đủ, R7/R11): seam `WalletRepository? repository`; **Scaffold riêng** (màn toàn màn hình, không bottom nav — FR-001) AppBar teal — `leading` icon X (đóng → R9), `actions` icon check (lưu), title "Thêm giao dịch"; body cuộn linh hoạt: **(1) segmented `Chi | Thu | Chuyển khoản`** (`SegmentedButton` tùy chỉnh: chosen teal/chữ trắng, unchosen nền trắng viền, track bo pill mockup — FR-002), default **Chi**; chạm **"Chuyển khoản"** → nếu dirty xác nhận bỏ (R9) rồi `Navigator.push(WalletTransferScreen(sourceWallet: ví đang chọn, controller: ensureWalletController()))` (PBI 8) — transfer xong `pop(true)` → màn tự `pop(true)` để shell làm mới; transfer hủy → quay lại màn thêm (FR-003, R10); **(2) vùng số tiền** — hiển thị `formatMoney` căn giữa kèm `đ`, font lớn, gạch chân mảnh accent **coral khi Chi / teal khi Thu**, `FittedBox` co số rất lớn không tràn/cắt (FR-004/FR-009, R6); **(3) 4 dòng trường** (Danh mục / Ví / Ngày giờ bắt buộc, Ghi chú tùy chọn — mỗi dòng icon tròn + nhãn phụ + value + chevron, mockup `02`): Danh mục → push `CategoryPickerScreen(type)` nhận `Category` về điền `category.name`; Ví → **pre-select ví mặc định** (`isDefault`) / nếu mặc định ẩn thì ví hoạt động đầu theo sortOrder, chạm → `showModalBottomSheet` danh sách ví hoạt động (`!isHidden`, **gồm thẻ tín dụng** — pattern PBI 8, R8), biên: **0 ví hoạt động** → thông báo "Chưa có ví hoạt động — hãy tạo ví trong Quản lý ví" đầu vùng trường + chặn Lưu + vẫn X/back thoát được / **1 ví** → nạp sẵn không cần mở sheet (FR-008, edge); Ngày giờ → Material date + time picker, default `DateTime.now()`, cho quá khứ/tương lai (FR-009); Ghi chú → TextField nhiều dòng tùy chọn lưu trọn (FR-010); **(4) `amount_keypad`** cố định dưới (T006, R6); **(5) nút "Lưu giao dịch"** bo 8 cao 44 cố định chân (cơ chế `bottomNavigationBar` như `SubPageScaffold`); **ghi nghiệp vụ**: nạp ví + categories(đúng type) khi mở/đổi tab Chi↔Thu; lưu qua `repository.addTransaction(...)` trong `try` với cờ **`_saving`** vô hiệu 2 điểm lưu (nút + check) chống tạo trùng (FR-012 edge); thành công → `Navigator.pop(true)`; lỗi repository → `SnackBar` giữ form (R9); **báo lỗi tại đúng trường** (`_missing`): caption coral dưới vùng số tiền "Vui lòng nhập số tiền lớn hơn 0" + dưới từng trường thiếu "Chưa chọn danh mục"/"Chưa chọn ví"/"Chưa chọn ngày giờ" (FR-011/SC-005, R9); **rời khi dirty** → dialog xác nhận "Hủy giao dịch? Hủy/Thoát" (X hoặc back; sạch → pop thẳng; `_saving` → chặn rời) — Thoát không tạo giao dịch (FR-014); loading/empty ví + categories đúng loại; không vỡ cỡ chữ lớn + safe area, nút Lưu không cắt (FR-015); phụ thuộc T015 (picker) + T010 (repo) + T006 (numpad)
- [X] T018 [P] [US1] Tạo `app/sora_thu_chi/test/add_transaction_screen_test.dart`: widget test **bơm `FakeWalletRepository`** — (a) bố cục mockup: AppBar title "Thêm giao dịch" + icon X + check, segmented Chi mặc định (chosen teal), số tiền `0 đ`, 4 trường Danh mục/Ví/Ngày giờ/Ghi chú + numpad + nút "Lưu giao dịch", **không** bottom nav (acceptance 1); (b) gõ numpad `1`,`2`,`5`,`0`,`0`,`0`,`0` → vùng số tiền `1.250.000 đ` (phân tách nghìn + `đ`, accent coral khi Chi — acceptance 2/SC-002); backspace xóa từng số; phím `,` no-op; chuyển Thu → accent teal (R6); (c) chạm Danh mục → mở `CategoryPickerScreen` (lưới chỉ chi khi ở Chi), chọn con "Ăn ngoài" → về, trường hiện "Ăn ngoài"; danh mục thu không xuất hiện khi Chi (acceptance 3/4, SC-004); (d) chọn ví/ngày giờ → trường điền đúng; ghi chú nhập (acceptance 4); (e) đủ dữ liệu + Lưu → `pop(true)` và fake ghi đúng 1 dòng loại/dấu/số/ví/ngày/ghi chú/`category_id` (acceptance 5, SC-003); (f) thiếu số tiền/danh mục/ví/ngày giờ → báo caption tại đúng trường, **không** tạo dòng (acceptance 6, SC-005); (g) nhập dở rồi chạm X → dialog xác nhận, chọn Thoát → pop không tạo giao dịch, Hủy → ở lại dữ liệu giữ; sạch → X thoát thẳng (acceptance 8, FR-014); (h) Lưu 2 lần nhanh → chỉ 1 dòng (FR-012 edge, SC-009); (i) 0 ví hoạt động → thông báo chặn Lưu, X thoát được; 1 ví → nạp sẵn (edge); tab "Chuyển khoản" → mở màn transfer PBI 8 (acceptance 7, FR-003); (j) `textScaleFactor` cao + safe area → cuộn được, nút Lưu/ô không `RenderFlex overflow` (acceptance 9, FR-015/SC-008); phụ thuộc T017
- [X] T019 [US1] Sửa `app/sora_thu_chi/lib/core/app_shell.dart`: đổi `_openAddTransaction` (FAB "+", hiện push stub no-op) → `await Navigator.push<bool>(MaterialPageRoute(builder: (_) => AddTransactionScreen()))`; kết quả **`true`** (lưu thu/chi thành công hoặc xong chuyển khoản qua tab — cả hai pop `true`) → gọi `ensureTransactionController().load()` làm mới danh sách giao dịch + card "Thu/Chi tháng này" ngay không cần thao tác tay (FR-013/SC-006, R10); giữ nguyên các tab/FAB vị trí khác; phụ thuộc T017
- [X] T020 [P] [US1] Sửa `app/sora_thu_chi/test/widget_test.dart`: cập nhật case FAB (PBI 2/9 trước kỳ vọng no-op — hồi quy có chủ đích, plan §Rủi ro) → chạm FAB "+" mở màn "Thêm giao dịch" (AppBar title, segmented Chi default, nút "Lưu giao dịch" — bơm `FakeWalletRepository` Get.put như shell thật); lưu xong (hoặc fake pop `true`) → shell gọi `load()` — assert danh sách/card cập nhật (FR-013); giữ nguyên các case còn lại (nav tab/PIN/boot) không hồi quy; phụ thuộc T019

## Pha cuối: Polish & Cross-cutting

- [X] T021 Chạy `flutter analyze` (sạch 0 warning) + `flutter test` toàn bộ tại `app/sora_thu_chi/` (test PBI 2–10 + PBI 11 mới pass, kể cả DAO có skip-guard; **đặc biệt** test PBI 6/8/9/10 chạy lại do domain `Transaction` + `categoryId`, seed `categories` chèn trước seed giao dịch, `FakeWalletRepository` + method, FAB no-op → màn thêm thật — khẳng định additive không hồi quy); xác nhận `app_database.g.dart` mới (schema v4) đã sinh & sẽ commit; đối chiếu danh sách "Không đổi" plan.md — `wallet_*` screens/controller, màn main, dashboard/report/settings, PIN/boot, `transaction_list.dart`/`transaction_detail.dart` (đọc text — `category_id` null không phá), `transaction_controller.dart`, `sub_page_scaffold.dart`, `date_label.dart`, `money_format.dart`, bảng `wallets`/schema v1→v2/v2→v3 migration không thay đổi + ghi nhận seed 11 dòng mẫu / quyết định mở #1 (R12) và `ponytail:` ceiling balance eager (data-model §bất biến 7)
- [X] T022 Kiểm chứng thủ công emulator theo `.specify/specs/11/quickstart.md` nhóm A–H: ghi khoản chi (mở từ FAB, numpad `1.250.000 đ`, chọn con Ăn ngoài, lưu → dòng mới + Chi tháng này tăng + ví giảm — A), ghi khoản thu Lương (accent teal, danh mục thu, không thấy danh mục chi — B), cha không con chọn ngay + ô "Thêm mới" no-op (C), tab Chuyển khoản mở luồng PBI 8 (D), validation báo tại trường + Lưu 2 lần → 1 giao dịch (E), X khi dirty → dialog xác nhận (F), biên: 0 ví/1 ví/ví ẩn/số rất lớn/ngày tương lai/ghi chú dài/empty danh mục/cỡ chữ lớn + safe area/khóa app không lộ số tiền (G), không hồi quy các tab/ví/chuyển tiền/PIN + `flutter test` xanh (H); ghi kết quả vào trạng thái PBI

## Sơ đồ phụ thuộc

```text
Foundational:
  T001 [P] category.dart (CategoryType + Category) ──▶ T003 category_source.dart ──▶ T007 app_database schema v4 + seed ──▶ T008 build_runner .g.dart
  T002 [P] transaction.dart (+categoryId) ───────────────────────────────┘                  └──▶ T010 wallet_repository_drift (2 method)
  T004 [P] add_form.dart ──▶ T012 [P] add_form_test              T009 [P] wallet_repository (interface) ──▶ T010
  T005 [P] category_icon.dart ──▶ T013 [P] category_source_test   T008 + T010 ──▶ T014 transactions_dao_test
  T006 [P] amount_keypad.dart ──▶ (dùng ở T017)                  T001/T002 ──▶ T011 [P] fake_wallet_repository
US1:
  T015 category_picker_screen ──▶ T016 [P] category_picker_screen_test
  T015 + T010 + T006 ──▶ T017 add_transaction_screen (viết lại) ──▶ T018 [P] add_transaction_screen_test
  T017 ──▶ T019 app_shell (FAB → push + refresh) ──▶ T020 [P] widget_test (thay no-op PBI 2/9)
Polish:
  T021 analyze + full test (không hồi quy PBI 2–10) ──▶ T022 QA emulator (quickstart A–H)
```

Thứ tự thực thi chính: `T001/T002 → T003 → T007 → T008 → T009 → T010 → T014 → T015 → T017 → T019 → T021 → T022` (T004/T005/T006/T011 khởi động song song từ đầu sau domain; test `[P]` T012/T013 chạy khi file nền tương ứng xong; T016/T018/T020 chạy khi màn/`app_shell` tương ứng xong). Mấu chốt: **chạy `build_runner` (T008) trước** `flutter run`/DAO test — schema v4 phải tái sinh `app_database.g.dart`.

## Ví dụ chạy song song

```text
# Foundational — khởi động đồng thời từ đầu (khác file, độc lập):
T001 [P] category.dart      T002 [P] transaction.dart (+categoryId)      T004 [P] add_form.dart
T005 [P] category_icon.dart      T006 [P] amount_keypad.dart

# Foundational — sau T001/T002/T003 (file nền xong), chạy cùng lúc:
T011 [P] fake_wallet_repository.dart      T012 [P] add_form_test (sau T004)
T013 [P] category_source_test (sau T003 + T005)      T009 [P] wallet_repository interface

# US1 — test màn chạy song song việc nối shell (sau màn tương ứng):
T016 [P] [US1] category_picker_screen_test (sau T015)
T018 [P] [US1] add_transaction_screen_test (sau T017)
T020 [P] [US1] widget_test (sau T019)
```

## Chiến lược triển khai

- **MVP đề xuất**: User Story 1 (toàn bộ PBI 11 — đặc tả là một lát cắt "màn thêm giao dịch + màn chọn danh mục" duy nhất, không chia user story P1/P2/P3). Foundational dựng phần dữ liệu/trình bày (schema v4 `Categories` + `category_id` + seed danh mục, domain `Category`, 2 method repository, hàm thuần `add_form`, map `categoryIcon` + widget numpad, fake mở rộng) làm tiền đề bắt buộc; US1 ghép `category_picker_screen` + viết lại `add_transaction_screen` + kích hoạt FAB (app_shell refresh) lên để ra giá trị end-to-end.
- **Thứ tự giao hàng tăng dần**: Foundational (schema v4 + domain + pure functions + repository seam — mọi logic test được trên `FakeWalletRepository`/hàm thuần, không cần sqlite native) → US1 màn chọn danh mục (screen + widget test) → màn thêm giao dịch (screen + widget test + tab Chuyển khoản nối PBI 8) → nối FAB/app_shell refresh + widget test → Polish (analyze/test toàn cục không hồi quy + QA emulator quickstart A–H).
