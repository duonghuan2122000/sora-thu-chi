# Danh sách Task: Màn hình thêm / sửa danh mục

**Mã PBI**: 14
**Nguồn**: plan.md, spec.md, data-model.md, research.md, quickstart.md

## Pha 1: Setup

*(Không có task — PBI 14 **không thêm dependency/pubspec mới**, **không đổi schema drift** (schemaVersion giữ **4**, không chạy `build_runner` — plan §Ngữ cảnh kỹ thuật, research R1/kết luận & data-model §Phạm vi). Tính năng **ghi** trên bảng `categories` v4 đã có: thêm 2 module thuần `category_form.dart`/`category_presets.dart` + 1 màn `category_form_screen.dart` + 3 seam (`categoryHasTransactions`/`insertCategory`/`updateCategory`) + 8 token màu `AppColors.category*` + nối 2 điểm vào no-op ở `category_list_screen.dart` (FAB "+" → Thêm; dòng không con → Sửa; dòng có con giữ no-op — màn `03` PBI sau). Không tạo `contracts/` — app nội bộ offline, không API/CLI (bám PBI 7–13).)*

## Pha 2: Foundational

*(Hạ tầng bắt buộc xong trước user story: presets icon/màu + module thuần validation + 8 token màu + 3 seam ghi/đếm trên `WalletRepository` (interface → drift + fake) — theo plan.md "Cấu trúc dự án dự kiến", research R2/R3/R4/R5 và data-model §Seam mới/§Module thuần mới.)*

- [X] T001 Tạo `app/sora_thu_chi/lib/core/category/category_presets.dart`: module **thuần** (bám pattern `wallet_presets.dart`, R2, data-model §Module thuần mới) — `categoryIconChoices`: `List<String>` **đúng 15 khóa** đang có trong map `categoryIcon` (thứ tự ổn định; mọi ô render được, không crash khi form chọn); `CategoryType defaultIconFor(CategoryType type)` + `int defaultColorFor(CategoryType type)`: chọn sẵn mặc định khác null, thuộc danh sách lựa chọn (FR-005); `categoryPresetColors`: `List<int>` **13 ARGB** = hợp 5 màu seed dữ liệu (teal `#0F6E56` = `AppColors.teal`, avatar `#3D8C77` = `AppColors.avatarBg`, listLabel `#5F5E5A` = `AppColors.listLabel`, coral `#D85A30` = `AppColors.coral`, tabInactive `#9B9B9B` = `AppColors.tabInactive`) + 8 token `AppColors.category*` (mockup `02`, T003) → danh mục seed hiện có luôn chọn lại được màu cũ; **màu là dữ liệu** (ARGB int), list hằng tập trung 1 chỗ (không hex cứng trong widget)
- [X] T002 [P] Tạo `app/sora_thu_chi/lib/core/category/category_form.dart`: module **thuần** (không đọc DB/state — nhận `List<Category>` đầu vào, R5, data-model §Module thuần mới) gồm — `String? categoryNameError({required String raw, required List<Category> siblings, int? excludeId})`: trim `raw` (FR-003), rỗng/trắng → `'Tên danh mục không được để trống'` (acceptance 12); trùng tên trong nhóm = cùng `type` + cùng `parentId` (2 null so bằng nhau, gồm bản **đang ẩn**) trừ `excludeId` (sửa bỏ chính nó — edge "giữ nguyên tên") → báo lỗi trùng; cùng tên khác loại/khác cha → hợp lệ (FR-004); `int endOfGroupSortOrder(List<Category> group)` = `max(sortOrder)+1`, `0` nếu rỗng (FR-009 "thêm vào cuối nhóm"); `bool canChangeType({required bool hasTransactions, required bool hasChildren, required bool isParent})` = `!hasTransactions && !hasChildren && isParent` (FR-002/008, chốt spec — con có `isParent=false` → luôn false); `List<Category> parentsOfType(List<Category> list)` = cấp 1 của list sắp tăng `sortOrder` (wrap `category_list.topLevelParents` đã có PBI 13 — không tách lại) — nguồn danh sách cha; **gồm cha ẩn** (FR-006 không chặn, màn tự loại chính nó khi Sửa)
- [X] T003 [P] Sửa `app/sora_thu_chi/lib/theme/app_colors.dart`: thêm **8 token** màu palette mockup `02` — `categoryOrange #F2994A`, `categoryBlue #2F80ED`, `categoryPurple #9B59B6`, `categoryYellow #F2C94C`, `categoryRed #EB5757`, `categoryCyan #56CCF2`, `categoryGreen #27AE60`, `categoryViolet #BB6BD9` (R2, design rule "token tập trung, không hex cứng trong widget"); các màu seed đã là token (`teal/avatarBg/listLabel/coral/tabInactive`) — không thêm bản trùng; phụ thuộc `categoryPresetColors` T001 dùng (tham chiếu token, chạy trước hoặc sau T001 đều được — 2 file độc lập)
- [X] T004 Sửa `app/sora_thu_chi/lib/data/wallet_repository.dart` (interface): thêm **3 seam** (R3/R4, data-model §Seam mới) — `Future<bool> categoryHasTransactions(int categoryId)`: `true` khi có ≥ 1 dòng `transactions.category_id == categoryId` (nguồn khóa đổi loại FR-002/008); `Future<Category> insertCategory(Category category)`: ghi `name/type/icon/color/parentId/sortOrder/isHidden`, bỏ qua `c.id` (DB sinh), `isSystem = false`, trả `Category` đã lưu (id + đủ trường); `Future<Category> updateCategory(Category category)`: `update where id`, ghi **tất cả** trường nghiệp vụ gồm `isHidden` (công tắc) và `isSystem` (giữ giá trị dòng đang sửa), trả `Category` đã lưu; doc: repo **không tự validate trùng tên/khóa loại/cây 2 cấp/tính sortOrder** (validation & sort thuộc module thuần + UI — bám seam hiện có `performTransfer`/`addTransaction`)
- [X] T005 [P] Sửa `app/sora_thu_chi/lib/data/wallet_repository_drift.dart`: implement 3 method — `@override` `categoryHasTransactions`: đếm `transactions.category_id == id` (`.count()`/select gọn theo bảng nhỏ, R3); `insertCategory`: `CategoriesCompanion.insert` (bỏ qua id — DB sinh; `isSystem = false`, `isHidden` theo `category.isHidden`) rồi đọc lại dòng → map `Category` trả về (như insert ví); `updateCategory`: `update where id` ghi đủ trường nghiệp vụ (gồm `isSystem` giữ giá trị dòng) rồi đọc lại → trả `Category`; **không** `build_runner`/`.g.dart` mới (schema v4, bảng đã có — plan §Rủi ro); phụ thuộc T004 (chữ ký method)
- [X] T006 [P] Sửa `app/sora_thu_chi/test/fakes/fake_wallet_repository.dart`: đổi bộ danh mục sang **store biến đổi** — giữ seed làm bản sao `List<Category>` (thay trả hằng `CategorySource.all`) + `int _nextCategoryId` (data-model §Fake & test, plan §Rủi ro — mọi test cũ chỉ đọc nên hành vi giữ); cài 3 method: `categoryHasTransactions(id)` đếm trên `_transactions` (gồm seed giao dịch tùy chọn truyền vào constructor — `transactions: []` cho danh mục "sạch", 1 dòng `categoryId` đích cho case khóa); `insertCategory` thêm `copyWith(id: _nextCategoryId++)` vào store + trả; `updateCategory` thay phần tử đúng `id` + trả; thêm read-accessor `List<Category> get categoriesStored` (additive — test cũ không đổi); giữ constructor `withCategories`/`categoriesSeed` ngữ nghĩa (data-model §Fake); phụ thuộc T004 (chữ ký method)
- [X] T007 [P] Tạo `app/sora_thu_chi/test/category_presets_test.dart`: unit **thuần** (không sqlite) — (1) bất biến R2/data-model: mọi `Category` trong `CategorySource.all` có `icon ∈ categoryIconChoices` và `color ∈ categoryPresetColors` (form Sửa luôn có ô/ô màu trùng lựa chọn sẵn — chặn hồi quy khi thêm seed); (2) `categoryIconChoices` đủ 15 khóa, mỗi khóa render được (nằm trong map `categoryIcon`); (3) `defaultIconFor`/`defaultColorFor` cho cả 2 `CategoryType` khác null + thuộc danh sách lựa chọn tương ứng; (4) `categoryPresetColors` 13 phần tử, distinct; phụ thuộc T001 (+ T003 nếu presets tham chiếu `AppColors.category*`)
- [X] T008 [P] Tạo `app/sora_thu_chi/test/category_form_test.dart`: unit **thuần** bơm `List<Category>` tự dựng (không sqlite, R5/data-model) — `categoryNameError`: tên rỗng/trắng → lỗi trống (acceptance 12); trùng cùng `(type, parentId)` **gồm bản đang ẩn** → lỗi; cùng tên **khác loại / khác cha** → hợp lệ; sửa bỏ chính nó (`excludeId`) → không báo trùng (edge "giữ nguyên tên"); `endOfGroupSortOrder`: nhóm rỗng → 0, có phần tử → `max+1`; `canChangeType` 4 góc — đã gd / có con / không-gốc → false, gốc sạch → true (data-model §Nhóm); `parentsOfType`: chỉ cấp 1, sort `sortOrder` tăng, gồm cha ẩn; phụ thuộc T002

## Pha 3: User Story 1 - Màn hình thêm / sửa danh mục (Ưu tiên: P1) 🎯 MVP

**Mục tiêu**: Màn con "Thêm danh mục"/"Sửa danh mục" theo bố cục `docs/category/02-them-sua-danh-muc.svg` (spec acceptance 1–14, FR-001…012) — mở từ màn danh sách danh mục (PBI 13): **FAB "+"** → chế độ Thêm (`initialType` = tab đang mở), chạm **danh mục không con** → chế độ Sửa (prefill 100%); danh mục **có con** giữ no-op (đi vào màn con `03` — PBI sau, chốt PBI 13/spec); màn con — app bar thương hiệu teal tiêu đề theo chế độ + nút back + chữ **"Lưu"** góc phải (`SubPageScaffold.actions`), nút chính **"Lưu danh mục"** teal cao 44 bo 8 cuối màn (`bottomNavigationBar`), **không** bottom nav; pill chọn loại **Chi tiêu/Thu nhập** (Thêm đổi được, loại mặc định theo tab lúc chạm FAB — mặc định Chi tiêu nếu không xác định; Sửa hiển thị loại hiện tại, đổi chỉ khi `canChangeType` — đã gd/có con/là con → **readonly box** khóa); `TextFormField` tên (`maxLength 30`, trim, validator lỗi trống/trùng dưới ô); lưới chọn **biểu tượng** (15 khóa `categoryIconChoices`, ô tròn 40, 1 ô chọn sẵn — chọn viền teal + nền teal nhạt) + **màu** (13 ô `categoryPresetColors`, ô tròn ~34, 1 ô chọn sẵn — viền đậm + dấu kiểm trắng); ô **"Danh mục cha (tùy chọn)"** mặc định "Không có — là danh mục gốc", bấm mở bottom sheet liệt kê **cấp 1 cùng loại đang chọn gồm cha ẩn** (đổi loại → lựa chọn cha đưa về "Không có"); công tắc **"Ẩn khỏi danh sách nhanh"** (Thêm tắt sẵn; Sửa theo `category.isHidden`); lưu hợp lệ → `insertCategory`/`updateCategory` với `sortOrder` (cùng nhóm giữ / đổi nhóm = `endOfGroupSortOrder` nhóm đích → cuối nhóm) → `Navigator.pop(saved)`; màn danh sách sau pop **refresh lặng cả 2 loại** phản ánh ngay; `_saving` chặn lưu trùng (FR-011), lỗi bất ngờ → SnackBar coral giữ dữ liệu (bám form ví FR-014); back trước lưu → không ghi (FR-010/SC-007); cuộn được + không vỡ cỡ chữ lớn/safe area (FR-012/SC-008).
**Tiêu chí kiểm thử độc lập**: `flutter test test/category_form_test.dart test/category_presets_test.dart test/category_form_screen_test.dart test/category_list_screen_test.dart` pass — seam bơm `FakeWalletRepository` (store biến đổi): form Thêm/Sửa render đúng mockup `02` (prefill 100% ở Sửa, khóa ô loại/cha đúng điều kiện), validator trống/trùng chặn, lưu ghi store đúng nhóm cuối + pop, chạm lưu nhanh 1 lần, back không ghi; màn danh sách nối 2 điểm vào (FAB → Thêm `initialType=tab`, dòng không con → Sửa, dòng có con no-op) + refresh sau pop; không sqlite native (R11).

- [X] T009 [US1] Tạo `app/sora_thu_chi/lib/screens/category_form_screen.dart`: `StatefulWidget` (R5/6/7) — seam `final WalletRepository? repository` (mặc định `ensureWalletRepository()` trong `initState`, pattern `CategoryListScreen` PBI 13) + params `{CategoryType? initialType, Category? category}` (null = chế độ Thêm; khác null = chế độ Sửa); `initState` nạp **1 lần** cache cả 2 loại `Future.wait([categoriesIncludingHidden(expense), categoriesIncludingHidden(income)])` + (chỉ Sửa) `categoryHasTransactions(category.id)`; dựng `SubPageScaffold(title: category == null ? 'Thêm danh mục' : 'Sửa danh mục', actions: [TextButton 'Lưu' trắng gọi `_save`], bottomNavigationBar: nút chính teal cao 44 bo 8 'Lưu danh mục' gọi `_save`)` — không FAB/không bottom nav; body `SafeArea` + `Form` + `ListView` cuộn (SC-008): **(1) pill loại** bo 20 hai nửa "Chi tiêu"/"Thu nhập" — chọn nền teal chữ trắng, chưa chọn `softCardBg` chữ xám (mockup `F1EFE8`/teal/`9B9B9B`); Thêm: đổi được, mặc định `initialType ?? expense` (acceptance 1/2); Sửa: `category` đổi loại khi `canChangeType(hasTransactions, hasChildren, isParent)` → đổi được (acceptance 7), không → **readonly box** dạng `_ReadonlyValue` form ví khóa rõ (acceptance 6/8, FR-002); **(2)** `TextFormField` tên (label "TÊN", `maxLength: 30`, `validator` gọi `categoryNameError` với `siblings` = danh mục cùng loại đang chọn + cha đang chọn — gồm bản ẩn — và `excludeId: category?.id`, lỗi hiện dưới ô FR-010); **(3) lưới biểu tượng** `Wrap` các ô tròn 40 (bám `_IconPicker` form ví thay emoji bằng `Icon(categoryIcon(k))`, R6): nguồn `categoryIconChoices` (T001), chọn sẵn = `category?.icon ?? defaultIconFor(type)`; chọn → viền teal 2 + nền `tealLightBg`, chưa `softCardBg`; **(4) lưới màu** `Wrap` ô tròn ~34 từ `categoryPresetColors`: chọn sẵn = `category?.color ?? defaultColorFor(type)`, chọn → viền đậm `#1A1A1A` + dấu kiểm trắng; **(5) ô "Danh mục cha (tùy chọn)"** — hiển thị "Không có — là danh mục gốc" khi null / tên cha khi có; bấm (khi được phép — danh mục **có con** không bấm: no-op có ripple, FR-008/acceptance 11) mở **bottom sheet** `parentsOfType(cache[type đang chọn])` loại trừ chính nó khi Sửa, mỗi dòng bubble `categoryIcon` + tên, đầu sheet mục "Không có — là danh mục gốc" (bỏ cha); chọn → set + đóng; **đổi loại → đưa cha về null** (acceptance 5, FR-006); **(6)** `SwitchListTile` "Ẩn khỏi danh sách nhanh" nền `softCardBg` bo 10 (pattern `_DefaultSwitch` form ví) — Thêm tắt sẵn / Sửa theo `category.isHidden` (edge đang ẩn → bật sẵn, FR-007); `_save()`: validate form (trống/trùng chặn — FR-010) + đánh giá khóa theo state snapshot lúc lưu (R8/FR-011) → tính `sortOrder`: cùng nhóm (type+parentId không đổi) giữ cũ / đổi nhóm (đổi cha hợp lệ hoặc đổi loại khi `canChangeType`) = `endOfGroupSortOrder(nhóm đích)` (R4/data-model §Luật lưu) → `isSystem` (Thêm false, Sửa giữ cũ) → `category == null ? repository.insertCategory(...) : repository.updateCategory(...)` dưới cờ `_saving` (chặn lưu trùng FR-011/SC-007) → `Navigator.pop(saved)`; lỗi bất ngờ → SnackBar coral "Không lưu được danh mục…", giữ dữ liệu; quay lại (back) bất kỳ lúc trước lưu → rời màn không ghi (FR-010); phụ thuộc T001 + T002 + T003 + T004 (seam) + `SubPageScaffold`/`category_list.dart`/`categoryIcon` đã có
- [X] T010 [US1] Sửa `app/sora_thu_chi/lib/screens/category_list_screen.dart` (R1): thay 2 điểm vào no-op ở PBI 13 — **(1) FAB "+"** `onPressed`: `Navigator.push` `CategoryFormScreen(initialType: _tab, repository: repo)` (acceptance 1/2 — loại mặc định theo tab đang mở); **(2) dòng cấp 1** `InkWell.onTap`: danh mục **không con** (`childrenOf(...).isEmpty`) → `Navigator.push` `CategoryFormScreen(category: cat, repository: repo)` chế độ Sửa (acceptance 6); danh mục **có con** → giữ no-op (màn con `03` PBI sau — chốt PBI 13/spec, không push); **(3)** sau `await Navigator.push(...)` trả về (bất kể saved/back) → **refresh lặng cả 2 loại** (`categoriesIncludingHidden` expense + income, không bật spinner — R1, FR-009/SC-003/SC-006 phản ánh ngay dữ liệu mới); tham chiếu cùng `repository` đã có ở initState (bơm seam test); giữ nguyên tab/empty/dòng phụ/ẩn/bố cục khác màn `01` (không hồi quy PBI 13); phụ thuộc T009 (class `CategoryFormScreen`)
- [X] T011 [P] [US1] Tạo `app/sora_thu_chi/test/category_form_screen_test.dart`: widget test **bơm `FakeWalletRepository`** (store biến đổi T006 — assert ghi qua store, không sqlite): **ADD** (a) mở `initialType` → pill loại chọn đúng (expense/income), app bar title + "Lưu" phải, không bottom nav, nút chính "Lưu danh mục" (acceptance 1/2, FR-001); (b) tên trống → lỗi "không được để trống"; trùng "Ăn uống" (cùng chi-gốc) → lỗi chặn lưu, không ghi (acceptance 4/12); (c) icon/màu chọn sẵn mặc định + đổi được (FR-005); (d) chọn cha chi → chuyển pill sang Thu nhập → ô cha về "Không có", sheet chỉ liệt kê cha thu (acceptance 5, FR-006); (e) lưu hợp lệ → `insertCategory` vào store cuối nhóm (verify `sortOrder` = `max+1`, `isHidden` theo switch) + `Navigator.pop(saved=true)`; chạm "Lưu" 2 lần nhanh → chỉ ghi 1 lần (FR-011); back → không insert (SC-007). **EDIT** (f) prefill 100% danh mục được sửa — tên/icon/màu/cha/ẩn bật (acceptance 6/9, SC-002); (g) danh mục gốc **sạch** (`transactions: []`, không con) → đổi loại chi→thu lưu → về cuối nhóm thu, biến mất khỏi chi (acceptance 7); (h) `categoryHasTransactions` true (seed 1 gd `categoryId`) → ô loại **readonly khóa** (không đổi được), tên/icon/màu vẫn sửa (acceptance 8, SC-005); (i) danh mục **có con** → loại khóa + bấm ô cha không mở sheet (FR-008, acceptance 11); (j) danh mục con pump trực tiếp (entry `03` sau) → loại khóa (con cùng loại cha), đổi cha/bỏ cha lưu đúng nhóm đích (acceptance 10); (k) giữ nguyên tên → lưu không báo trùng (edge); (l) bật ẩn → lưu → store `isHidden=true` (acceptance 9); (m) back trước lưu → `updateCategory` không xảy ra (SC-007); phụ thuộc T009 + T006
- [X] T012 [P] [US1] Sửa `app/sora_thu_chi/test/category_list_screen_test.dart` (R1): thay case cũ **"(f) no-op FAB/dòng"** (PBI 13, plan §Rủi ro chủ đích đổi) bằng — (f1) chạm FAB (tab Chi tiêu) → push `CategoryFormScreen` với `initialType == expense`; chạm FAB (tab Thu nhập) → `initialType == income` (acceptance 1/2, FR-001); (f2) chạm dòng **không con** ("Di chuyển") → push `CategoryFormScreen(category == dòng đó)` chế độ Sửa (acceptance 6); (f3) chạm dòng **có con** ("Ăn uống") → giữ no-op không push (chốt PBI 13); (f4) **refresh sau pop**: fake insert 1 danh mục qua form đã đẩy rồi bấm back → list màn `01` hiện dòng mới / đổi tên phản ánh ngay (FR-009/SC-003/006); giữ nguyên các case cũ khác (tab/đếm con/ẩn/empty/icon sắp xếp no-op… — không hồi quy PBI 13); phụ thuộc T010 (nối điểm vào)

## Pha cuối: Polish & Cross-cutting

- [X] T013 Chạy `flutter analyze` (sạch 0 warning) + `flutter test` toàn bộ tại `app/sora_thu_chi/` (test PBI 2–13 cũ + PBI 14 mới pass — **đặc biệt** chạy lại test dùng chung `FakeWalletRepository` sau khi đổi store biến đổi: PBI 11 `category_picker_screen_test`, PBI 12 `search_filter_screen_test`, PBI 13 `category_list_screen_test` case no-op đã sửa — khẳng định additive `categoriesStored`/constructor không hồi quy); xác nhận **không** cần `build_runner`/`.g.dart` mới (schema v4 giữ nguyên — plan §Rủi ro); đối chiếu danh sách "Không đổi" plan.md — `core/category/category.dart`, `category_source.dart`, `category_icon.dart`, `category_list.dart`, `data/db/app_database*.dart`, `sub_page_scaffold.dart`, `settings_screen.dart`, `wallet_*` screens/controller, màn PIN/boot, `category_picker_screen.dart`, `add_transaction_screen.dart`, `transaction_*`, `app_shell.dart`, `wallet_deps.dart`/`transaction_deps.dart` không thay đổi; ghi nhận quyết định (R2 presets icon 15/màu 13, R4 sortOrder thuần, R6 readonly box khóa loại, R7 khóa cha khi có con, R8 snapshot lưu) vào trạng thái PBI
- [X] T014 Kiểm chứng thủ công emulator theo `.specify/specs/14/quickstart.md` nhóm A–I: Cài đặt → Danh mục (màn `01`) → FAB "+" → màn "Thêm danh mục" — app bar teal + back + "Lưu" phải, pill loại theo tab, tên rỗng, icon/màu chọn sẵn, cha = "Không có — là danh mục gốc", công tắc ẩn tắt, nút chính "Lưu danh mục", không bottom nav (A); thêm "Ăn vặt" chi / thu → về cuối nhóm tương ứng không refresh tay, vào lại giữ (B); trùng "Ăn uống" bị chặn + trùng bản ẩn cũng chặn (C); đổi pill lọc danh sách cha đúng loại + về "Không có" (D); sửa danh mục gốc không con — prefill 100%, đổi tên/icon/màu/ẩn lưu phản ánh ngay, back không ghi, "Lưu" app bar cũng lưu (E); khóa đổi loại khi đã gắn giao dịch ("Mua sắm") + gốc sạch đổi chi→thu về cuối nhóm thu (F); tên trống/30 ký tự/giữ nguyên tên (G); cỡ chữ lớn + safe area không vỡ, nút "Lưu danh mục" không bị che (H); ghi rõ mục QA widget (sửa con/có con qua pump — điểm vào `03` chưa mở tay, quickstart §QA widget) vào trạng thái PBI; ghi kết quả từng mục
- [X] T015 Cập nhật wiki tri thức qua skill `sora-wiki` + append `wiki-knowledge/log.md`: entity **Danh mục** (plan §Kiểm tra ⚠️) — màn `02` thêm/sửa đã triển khai (Thêm: loại mặc định theo tab FAB; Sửa: prefill 100% + luật khóa — đổi loại chỉ khi gốc sạch không con chưa gd `canChangeType`, có con không đổi cha, con cùng loại cha, cây 2 cấp; xếp cuối nhóm khi thêm/đổi nhóm); 3 seam ghi/đếm `insertCategory`/`updateCategory`/`categoryHasTransactions` (schema v4 giữ nguyên, không build_runner); presets icon 15 khóa + bảng màu 13 ARGB; màn `01` PBI 13 nối điểm vào FAB/dòng không con + refresh lặng; ngoài phạm vi: xóa/gộp (PBI sau), entry sửa con/có con ở màn `03`

## Sơ đồ phụ thuộc

```text
Foundational:
  T001 category_presets.dart (thuần: iconChoices + default + presetColors 13)
    └──▶ T007 [P] category_presets_test.dart          (cần T003 token nếu presets tham chiếu AppColors.category*)
  T002 [P] category_form.dart (thuần: nameError/endOfGroupSortOrder/canChangeType/parentsOfType)
    └──▶ T008 [P] category_form_test.dart
  T003 [P] app_colors.dart (+ 8 token category*)      [độc lập, chạy trước/sau T001]
  T004 wallet_repository.dart (interface + categoryHasTransactions/insertCategory/updateCategory)
    ├──▶ T005 [P] wallet_repository_drift.dart (impl, không build_runner)
    └──▶ T006 [P] fake_wallet_repository.dart (store biến đổi + 3 method + categoriesStored)
US1:
  T001–T006 ──▶ T009 category_form_screen.dart (form Thêm/Sửa)
    ├──▶ T011 [P] [US1] category_form_screen_test.dart
  T009 ──▶ T010 category_list_screen.dart (FAB→Thêm initialType=tab; dòng không con→Sửa; có con no-op; refresh sau pop)
    └──▶ T012 [P] [US1] category_list_screen_test.dart (thay case no-op cũ)
Polish:
  T013 analyze + full test (không hồi quy PBI 2–13) ──▶ T014 QA emulator (quickstart A–I) ──▶ T015 wiki sync
```

Thứ tự thực thi chính: `T001 → T002 → T004 → T009 → T010 → T013 → T014 → T015` (test `[P]` chạy sau code tương ứng: T007 sau T001, T008 sau T002, T005/T006 sau T004; T003 token độc lập chạy bất kỳ trước T009; T011 sau T009; T012 sau T010). Mấu chốt: module thuần **T001 presets + T002 form** + seam **T004–T006** + token **T003** xong trước khi dựng màn **T009**; **T010** nối điểm vào màn `01` là bước cuối US1 (cần form đã tồn tại); **không có** `build_runner` (schema giữ v4) nên thứ tự thoải mái như PBI 12/13. T002 phụ thuộc `category_list.dart` (PBI 13 đã có) — không tách lại `topLevelParents`.

## Ví dụ chạy song song

```text
# Foundational — 3 module/token độc lập + interface (móc chuỗi repo):
T001 category_presets.dart        T002 [P] category_form.dart
T003 [P] app_colors.dart (token)  T004 wallet_repository.dart (interface)

# Sau T004:
T005 [P] wallet_repository_drift.dart (impl)     T006 [P] fake_wallet_repository.dart (store biến đổi)

# Test thuần chạy sau code tương ứng (khác file, không chặn nhau):
T007 [P] category_presets_test.dart (sau T001)   T008 [P] category_form_test.dart (sau T002)

# US1 — 2 test màn song song (sau file màn tương ứng):
T011 [P] [US1] category_form_screen_test.dart (sau T009)
T012 [P] [US1] category_list_screen_test.dart   (sau T010)
```

## Chiến lược triển khai

- **MVP đề xuất**: User Story 1 (toàn bộ PBI 14 — đặc tả là một lát cắt "màn thêm/sửa danh mục" duy nhất, không chia user story P1/P2/P3). Foundational dựng presets icon/màu (R2) + module thuần validation & sort (R5) + 8 token màu + 3 seam ghi/đếm trên `WalletRepository` (R3/R4) làm tiền đề bắt buộc; US1 ghép `CategoryFormScreen` (màn `02`) + nối 2 điểm vào no-op của màn `01` (FAB → Thêm, dòng không con → Sửa) + refresh lặng để ra giá trị end-to-end (FR-001/FR-009).
- **Thứ tự giao hàng tăng dần**: Foundational (presets + module `category_form` thuần + token — unit test chứng minh bất biến seed ⊆ choices/palette, chặn trùng nhóm gồm ẩn, sort cuối nhóm, `canChangeType` 4 góc; seam interface → drift + fake — mọi logic test trên `FakeWalletRepository`/hàm thuần, không sqlite native) → US1 màn `02` (form screen + widget test ADD/EDIT phủ cả entry chưa mở tay) → nối màn `01` (FAB/dòng + refresh + widget test) → Polish (analyze/test toàn cục không hồi quy + QA emulator quickstart A–I + sync wiki).






