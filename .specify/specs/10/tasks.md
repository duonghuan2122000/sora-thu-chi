# Danh sách Task: Màn hình chi tiết giao dịch

**Mã PBI**: 10
**Nguồn**: plan.md, spec.md, data-model.md, research.md, quickstart.md

## Pha 1: Setup

*(Không có task — PBI 10 **không thêm dependency/pubspec mới**: drift `^2.34.4`, drift_dev & build_runner đã khai báo từ PBI 7. Đây là bản **bump schema drift v2 → v3** (thêm 3 cột tùy chọn) — mọi thay đổi schema/seed/migration đặt ở Foundational, `build_runner` tái sinh `app_database.g.dart` chạy ngay trong task T006 (bắt buộc trước khi `flutter run`/DAO test, như PBI 8/9).)*

## Pha 2: Foundational

*(Hạ tầng bắt buộc xong trước user story: domain `Transaction` + 3 field tùy chọn; schema v3 `tags`/`receipt_image`/`location` + seed làm giàu row 1; hàm thuần `transaction_detail` (builder view + parseTags) + `formatDateTimeDetailLabel`; `TxnRow` mang ref mở detail; `SubPageScaffold` hỗ trợ `actions`; map 3 cột mới ở Drift — theo plan.md "Cấu trúc dự án dự kiến" và research R1–R10.)*

- [X] T001 [P] Sửa `app/sora_thu_chi/lib/core/transaction/transaction.dart`: thêm 3 field **`tags`/`receiptImage`/`location` (String, constructor mặc định `''`)** vào `Transaction` (data-model §Thay đổi trên domain, R3) — map 1-1 từ cột drift, phục vụ vùng chi tiết màn (spec FR-007); **additive** — không phá constructor/sort/`transactionsForWallet`/các màn đang dùng (test PBI 6/8/9 compile nguyên)
- [X] T002 [P] Sửa `app/sora_thu_chi/lib/core/date_label.dart`: thêm hàm thuần `String formatDateTimeDetailLabel(DateTime)` trả `'dd/MM/yyyy · HH:mm'` (mỗi số 2 chữ số, dấu `·`, tái dùng `_two`; research R8) — dòng "Ngày giờ" vùng chi tiết (spec FR-005, mockup 04 `03/09/2026 · 12:15`); **giữ nguyên** `formatDateTimeLabel` (PBI 8 đang dùng, không đổi hành vi)
- [X] T003 [P] Sửa `app/sora_thu_chi/lib/core/widgets/sub_page_scaffold.dart`: thêm tham số tùy chọn **`actions` (List<Widget>?, default null)** truyền vào `AppBar.actions` (giữ layout hiện tại — màn wallet_* không truyền → không vỡ; research R6) — đặt icon 3 chấm bên phải app bar màn chi tiết (spec FR-001/012)
- [X] T004 [P] Sửa `app/sora_thu_chi/lib/core/transaction/transaction_list.dart`: thêm 2 field vào `TxnRow` (**`int? detailTransactionId`**, **`int? detailGroupId`** — constructor default `null`, additive — research R2): trong `buildDisplayRows` gán **đúng 1 cái khác null** — dòng transfer đã gộp cặp → `detailGroupId: source.transferGroupId`; dòng còn lại (thu/chi/adjustment/vế lẻ fallback qua `_plainRow`) → `detailTransactionId: t.id` — ref để màn danh sách mở đúng chi tiết (spec FR-010, R2); dòng `TxnRow` dựng tay ở test cũ không truyền → null không phá
- [X] T005 Sửa `app/sora_thu_chi/lib/core/transaction/transaction_source.dart`: làm giàu **row 1** (id 1 — chi `85.000` Tiền mặt, note 'Ăn trưa văn phòng') — `tags: 'côngty'`, `location: '123 Láng Hạ, Đống Đa, Hà Nội'`, `receiptImage: ''` (giữ trống — R3) — vì `txn()` helper hiện không nhận 3 field, thêm tham số optional (default `''`) hoặc gán trực tiếp cho dòng đó; **giữ nguyên** số dòng (11), `category`/`note`/`amount`/ngày của mọi dòng để không hồi quy test PBI 6/8/9 (data-model §Seed); đảm bảo seed drift (T006) đọc được giá trị này
- [X] T006 Sửa `app/sora_thu_chi/lib/data/db/app_database.dart`: **bump `schemaVersion` 2 → 3**; thêm vào bảng `Transactions` 3 cột text có default `''` — `tags`, `receiptImage`, `location` (`text().withDefault(const Constant(''))()`, data-model §Thay đổi trên drift); sửa `_seedSampleTransactions` (cả 2 vòng insert: vế nguồn & vế đích) ghi kèm `tags: Value(t.tags)`, `location: Value(t.location)`, `receiptImage: Value(t.receiptImage)` từ `TransactionSource` để row 1 xuống DB có tag/vị trí; sửa `MigrationStrategy.onUpgrade` — giữ nhánh `if (from < 2)` tạo bảng `transactions` (bảng tạo theo định nghĩa mới **đã gồm 3 cột** → DB v1→v3 không cần addColumn tránh duplicate-column), thêm nhánh `else if (from < 3)` `await m.addColumn` 3 cột cho DB v2 cũ (an toàn, không mất dữ liệu — R3); chạy `dart run build_runner build --delete-conflicting-outputs` tái sinh `app/sora_thu_chi/lib/data/db/app_database.g.dart` (commit)
- [X] T007 Sửa `app/sora_thu_chi/lib/data/wallet_repository_drift.dart`: sửa `_toTransaction` map thêm **`tags: r.tags`, `receiptImage: r.receiptImage`, `location: r.location`** từ `TransactionsRow` lên `Transaction` (data-model §Thay đổi trên domain, R3) — nếu bỏ sót, màn đọc DB thật sẽ luôn thấy 3 trường rỗng dù DB có dữ liệu
- [X] T008 Tạo `app/sora_thu_chi/lib/core/transaction/transaction_detail.dart`: module **thuần** (không đọc DB/state, research R1/R4/R5/R7/R9) gồm — (1) `class TransactionDetailRef { final int? transactionId; final int? transferGroupId; }` (đúng 1 khác null, R2); (2) `class TransactionDetailView` (bất biến, data-model §View hiển thị): `type`, `summaryTitle` (nhãn tóm tắt), `summaryGlyph` (IconData), `amount` (int, đã abs với transfer/adjustment), `singleWalletName?`, `sourceWalletName?`, `destWalletName?`, `date`, `note`, `List<String> tags`, `receiptImage`, `location`; (3) `TransactionDetailView? buildTransactionDetail({required List<Transaction> all, required Map<int, String> walletName, required TransactionDetailRef ref})` — `ref.transactionId` → tìm theo `id`, dựng view đơn; `ref.transferGroupId` → lọc các vế `type=transfer` chung nhóm, vế `amount<0` = nguồn / `amount>0` = đích, `amount` = `abs(vế nguồn)`, đủ 2 vế → 1 view với `sourceWalletName`/`destWalletName` (FR-006/SC-005); nhóm thiếu vế → **fallback view đơn không crash**; `ref` không khớp dòng nào → **null** (màn báo trạng thái); tóm tắt thu/chi: `summaryTitle` = `category` (rỗng → `typeLabel`), glyph `categoryGlyph(category)` (tái dùng từ `transaction_list.dart`); transfer/adjustment: title = `typeLabel`, glyph `swap_horiz`/`tune`, amount abs; hàng ghi chú/tag/ảnh/vị trí chỉ điền khi field khác rỗng (FR-007 — màn ẩn hàng rỗng); (4) `List<String> parseTags(String tags)` — split `,`, trim, lọc rỗng (R4; rỗng → `[]`)
- [X] T009 [P] Sửa `app/sora_thu_chi/test/date_label_test.dart`: thêm case `formatDateTimeDetailLabel` — đúng `'dd/MM/yyyy · HH:mm'` bổ sung số 0 (vd `05/09/2026 · 09:05`), deterministic `DateTime` cụ thể (R8); giữ nguyên case `formatDateTimeLabel` (PBI 8)
- [X] T010 [P] Sửa `app/sora_thu_chi/test/transaction_list_test.dart`: thêm assert ref trên `buildDisplayRows` — dòng transfer đã gộp có `detailGroupId` đúng `transfer_group_id` & `detailTransactionId == null`; dòng thu/chi/adjustment/vế lẻ có `detailTransactionId == t.id` & `detailGroupId == null` (đúng 1 khác null — R2); không đổi các case cũ (gộp/nhóm/thống kê/glyph)
- [X] T011 [P] Tạo `app/sora_thu_chi/test/transaction_detail_test.dart`: unit thuần bơm `Transaction`/`walletName` tĩnh — (a) dựng theo `ref.transactionId` → view đúng loại/tiền/ví (acceptance 1–2); (b) transfer 2 vế chung group → 1 view `sourceWalletName`/`destWalletName` đúng chiều, `amount` = abs không dấu, nhãn `'Chuyển khoản'` trung tính (acceptance 3, FR-004/006/SC-005); group thiếu 1 vế → fallback view đơn không crash (edge); (c) adjustment → trung tính nhãn `typeLabel` không teal/coral (edge, FR-004); (d) tóm tắt thu/chi nhãn `category`, **cha·con hiển thị đúng chuỗi ghép `'Ăn uống · Ăn ngoài'`** (acceptance 5, R5 — dữ liệu thật chưa có bảng categories); `category` rỗng → `typeLabel`; (e) ẩn hàng: field note/tags/receiptImage/location rỗng → view không chứa dữ liệu dòng đó (acceptance 4, FR-007); (f) `parseTags`: `'côngty,ăntrưa'` → 2 tag, có khoảng trắng → trim, `''`/`,,` → `[]` (R4); (g) ref không khớp dòng → null
- [X] T012 [P] Sửa `app/sora_thu_chi/test/transactions_dao_test.dart`: drift `NativeDatabase.memory()` (giữ skip-guard nếu host thiếu sqlite, như PBI 8/9) — (1) fresh schema v3: seed 11 dòng, **row 1 map lên domain mang `tags: 'côngty'` & `location: '123 Láng Hạ…'`**, các field mới của row khác = `''`; (2) migration v2→v3: tạo DB cũ schema v2 rồi nâng lên v3 → 3 cột có sẵn, dữ liệu cũ không mất; (3) `performTransfer` PBI 8 vẫn đúng + 2 vế mới mang 3 field `''` (data-model §Migration); không phá case migration v1→v2

## Pha 3: User Story 1 - Màn hình chi tiết giao dịch (Ưu tiên: P1) 🎯 MVP

**Mục tiêu**: Màn "Chi tiết giao dịch" hiển thị đúng bố cục `docs/transaction/04-chi-tiet-giao-dich.svg` (spec acceptance 1–9, FR-001…015) — sub-page app bar teal tiêu đề "Chi tiết giao dịch" + back + icon 3 chấm, khối tóm tắt (icon bubble + tên danh mục/nhãn + số tiền lớn theo loại: thu teal `+` / chi coral `−` / chuyển khoản & điều chỉnh trung tính không dấu), vùng chi tiết hàng có điều kiện ngăn kẻ mảnh (Ví hoặc Ví nguồn/đích, Ngày giờ `·`, Ghi chú, Tag chip, Ảnh thumbnail, Vị trí), nút "Nhân bản"/"Sửa" + 3 chấm là điểm vào no-op không lỗi (màn đích PBI sau — FR-012); mở từ dòng danh sách (PBI 9, FR-013/SC-008 giữ vị trí cuộn), dữ liệu nạp lại mỗi lần mở (FR-010), ẩn hàng rỗng (FR-007), cuộn được không vỡ với cỡ chữ lớn/safe area (FR-014).
**Tiêu chí kiểm thử độc lập**: `flutter test test/transaction_detail_screen_test.dart test/transaction_screen_test.dart` pass — seam bơm view render đúng khối tóm tắt (màu/dấu theo loại) + từng hàng chi tiết có-điều-kiện, 3 điểm vào chạm không crash, cỡ chữ lớn + safe area cuộn không `RenderFlex overflow`; dòng danh sách chạm → mở đúng màn chi tiết tương ứng (fake repo, không sqlite native).

- [X] T013 [US1] Tạo `app/sora_thu_chi/lib/screens/transaction_detail_screen.dart`: `StatefulWidget` bám pattern `WalletDetailScreen` (research R10) — nhận `TransactionDetailRef ref` (từ T004/T008); **seam** `final Future<TransactionDetailView?> Function()? loader` (widget test bơm trực tiếp); loader mặc định đọc `ensureWalletRepository()` (singleton `wallet_deps.dart`) → `allTransactions()` + `loadAll()` (tên ví kể cả ví ẩn — FR-011/edge) → `buildTransactionDetail(...)`; dựng trên `SubPageScaffold(title: 'Chi tiết giao dịch', actions: [icon 3 chấm no-op InkWell — FR-012], bottomNavigationBar: ...)`; body theo trạng thái: `isLoading` lần đầu → spinner; lỗi đọc → thông báo + nút "Thử lại" gọi lại loader; view `null` (ref lỗi thời) → thông báo gọn "Không tìm thấy giao dịch"; có view → cuộn được nội dung: **(1) khối tóm tắt** đầu màn — bubble tròn: thu/chi nền `tealLightBg` + glyph `summaryGlyph` teal, transfer nền `softCardBg` glyph `swap_horiz` / adjustment glyph `tune` màu `listLabel` (R5); nhãn `summaryTitle` (font 15 w600 `textPrimary`); số tiền **lớn** (font 24 w700) — thu `formatSignedMoney(amount)` màu `teal` (dấu `+`), chi màu `coral` (dấu `−`), transfer/adjustment `formatMoney(amount.abs())` **không dấu** màu `textPrimary`, đặt trong `FittedBox` scale-down không tràn với số rất lớn (FR-009); **(2) vùng chi tiết** — `ListView`/`Column` hàng: mỗi hàng icon dẫn đầu nhỏ màu `teal` + cột (nhãn 13 `listLabel` / giá trị 15 `textPrimary`), ngăn cách `Border` dưới màu `listDivider` (FR-008); hàng theo thứ tự mockup, hiển thị **có điều kiện** (FR-007/edge): Ví — thu/chi/adjustment 1 hàng `walletName[walletId]`, transfer **2 hàng "Ví nguồn"/"Ví đích"** (FR-006); Ngày giờ — `formatDateTimeDetailLabel`; Ghi chú — gói dòng không cắt; Tag — dãy chip pill nền `softCardBg` chữ `teal` tiền tố `#` đủ mọi tag (R4); Ảnh hóa đơn — `Image.file(File(receiptImage))` 56×56 bo 8 + `errorBuilder` → hộp placeholder icon không crash (R3/R7); Vị trí — text gói dòng; **(3) chân màn** qua `bottomNavigationBar`: `SafeArea(Row)` 2 nút cạnh nhau — "Nhân bản" `OutlinedButton` (nút phụ) & "Sửa" `FilledButton` teal bo 8 cao 44 (nút chính), cả 2 `onPressed: () {}` **no-op không lỗi/treo/pop** (FR-012); dùng token `AppColors` (không hex cứng); back/gesture tự về danh sách giữ vị trí cuộn (route đè lên shell, `PageStorageKey` PBI 9 — SC-008)
- [X] T014 [P] [US1] Tạo `app/sora_thu_chi/test/transaction_detail_screen_test.dart`: widget test **bơm loader seam trả sẵn `TransactionDetailView`** (không sqlite) — (a) thu → summary bubble + tên + `+…` màu teal; (b) chi → `−` coral (acceptance 1–2, SC-003); (c) transfer → nhãn 'Chuyển khoản' glyph trung tính, số không dấu, **2 hàng "Ví nguồn"/"Ví đích"** đúng chiều (acceptance 3, SC-005); adjustment → trung tính (FR-004); (d) view không có note/tags/receiptImage/location → chỉ hàng Ví + Ngày giờ + tóm tắt, **không dòng trống/phân cách thừa** (acceptance 4, FR-007); (e) có đủ dữ liệu → hàng Ghi chú gói dòng, chip `#côngty`, thumbnail ảnh (path giả `Image.file` + `errorBuilder` placeholder không crash), Vị trí hiện (acceptance 1); (f) tiêu đề app bar 'Chi tiết giao dịch' + back + icon 3 chấm hiện; chạm 3 chấm / "Nhân bản" / "Sửa" → **không crash không rời màn** (FR-012); (g) loader trả null → thông báo không crash; loader ném lỗi → thông báo + nút "Thử lại" gọi lại loader thành công → hiện view; (h) `textScaleFactor` cao + safe area inset → cuộn tới hàng cuối + nút dưới không `RenderFlex overflow` (acceptance 9, FR-014); (i) số tiền rất lớn không tràn (FR-009); (j) ngày tương lai hiển thị bình thường (edge)
- [X] T015 [US1] Sửa `app/sora_thu_chi/lib/screens/transaction_screen.dart`: đổi `_TransactionRow.onTap` (hiện `onTap: () {}` no-op, dòng ~280) → đẩy mở màn chi tiết (FR-001, R2): tạo `TransactionDetailRef` từ `row` — `detailGroupId` khác null (dòng transfer đã gộp) → `TransactionDetailRef(transferGroupId: row.detailGroupId)`, ngược lại → `TransactionDetailRef(transactionId: row.detailTransactionId)`; `Navigator.push(MaterialPageRoute(builder: (_) => TransactionDetailScreen(ref: ...)))` — màn là sub-page đè lên shell (AppBar riêng, không bottom nav), quay lại giữ nguyên vị trí cuộn danh sách nhờ `PageStorageKey` + route dưới (SC-008); bỏ comment "no-op PBI sau"; không đổi FAB/lọc/header/card
- [X] T016 [P] [US1] Sửa `app/sora_thu_chi/test/transaction_screen_test.dart`: đổi case "chạm một dòng → không crash (no-op)" thành — chạm dòng thu/chi → `TransactionDetailScreen` mở ra (pumpAndSettle) hiển thị đúng dữ liệu dòng đó (bơm `FakeWalletRepository` Get.put trước pump, detail dùng chung fake — không sqlite; acceptance 6/FR-010); chạm dòng **transfer đã gộp** → màn chi tiết chuyển khoản hiện 2 hàng ví nguồn/đích (FR-006/SC-005); **back** → trở về đúng danh sách vị trí đã cuộn; giữ nguyên các case còn lại (header/card/nhóm/màu/empty/ví ẩn/cỡ chữ — không hồi quy PBI 9)

## Pha cuối: Polish & Cross-cutting

- [X] T017 Chạy `flutter analyze` (sạch 0 warning) + `flutter test` toàn bộ tại `app/sora_thu_chi/` (test PBI 2–9 + PBI 10 mới pass, kể cả DAO có skip-guard; **đặc biệt** test PBI 6/8/9 chạy lại do domain `Transaction` + 3 field, seed row 1 thêm tags/location, `TxnRow` + ref, dòng danh sách đổi no-op → mở detail — khẳng định additive không hồi quy); xác nhận `app_database.g.dart` mới (schema v3) đã sinh & sẽ commit; đối chiếu danh sách "Không đổi" plan.md — `main.dart`, `app.dart`, `boot_gate.dart`, `pin_*`, `app_shell.dart`, `wallet_*` screens, dashboard/report/settings/add_transaction screens, `wallet_deps.dart`/`transaction_controller.dart`/`wallet_repository.dart` (interface), bảng `wallets`/schema v1→v2 migration không thay đổi + ghi nhận seed 11 dòng mẫu / quyết định mở #1 (R5)
- [X] T018 Kiểm chứng thủ công emulator theo `.specify/specs/10/quickstart.md` nhóm A–G: mở chi tiết từ dòng chi row 1 → khớp mockup 04 (tóm tắt bubble + `−85.000 đ` coral, Ví/Ngày giờ `·`/Ghi chú/chip `#côngty`/Vị trí — A), thu `+…` teal phân biệt chi coral (A, SC-003), chuyển khoản 1 màn 2 hàng Ví nguồn/đích `700.000 đ` không dấu (B), dòng không ghi chú/tag → chỉ Ví + Ngày giờ không hàng trống (C), chạm Sửa/Nhân bản/3 chấm không lỗi không rời màn (D), back giữ vị trí cuộn (E), cỡ chữ lớn + safe area không vỡ/nút không cắt + ví ẩn hiện tên + khóa app không lộ số tiền (F), không hồi quy các tab/FAB/ví/chuyển tiền/PIN + `flutter test` xanh (G); ghi kết quả vào trạng thái PBI

## Sơ đồ phụ thuộc

```text
Foundational:
  T001 transaction.dart (+3 field) ──▶ T005 transaction_source (enrich row 1) ──▶ T006 app_database schema v3 + seed ──▶ T007 _toTransaction map
                                                                            └──────────────▶ T012 [P] transactions_dao_test
  T001 ──▶ T008 transaction_detail.dart (builder/parseTags/view) ──▶ T011 [P] transaction_detail_test
  T001 ──▶ T004 [P] transaction_list (TxnRow + ref) ──▶ T010 [P] transaction_list_test
  T002 [P] date_label.formatDateTimeDetailLabel ──▶ T009 [P] date_label_test
  T003 [P] sub_page_scaffold (+actions) ──▶ (dùng ở T013)
US1:
  T008 + T003 ──▶ T013 transaction_detail_screen (seam loader) ──▶ T014 [P] detail_screen_test
  T013 + T004 ──▶ T015 transaction_screen (onTap → push) ──▶ T016 [P] screen_test (thay no-op PBI 9)
Polish:
  T017 analyze + full test (không hồi quy PBI 2–9) ──▶ T018 QA emulator (quickstart A–G)
```

Thứ tự thực thi chính: `T001 → T005 → T006 → T007 → T012 → T008 → T011 → T013 → T014 → T015 → T016 → T017 → T018` (T002/T003/T004 khởi động song song từ đầu; test `[P]` T009/T010 chạy khi file nền tương ứng xong).

## Ví dụ chạy song song

```text
# Foundational — khởi động đồng thời từ đầu (khác file, độc lập):
T001 [P] transaction.dart      T002 [P] date_label.dart      T003 [P] sub_page_scaffold.dart
T004 [P] transaction_list.dart (TxnRow ref — không phụ thuộc 3 field mới)

# Foundational — tầng test sau khi file tương ứng xong:
T009 [P] date_label_test (sau T002)   T010 [P] transaction_list_test (sau T004)
T011 [P] transaction_detail_test (sau T008)   T012 [P] transactions_dao_test (sau T006/T007)

# US1 — test màn chi tiết chạy song song việc nối màn danh sách (sau T013):
T014 [P] [US1] transaction_detail_screen_test.dart
T015     [US1] transaction_screen.dart wiring (onTap → push)
```

## Chiến lược triển khai

- **MVP đề xuất**: User Story 1 (toàn bộ PBI 10 — đặc tả là một lát cắt "hiển thị màn chi tiết + kích hoạt điểm vào từ danh sách" duy nhất, không chia user story P1/P2/P3). Foundational dựng phần dữ liệu/trình bày (schema v3 + 3 field + seed enrich + hàm thuần `transaction_detail` + ref trên `TxnRow` + `actions` scaffold) làm tiền đề bắt buộc; US1 ghép `transaction_detail_screen` + kích hoạt `onTap` dòng danh sách lên để ra giá trị end-to-end.
- **Thứ tự giao hàng tăng dần**: Foundational (schema v3 + domain + pure functions + seam — mọi logic test được trên `FakeWalletRepository`/loader seam, không cần sqlite native) → US1 màn chi tiết (screen + seam loader + widget test) + nối màn danh sách (onTap → push + widget test) → Polish (analyze/test toàn cục không hồi quy + QA emulator quickstart A–G). Điểm mấu chốt: **chạy `build_runner` (T006) trước** khi `flutter run`/DAO test — schema v3 phải tái sinh `app_database.g.dart`.
