# Danh sách Task: Tiện ích & Cá nhân hóa — màn danh sách

**Mã PBI**: 17
**Nguồn**: plan.md, spec.md, data-model.md, research.md, quickstart.md

## Pha 1: Setup

*(Không có task — PBI 17 **không thêm dependency/pubspec mới** (research R1 — lưu công tắc bằng drift key-value, không `shared_preferences`/`flutter_secure_storage`), **không tạo `contracts/`** (app nội bộ offline — plan §Giai đoạn 1), **không** thư mục `core/utilities/` trước — dựng luôn ở Foundational.)*

## Pha 2: Foundational

*(Hạ tầng bắt buộc xong trước khi dựng màn `01`: domain `UtilitiesPrefs` + hằng khóa (data-model §Domain), abstract seam `UtilitiesStore`, nâng schema drift **v4 → v5** thêm bảng key-value `AppSettings` (data-model §Bảng drift mới) + chạy `build_runner` sinh lại `.g.dart`, impl drift + GetX singleton + fake (R1/R7, bám pattern PBI 7/13) — theo plan.md "Cấu trúc dự án dự kiến", research R1/R6/R7 và data-model §Domain.)*

- [X] T001 [P] Tạo `app/sora_thu_chi/lib/core/utilities/utilities.dart`: module domain thuần (không đọc DB) — class bất biến **`UtilitiesPrefs`** với 2 field `bool hideBalance` (mặc định `false` — Ẩn số dư mockup TẮT) và `bool amountCalculatorEnabled` (mặc định `true` — Máy tính mockup BẬT); hằng khóa `const kKeyHideBalance = 'hideBalance'` và `const kKeyAmountCalculatorEnabled = 'amountCalculatorEnabled'` (data-model §Khóa); `Map<String, String> toSettings()` trả 2 row `(kKeyHideBalance, 'true'/'false')`, `(kKeyAmountCalculatorEnabled, …)` (bool lưu chuỗi `'true'`/`'false'`); **`factory UtilitiesPrefs.fromSettings(Map<String, String> rows)`** — key **vắng mặt** → giữ mặc định, chuỗi **không parse được** (ngoài `'true'`/`'false'`) → mặc định (an toàn, **không ném** — data-model §Luật chuyển đổi); `copyWith({bool? hideBalance, bool? amountCalculatorEnabled})` cho toggle; cập nhật doc comment (key vắng = chưa từng đổi → mặc định domain, row chỉ ghi khi user bật/tắt)
- [X] T002 [P] Tạo `app/sora_thu_chi/lib/core/utilities/utilities_store.dart`: **seam đọc/ghi** trừu tượng **`UtilitiesStore`** (bám pattern `WalletRepository` — màn & seam chỉ phụ thuộc interface để test bơm fake, không cần sqlite native, research R7) với 2 method: `Future<UtilitiesPrefs> load()` (key vắng → mặc định qua `UtilitiesPrefs.fromSettings`) và `Future<void> save(UtilitiesPrefs prefs)` (ghi write-through trạng thái 2 công tắc); doc comment ngữ nghĩa (trạng thái màn Tiện ích; PBI sau màn khác đọc cùng store qua GetX singleton)
- [X] T003 [P] Sửa `app/sora_thu_chi/lib/data/db/app_database.dart`: thêm **bảng `AppSettings`** key-value (data-model §Bảng drift mới) — `class AppSettings extends Table` gồm `TextColumn get key => text()();` + override `Set<Column> get primaryKey => {key};` (khóa = PK để `insertOnConflictUpdate`) và `TextColumn get value => text()();`; đổi `schemaVersion` **4 → 5**; thêm `AppSettings` vào danh sách `@DriftDatabase(tables: [Wallets, Categories, Transactions, AppSettings])`; trong `onUpgrade` thêm nhánh **`if (from < 5) await m.createTable(appSettings);`** (thuần tạo bảng, **không seed** — R8; giữ nguyên mọi nhánh `onCreate`/`onUpgrade`/seed ví/danh mục/giao dịch hiện có, bám research R1/R8); cập nhật doc comment lớp (bảng key-value chung cho mọi cài đặt về sau — thêm row, không thêm migration); **phụ thuộc: không** (khác file)
- [X] T004 Sinh lại `app/sora_thu_chi/lib/data/db/app_database.g.dart` sau T003: chạy `dart run build_runner build --delete-conflicting-outputs` tại `app/sora_thu_chi/` — sinh `AppSettingsRow`/`AppSettingsCompanion` + member `appSettings` (schema v5, plan §Rủi ro "phải chạy build_runner sinh lại `.g.dart`"); **phụ thuộc: T003**
- [X] T005 Tạo `app/sora_thu_chi/lib/data/utilities_store_drift.dart`: class **`DriftUtilitiesStore implements UtilitiesStore`** (constructor `DriftUtilitiesStore(this._db)` với `final AppDatabase _db`; bám pattern `DriftWalletRepository`) — `load()`: `final rows = await _db.select(_db.appSettings).get();` → map `{r.key: r.value}` → trả `UtilitiesPrefs.fromSettings(map)` (key vắng → mặc định); `save(prefs)`: với từng cặp trong `prefs.toSettings()` gọi `_db.into(_db.appSettings).insertOnConflictUpdate(AppSettingsCompanion.insert(key: k, value: v))` — **chỉ upsert 2 key của prefs, không xoá row khác** (giữ key cài đặt PBI sau — data-model §Bảng drift mới); **phụ thuộc: T004 + T001 + T002**
- [X] T006 Tạo `app/sora_thu_chi/lib/data/utilities_deps.dart`: hàm **`ensureUtilitiesStore()`** (bám `ensureWalletRepository` — `wallet_deps.dart`): nếu `Get.isRegistered<UtilitiesStore>()` → `Get.find<UtilitiesStore>()` (test đăng ký fake trước → trả fake, không tạo drift, research R7); ngược lại tạo `DriftUtilitiesStore(AppDatabase())`, `Get.put<UtilitiesStore>(...)` và trả về (đúng 1 connection drift); **phụ thuộc: T005 + T002**
- [X] T007 [P] Tạo `app/sora_thu_chi/test/fakes/fake_utilities_store.dart`: fake bộ nhớ implements **`UtilitiesStore`** (bám `FakeWalletRepository`) — giữ `UtilitiesPrefs _prefs` (khởi tạo mặc định `UtilitiesPrefs()`); `load()` trả `_prefs`; `save(prefs)` gán `_prefs = prefs`; getter `UtilitiesPrefs get storedPrefs` (để widget test assert trạng thái store sau toggle); **phụ thuộc: T001 + T002** (song song với T003–T006 — khác file)

## Pha 3: User Story 1 - Màn Tiện ích & Cá nhân hóa (Ưu tiên: P1) 🎯 MVP

**Mục tiêu**: Màn con "Tiện ích & Cá nhân hóa" theo đúng bố cục `docs/tool/01-danh-sach-tien-ich.svg` (spec acceptance 1–8, FR-001…010) — mở từ nhóm **KHÁC** màn Cài đặt khi chạm hàng "Tiện ích & Cá nhân hóa" (ngay dưới "Danh mục"); `SubPageScaffold` app bar teal: back (trái), tiêu đề "Tiện ích & Cá nhân hóa", **không** bottom nav; thân màn cuộn được liệt kê **3 nhóm / 8 hàng** (HIỂN THỊ — Giao diện, Ngôn ngữ, Định dạng & Tiền tệ; TRẢI NGHIỆM — Widget màn hình chính, Ẩn số dư (Privacy mode), Máy tính khi nhập số tiền; DỮ LIỆU & TÌM KIẾM — Tìm kiếm toàn cục, Quản lý Tag), mỗi hàng vòng nền nhạt + icon teal trái + tên + dòng phụ + phần cuối; 5 hàng điều hướng no-op; hàng Widget = switch "bật" câm + chạm toàn hàng → dialog hướng dẫn ghim widget (R5); 2 công tắc thật (Ẩn số dư tắt / Máy tính bật) bật/tắt + **nhớ trạng thái** qua store (FR-006) nhưng **không** đổi hành vi màn khác (FR-007).
**Tiêu chí kiểm thử độc lập**: `flutter test test/utilities_test.dart test/utilities_store_drift_test.dart test/utilities_screen_test.dart test/settings_screen_test.dart` pass — bơm `FakeUtilitiesStore`: màn render đủ 3 nhóm/8 hàng đúng thứ tự + trailing mặc định ("Hệ thống"/"Tiếng Việt", switch Ẩn số dư tắt/Máy tính bật), toggle → store ghi + tái mở giữ trạng thái, hàng Widget chạm → dialog + switch không đảo, 5 hàng no-op không mở màn, cỡ chữ lớn không vỡ; từ Cài đặt chạm hàng mới → mở màn + back về; unit `UtilitiesPrefs` phủ mặc định/parse/to-from; drift in-memory phủ load-mặc-định + round-trip; không sqlite native ngoài skip-guard drift test.

- [X] T008 [US1] Tạo `app/sora_thu_chi/lib/screens/utilities_screen.dart`: `StatefulWidget` **`UtilitiesScreen`** (pattern `CategoryListScreen` PBI 13 — research R6: StatefulWidget, **không** GetX controller) — params `{UtilitiesStore? store}` (seam test; `initState`: `_store = widget.store ?? ensureUtilitiesStore()` rồi `_load()`); `_load()` đọc `_store.load()` 1 lần → `_prefs` (spinner / lỗi + "Thử lại" gọi `_load` lại — bám pattern màn `01`); `build` = `SubPageScaffold(title: 'Tiện ích & Cá nhân hóa', child: SafeArea(top: false, child: ...))` — back hệ thống pop, **không** bottom nav (FR-001); thân `ListView` cuộn (padding đáy đủ — FR-010) gồm 3 nhóm, mỗi nhóm tiêu đề viết hoa màu mờ (pattern `_SectionLabel` settings) rồi các hàng **đúng thứ tự bố cục plan §Giai đoạn 1**; mỗi hàng: dòng `Row` = vòng tròn nền nhạt (`AppColors` sẵn có — tham chiếu cách màn danh mục vẽ bubble nhạt, đối chiếu mockup) chứa icon **teal** đúng nghĩa mục + tên (`Expanded` chống tràn) + dòng phụ (nếu có) + phần cuối, kèm `Divider` như settings; nhóm HIỂN THỊ: (1) "Giao diện" dòng phụ "Sáng / Tối / Theo hệ thống" + trailing `Text('Hệ thống')` + chevron; (2) "Ngôn ngữ" + trailing `Text('Tiếng Việt')` + chevron; (3) "Định dạng & Tiền tệ" dòng phụ "Ngày, tiền tệ, tuần, kỳ tài chính" + chevron; nhóm TRẢI NGHIỆM: (4) "Widget màn hình chính" dòng phụ "Hiện số dư & chi tiêu hôm nay" + trailing `Switch(value: true, onChanged: null)` (R5 — **không** đảo, không lưu), hàng bọc `InkWell` **toàn hàng** (kể cả vùng switch — switch câm không nuốt chạm, R5) → `_showWidgetHelp()` mở `AlertDialog` hướng dẫn ghim widget theo `defaultTargetPlatform` (Android: chạm-giữ màn hình chính → Widgets → kéo widget app; iOS: chạm-giữ → nút "+" → chọn app; giải thích ghim do hệ điều hành quản lý); (5) "Ẩn số dư (Privacy mode)" dòng phụ "Che số tiền trên màn hình chính" + trailing `Switch(value: _prefs.hideBalance, onChanged: (_) => _setPrefs(_prefs.copyWith(hideBalance: !_prefs.hideBalance)))` — toggle ghi-through `_store.save` (write-through, không await-giữa-chạm — plan §Rủi ro) + `setState`; (6) "Máy tính khi nhập số tiền" dòng phụ "Cho phép +, -, x, / khi nhập" + trailing `Switch(value: _prefs.amountCalculatorEnabled, …)` tương tự; nhóm DỮ LIỆU & TÌM KIẾM: (7) "Tìm kiếm toàn cục" dòng phụ "Giao dịch, danh mục, ví" + chevron; (8) "Quản lý Tag" dòng phụ **mô tả sạch** (VD "Gắn nhãn cho giao dịch" — **không** `#…`, **không** "12 tag"/số giả, R4/FR-009/SC-008) + chevron; 5 hàng điều hướng (Giao diện, Ngôn ngữ, Định dạng & Tiền tệ, Tìm kiếm toàn cục, Quản lý Tag) `onTap` **no-op** (R3/FR-005 — không push màn, không lỗi); chọn icon material teal đúng nghĩa mục — đối chiếu mockup `01` lúc thi công; chống tràn cỡ chữ lớn (tên `Expanded`, dòng phụ tối đa, ListView cuộn — FR-010); import `core/utilities/*` + `data/utilities_deps.dart` + `theme/app_colors.dart` + `core/widgets/sub_page_scaffold.dart`; **phụ thuộc: T001 + T002 + T005 + T006**
- [X] T009 [P] [US1] Tạo `app/sora_thu_chi/test/utilities_test.dart`: unit test thuần **`UtilitiesPrefs`** (không fake/widget/sqlite): mặc định `UtilitiesPrefs()` → `hideBalance=false`, `amountCalculatorEnabled=true`; `toSettings()` → đủ 2 khóa `kKeyHideBalance='false'`/`kKeyAmountCalculatorEnabled='true'`; `fromSettings` **rỗng** → mặc định; **đủ 2 row** → parse đúng ('true'/'false'); **key vắng 1 trong 2** → giữ mặc định phần vắng; chuỗi không parse ('yes'/''/'TRUE') → mặc định **không ném**; `copyWith` đổi từng field giữ field kia; round-trip `fromSettings(toSettings())` = giá trị; **phụ thuộc: T001** (độc lập, chạy sớm được)
- [X] T010 [P] [US1] Tạo `app/sora_thu_chi/test/utilities_store_drift_test.dart`: drift test in-memory (bám `wallets_dao_test.dart` — helper `_tryMemoryDb()` mở `AppDatabase(NativeDatabase.memory())`, host thiếu sqlite → `markTestSkipped`): (a) **chưa có row** → `load()` trả `UtilitiesPrefs()` mặc định (không seed — R8); (b) **round-trip**: `save(prefs hideBalance: true, amountCalculatorEnabled: false)` rồi `load()` lại → đúng giá trị vừa lưu; (c) save nhiều lần đè nhau → load ra lần cuối; (d) `save` không xoá key khác trong bảng (chèn 1 row key lạ bằng SQL/`into(appSettings)` trước, `save` prefs → key lạ vẫn còn); (e) upgrade schema v5 từ v4 — tạo DB schema cũ 4 (bỏ `AppSettings`) rồi nâng lên 5 qua `Migrator`/mở lại (nếu phức tạp → thay bằng assert `schemaVersion == 5` + case (a) đã phủ tạo-bảng); **phụ thuộc: T005** (độc lập, chạy sớm được)
- [X] T011 [US1] Tạo `app/sora_thu_chi/test/utilities_screen_test.dart`: widget test **bơm `FakeUtilitiesStore`** (inject qua `UtilitiesScreen(store: fake)` — không GetX, R7): **(a)** bố cục mockup `01` — pump màn: app bar back + tiêu đề "Tiện ích & Cá nhân hóa", **không** bottom nav; đủ 3 nhóm tiêu đề viết hoa "HIỂN THỊ"/"TRẢI NGHIỆM"/"DỮ LIỆU & TÌM KIẾM" + 8 hàng đúng tên + thứ tự nhóm (acceptance 2/FR-002); mỗi hàng có icon teal (find icon trong vòng nền — assert đủ icon, bỏ qua màu); **(b)** trailing mặc định — "Giao diện" cuối `Text('Hệ thống')`, "Ngôn ngữ" cuối `Text('Tiếng Việt')` (FR-004), đủ chevron các hàng điều hướng; **(c)** giá trị switch từ fake — Ẩn số dư `false`, Máy tính `true` (assert `tester.widget<Switch>`); **(d)** **toggle + persist**: chạm switch "Ẩn số dư" → UI đảo `true` + fake `storedPrefs.hideBalance == true` (save được gọi); chạm switch "Máy tính" → `false` + store ghi; bật/tắt **nhanh liên tục** → trạng thái cuối đúng lần chạm cuối (biên spec, FR-006); **(e)** **round-trip mở lại**: toggle xong, pump lại màn với `FakeUtilitiesStore(storedPrefs: fake.storedPrefs)` mới → switch giữ trạng thái đã đổi (acceptance 3); **(f)** hàng "Widget màn hình chính" — `Switch` `value: true`, chạm toàn hàng (kể cả vùng switch) → `AlertDialog` hướng dẫn ghim hiện (tìm text tiêu đề dialog), đóng dialog → switch **giữ `true`** (không đảo — FR-008/acceptance 5); **(g)** 5 hàng điều hướng no-op — lần lượt chạm "Giao diện"/"Ngôn ngữ"/"Định dạng & Tiền tệ"/"Tìm kiếm toàn cục"/"Quản lý Tag" → không route/màn mới (không BackButton/AppBar lạ), app không lỗi, "Hệ thống"/"Tiếng Việt" không đổi (FR-005/acceptance 6); **(h)** "Quản lý Tag" **không** hiện "12 tag"/`#…`/số giả (SC-008); **(i)** cỡ chữ lớn + màn nhỏ (textScaleFactor 2.0, surface 360x640) → cuộn tới hàng cuối không overflow (`tester.takeException()` null — FR-010); **phụ thuộc: T008 + T007** (test sau khi screen tồn tại)
- [X] T012 [US1] Sửa `app/sora_thu_chi/lib/screens/settings_screen.dart` (R7): thêm seam — param `this.onManageUtilitiesTap` (kiểu `VoidCallback?`) + method `_openManageUtilities(BuildContext)` bám `_openManageCategory`: callback bơm → gọi không push; mặc định `Navigator.push(... MaterialPageRoute(builder: (_) => UtilitiesScreen()))` (UtilitiesScreen tự `ensureUtilitiesStore` khi vào); thêm **hàng "Tiện ích & Cá nhân hóa" ngay dưới hàng "Danh mục"** trong nhóm KHÁC — `_SettingsRow(label: 'Tiện ích & Cá nhân hóa', onTap: () => _openManageUtilities(context), trailing: Icon(Icons.chevron_right, color: AppColors.tabInactive))` (FR-001 — nhóm KHÁC, dưới "Danh mục", spec Giả định); import `utilities_screen.dart`; cập nhật doc comment class (liệt kê thêm seam + hàng mới, không đụng profile/các hàng khác); **phụ thuộc: T008** (cần class `UtilitiesScreen`)
- [X] T013 [US1] Sửa `app/sora_thu_chi/test/settings_screen_test.dart` (R7/R10): mở rộng helper `pumpSettings` thêm param `onManageUtilitiesTap`; thêm helper `_registerUtilitiesStore()` bám `_registerRepository` — `Get.reset(); Get.put<UtilitiesStore>(FakeUtilitiesStore()); addTearDown(Get.reset);` (để màn Tiện ích khi mở qua SettingsScreen không khởi tạo drift); **cập nhật case cũ** — case "đủ 2 nhóm/5 hàng" (dòng ~55) và case "thứ tự" (dòng ~80) thêm "Tiện ích & Cá nhân hóa" vào list/assert (giờ 6 hàng, nằm **dưới** "Danh mục", sau nhóm KHÁC); **thêm case mới**: (a) chạm "Tiện ích & Cá nhân hóa" → đẩy `UtilitiesScreen` (find.byType) + back → về SettingsScreen (bám case "Danh mục", FR-001); (b) bơm `onManageUtilitiesTap: () => tapped = true` → chạm hàng → callback gọi, **không** push `UtilitiesScreen` (bám case callback Danh mục); giữ nguyên mọi case cũ khác (profile/cỡ chữ/no-op switch/Quản lý ví/Danh mục — không hồi quy PBI 5/13); **phụ thuộc: T012 + T007** (test sau khi settings nối hàng)

## Pha cuối: Polish & Cross-cutting

- [X] T014 Chạy `flutter analyze` (sạch 0 warning) + `flutter test` toàn bộ tại `app/sora_thu_chi/` (test PBI 2–16 cũ + PBI 17 mới pass — **đặc biệt** chạy lại `wallets_dao_test`/`transactions_dao_test` sau khi nâng schema v5 (T003/T004, research R8: nhánh migration cũ giữ nguyên, seed không đụng) và `settings_screen_test` sau khi thêm hàng (T013: các case thứ tự/đếm cũ đã cập nhật), khẳng định không hồi quy PBI 5/13/15/16); xác nhận `build_runner` chạy đúng 1 lần (T004) và `app_database.g.dart` mới đã sinh; đối chiếu danh sách "Không đổi" plan.md — `core/category/*`, `core/transaction/*`, `core/wallet/*`, `core/widgets/category_row.dart`/`category_icon.dart`/`sub_page_scaffold.dart`, `data/wallet_repository*.dart`, `data/wallet_deps.dart`, `data/transaction_deps.dart`, `theme/*`, màn wallet/transaction/category (trừ settings_screen) không thay đổi; ghi nhận quyết định (R1 bảng `AppSettings` key-value drift schema v5 không seed, R2 giá trị Giao diện/Ngôn ngữ hằng UI tĩnh, R3 5 hàng no-op, R4 dòng phụ Tag mô tả sạch không số giả, R5 Widget = switch câm + dialog ghim theo nền tảng, R6 StatefulWidget đọc store 1 lần write-through, R7 seam `store`/`onManageUtilitiesTap`, R8 không đụng seed cũ) vào trạng thái PBI
- [X] T015 Kiểm chứng thủ công emulator theo `.specify/specs/17/quickstart.md` bước 1–9: Cài đặt → nhóm KHÁC → chạm "Tiện ích & Cá nhân hóa" ngay dưới "Danh mục" → màn con: app bar teal + back trái + tiêu đề "Tiện ích & Cá nhân hóa", không bottom nav, mở < 1 s (1–3); đối chiếu trực quan `01-danh-sach-tien-ich.svg`: đủ 3 nhóm/8 hàng đúng thứ tự, mỗi hàng vòng nền nhạt + icon teal + tên + dòng phụ + phần cuối (3); giá trị/trailing mặc định — Giao diện "Hệ thống" →, Ngôn ngữ "Tiếng Việt" →, Ẩn số dư tắt/Máy tính bật, Widget hiển thị switch bật (4); chạm toàn hàng Widget → dialog hướng dẫn ghim theo nền tảng, switch không đảo (5); bật "Ẩn số dư" + tắt "Máy tính" → đảo ngay, back hệ thống → mở lại giữ nguyên, **tắt hẳn app** → mở lại vẫn giữ (6); chạm 5 hàng điều hướng → không mở màn/không lỗi, hàng Quản lý Tag không hiện "12 tag"/tag giả (7); regression Tổng quan/Giao dịch/thêm giao dịch không đổi sau bật/tắt (8); cỡ chữ lớn + màn nhỏ cuộn tới cuối không cắt/tràn (9); ghi kết quả từng bước vào trạng thái PBI
- [X] T016 Cập nhật wiki tri thức qua skill `sora-wiki` + append `wiki-knowledge/log.md`: entity **Hồ sơ & Bảo mật / Cài đặt** (hoặc page phù hợp nhất) + **Lộ trình** — màn "Tiện ích & Cá nhân hóa" **đã triển khai** (thay mục "PBI sau" còn trống): bố cục mockup `01` (`SubPageScaffold` tiêu đề "Tiện ích & Cá nhân hóa"; 3 nhóm/8 hàng — HIỂN THỊ/TRẢI NGHIỆM/DỮ LIỆU & TÌM KIẾM; vòng nền nhạt + icon teal); entry từ Cài đặt nhóm KHÁC ngay dưới "Danh mục"; **2 công tắc thật nhớ trạng thái** (Ẩn số dư mặc định tắt, Máy tính mặc định bật) lưu bằng **bảng drift key-value `AppSettings` schema v5** (key `hideBalance`/`amountCalculatorEnabled`, key vắng = mặc định, row ghi khi bật/tắt — write-through), **chưa kéo hiệu ứng chức năng** (che số tiền/đổi bàn phím = PBI sau); hàng Widget màn hình chính = switch "bật" câm + chạm mở hướng dẫn ghim theo nền tảng (không lưu); 5 hàng điều hướng no-op chờ màn con 02–07; hàng Quản lý Tag dòng phụ mô tả không số giả; schema v5 nâng 1 lần (build_runner), PBI cài đặt sau chỉ thêm row; ngoài phạm vi: màn con 02–07, hiệu ứng công tắc, dark mode cho màn này, tablet

## Sơ đồ phụ thuộc

```text
Foundational:
  T001 [P] core/utilities/utilities.dart         (UtilitiesPrefs + hằng khóa + to/from — thuần)
  T002 [P] core/utilities/utilities_store.dart   (abstract UtilitiesStore — seam interface)
  T003 [P] data/db/app_database.dart             (+ bảng AppSettings, schemaVersion 5, onUpgrade from<5)
     └──▶ T004 data/db/app_database.g.dart       (chạy build_runner sinh lại — sau T003)
             └──▶ T005 data/utilities_store_drift.dart (DriftUtilitiesStore — sau T004 + T001/T002)
                     └──▶ T006 data/utilities_deps.dart (ensureUtilitiesStore GetX — sau T005)
  T001 + T002 ──▶ T007 [P] test/fakes/fake_utilities_store.dart (song song T003–T006)
US1:
  T001 + T002 + T005 + T006 ──▶ T008 [US1] utilities_screen.dart (màn 01: 8 hàng + 2 công tắc write-through + dialog widget)
                                 ├──▶ T009 [P] [US1] utilities_test.dart          (unit UtilitiesPrefs — sau T001)
                                 ├──▶ T010 [P] [US1] utilities_store_drift_test.dart (drift in-memory — sau T005)
                                 └──▶ T011 [US1]     utilities_screen_test.dart   (sau T008 + T007)
  T008 ──▶ T012 [US1] settings_screen.dart (hàng "Tiện ích & Cá nhân hóa" + seam onManageUtilitiesTap)
            └──▶ T013 [US1] settings_screen_test.dart (sau T012 + T007)
Polish:
  T014 analyze + full test (không hồi quy PBI 2–16) ──▶ T015 QA emulator (quickstart 1–9) ──▶ T016 wiki sync
```

Thứ tự thực thi chính: `T001 → T002 → T003 → T004 → T005 → T006 → T008 → T012 → T013 → T014 → T015 → T016` (test `[P]` chạy sau code tương ứng: T007 sau T001/T002, T009 sau T001, T010 sau T005, T011 sau T008; T013 sau T012). Mấu chốt: **3 mảnh Foundational độc lập đầu** (T001 domain thuần, T002 seam interface, T003 nâng schema — khác file) xong trước — T004 `build_runner` là bắt buộc sau T003 (schema v5, plan §Rủi ro), T005/T006 là implement store theo interface T002; **T008 màn `01`** ghép domain + store (2 công tắc ghi-through + dialog widget + no-op); **T012 nối điểm vào Cài đặt** là bước cuối US1 (cần màn đã tồn tại); **duy nhất schema drift v5** nên mọi PBI cài đặt sau chỉ thêm row, không migration.

## Ví dụ chạy song song

```text
# Foundational — 3 mảnh độc lập (khác file, không chặn nhau):
T001 [P] core/utilities/utilities.dart           (UtilitiesPrefs)
T002 [P] core/utilities/utilities_store.dart     (seam UtilitiesStore — T005/T006 nối sau)
T003 [P] data/db/app_database.dart               (bảng AppSettings + v5 — T004 build_runner nối sau)

# T007 fake có thể chạy ngay sau T001/T002 (không cần chờ T003–T006).

# US1 — 3 test file độc lập (sau code tương ứng, khác file):
T009 [P] [US1] utilities_test.dart                (sau T001 — unit thuần, không cần chờ screen)
T010 [P] [US1] utilities_store_drift_test.dart    (sau T005 — drift in-memory)
T011 [US1]     utilities_screen_test.dart         (sau T008 — widget test, dùng fake T007)

# T013 settings_screen_test chạy sau T012 (Cài đặt đã nối màn) — tuần tự không song song [P].
```

## Chiến lược triển khai

- **MVP đề xuất**: User Story 1 (toàn bộ PBI 17 — đặc tả là một lát cắt "màn danh sách Tiện ích & Cá nhân hóa" duy nhất, không chia user story P1/P2/P3; spec không gán mức ưu tiên theo story). Foundational dựng 3 mảnh nền — domain thuần `UtilitiesPrefs` (T001), seam `UtilitiesStore` (T002), schema drift v5 + bảng `AppSettings` (T003/T004) — làm tiền đề bắt buộc; US1 ghép `UtilitiesScreen` (màn `01`, 3 nhóm/8 hàng + 2 công tắc ghi-through + dialog ghim widget) + nối điểm vào Cài đặt (hàng "Tiện ích & Cá nhân hóa" → màn) để ra giá trị end-to-end (FR-001/FR-006/FR-008).
- **Thứ tự giao hàng tăng dần**: Foundational (domain + seam + schema v5 + store drift + deps + fake — không đổi hành vi màn nào hiện có, test PBI 2–16 giữ) → US1 màn `01` (screen + widget test phủ bố cục/giá trị-default/toggle-persist/dialog-widget/no-op/cỡ chữ + unit test domain + drift test) → nối Cài đặt (hàng mới + seam + widget test) → Polish (analyze/test toàn cục không hồi quy — đặc biệt DAO sau migration v5 + QA emulator quickstart 1–9 + sync wiki).
